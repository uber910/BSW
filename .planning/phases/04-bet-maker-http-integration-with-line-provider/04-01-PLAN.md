---
phase: 04
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - pyproject.toml
  - uv.lock
autonomous: true
requirements: [BM-04]
requirements_addressed: [BM-04]

must_haves:
  truths:
    - "REQUIREMENTS.md BM-04 does not mention TTL cache as a planned feature"
    - "ROADMAP.md Phase 4 Goal does not mention 'tiny TTL cache'"
    - "ROADMAP.md Phase 4 Success Criterion #1 does not mention TTL/cached lag"
    - "respx>=0.22,<0.23 is a dev dependency"
    - "uv.lock is regenerated and reflects respx pin"
    - "Existing test suite remains green (193 passed)"
  artifacts:
    - path: .planning/REQUIREMENTS.md
      provides: "BM-04 wording aligned with D-01 (no cache)"
      contains: "Per D-01 (Phase 4 CONTEXT.md)"
    - path: .planning/ROADMAP.md
      provides: "Phase 4 Goal + SC#1 aligned with D-01"
      contains: "свежий результат каждого запроса"
    - path: pyproject.toml
      provides: "respx dev dep entry"
      contains: "respx>=0.22,<0.23"
    - path: uv.lock
      provides: "lock entry for respx"
      contains: "respx"
  key_links:
    - from: ".planning/REQUIREMENTS.md BM-04"
      to: "CONTEXT.md D-01"
      via: "doc-sync per first-task pattern (mirror P2 Plan 02-01 / P3 Plan 03-01)"
      pattern: "per D-01"
    - from: pyproject.toml
      to: tests/bet_maker/test_http_event_lookup.py
      via: "respx import"
      pattern: "import respx"
---

<objective>
Plan 04-01 — Doc-sync (Wave 1, blocking for all downstream code work)

Synchronise REQUIREMENTS.md BM-04 and ROADMAP.md Phase 4 (Goal line + SC#1)
with CONTEXT.md D-01: remove all wording that implies a TTL cache. Add the
single new dev-dependency for Phase 4 (`respx>=0.22,<0.23`) and regenerate the
lock file. No production code is touched.

Purpose: every code plan in Phase 4 depends on (a) a consistent source-of-truth
document set (REQUIREMENTS.md/ROADMAP.md no longer asks for a cache) and
(b) respx being installable through `uv sync --frozen`. This is the mirror of
P2 Plan 02-01 and P3 Plan 03-01 — phase-opening doc-sync.

Output:
- REQUIREMENTS.md BM-04 line rewritten verbatim per D-01.
- ROADMAP.md Phase 4 Goal line (114) rewritten — drops "+ tiny TTL cache".
- ROADMAP.md Phase 4 Success Criterion #1 (line 118) rewritten verbatim per D-01.
- pyproject.toml [dependency-groups] dev array contains respx pin.
- uv.lock regenerated by `uv lock`.
- All existing tests still pass (no production change).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-bet-maker-http-integration-with-line-provider/04-CONTEXT.md
@.planning/phases/04-bet-maker-http-integration-with-line-provider/04-PATTERNS.md

<analog_excerpts>
P3 Plan 03-01 (BM-01 coefficient removal — doc-sync first-task analog):
- read first
- locate exact source line in REQUIREMENTS.md
- replace verbatim with new text that cites the decision ID
- grep-verify the change

Existing dev-deps array (pyproject.toml lines 22-32) ends with
`"testcontainers>=4.9,<5",` — respx pin appends as a new array element.
</analog_excerpts>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify TZ does not require cache (read-only)</name>
  <files></files>
  <read_first>
    - .planning/phases/04-bet-maker-http-integration-with-line-provider/04-CONTEXT.md (D-01 in full + canonical_refs section about TZ pages 2-3)
    - ./Тестовое задание Middle Python developer.pdf (pages 2-3 — sections about line-provider GET /events and "допускается небольшое отставание в свежести")
  </read_first>
  <action>
    Verification-only task (no file modifications). Confirms that the TZ PDF
    does not require a cache — it only ALLOWS "небольшое отставание в свежести"
    (lag). This sets the foundation for the sync edits in Tasks 2-3.

    Read the TZ PDF (path: ./Тестовое задание Middle Python developer.pdf)
    pages 2-3. Search the body for any occurrence of the word "кэш" / "cache"
    / "TTL". Confirm result: ZERO occurrences (the TZ only mentions lag, not
    caching). Confirm the wording "допускается небольшое отставание в свежести"
    is exactly what justifies our decision NOT to cache.

    Output a one-line confirmation in the commit message of Task 2:
    "Verified TZ pages 2-3: no 'кэш'/'cache'/'TTL' occurrence; only allows lag (D-01)."

    No git changes in this task.
  </action>
  <verify>
    <automated>! grep -i -E "кэш|cache|TTL" "./Тестовое задание Middle Python developer.pdf" 2>/dev/null || true</automated>
  </verify>
  <acceptance_criteria>
    - Task is purely verification — no files in `files` field, no git diff for this task alone.
    - Executor confirms (in their summary or commit message of Task 2) that TZ pages 2-3 contain no "кэш"/"cache"/"TTL" tokens.
    - Confirmation explicitly cites the TZ wording "допускается небольшое отставание в свежести" as the source of D-01.
  </acceptance_criteria>
  <done>
    Operator (or task executor) has read the TZ PDF and recorded the absence
    of cache-related wording. Decision D-01 confirmed against first source.
  </done>
</task>

<task type="auto">
  <name>Task 2: Rewrite REQUIREMENTS.md BM-04 per D-01</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (current BM-04 entry — line 35)
    - .planning/phases/04-bet-maker-http-integration-with-line-provider/04-CONTEXT.md (D-01 in full, including the exact replacement-text guidance)
    - .planning/phases/03-bet-maker-domain-db/03-01-PLAN.md (Task 2 — analog of in-place REQUIREMENTS.md rewrite with inline per-D-XX citation)
  </read_first>
  <action>
    Open `.planning/REQUIREMENTS.md`. Find line 35, which currently reads
    (exactly — confirm via `grep -n "BM-04" .planning/REQUIREMENTS.md`):

    ```
    - [ ] **BM-04**: `GET /events` — проксирует список активных событий из line-provider через httpx с retry (tenacity)
    ```

    The current wording already does NOT include the literal string "TTL cache"
    (it was inherited as-is from the original requirement). BUT we add an
    inline citation of D-01 to make the no-cache decision grep-visible.

    Replace line 35 exactly with:

    ```
    - [ ] **BM-04**: `GET /events` — проксирует список активных событий из line-provider через httpx с retry (tenacity). Per D-01 (Phase 4 CONTEXT.md): TTL cache не реализуется в P4 — ТЗ кэш не требует, только разрешает отставание в свежести; кэш отложен в README P7 как "next-step extension".
    ```

    No other lines in REQUIREMENTS.md are touched.

    Commit message:
    `docs(04): sync BM-04 wording with D-01 (no TTL cache) — verified against TZ pages 2-3`
  </action>
  <verify>
    <automated>grep -c "Per D-01 (Phase 4 CONTEXT.md)" .planning/REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "Per D-01 (Phase 4 CONTEXT.md)" .planning/REQUIREMENTS.md` returns at least 1.
    - `grep -c "TTL cache не реализуется" .planning/REQUIREMENTS.md` returns exactly 1 (the explicit negation in BM-04).
    - The BM-04 line still contains the substring "проксирует список активных событий из line-provider через httpx с retry (tenacity)".
    - `git diff .planning/REQUIREMENTS.md` shows ONLY line 35 changed; no other lines modified.
  </acceptance_criteria>
  <done>
    REQUIREMENTS.md BM-04 explicitly cites D-01 and acknowledges no cache.
    Diff is single-line.
  </done>
</task>

<task type="auto">
  <name>Task 3: Rewrite ROADMAP.md Phase 4 Goal + Success Criterion #1 per D-01</name>
  <files>.planning/ROADMAP.md</files>
  <read_first>
    - .planning/ROADMAP.md (Phase 4 block lines 113-126 — current Goal at line 114 and SC#1 at line 118)
    - .planning/phases/04-bet-maker-http-integration-with-line-provider/04-CONTEXT.md (D-01 exact replacement text)
    - .planning/phases/02-line-provider-domain/02-01-PLAN.md (analog — Plan 02-01 also synced REQUIREMENTS.md drift to CONTEXT.md decision)
  </read_first>
  <action>
    Open `.planning/ROADMAP.md`. Apply TWO line edits inside the Phase 4 block.

    ### Edit 1 — Phase 4 Goal (line 114)

    Find line 114 which currently reads exactly:

    ```
    **Goal**: bet-maker exposes `GET /events` by proxying line-provider via `httpx.AsyncClient` with retry + tiny TTL cache, and establishes the HTTP client that reconciliation will reuse in P6.
    ```

    Replace line 114 exactly with:

    ```
    **Goal**: bet-maker exposes `GET /events` by proxying line-provider via `httpx.AsyncClient` with retry (tenacity), and establishes the HTTP client that reconciliation will reuse in P6. Per D-01 (Phase 4 CONTEXT.md): TTL cache не реализуется — ТЗ кэш не требует.
    ```

    ### Edit 2 — Phase 4 Success Criterion #1 (line 118)

    Find line 118 (under `### Phase 4: bet-maker HTTP integration with line-provider`, `**Success Criteria** (what must be TRUE):` block, item 1) which currently reads exactly:

    ```
      1. bet-maker's `GET /events` returns the same active-events payload that `line-provider` exposes, with acceptable lag (cached via TTL dict)
    ```

    Replace line 118 exactly with:

    ```
      1. bet-maker's `GET /events` returns the same active-events payload that `line-provider` exposes, with acceptable lag (свежий результат каждого запроса; отставание = длительность одного HTTP-вызова к LP плюс retry-backoff). Per D-01 (Phase 4 CONTEXT.md): TTL cache не реализуется в P4.
    ```

    No other lines in ROADMAP.md are touched in this task (the Plans list
    update is handled by the planner outside Plan 04-01).

    Commit message:
    `docs(04): sync ROADMAP Phase 4 Goal + SC#1 with D-01 (no TTL cache)`
  </action>
  <verify>
    <automated>grep -c "свежий результат каждого запроса" .planning/ROADMAP.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "свежий результат каждого запроса" .planning/ROADMAP.md` returns exactly 1.
    - `grep -c "cached via TTL dict" .planning/ROADMAP.md` returns exactly 0 (old wording removed).
    - `grep -c "tiny TTL cache" .planning/ROADMAP.md` returns exactly 0 (Goal-line wording removed).
    - `grep -c "Per D-01 (Phase 4 CONTEXT.md)" .planning/ROADMAP.md` returns at least 2 (one in Goal, one in SC#1).
    - The Phase 4 success-criteria list still has 5 items: in the Phase 4 block, the literal patterns `^  1\. `, `^  2\. `, `^  3\. `, `^  4\. `, `^  5\. ` each appear at least once.
    - `git diff .planning/ROADMAP.md` shows ONLY lines 114 and 118 changed.
  </acceptance_criteria>
  <done>
    ROADMAP.md Phase 4 Goal and SC#1 both reflect the no-cache decision with inline D-01 citations.
    Diff is two-line.
  </done>
</task>

<task type="auto">
  <name>Task 4: Add respx dev-dependency and regenerate uv.lock</name>
  <files>pyproject.toml, uv.lock</files>
  <read_first>
    - pyproject.toml (lines 22-32 — current `[dependency-groups] dev` array)
    - .planning/phases/04-bet-maker-http-integration-with-line-provider/04-CONTEXT.md (D-15 — respx pin range `>=0.22,<0.23`)
    - .planning/phases/04-bet-maker-http-integration-with-line-provider/04-PATTERNS.md (pyproject.toml section — sibling `asgi-lifespan>=2.1,<3` shows the exact insertion shape)
  </read_first>
  <action>
    Add respx to dev-deps. Use the canonical uv flow that edits pyproject.toml
    and regenerates uv.lock in one step:

    ```bash
    uv add --group dev "respx>=0.22,<0.23"
    ```

    Verify after the command:
    - `pyproject.toml` `[dependency-groups] dev` array contains a new line
      `"respx>=0.22,<0.23",` (placed by uv near the alphabetical position;
      manual reorder is NOT required — uv preserves insertion order).
    - `uv.lock` contains a new `[[package]]` block with `name = "respx"`.
    - Run `uv sync --frozen` to confirm the lock is consistent with the
      manifest.

    Then run the existing test suite to confirm no regression:

    ```bash
    uv run pytest -q
    ```

    Must show 193 passed (matches STATE.md post-Plan 03-09 baseline).

    Commit message:
    `chore(04): add respx>=0.22,<0.23 to dev deps for Plan 04-05/04-06/04-08 unit tests (D-15)`
  </action>
  <verify>
    <automated>grep -c 'respx>=0.22,<0.23' pyproject.toml &amp;&amp; uv sync --frozen &amp;&amp; uv run pytest -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'respx>=0.22,<0.23' pyproject.toml` returns at least 1 (pin present).
    - `grep -c 'name = "respx"' uv.lock` returns at least 1 (lock entry present).
    - `uv sync --frozen` exits 0 with no warnings about lock drift.
    - `uv run pytest -q` exits 0 with output line matching `193 passed` (or equivalent — pre-P4 baseline).
    - `uv run ruff check` exits 0 (no new lint).
    - `uv run mypy src` exits 0 (no new type errors — respx is dev-only).
    - `git diff pyproject.toml uv.lock` shows additive changes only; nothing removed.
  </acceptance_criteria>
  <done>
    respx is installable via `uv sync --frozen`. No production code changed.
    All existing tests pass.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo files → CI pipeline | doc-sync edits and dep-manifest edits both feed CI; both are auditable through git diff |
| pyproject.toml → uv.lock | a tampered manifest with a typo'd pin range would silently install wrong version; mitigated by `uv lock` regenerating canonical lock |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01-DocDrift | Tampering | REQUIREMENTS.md / ROADMAP.md | mitigate | Single-line literal replacements with inline `Per D-01 (Phase 4 CONTEXT.md)` citations; verified by grep in Task 2/3 acceptance criteria. Drift becomes immediately grep-visible in future audits. |
| T-04-01-DepSupplyChain | Tampering | pyproject.toml / uv.lock | mitigate | Pin range `respx>=0.22,<0.23` is bounded (no `*`, no unpinned); `uv sync --frozen` enforces lock exactness in CI. respx is a well-known PyPI package; CONTEXT.md D-15 fixes range. |
| T-04-01-CIRegression | Denial of Service | full test suite | mitigate | Task 4 acceptance requires `uv run pytest -q` to still show 193 passed before commit. |
| T-04-01-InfoDisclosure | Information Disclosure | doc-sync edits | accept | Documents are public planning artefacts; no secrets. Rationale: scope is REQUIREMENTS.md + ROADMAP.md text only. |
</threat_model>

<verification>
- `uv run pytest -q` → 193 passed (P3 baseline preserved).
- `uv run ruff check` → no new lint errors.
- `uv run mypy src` → strict-mode clean (95 files).
- `grep -c "Per D-01 (Phase 4 CONTEXT.md)" .planning/REQUIREMENTS.md` ≥ 1.
- `grep -c "Per D-01 (Phase 4 CONTEXT.md)" .planning/ROADMAP.md` ≥ 2.
- `grep -c "свежий результат каждого запроса" .planning/ROADMAP.md` == 1.
- `! grep "tiny TTL cache" .planning/ROADMAP.md` returns 0.
- `! grep "cached via TTL dict" .planning/ROADMAP.md` returns 0.
- `grep -c 'respx>=0.22,<0.23' pyproject.toml` ≥ 1.
- `grep -c 'name = "respx"' uv.lock` ≥ 1.
</verification>

<success_criteria>
- REQUIREMENTS.md BM-04 explicitly cites D-01 + acknowledges no cache.
- ROADMAP.md Phase 4 Goal AND SC#1 reflect no-cache lag wording.
- pyproject.toml + uv.lock include respx>=0.22,<0.23.
- 193 tests still pass (no regression).
- Downstream plans (04-02 onward) can `import respx` in their test files.
</success_criteria>

<output>
After completion, create `.planning/phases/04-bet-maker-http-integration-with-line-provider/04-01-SUMMARY.md`
documenting: TZ verification result (Task 1), exact diff hunks of Tasks 2/3,
respx pin and lock-package version that landed.
</output>
