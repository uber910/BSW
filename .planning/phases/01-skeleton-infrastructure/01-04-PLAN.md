---
phase: 01-skeleton-infrastructure
plan: 04
type: execute
wave: 3
depends_on: [01, 02]
files_modified:
  - src/bet_maker/__init__.py
  - src/bet_maker/__main__.py
  - src/bet_maker/app.py
  - src/bet_maker/py.typed
  - src/bet_maker/settings/__init__.py
  - src/bet_maker/settings/config.py
  - src/bet_maker/entrypoints/__init__.py
  - src/bet_maker/entrypoints/lifespan.py
  - src/bet_maker/entrypoints/middleware.py
  - src/bet_maker/entrypoints/api/__init__.py
  - src/bet_maker/entrypoints/api/health.py
  - alembic.ini
  - alembic/env.py
  - alembic/script.py.mako
  - alembic/versions/.gitkeep
autonomous: true
requirements: [INFR-01, INFR-06, INFR-07, INFR-08]
requirements_addressed: [INFR-01, INFR-06, INFR-07, INFR-08]
tags: [bet-maker, fastapi, alembic, structlog, health]

must_haves:
  truths:
    - "src/bet_maker/ — импортируемый пакет, зеркальный по форме line_provider, отличия только в порте (8001) и settings"
    - "`python -m bet_maker` запускает uvicorn по host=0.0.0.0, port=8001 (D-03, D-08)"
    - "BetMakerSettings(BaseAppSettings) env_prefix=BET_MAKER_; поля: postgres_dsn, rabbitmq_url, line_provider_base_url, reconciliation_interval_s=30, host, port (D-15)"
    - "GET /health возвращает 200 {\"status\": \"ok\"} (D-19) — deep dep-pings PG/RMQ добавляются в P3/P5"
    - "RequestContextMiddleware биндит/чистит request_id с try/finally (D-18, A7)"
    - "alembic.ini + alembic/env.py (async template, INFR-06) скопированы — миграции создаются в P3, в P1 только каркас"
    - "alembic/env.py читает DSN из BetMakerSettings, НЕ из захардкоженного sqlalchemy.url (Anti-Pattern 7)"
  artifacts:
    - path: "src/bet_maker/__main__.py"
      provides: "Module entry: python -m bet_maker"
      contains: "uvicorn.run"
    - path: "src/bet_maker/app.py"
      provides: "build_app() factory + middleware + lifespan wiring"
      contains: "def build_app"
    - path: "src/bet_maker/settings/config.py"
      provides: "BetMakerSettings with env_prefix BET_MAKER_"
      contains: "env_prefix=\"BET_MAKER_\""
    - path: "src/bet_maker/entrypoints/api/health.py"
      provides: "GET /health stub"
      contains: "@router.get(\"/health\""
    - path: "src/bet_maker/entrypoints/middleware.py"
      provides: "RequestContextMiddleware with try/finally clear_contextvars (A7)"
      contains: "clear_contextvars"
    - path: "alembic.ini"
      provides: "Alembic config (sqlalchemy.url placeholder; env.py overrides it)"
      contains: "[alembic]"
    - path: "alembic/env.py"
      provides: "Async migration env reading DSN from BetMakerSettings (INFR-06, Anti-Pattern 7)"
      contains: "BetMakerSettings"
    - path: "alembic/versions/.gitkeep"
      provides: "Empty versions dir tracked in git"
      min_lines: 0
  key_links:
    - from: "src/bet_maker/app.py"
      to: "src/bet_maker/entrypoints/lifespan.py"
      via: "FastAPI(lifespan=lifespan)"
      pattern: "lifespan=lifespan"
    - from: "src/bet_maker/app.py"
      to: "src/bet_maker/entrypoints/api/health.py"
      via: "app.include_router"
      pattern: "include_router"
    - from: "src/bet_maker/__main__.py"
      to: "src/bet_maker/app.py"
      via: "uvicorn.run(\"bet_maker.app:build_app\", factory=True, host=\"0.0.0.0\", port=8001)"
      pattern: "factory=True"
    - from: "alembic/env.py"
      to: "src/bet_maker/settings/config.py"
      via: "BetMakerSettings().postgres_dsn"
      pattern: "BetMakerSettings"
---

<objective>
Создать каркас сервиса `bet-maker` — зеркальный по форме `line_provider`, но с портом 8001, расширенным settings (PG DSN, RMQ URL, line-provider base URL, reconciliation interval) и async Alembic environment, готовым к миграциям в P3. Бизнес-логики и подключения к PG/RMQ в P1 нет.

Purpose: D-03 (python -m), D-04 (alembic копируется в образ), D-15 (BetMakerSettings), D-17/D-18 (structlog + middleware), D-19 (/health stub), INFR-06 (Alembic async template), Anti-Pattern 7 (env.py читает settings, не ini).

Output: 11 файлов под src/bet_maker/ + 4 файла alembic-каркаса.

**Структура задач:** Task 1a (scaffold, 8 файлов: settings + entrypoints/api/health + middleware + lifespan), Task 1b (wiring, 3 файла: app.py + __main__.py + ASGI smoke), Task 2 (alembic, 4 файла). Разделение Task 1 — для удержания каждой подзадачи под 10 файлами (scope_sanity).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-skeleton-infrastructure/01-CONTEXT.md
@.planning/research/ARCHITECTURE.md
@.planning/research/PITFALLS.md
@src/config/__init__.py
@src/config/logging.py
@src/config/settings_base.py

<interfaces>
<!-- Контракты из плана 02 -->
From src/config/__init__.py:
```python
from config.logging import configure_structlog
from config.settings_base import BaseAppSettings
from config.time import utc_now
```

From src/config/settings_base.py:
```python
class BaseAppSettings(BaseSettings):
    service_name: str = Field(...)
    log_level: str = Field(default="INFO")
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1a: Scaffold bet_maker settings, /health route, middleware, lifespan (8 files)</name>
  <files>src/bet_maker/__init__.py, src/bet_maker/py.typed, src/bet_maker/settings/__init__.py, src/bet_maker/settings/config.py, src/bet_maker/entrypoints/__init__.py, src/bet_maker/entrypoints/api/__init__.py, src/bet_maker/entrypoints/api/health.py, src/bet_maker/entrypoints/middleware.py, src/bet_maker/entrypoints/lifespan.py</files>
  <read_first>
    - src/config/* (interfaces above)
    - src/line_provider/* (Task 1a of plan 03 is the reference shape — mirror it)
    - .planning/research/ARCHITECTURE.md "src/bet_maker/ — paste-ready tree" (lines ~176-246)
    - .planning/research/PITFALLS.md "Pitfall A7: structlog contextvars cross-task contamination" (lines 494-522)
    - .planning/phases/01-skeleton-infrastructure/01-CONTEXT.md (D-15 BetMakerSettings fields)
  </read_first>
  <behavior>
    - BetMakerSettings reads BET_MAKER_POSTGRES_DSN, BET_MAKER_RABBITMQ_URL, BET_MAKER_LINE_PROVIDER_BASE_URL, BET_MAKER_RECONCILIATION_INTERVAL_S, BET_MAKER_HOST=0.0.0.0, BET_MAKER_PORT=8001 from env
    - GET /health returns 200 {"status": "ok"} (D-19; deep pings in P3/P5)
    - **Middleware вызывает `clear_contextvars()` дважды**: defensive на входе (на случай если предыдущий task оставил state) + cleanup в `finally`. Паттерн A7 из PITFALLS.md, симметричен middleware в line_provider.
  </behavior>
  <action>
Шаг A — `src/bet_maker/__init__.py` (пустой пакет init); `src/bet_maker/py.typed` (пустой PEP 561 marker).

Шаг B — `src/bet_maker/settings/__init__.py`:

```python
from bet_maker.settings.config import BetMakerSettings

__all__ = ["BetMakerSettings"]
```

Шаг C — `src/bet_maker/settings/config.py`:

```python
from __future__ import annotations

from pydantic import Field, PostgresDsn, AmqpDsn, HttpUrl
from pydantic_settings import SettingsConfigDict

from config.settings_base import BaseAppSettings


class BetMakerSettings(BaseAppSettings):
    model_config = SettingsConfigDict(
        env_file=".env",
        env_file_encoding="utf-8",
        env_prefix="BET_MAKER_",
        case_sensitive=False,
        extra="ignore",
    )

    service_name: str = Field(default="bet-maker")
    host: str = Field(default="0.0.0.0")
    port: int = Field(default=8001, ge=1, le=65535)

    postgres_dsn: PostgresDsn = Field(
        default=PostgresDsn("postgresql+asyncpg://bsw:bsw@postgres:5432/bsw"),
    )
    rabbitmq_url: AmqpDsn = Field(
        default=AmqpDsn("amqp://guest:guest@rabbitmq:5672/"),
    )
    line_provider_base_url: HttpUrl = Field(
        default=HttpUrl("http://line-provider:8000"),
    )
    reconciliation_interval_s: float = Field(default=30.0, gt=0)
```

Шаг D — `src/bet_maker/entrypoints/__init__.py` и `src/bet_maker/entrypoints/api/__init__.py` — пустые.

Шаг E — `src/bet_maker/entrypoints/api/health.py`:

```python
from __future__ import annotations

from fastapi import APIRouter

router = APIRouter(tags=["health"])


@router.get("/health")
async def health() -> dict[str, str]:
    return {"status": "ok"}
```

Шаг F — `src/bet_maker/entrypoints/middleware.py` (точно повторяет line_provider — A7 двойной clear_contextvars: defensive на входе для случая утечки state из предыдущего request, plus cleanup в finally):

```python
from __future__ import annotations

from collections.abc import Awaitable, Callable
from uuid import uuid4

import structlog
from starlette.middleware.base import BaseHTTPMiddleware
from starlette.requests import Request
from starlette.responses import Response


class RequestContextMiddleware(BaseHTTPMiddleware):
    async def dispatch(
        self,
        request: Request,
        call_next: Callable[[Request], Awaitable[Response]],
    ) -> Response:
        structlog.contextvars.clear_contextvars()
        request_id = request.headers.get("X-Request-ID", uuid4().hex)
        structlog.contextvars.bind_contextvars(request_id=request_id)
        try:
            response = await call_next(request)
            response.headers["X-Request-ID"] = request_id
            return response
        finally:
            structlog.contextvars.clear_contextvars()
```

Шаг G — `src/bet_maker/entrypoints/lifespan.py`:

```python
from __future__ import annotations

from collections.abc import AsyncIterator
from contextlib import asynccontextmanager

import structlog
from fastapi import FastAPI

from bet_maker.settings.config import BetMakerSettings
from config.logging import configure_structlog


@asynccontextmanager
async def lifespan(app: FastAPI) -> AsyncIterator[None]:
    settings = BetMakerSettings()
    configure_structlog(settings.log_level)
    log = structlog.get_logger()
    log.info("bet_maker.startup", service=settings.service_name)
    app.state.settings = settings
    try:
        yield
    finally:
        log.info("bet_maker.shutdown")
```
  </action>
  <verify>
    <automated>uv run python -c "from bet_maker.settings.config import BetMakerSettings; from bet_maker.entrypoints.api.health import router; from bet_maker.entrypoints.middleware import RequestContextMiddleware; from bet_maker.entrypoints.lifespan import lifespan; s = BetMakerSettings(); assert s.host == '0.0.0.0' and s.port == 8001"</automated>
  </verify>
  <acceptance_criteria>
    - All 9 files from steps A-G exist (`test -f` each: __init__.py, py.typed, settings/__init__.py, settings/config.py, entrypoints/__init__.py, entrypoints/api/__init__.py, entrypoints/api/health.py, entrypoints/middleware.py, entrypoints/lifespan.py)
    - `grep -q "env_prefix=\"BET_MAKER_\"" src/bet_maker/settings/config.py`
    - `grep -q "port: int = Field(default=8001" src/bet_maker/settings/config.py`
    - `grep -q "host: str = Field(default=\"0.0.0.0\")" src/bet_maker/settings/config.py`
    - `grep -q "postgres_dsn: PostgresDsn" src/bet_maker/settings/config.py`
    - `grep -q "rabbitmq_url: AmqpDsn" src/bet_maker/settings/config.py`
    - `grep -q "line_provider_base_url: HttpUrl" src/bet_maker/settings/config.py`
    - `grep -q "reconciliation_interval_s: float" src/bet_maker/settings/config.py`
    - `grep -q "@router.get(\"/health\")" src/bet_maker/entrypoints/api/health.py`
    - `grep -q "return {\"status\": \"ok\"}" src/bet_maker/entrypoints/api/health.py`
    - `grep -c "clear_contextvars" src/bet_maker/entrypoints/middleware.py | awk '{ exit ($1 < 2) }'` exits 0 (called twice: defensive entry + finally cleanup; A7)
    - `grep -q "finally:" src/bet_maker/entrypoints/middleware.py`
    - `grep -q "configure_structlog(settings.log_level)" src/bet_maker/entrypoints/lifespan.py`
    - `uv run mypy --strict src/bet_maker/` exits 0
    - `uv run ruff check src/bet_maker/` exits 0
    - No emoji: `find src/bet_maker -name '*.py' | xargs -I{} python3 -c "import re,sys; s=open('{}').read(); sys.exit(1) if re.search(r'[\U0001F300-\U0001FAFF\U00002600-\U000027BF]', s) else None"`
  </acceptance_criteria>
  <done>
    Scaffold готов: BetMakerSettings импортируется со всеми полями из D-15; /health stub зеркалит line_provider; middleware с двойным clear_contextvars (defensive + cleanup, A7); lifespan настраивает structlog; mypy + ruff зелёные. Wiring через build_app() и __main__ — в Task 1b.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 1b: Wire bet_maker — app.py factory + __main__.py uvicorn entrypoint; verify ASGI /health (2 files)</name>
  <files>src/bet_maker/app.py, src/bet_maker/__main__.py</files>
  <read_first>
    - src/bet_maker/entrypoints/lifespan.py, middleware.py, api/health.py (созданы в Task 1a)
    - .planning/phases/01-skeleton-infrastructure/01-CONTEXT.md (D-03, D-11)
    - src/line_provider/app.py, src/line_provider/__main__.py (reference shape from plan 03 Task 1b)
  </read_first>
  <behavior>
    - build_app() returns FastAPI with lifespan + middleware + health router
    - ASGI smoke against /health works exactly like line_provider (port doesn't matter for ASGITransport)
    - python -m bet_maker uses uvicorn.run with factory=True, reads host/port from BetMakerSettings
  </behavior>
  <action>
Шаг A — `src/bet_maker/app.py`:

```python
from __future__ import annotations

from fastapi import FastAPI

from bet_maker.entrypoints.api import health
from bet_maker.entrypoints.lifespan import lifespan
from bet_maker.entrypoints.middleware import RequestContextMiddleware


def build_app() -> FastAPI:
    app = FastAPI(
        title="bet-maker",
        version="0.1.0",
        lifespan=lifespan,
    )
    app.add_middleware(RequestContextMiddleware)
    app.include_router(health.router)
    return app
```

Шаг B — `src/bet_maker/__main__.py`:

```python
from __future__ import annotations

import uvicorn

from bet_maker.settings.config import BetMakerSettings


def main() -> None:
    settings = BetMakerSettings()
    uvicorn.run(
        "bet_maker.app:build_app",
        factory=True,
        host=settings.host,
        port=settings.port,
        log_config=None,
    )


if __name__ == "__main__":
    main()
```

Шаг C — smoke-check (не запускать сервер):

```
uv run python -c "
import asyncio
from httpx import AsyncClient, ASGITransport
from bet_maker.app import build_app

async def main():
    app = build_app()
    async with AsyncClient(transport=ASGITransport(app=app), base_url='http://test') as c:
        r = await c.get('/health')
        assert r.status_code == 200
        assert r.json() == {'status': 'ok'}
        assert 'X-Request-ID' in r.headers
        print('bet_maker smoke ok')

asyncio.run(main())
"
```
  </action>
  <verify>
    <automated>uv run python -c "
import asyncio
from httpx import AsyncClient, ASGITransport
from bet_maker.app import build_app

async def main():
    app = build_app()
    async with AsyncClient(transport=ASGITransport(app=app), base_url='http://test') as c:
        r = await c.get('/health')
        assert r.status_code == 200
        assert r.json() == {'status': 'ok'}
        assert 'X-Request-ID' in r.headers

asyncio.run(main())
"</automated>
  </verify>
  <acceptance_criteria>
    - `test -f src/bet_maker/app.py` exits 0
    - `test -f src/bet_maker/__main__.py` exits 0
    - `grep -q "def build_app() -> FastAPI:" src/bet_maker/app.py`
    - `grep -q "lifespan=lifespan" src/bet_maker/app.py`
    - `grep -q "RequestContextMiddleware" src/bet_maker/app.py`
    - `grep -q "app.include_router(health.router)" src/bet_maker/app.py`
    - `grep -q "factory=True" src/bet_maker/__main__.py`
    - `grep -q "host=settings.host" src/bet_maker/__main__.py`
    - `grep -q "port=settings.port" src/bet_maker/__main__.py`
    - `grep -q "bet_maker.app:build_app" src/bet_maker/__main__.py`
    - `uv run mypy --strict src/bet_maker/` exits 0
    - `uv run ruff check src/bet_maker/` exits 0
    - smoke-check command prints `bet_maker smoke ok`
    - No emoji: `find src/bet_maker -name '*.py' | xargs -I{} python3 -c "import re,sys; s=open('{}').read(); sys.exit(1) if re.search(r'[\U0001F300-\U0001FAFF\U00002600-\U000027BF]', s) else None"`
  </acceptance_criteria>
  <done>
    bet_maker запускается через `python -m bet_maker` на 0.0.0.0:8001; build_app() конструирует FastAPI app; ASGITransport smoke зелёный; mypy + ruff чистые.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Initialise async Alembic skeleton (alembic.ini + env.py reading BetMakerSettings)</name>
  <files>alembic.ini, alembic/env.py, alembic/script.py.mako, alembic/versions/.gitkeep</files>
  <read_first>
    - src/bet_maker/settings/config.py (BetMakerSettings.postgres_dsn — DSN source)
    - .planning/research/ARCHITECTURE.md "Anti-Pattern 7: Mixing alembic configuration with app settings" (lines ~1072-1076)
    - .planning/phases/01-skeleton-infrastructure/01-CONTEXT.md (D-04: alembic.ini копируется в образ; INFR-06: async template)
    - .planning/research/STACK.md Alembic row (`alembic init -t async`, version 1.18.4)
  </read_first>
  <behavior>
    - `uv run alembic --version` works after sync (already in deps)
    - `alembic.ini` exists at repo root with [alembic] section pointing script_location=alembic
    - `alembic/env.py` imports BetMakerSettings(), reads postgres_dsn, runs migrations against asyncpg via run_async_migrations()
    - `alembic/versions/` directory exists (empty + .gitkeep) — populated in P3
    - `uv run alembic current` exits 0 against an unreachable DB (acceptable: prints nothing or "ERROR" but env.py module loads); we verify only module-import for now
  </behavior>
  <action>
Шаг A — `alembic.ini` в корне репозитория:

```ini
[alembic]
script_location = alembic
prepend_sys_path = . src
file_template = %%(year)d%%(month).2d%%(day).2d_%%(rev)s_%%(slug)s
timezone = UTC

[post_write_hooks]

[loggers]
keys = root,sqlalchemy,alembic

[handlers]
keys = console

[formatters]
keys = generic

[logger_root]
level = WARNING
handlers = console
qualname =

[logger_sqlalchemy]
level = WARNING
handlers =
qualname = sqlalchemy.engine

[logger_alembic]
level = INFO
handlers =
qualname = alembic

[handler_console]
class = StreamHandler
args = (sys.stderr,)
level = NOTSET
formatter = generic

[formatter_generic]
format = %(levelname)-5.5s [%(name)s] %(message)s
datefmt = %H:%M:%S
```

Шаг B — `alembic/script.py.mako` (стандартный шаблон Alembic; точное содержимое из `alembic init -t async` Alembic 1.18.4):

```mako
"""${message}

Revision ID: ${up_revision}
Revises: ${down_revision | comma,n}
Create Date: ${create_date}

"""
from typing import Sequence, Union

from alembic import op
import sqlalchemy as sa
${imports if imports else ""}

# revision identifiers, used by Alembic.
revision: str = ${repr(up_revision)}
down_revision: Union[str, Sequence[str], None] = ${repr(down_revision)}
branch_labels: Union[str, Sequence[str], None] = ${repr(branch_labels)}
depends_on: Union[str, Sequence[str], None] = ${repr(depends_on)}


def upgrade() -> None:
    ${upgrades if upgrades else "pass"}


def downgrade() -> None:
    ${downgrades if downgrades else "pass"}
```

Шаг C — `alembic/env.py` (async template, читает DSN из BetMakerSettings — устраняет Anti-Pattern 7):

```python
from __future__ import annotations

import asyncio
from logging.config import fileConfig

from alembic import context
from sqlalchemy import pool
from sqlalchemy.engine import Connection
from sqlalchemy.ext.asyncio import async_engine_from_config

from bet_maker.settings.config import BetMakerSettings

config = context.config

if config.config_file_name is not None:
    fileConfig(config.config_file_name)

settings = BetMakerSettings()
config.set_main_option("sqlalchemy.url", str(settings.postgres_dsn))

target_metadata = None


def run_migrations_offline() -> None:
    url = config.get_main_option("sqlalchemy.url")
    context.configure(
        url=url,
        target_metadata=target_metadata,
        literal_binds=True,
        dialect_opts={"paramstyle": "named"},
    )
    with context.begin_transaction():
        context.run_migrations()


def do_run_migrations(connection: Connection) -> None:
    context.configure(connection=connection, target_metadata=target_metadata)
    with context.begin_transaction():
        context.run_migrations()


async def run_async_migrations() -> None:
    connectable = async_engine_from_config(
        config.get_section(config.config_ini_section, {}),
        prefix="sqlalchemy.",
        poolclass=pool.NullPool,
    )
    async with connectable.connect() as connection:
        await connection.run_sync(do_run_migrations)
    await connectable.dispose()


def run_migrations_online() -> None:
    asyncio.run(run_async_migrations())


if context.is_offline_mode():
    run_migrations_offline()
else:
    run_migrations_online()
```

Шаг D — `alembic/versions/.gitkeep` — пустой файл (директория попадает в git).
  </action>
  <verify>
    <automated>test -f alembic.ini &amp;&amp; test -f alembic/env.py &amp;&amp; test -f alembic/script.py.mako &amp;&amp; test -f alembic/versions/.gitkeep &amp;&amp; uv run python -c "from alembic.config import Config; cfg = Config('alembic.ini'); assert cfg.get_main_option('script_location') == 'alembic'"</automated>
  </verify>
  <acceptance_criteria>
    - `test -f alembic.ini` exits 0
    - `test -f alembic/env.py` exits 0
    - `test -f alembic/script.py.mako` exits 0
    - `test -f alembic/versions/.gitkeep` exits 0
    - `grep -q "^script_location = alembic" alembic.ini`
    - `grep -q "prepend_sys_path = . src" alembic.ini`
    - `grep -q "from bet_maker.settings.config import BetMakerSettings" alembic/env.py`
    - `grep -q "async_engine_from_config" alembic/env.py`
    - `grep -q "run_async_migrations" alembic/env.py`
    - `grep -q "config.set_main_option(\"sqlalchemy.url\", str(settings.postgres_dsn))" alembic/env.py`
    - `grep -c '^sqlalchemy.url' alembic.ini` returns 0 (NO hardcoded DSN in alembic.ini — Anti-Pattern 7 mitigation)
    - `uv run python -c "from alembic.config import Config; Config('alembic.ini')"` exits 0
    - `uv run python -c "import sys; sys.path.insert(0, 'src'); import alembic.env" 2>&1 | grep -v "Error" || true` — env.py is at least syntactically valid
    - `uv run python -c "import ast; ast.parse(open('alembic/env.py').read())"` exits 0
    - No emoji in alembic.ini / env.py / script.py.mako
  </acceptance_criteria>
  <done>
    alembic.ini + alembic/env.py + alembic/script.py.mako + alembic/versions/ существуют; env.py читает DSN из BetMakerSettings; миграции готовы к созданию в P3.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HTTP client → /health | Untrusted public endpoint (compose network) |
| .env / Settings → BetMakerSettings | PG/RMQ DSNs may contain credentials; never logged |
| Alembic CLI → DB | Migrations executed with elevated DB role; should be controlled by deployer |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Information Disclosure | /health echoes internal config | mitigate | /health returns only `{"status": "ok"}` per D-19; future deep pings in P3/P5 must follow same minimality |
| T-04-02 | Information Disclosure | Alembic prints DSN to stderr in logs | mitigate | alembic.ini's [logger_sqlalchemy] set to WARNING; env.py does not print settings.postgres_dsn; structlog not used at migration time so no secret leakage |
| T-04-03 | Tampering | alembic.ini hardcoded URL clashes with env.py override | mitigate | alembic.ini has NO `sqlalchemy.url` line at all (Anti-Pattern 7); env.py calls config.set_main_option at runtime — single source of truth |
| T-04-04 | Elevation of Privilege | downgrade migrations run unintentionally | accept | Out of scope for P1 (no migrations yet); P3 will create initial migration with upgrade only |
| T-04-05 | Spoofing | X-Request-ID header injection | mitigate | Same pattern as line_provider plan 03 — uuid4 fallback; never used as authz |
</threat_model>

<verification>
- `uv run mypy --strict src/bet_maker/` exits 0
- `uv run ruff check src/bet_maker/` exits 0
- `uv run ruff format --check src/bet_maker/` exits 0
- ASGI smoke-check from Task 1b step C prints "bet_maker smoke ok"
- `uv run python -c "from alembic.config import Config; Config('alembic.ini')"` exits 0
- `grep -c '^sqlalchemy.url' alembic.ini` returns 0
- `grep -rn "clear_contextvars" src/bet_maker/` shows two occurrences in middleware.py (defensive entry + finally cleanup, A7)
</verification>

<success_criteria>
- bet_maker запускается через `python -m bet_maker` на 0.0.0.0:8001 (D-03)
- BetMakerSettings содержит postgres_dsn, rabbitmq_url, line_provider_base_url, reconciliation_interval_s=30 (D-15)
- /health возвращает 200 (D-19); deep dep-pings отложены в P3/P5
- structlog + request_id middleware (D-17, D-18, A7) — двойной clear_contextvars: defensive на входе + cleanup в finally
- Alembic async-template настроен; env.py читает DSN из settings (INFR-06, Anti-Pattern 7)
- mypy strict + ruff зелёные
</success_criteria>

<output>
After completion, create `.planning/phases/01-skeleton-infrastructure/01-04-SUMMARY.md` documenting the bet_maker skeleton, the alembic async setup, и confirmation that ASGI smoke-check passes. Note the double clear_contextvars pattern (A7) — defensive entry + finally cleanup, symmetric with line_provider.
</output>
