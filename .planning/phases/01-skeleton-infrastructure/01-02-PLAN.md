---
phase: 01-skeleton-infrastructure
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - src/config/__init__.py
  - src/config/logging.py
  - src/config/settings_base.py
  - src/config/time.py
  - src/config/py.typed
autonomous: true
requirements: [INFR-07, INFR-08]
requirements_addressed: [INFR-07, INFR-08]
tags: [config, structlog, pydantic-settings, shared]

must_haves:
  truths:
    - "src/config/ существует как shared internal-only пакет (D-02), импортируется обоими сервисами"
    - "configure_structlog(level: str) настраивает JSON-вывод с processors=[contextvars, add_log_level, TimeStamper iso utc, dict_tracebacks, JSONRenderer] (D-17)"
    - "BaseAppSettings(BaseSettings) родитель для LineProviderSettings / BetMakerSettings; поля log_level, service_name"
    - "utc_now() возвращает aware datetime UTC — центральная точка для freeze_time в тестах (D-02)"
    - "py.typed маркер присутствует — mypy strict видит пакет как типизированный"
  artifacts:
    - path: "src/config/__init__.py"
      provides: "Package init, re-exports configure_structlog / BaseAppSettings / utc_now"
      contains: "configure_structlog"
    - path: "src/config/logging.py"
      provides: "configure_structlog(level) — structlog JSON setup with bind_contextvars-ready processors"
      contains: "JSONRenderer"
    - path: "src/config/settings_base.py"
      provides: "BaseAppSettings(BaseSettings) parent class"
      contains: "log_level"
    - path: "src/config/time.py"
      provides: "utc_now() centralised for freeze_time"
      contains: "def utc_now"
    - path: "src/config/py.typed"
      provides: "PEP 561 marker for mypy strict"
      min_lines: 0
  key_links:
    - from: "src/line_provider/* (next plan)"
      to: "src/config.logging.configure_structlog"
      via: "import"
      pattern: "from config.logging import configure_structlog"
    - from: "src/bet_maker/* (next plan)"
      to: "src/config.logging.configure_structlog"
      via: "import"
      pattern: "from config.logging import configure_structlog"
---

<objective>
Создать shared internal-only пакет `src/config/` — единая точка для structlog setup, базового класса pydantic-settings и `utc_now()`. Оба сервиса будут импортировать отсюда, что устраняет дублирование и даёт одну точку patching в тестах.

Purpose: D-02 (shared `src/config/`), D-17 (structlog processors), D-18 (bind_contextvars-ready), A7 из PITFALLS (clear_contextvars-pattern требует правильно настроенных processors), INFR-07 (pydantic-settings), INFR-08 (structlog JSON).

Output: 4 модуля + py.typed marker.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-skeleton-infrastructure/01-CONTEXT.md
@.planning/research/STACK.md
@.planning/research/PITFALLS.md
@pyproject.toml
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create src/config/ package with logging.py and time.py</name>
  <files>src/config/__init__.py, src/config/logging.py, src/config/time.py, src/config/py.typed</files>
  <read_first>
    - .planning/phases/01-skeleton-infrastructure/01-CONTEXT.md (D-02, D-17, D-18)
    - .planning/research/PITFALLS.md sections "Pitfall A7: structlog contextvars cross-task contamination" (lines 494-522)
    - pyproject.toml (verify structlog>=25.5 is locked)
  </read_first>
  <behavior>
    - configure_structlog("INFO") sets stdlib root logger level to INFO and structlog processors chain
    - First log call after configure produces a single JSON line on stdout with `timestamp`, `level`, `event`, no traceback unless exception
    - bind_contextvars/clear_contextvars from structlog.contextvars work (smoke-check via repl)
    - utc_now() returns timezone-aware datetime with tzinfo == timezone.utc
  </behavior>
  <action>
Создать `src/config/__init__.py`:

```python
from config.logging import configure_structlog
from config.settings_base import BaseAppSettings
from config.time import utc_now

__all__ = ["BaseAppSettings", "configure_structlog", "utc_now"]
```

Создать `src/config/py.typed` — пустой файл (PEP 561 marker).

Создать `src/config/time.py`:

```python
from __future__ import annotations

from datetime import datetime, timezone


def utc_now() -> datetime:
    return datetime.now(timezone.utc)
```

Создать `src/config/logging.py` (точные processors из D-17, без emoji в коде/комментариях):

```python
from __future__ import annotations

import logging
import sys
from typing import Any

import structlog


def configure_structlog(level: str = "INFO") -> None:
    log_level = getattr(logging, level.upper(), logging.INFO)

    logging.basicConfig(
        format="%(message)s",
        stream=sys.stdout,
        level=log_level,
    )

    structlog.configure(
        processors=[
            structlog.contextvars.merge_contextvars,
            structlog.processors.add_log_level,
            structlog.processors.TimeStamper(fmt="iso", utc=True),
            structlog.processors.dict_tracebacks,
            structlog.processors.JSONRenderer(),
        ],
        wrapper_class=structlog.make_filtering_bound_logger(log_level),
        context_class=dict[str, Any],
        logger_factory=structlog.PrintLoggerFactory(file=sys.stdout),
        cache_logger_on_first_use=True,
    )
```

Никакой шаг не должен принимать decisions, отложенные в `<deferred>`: никаких pytest-cov порогов, никаких OpenTelemetry processors.
  </action>
  <verify>
    <automated>uv run python -c "from config.logging import configure_structlog; from config.time import utc_now; configure_structlog('INFO'); import structlog; structlog.get_logger().info('boot.ok'); print(utc_now().tzinfo)"</automated>
  </verify>
  <acceptance_criteria>
    - `test -f src/config/__init__.py` exits 0
    - `test -f src/config/py.typed` exits 0
    - `test -f src/config/logging.py` exits 0
    - `test -f src/config/time.py` exits 0
    - `grep -q "from config.logging import configure_structlog" src/config/__init__.py`
    - `grep -q "JSONRenderer" src/config/logging.py`
    - `grep -q "merge_contextvars" src/config/logging.py`
    - `grep -q "TimeStamper(fmt=\"iso\", utc=True)" src/config/logging.py`
    - `grep -q "dict_tracebacks" src/config/logging.py`
    - `grep -q "def utc_now" src/config/time.py`
    - `grep -q "timezone.utc" src/config/time.py`
    - `uv run python -c "from config.logging import configure_structlog; configure_structlog('INFO')"` exits 0
    - `uv run python -c "from config.time import utc_now; from datetime import timezone; assert utc_now().tzinfo == timezone.utc"` exits 0
    - `uv run mypy --strict src/config/logging.py src/config/time.py src/config/__init__.py` exits 0
    - No emoji in any file: `python3 -c "import re; files=['src/config/__init__.py','src/config/logging.py','src/config/time.py']; [exit(1) for f in files if re.search(r'[\U0001F300-\U0001FAFF\U00002600-\U000027BF]', open(f).read())]"` exits 0
  </acceptance_criteria>
  <done>
    src/config/ существует как импортируемый пакет; configure_structlog настраивает JSON-выход; utc_now() возвращает aware UTC datetime; mypy strict зелёный.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Create src/config/settings_base.py with BaseAppSettings parent</name>
  <files>src/config/settings_base.py</files>
  <read_first>
    - src/config/__init__.py (re-export needs BaseAppSettings)
    - .planning/phases/01-skeleton-infrastructure/01-CONTEXT.md (D-15, D-16)
    - .planning/research/STACK.md "pydantic-settings" row
  </read_first>
  <behavior>
    - BaseAppSettings reads from environment (case-insensitive); accepts .env file via model_config
    - log_level defaults to "INFO" (str)
    - service_name has no default — must be supplied by subclass (LineProviderSettings, BetMakerSettings) or env
    - Unknown env vars are ignored (so two services sharing one .env file each pick their own prefix in subclasses)
  </behavior>
  <action>
Создать `src/config/settings_base.py`:

```python
from __future__ import annotations

from pydantic import Field
from pydantic_settings import BaseSettings, SettingsConfigDict


class BaseAppSettings(BaseSettings):
    model_config = SettingsConfigDict(
        env_file=".env",
        env_file_encoding="utf-8",
        case_sensitive=False,
        extra="ignore",
    )

    service_name: str = Field(..., description="Logical service name for logs and OpenAPI title")
    log_level: str = Field(default="INFO", description="Root log level: DEBUG / INFO / WARNING / ERROR")
```

Subclasses в планах 03/04 переопределят `model_config` для добавления `env_prefix` (`LINE_PROVIDER_` / `BET_MAKER_`).
  </action>
  <verify>
    <automated>uv run python -c "from config.settings_base import BaseAppSettings; s = BaseAppSettings(service_name='test'); assert s.log_level == 'INFO' and s.service_name == 'test'"</automated>
  </verify>
  <acceptance_criteria>
    - `test -f src/config/settings_base.py` exits 0
    - `grep -q "class BaseAppSettings(BaseSettings)" src/config/settings_base.py`
    - `grep -q "env_file=\".env\"" src/config/settings_base.py`
    - `grep -q "case_sensitive=False" src/config/settings_base.py`
    - `grep -q "extra=\"ignore\"" src/config/settings_base.py`
    - `grep -q "service_name:" src/config/settings_base.py`
    - `grep -q "log_level:" src/config/settings_base.py`
    - `uv run python -c "from config.settings_base import BaseAppSettings; s = BaseAppSettings(service_name='test'); assert s.log_level == 'INFO'"` exits 0
    - `uv run mypy --strict src/config/settings_base.py` exits 0
    - No emoji: `python3 -c "import re; assert not re.search(r'[\U0001F300-\U0001FAFF\U00002600-\U000027BF]', open('src/config/settings_base.py').read())"` exits 0
  </acceptance_criteria>
  <done>
    BaseAppSettings импортируется, читает .env, поля service_name (обязательное) и log_level (default INFO) валидируются, extra ignored, mypy strict зелёный.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| .env file → process | Untrusted at deploy time (operator provides secrets; .env never committed) |
| stdout logs → log collector | Logs may contain sensitive payload data if not careful |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Information Disclosure | structlog logs leak secrets (env vars, message bodies with PII) | mitigate | configure_structlog uses JSONRenderer with dict_tracebacks but no auto-binding of env state; per D-18, request_id only is bound, not request bodies; downstream phases must follow this rule |
| T-02-02 | Tampering | log injection (newline / JSON escape in user-controlled fields) | mitigate | JSONRenderer escapes structured data correctly; user-controlled strings go into structured fields, never into the `event` log key directly |
| T-02-03 | Information Disclosure | BaseAppSettings reads .env which may contain DSN with credentials | accept | Standard for pydantic-settings; .env stays out of git via plan 01 .gitignore; .env.example (plan 05) uses placeholders only |
| T-02-04 | Spoofing | service_name unset → logs misattributed across two services | mitigate | service_name is required (no default) — subclasses must set it via env or default |
</threat_model>

<verification>
- `uv run mypy --strict src/config/` exits 0
- `uv run ruff check src/config/` exits 0
- `uv run ruff format --check src/config/` exits 0
- `uv run python -c "from config import BaseAppSettings, configure_structlog, utc_now; print('imports ok')"` prints "imports ok"
</verification>

<success_criteria>
- src/config/ существует как импортируемый shared пакет (D-02)
- configure_structlog настраивает structlog с точно processors из D-17 (включая merge_contextvars для bind/clear pattern)
- BaseAppSettings — родитель с log_level, service_name, чтением .env
- utc_now() — единая точка для timezone-aware UTC времени
- mypy --strict зелёный по всему пакету
</success_criteria>

<output>
After completion, create `.planning/phases/01-skeleton-infrastructure/01-02-SUMMARY.md` documenting the shared config surface, processors order, и any deviations from D-02/D-17.
</output>
