---
phase: 01-skeleton-infrastructure
plan: 07
type: execute
wave: 4
depends_on: [01, 03, 04]
files_modified:
  - tests/__init__.py
  - tests/conftest.py
  - tests/line_provider/__init__.py
  - tests/line_provider/conftest.py
  - tests/line_provider/test_health.py
  - tests/bet_maker/__init__.py
  - tests/bet_maker/conftest.py
  - tests/bet_maker/test_health.py
  - tests/e2e/__init__.py
  - README.md
autonomous: true
requirements: [QA-10, INFR-08]
requirements_addressed: [QA-10, INFR-08]
tags: [tests, readme, smoke, asgi, request-id]

must_haves:
  truths:
    - "tests/ дерево создано по D-12: conftest корень, line_provider/, bet_maker/, e2e/ (e2e в P1 пустая)"
    - "tests/line_provider/test_health.py использует httpx.AsyncClient(transport=ASGITransport(app=build_app())) и ассертит status_code==200 + body=={\"status\":\"ok\"} (D-11, QA-10)"
    - "tests/bet_maker/test_health.py делает то же самое (D-11, QA-10)"
    - "test_health_echoes_request_id_header — единственная HTTP-уровневая E2E-проверка INFR-08 (X-Request-ID echo через RequestContextMiddleware) в P1; логирование-уровень INFR-08 валидируется в плане 02 (configure_structlog processors)"
    - "Тесты не публикуют PG/RMQ-порты — работают через ASGI, не нужны живые контейнеры в P1"
    - "`uv run pytest -q` собирает и проходит оба smoke-теста зелёным"
    - "README.md существует как stub с разделами Quick start / Architecture (TODO) / Development / Reliability (TODO) (D-14)"
    - "README содержит curl-команды :8000/health и :8001/health"
    - "README содержит CI badge ссылку на .github/workflows/ci.yml"
    - "README содержит явный дисклеймер про дефолтные credentials guest:guest (local-dev only, bound to 127.0.0.1, не для production)"
    - "Никакой emoji ни в README, ни в тестах (хард-правило проекта)"
  artifacts:
    - path: "tests/conftest.py"
      provides: "Empty root conftest (D-12)"
      min_lines: 0
    - path: "tests/line_provider/test_health.py"
      provides: "ASGITransport smoke test for line_provider /health + INFR-08 X-Request-ID echo E2E"
      contains: "ASGITransport"
    - path: "tests/bet_maker/test_health.py"
      provides: "ASGITransport smoke test for bet_maker /health + INFR-08 X-Request-ID echo E2E"
      contains: "ASGITransport"
    - path: "tests/e2e/__init__.py"
      provides: "Empty e2e package (populated in P6)"
      min_lines: 0
    - path: "README.md"
      provides: "Project stub with Quick start + Development sections (D-14) + guest:guest disclaimer"
      contains: "Quick start"
  key_links:
    - from: "tests/line_provider/test_health.py"
      to: "src/line_provider/app.py"
      via: "from line_provider.app import build_app"
      pattern: "build_app"
    - from: "tests/bet_maker/test_health.py"
      to: "src/bet_maker/app.py"
      via: "from bet_maker.app import build_app"
      pattern: "build_app"
    - from: "tests/{service}/test_health.py::test_health_echoes_request_id_header"
      to: "src/{service}/entrypoints/middleware.py (RequestContextMiddleware) — INFR-08 E2E"
      via: "asserts X-Request-ID present and non-empty"
      pattern: "X-Request-ID"
    - from: "README.md"
      to: ".github/workflows/ci.yml"
      via: "CI badge URL"
      pattern: "workflows/ci.yml"
---

<objective>
Создать тестовый каркас (один smoke-тест на сервис через ASGITransport — D-11) и README stub (D-14). Это закрывает CI шаг `uv run pytest -q` зелёным с P1 (есть что запускать) и даёт ревьюеру entry-point в проект.

Покрытие требований:
- **QA-10** — `uv run pytest -q` зелёный, smoke /health для обоих сервисов.
- **INFR-08** — этот план обеспечивает **HTTP-уровневую E2E-проверку** structlog request-id propagation: `test_health_echoes_request_id_header` ассертит, что `RequestContextMiddleware` биндит request_id через `bind_contextvars` и эхо-возвращает его в `X-Request-ID` response header. Это единственный HTTP-level E2E-test INFR-08 в P1. Логирование-уровень INFR-08 (processors order, JSONRenderer, merge_contextvars) уже валидируется юнитарно в плане 02 (configure_structlog imports + structure greps).

Purpose: D-11 (smoke-тесты через httpx.AsyncClient + ASGITransport), D-12 (структура tests/), D-13 (pytest-cov не настраивается до P7), D-14 (README stub разделы), INFR-08 (E2E request-id echo).

Output: 9 test-файлов + README.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-skeleton-infrastructure/01-CONTEXT.md
@.planning/research/ARCHITECTURE.md
@src/line_provider/app.py
@src/bet_maker/app.py

<interfaces>
<!-- Имена, на которые опираются тесты — выписаны из планов 03/04 -->
From src/line_provider/app.py:
```python
def build_app() -> FastAPI: ...
```

From src/bet_maker/app.py:
```python
def build_app() -> FastAPI: ...
```

Both /health endpoints return 200 with `{"status": "ok"}` and echo `X-Request-ID` header (per planes 03/04 middleware — INFR-08).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create tests/ skeleton with two ASGI smoke tests on /health (incl. INFR-08 X-Request-ID echo)</name>
  <files>tests/__init__.py, tests/conftest.py, tests/line_provider/__init__.py, tests/line_provider/conftest.py, tests/line_provider/test_health.py, tests/bet_maker/__init__.py, tests/bet_maker/conftest.py, tests/bet_maker/test_health.py, tests/e2e/__init__.py</files>
  <read_first>
    - src/line_provider/app.py (build_app factory)
    - src/bet_maker/app.py (build_app factory)
    - src/line_provider/entrypoints/middleware.py (RequestContextMiddleware — INFR-08 implementation)
    - pyproject.toml [tool.pytest.ini_options] — verify asyncio_mode=auto + pythonpath=src
    - .planning/phases/01-skeleton-infrastructure/01-CONTEXT.md (D-11, D-12, D-13, INFR-08)
  </read_first>
  <behavior>
    - `uv run pytest -q` collects exactly 4 tests (2 per service)
    - All tests pass
    - Tests do not require docker; ASGITransport drives the apps in-process
    - Tests do not depend on real PG/RMQ
    - **`test_health_echoes_request_id_header` is the HTTP-level E2E proof of INFR-08** — without RequestContextMiddleware binding `request_id` via `bind_contextvars` and echoing it via response header, this test fails
  </behavior>
  <action>
Шаг A — `tests/__init__.py` — пустой файл (делает tests importable пакетом).

Шаг B — `tests/conftest.py` — пустой плюс docstring-комментарий:

```python
"""Root conftest. Shared fixtures land here as the test suite grows (P3+)."""
```

Шаг C — `tests/line_provider/__init__.py` — пустой.

Шаг D — `tests/line_provider/conftest.py`:

```python
from __future__ import annotations

from collections.abc import AsyncIterator

import pytest_asyncio
from httpx import ASGITransport, AsyncClient

from line_provider.app import build_app


@pytest_asyncio.fixture
async def client() -> AsyncIterator[AsyncClient]:
    app = build_app()
    transport = ASGITransport(app=app)
    async with AsyncClient(transport=transport, base_url="http://test") as ac:
        yield ac
```

Шаг E — `tests/line_provider/test_health.py` (имена тестов прописывают QA-10 и INFR-08 для трассировки):

```python
"""Smoke tests for line_provider /health endpoint.

QA-10: pytest must collect and pass green.
INFR-08: HTTP-level E2E proof of structlog request-id propagation via
RequestContextMiddleware — X-Request-ID is echoed back in response headers
(logging-side INFR-08 validated unit-wise in plan 02).
"""

from __future__ import annotations

from httpx import AsyncClient


async def test_health_returns_status_ok(client: AsyncClient) -> None:
    """QA-10: /health returns 200 with {"status": "ok"}."""
    response = await client.get("/health")
    assert response.status_code == 200
    assert response.json() == {"status": "ok"}


async def test_health_echoes_request_id_header(client: AsyncClient) -> None:
    """INFR-08 (HTTP-level E2E): RequestContextMiddleware binds request_id via
    bind_contextvars and echoes X-Request-ID header on response. Without correct
    middleware wiring (bind_contextvars + finally clear_contextvars), this fails.
    """
    response = await client.get("/health")
    assert "X-Request-ID" in response.headers
    assert len(response.headers["X-Request-ID"]) > 0
```

Шаг F — `tests/bet_maker/__init__.py` — пустой.

Шаг G — `tests/bet_maker/conftest.py` (зеркалит line_provider):

```python
from __future__ import annotations

from collections.abc import AsyncIterator

import pytest_asyncio
from httpx import ASGITransport, AsyncClient

from bet_maker.app import build_app


@pytest_asyncio.fixture
async def client() -> AsyncIterator[AsyncClient]:
    app = build_app()
    transport = ASGITransport(app=app)
    async with AsyncClient(transport=transport, base_url="http://test") as ac:
        yield ac
```

Шаг H — `tests/bet_maker/test_health.py`:

```python
"""Smoke tests for bet_maker /health endpoint.

QA-10: pytest must collect and pass green.
INFR-08: HTTP-level E2E proof of structlog request-id propagation via
RequestContextMiddleware — X-Request-ID is echoed back in response headers
(logging-side INFR-08 validated unit-wise in plan 02).
"""

from __future__ import annotations

from httpx import AsyncClient


async def test_health_returns_status_ok(client: AsyncClient) -> None:
    """QA-10: /health returns 200 with {"status": "ok"}."""
    response = await client.get("/health")
    assert response.status_code == 200
    assert response.json() == {"status": "ok"}


async def test_health_echoes_request_id_header(client: AsyncClient) -> None:
    """INFR-08 (HTTP-level E2E): RequestContextMiddleware binds request_id via
    bind_contextvars and echoes X-Request-ID header on response. Without correct
    middleware wiring (bind_contextvars + finally clear_contextvars), this fails.
    """
    response = await client.get("/health")
    assert "X-Request-ID" in response.headers
    assert len(response.headers["X-Request-ID"]) > 0
```

Шаг I — `tests/e2e/__init__.py` — пустой (D-12: каталог существует, заполняется в P6).
  </action>
  <verify>
    <automated>uv run pytest -q tests/line_provider/test_health.py tests/bet_maker/test_health.py</automated>
  </verify>
  <acceptance_criteria>
    - All 9 test-files from steps A-I exist (`test -f` each)
    - `grep -q "ASGITransport" tests/line_provider/conftest.py`
    - `grep -q "ASGITransport" tests/bet_maker/conftest.py`
    - `grep -q "from line_provider.app import build_app" tests/line_provider/conftest.py`
    - `grep -q "from bet_maker.app import build_app" tests/bet_maker/conftest.py`
    - `grep -q 'response.json() == {"status": "ok"}' tests/line_provider/test_health.py`
    - `grep -q 'response.json() == {"status": "ok"}' tests/bet_maker/test_health.py`
    - **INFR-08 trace explicit in test files:**
      - `grep -q "INFR-08" tests/line_provider/test_health.py` (module docstring + test docstring cite the requirement)
      - `grep -q "INFR-08" tests/bet_maker/test_health.py`
      - `grep -q "test_health_echoes_request_id_header" tests/line_provider/test_health.py`
      - `grep -q "test_health_echoes_request_id_header" tests/bet_maker/test_health.py`
    - `uv run pytest -q --collect-only tests/ 2>&1 | grep -c 'test_health' | awk '{ exit ($1 < 4) }'` exits 0 (4 tests collected: 2 per service)
    - `uv run pytest -q` exits 0 with all tests passing
    - `uv run mypy --strict tests/` exits 0
    - `uv run ruff check tests/` exits 0
    - No emoji in any test file
  </acceptance_criteria>
  <done>
    Tests collect & pass; 4 smoke-теста (2 на сервис) бьются в /health через ASGITransport; QA-10 (pytest зелёный) и INFR-08 (X-Request-ID echo) трассируются по именам и docstrings; mypy + ruff чисты.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Create README.md stub with Quick start, Development, Architecture/Reliability placeholders (D-14) + guest:guest disclaimer</name>
  <files>README.md</files>
  <read_first>
    - .planning/phases/01-skeleton-infrastructure/01-CONTEXT.md (D-14 — точная структура stub)
    - .github/workflows/ci.yml (badge URL pattern)
    - docker-compose.yml (для Quick start)
    - .planning/PROJECT.md (one-paragraph project description)
  </read_first>
  <behavior>
    - README parses as valid Markdown
    - Has H1 title, Quick start with `docker compose up` + two curl commands, Development with uv commands, Architecture TODO link, Reliability TODO
    - Has CI badge URL
    - **Has explicit disclaimer about `guest:guest` RabbitMQ credentials immediately after the line that mentions Management UI** (test-task scope, bound to 127.0.0.1, not for production deploys)
    - No emoji anywhere
  </behavior>
  <action>
Создать `README.md` в корне:

```markdown
# BSW Betting System

[![ci](https://github.com/OWNER/REPO/actions/workflows/ci.yml/badge.svg)](https://github.com/OWNER/REPO/actions/workflows/ci.yml)

Тестовое задание Middle Python developer: микросервисная система приёма ставок на спортивные события. Два асинхронных сервиса (`line-provider`, `bet-maker`), интеграция через RabbitMQ, история ставок в PostgreSQL, reconciliation как защита от потерянных сообщений.

Полная архитектура и требования: [.planning/PROJECT.md](.planning/PROJECT.md), [.planning/ROADMAP.md](.planning/ROADMAP.md).

## Quick start

Запуск всего стека из корня репозитория:

```bash
cp .env.example .env
docker compose up -d
```

Дождаться `(healthy)` на всех сервисах (около 30 секунд):

```bash
docker compose ps
```

Проверить health-эндпоинты обоих сервисов:

```bash
curl -s http://localhost:8000/health   # line-provider
curl -s http://localhost:8001/health   # bet-maker
```

Оба должны вернуть `{"status":"ok"}`.

RabbitMQ Management UI: http://127.0.0.1:15672 (логин/пароль: `guest` / `guest`).

> **Note:** `guest:guest` — это дефолтные test-credentials RabbitMQ, использующиеся только в локальной разработке. Management UI забинден на `127.0.0.1` (loopback) и недоступен извне хоста; AMQP-порт 5672 не публикуется наружу. Эти credentials НЕ предназначены для production или любого non-local развёртывания — перед публичным деплоем нужно сменить и применить тонкую настройку доступа RabbitMQ.

Остановить стек:

```bash
docker compose down
```

## Development

Установить зависимости (требуется `uv` 0.11.x, Python 3.10.20):

```bash
uv sync
```

Запустить сервисы локально (без Docker, нужны PG + RabbitMQ доступными):

```bash
uv run python -m line_provider   # порт 8000
uv run python -m bet_maker       # порт 8001
```

Линтеры и тесты:

```bash
uv run ruff check .
uv run ruff format --check .
uv run mypy src
uv run pytest -q
```

Установить pre-commit хуки в локальный clone:

```bash
uv run pre-commit install
```

Миграции для bet-maker (создаются начиная с Phase 3):

```bash
uv run alembic upgrade head
```

## Architecture

TODO: подробности в [.planning/research/ARCHITECTURE.md](.planning/research/ARCHITECTURE.md). Будет наполнено в Phase 7.

## Reliability

TODO: гарантии durable queue, manual ack, DLQ, reconciliation job, `FOR UPDATE SKIP LOCKED`. Будет наполнено в Phase 7.

## Project status

| Phase | Description | Status |
|-------|-------------|--------|
| 1 | Skeleton + Infrastructure | in progress |
| 2 | line-provider domain | pending |
| 3 | bet-maker domain (DB) | pending |
| 4 | bet-maker HTTP integration | pending |
| 5 | RabbitMQ integration | pending |
| 6 | Reconciliation job | pending |
| 7 | Polish + Documentation | pending |
```

Замечания:
- `OWNER/REPO` в badge — placeholder; реальный owner и название репозитория подставит автор репозитория после `git remote add` (документировать в SUMMARY этого плана как known follow-up для P7).
- Никаких emoji, никаких ASCII-арт-баннеров.
- "Architecture" и "Reliability" — explicit TODO с ссылкой на research/ — это D-14: «пустой с TODO ссылкой».
- Project status таблица — это maybe-полезный для ревьюера сигнал, что фазы планируются, P1 — start.
- **Дисклеймер про guest:guest** — прямо после строки с Management UI URL; явно указывает test-task scope, loopback binding, AMQP-порт не открыт наружу, и недопустимость production-использования.
  </action>
  <verify>
    <automated>test -f README.md &amp;&amp; grep -q "^# BSW Betting System" README.md &amp;&amp; grep -q "## Quick start" README.md &amp;&amp; grep -q "docker compose up" README.md</automated>
  </verify>
  <acceptance_criteria>
    - `test -f README.md` exits 0
    - `grep -q "^# BSW Betting System" README.md`
    - `grep -q "## Quick start" README.md`
    - `grep -q "## Development" README.md`
    - `grep -q "## Architecture" README.md`
    - `grep -q "## Reliability" README.md`
    - `grep -q "docker compose up -d" README.md`
    - `grep -q "curl -s http://localhost:8000/health" README.md`
    - `grep -q "curl -s http://localhost:8001/health" README.md`
    - `grep -q "uv sync" README.md`
    - `grep -q "uv run ruff check" README.md`
    - `grep -q "uv run mypy src" README.md`
    - `grep -q "uv run pytest -q" README.md`
    - `grep -q "workflows/ci.yml/badge.svg" README.md` (CI badge)
    - `grep -q "127.0.0.1:15672" README.md` (correct Management UI binding documented)
    - **guest:guest disclaimer required (rendered in README text, not only inferred):**
      - `grep -q "guest:guest" README.md` OR `grep -q "guest\` / \`guest" README.md` (mention exists)
      - `grep -E "(test-credentials|test credentials|local|loopback|127\.0\.0\.1)" README.md | grep -i -E "(guest|production|local-dev|do not use|не предназначены|не для production)" | head -1 | wc -l | awk '{ exit ($1 < 1) }'` exits 0 (disclaimer string is present)
      - Robust grep alternatives accepted: any of `grep -q "не предназначены для production" README.md`, `grep -q "Do not use" README.md`, `grep -q "test-credentials RabbitMQ" README.md` — at least one must match
    - `grep -q "TODO" README.md` (Architecture and Reliability TODOs)
    - No emoji: `python3 -c "import re; assert not re.search(r'[\U0001F300-\U0001FAFF\U00002600-\U000027BF\U0001F1E0-\U0001F1FF]', open('README.md').read())"` exits 0
  </acceptance_criteria>
  <done>
    README.md существует с H1, разделами Quick start / Development / Architecture (TODO) / Reliability (TODO), CI badge ссылкой, curl-командами для обоих /health, дисклеймером про guest:guest credentials (loopback-only, test-task scope, не для production), и project status таблицей. Никаких emoji.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test client → app (ASGI) | Trusted (in-process tests run the app directly) |
| README → end user (reviewer) | Trusted (developer-facing doc) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Information Disclosure | README leaks default credentials (`guest/guest`, `bsw/bsw`) without scope warning | mitigate | Test-task defaults documented WITH explicit disclaimer immediately after Management UI mention: scope=local-dev only, bound to 127.0.0.1, не для production. Reviewer sees the credentials and the constraint together |
| T-07-02 | Tampering | smoke test passes locally but fails in CI due to missing env | mitigate | Tests use ASGITransport (no env required); BetMakerSettings has defaults for all fields per plan 04; collected with `uv run pytest -q` in CI workflow |
| T-07-03 | Spoofing | CI badge URL points at wrong repo → false-positive green status | accept | OWNER/REPO placeholder; documented as P7 follow-up; not a security issue, just a presentation gap |
| T-07-04 | Information Disclosure | tests write logs to stdout that leak request_id (PII?) | accept | request_id is uuid4 — no identity binding; structlog JSONRenderer outputs structured events; no real bodies in P1 |
| T-07-05 | Repudiation | INFR-08 X-Request-ID echo silently broken → request correlation gone, no audit trail | mitigate | `test_health_echoes_request_id_header` per service asserts header presence and non-empty value (E2E HTTP-level proof of RequestContextMiddleware) |
</threat_model>

<verification>
- `test -f tests/conftest.py tests/line_provider/test_health.py tests/bet_maker/test_health.py tests/e2e/__init__.py README.md`
- `uv run pytest -q` exits 0
- `uv run mypy --strict tests/` exits 0
- `uv run ruff check tests/` exits 0
- `grep -q "INFR-08" tests/line_provider/test_health.py && grep -q "INFR-08" tests/bet_maker/test_health.py` (requirement trace explicit in test files)
- README contains exact phrases for Quick start, Development, Architecture TODO, Reliability TODO
- README contains explicit guest:guest disclaimer (loopback-only, test-task scope)
- No emoji anywhere
</verification>

<success_criteria>
- tests/ дерево по D-12 (root conftest, line_provider/, bet_maker/, e2e/ пустая)
- По одному smoke-тесту на сервис через ASGITransport (D-11); + 1 ассерт на X-Request-ID header — единственная HTTP-уровневая E2E-проверка INFR-08 в P1
- `uv run pytest -q` зелёный — CI шаг pytest имеет что выполнить (QA-10)
- INFR-08 трассировка в docstrings + именах тестов (`test_health_echoes_request_id_header`)
- README.md с разделами Quick start / Development и TODO-ссылками на research/ для Architecture/Reliability (D-14)
- README содержит точные curl-команды, CI badge ссылку и явный дисклеймер про guest:guest credentials
- Никакого emoji
</success_criteria>

<output>
After completion, create `.planning/phases/01-skeleton-infrastructure/01-07-SUMMARY.md` documenting the test layout, the smoke-test pattern, the INFR-08 trace via test_health_echoes_request_id_header, the guest:guest disclaimer placement, and the README sections. Note as follow-up: OWNER/REPO placeholder in CI badge — replace once GitHub remote is configured.
</output>
