---
phase: 01-skeleton-infrastructure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - .python-version
  - .gitignore
  - uv.lock
autonomous: true
requirements: [INFR-01, INFR-02, QA-02, QA-10]
requirements_addressed: [INFR-01, INFR-02, QA-02, QA-10]
tags: [python, uv, ruff, mypy, monorepo]

must_haves:
  truths:
    - "Один корневой pyproject.toml существует и валиден по PEP 621"
    - "uv.lock существует и закоммичен в репозиторий"
    - "Distribution name = 'bsw'; requires-python = '>=3.10,<3.11'"
    - "Все рантайм-зависимости пиннованы по диапазонам из CLAUDE.md Technology Stack (fastapi, faststream[rabbit], sqlalchemy[asyncio], asyncpg, alembic, httpx, tenacity, structlog, pydantic, pydantic-settings, uvicorn[standard])"
    - "Dev-группа содержит pytest, pytest-asyncio, pytest-cov, ruff, mypy, pre-commit"
    - "[tool.hatch.build.targets.wheel] packages = ['src/line_provider', 'src/bet_maker', 'src/config']  (D-01)"
    - "[tool.ruff] и [tool.mypy] настроены: ruff правила E,W,F,I,B,UP,N,SIM,ASYNC,PL,RUF; mypy strict=true с pydantic.mypy plugin"
    - "[tool.pytest.ini_options] asyncio_mode = 'auto', pythonpath = ['src'] (D-13)"
    - ".gitignore исключает .venv/, __pycache__/, .env, .pytest_cache/, .coverage, .mypy_cache/, .ruff_cache/, dist/, build/, *.egg-info/"
  artifacts:
    - path: "pyproject.toml"
      provides: "Root project config — deps, build, ruff, mypy, pytest"
      contains: "[project]"
    - path: "uv.lock"
      provides: "Pinned dependency graph"
      min_lines: 50
    - path: ".python-version"
      provides: "Pin uv to Python 3.10.20"
      contains: "3.10.20"
    - path: ".gitignore"
      provides: "Standard Python ignores plus .env"
      contains: ".venv"
  key_links:
    - from: "pyproject.toml"
      to: "uv.lock"
      via: "uv lock"
      pattern: "uv lock --upgrade-package"
    - from: "pyproject.toml [tool.hatch.build.targets.wheel]"
      to: "src/line_provider, src/bet_maker, src/config"
      via: "packages list (D-01)"
      pattern: "packages = \\[.*src/line_provider.*src/bet_maker.*src/config"
---

<objective>
Заложить корневой `pyproject.toml` с единым `uv.lock`, версиями инструментов из CLAUDE.md, чёткой конфигурацией ruff/mypy/pytest и .gitignore. На этом фундаменте стоят все последующие планы.

Purpose: D-01 (single root pyproject, distribution `bsw`, packages = `src/line_provider`, `src/bet_maker`, `src/config`); D-09 (single Python 3.10.20 pin); D-13 (pytest config); INFR-01/02 (monorepo src/ layout + uv).

Output: pyproject.toml, uv.lock, .python-version, .gitignore.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-skeleton-infrastructure/01-CONTEXT.md
@.planning/research/STACK.md
@CLAUDE.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create pyproject.toml with full dependency, build, ruff, mypy, pytest config</name>
  <files>pyproject.toml</files>
  <read_first>
    - CLAUDE.md (Technology Stack section — exact version pins and rationale)
    - .planning/research/STACK.md (Installation section + Version Compatibility Matrix)
    - .planning/phases/01-skeleton-infrastructure/01-CONTEXT.md (D-01, D-09, D-13)
  </read_first>
  <behavior>
    - File pyproject.toml parses as valid TOML
    - `uv sync --dry-run` succeeds (locks resolvable)
    - `uv run ruff check .` works after sync (no src/ yet → ruff finds nothing to check, exit 0)
    - `uv run pytest --collect-only` works after sync (no tests yet → exit non-zero with "no tests collected" is acceptable; valid config is the gate)
    - NOTE: `uv run mypy src` is NOT executed in this wave — src/ directory does not exist yet (materialised by plans 02/03/04). Full mypy gate lands in plan 06 (CI workflow) and plan 07 (smoke tests). Only `uv run mypy --version` proves mypy is installed in Wave 1.
  </behavior>
  <action>
Создать файл `pyproject.toml` со следующим содержимым (точные версии из CLAUDE.md и STACK.md):

```toml
[project]
name = "bsw"
version = "0.1.0"
description = "BSW Betting System — two async Python microservices for a sports-betting test task"
readme = "README.md"
requires-python = ">=3.10,<3.11"
authors = [{ name = "Dmitry Dankov" }]
dependencies = [
    "fastapi>=0.115,<0.137",
    "uvicorn[standard]>=0.46,<0.47",
    "pydantic>=2.13,<3",
    "pydantic-settings>=2.14,<3",
    "faststream[rabbit]>=0.6,<0.7",
    "sqlalchemy[asyncio]>=2.0.40,<2.1",
    "asyncpg>=0.31,<0.32",
    "alembic>=1.18,<2",
    "httpx>=0.28,<0.29",
    "tenacity>=9.1,<10",
    "structlog>=25.5,<26",
]

[dependency-groups]
dev = [
    "pytest>=9.0,<10",
    "pytest-asyncio>=1.1,<2",
    "pytest-cov>=7.1,<8",
    "ruff>=0.15,<0.16",
    "mypy>=2.1,<3",
    "pre-commit>=4.6,<5",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/line_provider", "src/bet_maker", "src/config"]

[tool.ruff]
line-length = 100
target-version = "py310"
src = ["src", "tests"]

[tool.ruff.lint]
select = ["E", "W", "F", "I", "B", "UP", "N", "SIM", "ASYNC", "PL", "RUF"]
ignore = ["PLR0913"]

[tool.ruff.lint.per-file-ignores]
"tests/**" = ["PLR2004", "S101"]
"alembic/versions/*.py" = ["E501", "N999"]

[tool.ruff.format]
quote-style = "double"
indent-style = "space"

[tool.mypy]
strict = true
python_version = "3.10"
plugins = ["pydantic.mypy"]
mypy_path = "src"
explicit_package_bases = true
namespace_packages = true

[[tool.mypy.overrides]]
module = ["tests.*"]
disallow_untyped_defs = false

[tool.pydantic-mypy]
init_forbid_extra = true
init_typed = true
warn_required_dynamic_aliases = true

[tool.pytest.ini_options]
asyncio_mode = "auto"
pythonpath = ["src"]
testpaths = ["tests"]
addopts = "-ra --strict-markers"

[tool.uv]
package = true
```

Не добавлять секции, не указанные выше (не `[tool.coverage]`, не `[tool.uv.workspace]` — D-01 явно НЕ workspace).
  </action>
  <verify>
    <automated>test -f pyproject.toml &amp;&amp; python3 -c "import tomllib; tomllib.loads(open('pyproject.toml').read())"</automated>
  </verify>
  <acceptance_criteria>
    - `test -f pyproject.toml` exits 0
    - `python3 -c "import tomllib; data = tomllib.loads(open('pyproject.toml').read()); assert data['project']['name'] == 'bsw'"` exits 0
    - `grep -q 'requires-python = ">=3.10,<3.11"' pyproject.toml`
    - `grep -q 'packages = \["src/line_provider", "src/bet_maker", "src/config"\]' pyproject.toml`
    - `grep -q 'fastapi>=0.115,<0.137' pyproject.toml`
    - `grep -q 'faststream\[rabbit\]>=0.6,<0.7' pyproject.toml`
    - `grep -q 'sqlalchemy\[asyncio\]>=2.0.40,<2.1' pyproject.toml`
    - `grep -q 'asyncpg>=0.31,<0.32' pyproject.toml`
    - `grep -q 'alembic>=1.18,<2' pyproject.toml`
    - `grep -q 'pydantic-settings>=2.14,<3' pyproject.toml`
    - `grep -q 'tenacity>=9.1,<10' pyproject.toml`
    - `grep -q 'structlog>=25.5,<26' pyproject.toml`
    - `grep -q 'httpx>=0.28,<0.29' pyproject.toml`
    - `grep -q 'uvicorn\[standard\]>=0.46,<0.47' pyproject.toml`
    - `grep -q 'strict = true' pyproject.toml` (mypy section)
    - `grep -q 'asyncio_mode = "auto"' pyproject.toml`
    - `grep -q 'pythonpath = \["src"\]' pyproject.toml`
    - `grep -E 'select = .*"ASYNC".*' pyproject.toml` matches (ruff rule set includes ASYNC)
    - No `[tool.uv.workspace]` section: `grep -c '\[tool.uv.workspace\]' pyproject.toml` returns 0
    - `uv run mypy --version` exits 0 (mypy installed; NOT `uv run mypy src` — see <behavior> note: src/ not yet present in Wave 1)
  </acceptance_criteria>
  <done>
    `pyproject.toml` создан, валиден по TOML и PEP 621, все версии совпадают со STACK.md/CLAUDE.md, hatch packages указывает на три пакета под `src/`. Wave 1 НЕ запускает `uv run mypy src` — src/ материализуется в планах 02/03/04, полный mypy-гейт начинается в плане 06 (CI) и плане 07 (smoke).
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Create .python-version, .gitignore, then generate uv.lock</name>
  <files>.python-version, .gitignore, uv.lock</files>
  <read_first>
    - pyproject.toml (созданный Task 1)
    - .planning/research/STACK.md (Installation — `uv sync --frozen`)
  </read_first>
  <behavior>
    - .python-version pins 3.10.20 (uv reads this on `uv sync`)
    - .gitignore excludes standard Python artefacts, .env, .venv, IDE files
    - `uv lock` succeeds and produces uv.lock
    - `uv sync --frozen` succeeds (lock not stale)
  </behavior>
  <action>
Шаг A — создать `.python-version`:

```
3.10.20
```

Один файл, одна строка, без trailing newline спецсимволов кроме одного `\n`.

Шаг B — создать `.gitignore` со следующим содержимым (без комментариев в коде запрещено правилом проекта, но в .gitignore комментарии — стандарт; короткие группы заголовков допустимы):

```
# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python

# Virtual envs
.venv/
venv/
env/

# Distribution / build
build/
dist/
*.egg-info/
*.egg
.eggs/

# Tooling caches
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
.coverage.*
coverage.xml
htmlcov/

# Env files (NEVER commit .env; .env.example is committed)
.env
.env.local
.env.*.local

# IDE
.idea/
.vscode/
*.swp
.DS_Store

# Alembic — versions are committed, but local-only sqlite snapshots are not
*.sqlite3
*.db
```

Шаг C — сгенерировать lock-файл (требуется работающий `uv` на машине, версия из CLAUDE.md = 0.11.14):

```
uv lock
```

Это создаст `uv.lock` в корне. Если `uv` не установлен, остановиться и поднять сообщение оператору; не пытаться имитировать lock-файл руками.

Шаг D — проверить, что lock детерминированно реcинхронизируется:

```
uv sync --frozen
```

Эта команда должна завершиться 0; никаких dev-зависимостей вне `[dependency-groups] dev` появиться не должно.
  </action>
  <verify>
    <automated>test -f .python-version &amp;&amp; test -f .gitignore &amp;&amp; test -f uv.lock &amp;&amp; uv sync --frozen</automated>
  </verify>
  <acceptance_criteria>
    - `cat .python-version` returns `3.10.20` (exact, single line)
    - `grep -q '^\.env$' .gitignore`
    - `grep -q '^\.venv/$' .gitignore`
    - `grep -q '^__pycache__/$' .gitignore`
    - `grep -q '^\.pytest_cache/$' .gitignore`
    - `grep -q '^\.mypy_cache/$' .gitignore`
    - `grep -q '^\.ruff_cache/$' .gitignore`
    - `test -f uv.lock` exits 0
    - `wc -l uv.lock | awk '{ exit ($1 < 50) }'` exits 0 (lock has substantive content)
    - `uv sync --frozen` exits 0
    - `grep -c 'fastapi' uv.lock` >= 1 (sanity: deps are in the lock)
    - `grep -c 'faststream' uv.lock` >= 1
    - `grep -c 'sqlalchemy' uv.lock` >= 1
  </acceptance_criteria>
  <done>
    `.python-version`, `.gitignore`, `uv.lock` существуют; `uv sync --frozen` завершается успехом; lock содержит все рантайм и dev зависимости.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Repo → CI/runner | Untrusted contributors (none here, solo dev, but lockfile pins still matter) |
| Dependency tree → app | Untrusted upstream packages (pinned by uv.lock SHA-256) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | uv.lock (dependency integrity) | mitigate | Pin major+minor ranges in pyproject.toml; commit uv.lock; CI runs `uv sync --frozen` so any drift fails fast |
| T-01-02 | Information Disclosure | .env vs .env.example | mitigate | `.env` listed in .gitignore at this plan; .env.example produced in plan 05 with placeholder values only (no secrets) |
| T-01-03 | Denial of Service | unpinned rolling deps | mitigate | All deps pinned to `>=X,<Y` ranges from STACK.md; transitive deps pinned by uv.lock |
| T-01-04 | Elevation of Privilege | build-time code execution by malicious dependency | accept | Standard PyPI trust model; uv verifies hashes via uv.lock; no further hardening for a test task |
</threat_model>

<verification>
- `test -f pyproject.toml && test -f uv.lock && test -f .python-version && test -f .gitignore`
- `python3 -c "import tomllib; tomllib.loads(open('pyproject.toml').read())"` exits 0
- `uv sync --frozen` exits 0
- `grep -q 'packages = \["src/line_provider", "src/bet_maker", "src/config"\]' pyproject.toml`
- `grep -q '^\.env$' .gitignore`
- `uv run mypy --version` exits 0 (proves mypy is installed; full `uv run mypy src` deferred to plan 06/07 once src/ is materialised)
</verification>

<success_criteria>
- pyproject.toml парсится, содержит все 11 runtime deps и 6 dev deps по точным диапазонам версий из CLAUDE.md
- hatch packages указывает на три пакета под `src/` (D-01)
- mypy strict, ruff rule set с ASYNC и PL, pytest asyncio_mode=auto + pythonpath=src настроены
- uv.lock сгенерирован и `uv sync --frozen` детерминированно реcинхронизируется
- .gitignore исключает .env и стандартные Python артефакты
- mypy установлен (`uv run mypy --version` зелёный); полный `uv run mypy src` НЕ выполняется в Wave 1 — src/ материализуется в планах 02/03/04
</success_criteria>

<output>
After completion, create `.planning/phases/01-skeleton-infrastructure/01-01-SUMMARY.md` documenting the pyproject schema, the version pins applied, and any deviations (there should be none). Explicitly note that `uv run mypy src` is intentionally deferred to plan 06 (CI workflow) and plan 07 (smoke tests) because src/ materialises in plans 02/03/04.
</output>
