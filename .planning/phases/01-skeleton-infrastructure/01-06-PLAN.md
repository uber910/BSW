---
phase: 01-skeleton-infrastructure
plan: 06
type: execute
wave: 4
depends_on: [01]
files_modified:
  - .github/workflows/ci.yml
  - .pre-commit-config.yaml
autonomous: true
requirements: [QA-02, QA-03, QA-10]
requirements_addressed: [QA-02, QA-03, QA-10]
tags: [ci, github-actions, pre-commit, ruff, mypy, pytest]

must_haves:
  truths:
    - "GitHub Actions workflow .github/workflows/ci.yml существует и валиден по схеме (D-06, D-07)"
    - "CI триггеры: push на любую ветку, pull_request в main (D-07)"
    - "CI job 'quality' на ubuntu-latest выполняет: checkout → setup-uv (с кешем по uv.lock) → uv python install 3.10.20 → uv sync --frozen --all-extras → uv run ruff check . → uv run ruff format --check . → uv run mypy src → uv run pytest -q (D-06)"
    - "Один job, без matrix (D-09)"
    - "Никаких PG/RMQ services в CI (D-08 — отложено до P3/P5)"
    - ".pre-commit-config.yaml содержит хуки: ruff check --fix, ruff format, mypy strict (local hook через uv run), check-merge-conflict, end-of-file-fixer, trailing-whitespace, check-yaml, check-toml, check-added-large-files (D-10)"
    - "pre-commit mypy hook — local entry: uv run mypy --strict src; pass_filenames: false; types_or: [python, pyi] (D-10)"
  artifacts:
    - path: ".github/workflows/ci.yml"
      provides: "CI pipeline: ruff + mypy + pytest on every push and PR to main"
      contains: "uv run mypy"
    - path: ".pre-commit-config.yaml"
      provides: "Local quality gate before commit (D-10)"
      contains: "ruff"
  key_links:
    - from: ".github/workflows/ci.yml"
      to: "pyproject.toml + uv.lock"
      via: "uv sync --frozen"
      pattern: "uv sync --frozen"
    - from: ".pre-commit-config.yaml"
      to: "pyproject.toml ruff/mypy config"
      via: "shared config"
      pattern: "ruff"
---

<objective>
Зашить CI и pre-commit конвейеры качества: ruff + mypy strict + pytest на каждом push/PR (GitHub Actions), плюс локальные хуки перед коммитом. Это закрывает QA-10 (CI), QA-02 (ruff), QA-03 (pre-commit). На уровне CI пока запускается только unit-уровень (smoke /health) — PG/RMQ services добавятся в P3/P5.

Purpose: D-06 (workflow steps), D-07 (triggers), D-08 (нет PG/RMQ в CI на P1), D-09 (single Python pin без matrix), D-10 (pre-commit hooks list).

Output: 2 файла.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-skeleton-infrastructure/01-CONTEXT.md
@.planning/research/STACK.md
@pyproject.toml
@uv.lock
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create .github/workflows/ci.yml — quality job (ruff + mypy + pytest)</name>
  <files>.github/workflows/ci.yml</files>
  <read_first>
    - pyproject.toml (Task 1 plan 01 — verify ruff/mypy/pytest deps)
    - uv.lock (locked deps for frozen install)
    - .planning/phases/01-skeleton-infrastructure/01-CONTEXT.md (D-06, D-07, D-08, D-09)
    - .planning/research/STACK.md "uv" + "ruff" + "mypy" rows
  </read_first>
  <behavior>
    - GitHub Actions YAML parses
    - Workflow triggers on push (any branch) and pull_request (main only) — D-07
    - Single quality job runs: checkout → setup-uv with cache → uv python install 3.10.20 → uv sync --frozen → ruff check → ruff format --check → mypy src → pytest -q
    - No services (PG/RMQ) declared — D-08 (deferred to P3/P5)
    - No matrix — D-09
  </behavior>
  <action>
Создать `.github/workflows/ci.yml`:

```yaml
name: ci

on:
  push:
    branches: ["**"]
  pull_request:
    branches: [main]

permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  quality:
    name: quality
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install uv
        uses: astral-sh/setup-uv@v3
        with:
          version: "0.11.14"
          enable-cache: true
          cache-dependency-glob: "uv.lock"

      - name: Set up Python
        run: uv python install 3.10.20

      - name: Sync dependencies (frozen)
        run: uv sync --frozen --all-extras

      - name: Ruff lint
        run: uv run ruff check .

      - name: Ruff format check
        run: uv run ruff format --check .

      - name: Mypy strict
        run: uv run mypy src

      - name: Pytest
        run: uv run pytest -q
```

Не добавлять то, что отложено в `<deferred>` CONTEXT.md:
- никаких `services:` для PG/RMQ (D-08, отложено в P3/P5)
- никаких `matrix:` по Python (D-09)
- никакого `pytest-cov --cov-fail-under` (отложено в P7)
- никакого badge URL — README создаст ссылку на этот workflow в плане 07
  </action>
  <verify>
    <automated>test -f .github/workflows/ci.yml &amp;&amp; python3 -c "import yaml; data = yaml.safe_load(open('.github/workflows/ci.yml')); assert data['name'] == 'ci'; assert 'push' in data['on']; assert 'pull_request' in data['on']; assert 'quality' in data['jobs']"</automated>
  </verify>
  <acceptance_criteria>
    - `test -f .github/workflows/ci.yml` exits 0
    - `python3 -c "import yaml; yaml.safe_load(open('.github/workflows/ci.yml'))"` exits 0 (valid YAML)
    - `grep -q "^name: ci$" .github/workflows/ci.yml`
    - `grep -q "branches: \[\"\*\*\"\]" .github/workflows/ci.yml` (push on any branch — D-07)
    - `grep -q "branches: \[main\]" .github/workflows/ci.yml` (PR to main only — D-07)
    - `grep -q "runs-on: ubuntu-latest" .github/workflows/ci.yml`
    - `grep -q "actions/checkout@v4" .github/workflows/ci.yml`
    - `grep -q "astral-sh/setup-uv@v3" .github/workflows/ci.yml`
    - `grep -q "enable-cache: true" .github/workflows/ci.yml`
    - `grep -q "cache-dependency-glob: \"uv.lock\"" .github/workflows/ci.yml`
    - `grep -q "uv python install 3.10.20" .github/workflows/ci.yml` (D-09 pin)
    - `grep -q "uv sync --frozen --all-extras" .github/workflows/ci.yml`
    - `grep -q "uv run ruff check \." .github/workflows/ci.yml`
    - `grep -q "uv run ruff format --check \." .github/workflows/ci.yml`
    - `grep -q "uv run mypy src" .github/workflows/ci.yml`
    - `grep -q "uv run pytest -q" .github/workflows/ci.yml`
    - No `services:` section: `grep -c "^\s*services:" .github/workflows/ci.yml == 0` (D-08)
    - No matrix: `grep -c "^\s*matrix:" .github/workflows/ci.yml == 0` (D-09)
    - No emoji
  </acceptance_criteria>
  <done>
    CI workflow exists, parses, и описывает ровно один job 'quality' с шагами по D-06; триггеры по D-07; никаких PG/RMQ services и matrix.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Create .pre-commit-config.yaml with hook set from D-10</name>
  <files>.pre-commit-config.yaml</files>
  <read_first>
    - pyproject.toml (ruff/mypy already configured here)
    - .planning/phases/01-skeleton-infrastructure/01-CONTEXT.md (D-10 — точный список hooks)
    - .planning/research/STACK.md "pre-commit" + "ruff" rows
  </read_first>
  <behavior>
    - `uv run pre-commit run --all-files` succeeds (after a fresh `uv run pre-commit install`)
    - All hooks from D-10 are present
    - mypy hook is local (uses `uv run mypy --strict src`) per D-10 — NOT the upstream mirror that recreates a venv
    - `pass_filenames: false` on the mypy hook so it runs against the whole `src/` tree, not stale individual files
  </behavior>
  <action>
Создать `.pre-commit-config.yaml`:

```yaml
default_install_hook_types: [pre-commit]

repos:
  - repo: https://github.com/astral-sh/ruff-pre-commit
    rev: v0.15.12
    hooks:
      - id: ruff
        args: [--fix]
      - id: ruff-format

  - repo: https://github.com/pre-commit/pre-commit-hooks
    rev: v5.0.0
    hooks:
      - id: check-merge-conflict
      - id: end-of-file-fixer
      - id: trailing-whitespace
      - id: check-yaml
      - id: check-toml
      - id: check-added-large-files
        args: [--maxkb=500]

  - repo: local
    hooks:
      - id: mypy
        name: mypy strict
        entry: uv run mypy --strict src
        language: system
        pass_filenames: false
        types_or: [python, pyi]
        require_serial: true
```

Точные версии:
- `ruff-pre-commit` rev = `v0.15.12` совпадает с pinned ruff в pyproject.toml — никакого drift между hook и CI.
- `pre-commit-hooks` v5.0.0 — последний стабильный (правила check-merge-conflict / end-of-file-fixer / trailing-whitespace / check-yaml / check-toml / check-added-large-files).
- mypy — local hook (D-10 явно «local hook через entry: uv run mypy --strict src, pass_filenames: false, types_or: [python, pyi]»), а не mirror upstream — потому что mirror в pre-commit создаёт изолированный venv и не видит наших Pydantic/SQLAlchemy типов через uv.
  </action>
  <verify>
    <automated>test -f .pre-commit-config.yaml &amp;&amp; python3 -c "import yaml; data = yaml.safe_load(open('.pre-commit-config.yaml')); ids = [h['id'] for r in data['repos'] for h in r['hooks']]; required = {'ruff','ruff-format','check-merge-conflict','end-of-file-fixer','trailing-whitespace','check-yaml','check-toml','check-added-large-files','mypy'}; assert required.issubset(set(ids)), f'missing: {required - set(ids)}'"</automated>
  </verify>
  <acceptance_criteria>
    - `test -f .pre-commit-config.yaml` exits 0
    - `python3 -c "import yaml; yaml.safe_load(open('.pre-commit-config.yaml'))"` exits 0 (valid YAML)
    - `grep -q "astral-sh/ruff-pre-commit" .pre-commit-config.yaml`
    - `grep -q "rev: v0.15.12" .pre-commit-config.yaml` (matches pyproject ruff pin)
    - `grep -q "id: ruff$" .pre-commit-config.yaml`
    - `grep -q "id: ruff-format$" .pre-commit-config.yaml`
    - `grep -q "id: check-merge-conflict$" .pre-commit-config.yaml`
    - `grep -q "id: end-of-file-fixer$" .pre-commit-config.yaml`
    - `grep -q "id: trailing-whitespace$" .pre-commit-config.yaml`
    - `grep -q "id: check-yaml$" .pre-commit-config.yaml`
    - `grep -q "id: check-toml$" .pre-commit-config.yaml`
    - `grep -q "id: check-added-large-files$" .pre-commit-config.yaml`
    - `grep -q "id: mypy$" .pre-commit-config.yaml`
    - `grep -q "entry: uv run mypy --strict src" .pre-commit-config.yaml` (D-10 exact)
    - `grep -q "pass_filenames: false" .pre-commit-config.yaml` (D-10 exact)
    - `grep -q "types_or: \[python, pyi\]" .pre-commit-config.yaml` (D-10 exact)
    - `grep -q "language: system" .pre-commit-config.yaml` (local hook uses uv from system)
    - No emoji
  </acceptance_criteria>
  <done>
    .pre-commit-config.yaml содержит все 9 hooks из D-10, ruff rev совпадает с pyproject pin, mypy — local hook через uv run.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub Actions runner → repo code | Trusted within ephemeral runner (no secrets injected at this phase) |
| Pre-commit hook → developer machine | Trusted (local) |
| Pinned ruff/setup-uv versions → action repositories | Third-party action code runs with read-only permissions |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Elevation of Privilege | CI workflow has write access to repo and could push secrets | mitigate | `permissions: { contents: read }` at workflow level — explicit least privilege |
| T-06-02 | Tampering | mutable action tag pulls drift over time (`@v4`, `@v3`) | accept | For a test task, mutable major-version pins are acceptable; production would use SHA-pinned `@<commit-sha>` |
| T-06-03 | Tampering | rolling python:3.10 inside uv pull → drift | mitigate | `uv python install 3.10.20` pins exact patch (D-09) |
| T-06-04 | Tampering | ruff version drift between pre-commit and CI | mitigate | Ruff rev in `.pre-commit-config.yaml` (`v0.15.12`) matches pyproject pin (`ruff>=0.15,<0.16`) |
| T-06-05 | Denial of Service | concurrent CI runs on rapid pushes waste runner minutes | mitigate | `concurrency.group + cancel-in-progress: true` cancels stale runs on the same ref |
| T-06-06 | Information Disclosure | large file accidentally committed (.sqlite, .pdf) | mitigate | `check-added-large-files --maxkb=500` blocks files >500kb at pre-commit |
</threat_model>

<verification>
- `test -f .github/workflows/ci.yml && test -f .pre-commit-config.yaml`
- Both files are valid YAML
- All 9 pre-commit hooks present
- CI workflow has exactly the 8 steps from D-06
- `uv run pre-commit run --all-files` (smoke, may be checked manually after Wave 4 fully done; not blocking this plan)
</verification>

<success_criteria>
- CI workflow runs ruff check + ruff format --check + mypy --strict + pytest на каждый push (любая ветка) и PR в main (D-06, D-07, QA-10)
- Один job, без matrix (D-09)
- Никаких services PG/RMQ в P1 (D-08)
- Pre-commit с 9 hooks из D-10 (ruff fix/format, check-merge-conflict, end-of-file, trailing-whitespace, check-yaml, check-toml, check-added-large-files, mypy local)
- Ruff version pinned identically across pyproject и pre-commit (no drift)
</success_criteria>

<output>
After completion, create `.planning/phases/01-skeleton-infrastructure/01-06-SUMMARY.md` documenting the CI shape, the hook set, and confirmation that the workflow YAML is valid and ruff pin matches pyproject.
</output>
