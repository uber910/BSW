---
phase: 03-bet-maker-domain-db
plan: 03
type: execute
wave: 1
depends_on: [02]
files_modified:
  - src/bet_maker/schemas/__init__.py
  - src/bet_maker/schemas/events.py
  - src/bet_maker/schemas/bets.py
  - src/bet_maker/helpers/__init__.py
  - src/bet_maker/helpers/money.py
  - src/bet_maker/helpers/status.py
  - tests/bet_maker/test_schemas.py
autonomous: true
requirements: [BM-03, BM-05, BM-06]
tags: [bet-maker, schemas, pydantic-v2, decimal-validation, helpers, wave-1]

must_haves:
  truths:
    - "EventState (str, Enum) дублирован в bet_maker/schemas/events.py с 3 значениями (NEW/FINISHED_WIN/FINISHED_LOSE) — value-parity с line_provider"
    - "BetStatus (str, Enum) в bet_maker/schemas/bets.py с 3 значениями (PENDING/WON/LOST)"
    - "BetCreate содержит event_id: UUID + amount: Annotated[Decimal, Field(gt=0, max_digits=12, decimal_places=2), AfterValidator(quantize_amount)] + ConfigDict(extra='forbid')"
    - "BetRead содержит id+event_id+amount+status+created_at + ConfigDict(extra='forbid', from_attributes=True) — без coefficient"
    - "helpers/money.quantize_amount(Decimal) возвращает Decimal.quantize(_TWO_PLACES, ROUND_HALF_UP)"
    - "helpers/status.event_state_to_bet_status — stub raise NotImplementedError для P5"
    - "tests/bet_maker/test_schemas.py имеет >=18 тестов покрывая Quantize, BetCreate валидацию, BetRead from_attributes, EventState/BetStatus value parity, extra='forbid'"
    - "Все Pydantic schemas декларируют `model_config = ConfigDict(extra='forbid')` (zero extra-field tolerance — T-03-3 input mitigation)"
    - "Pydantic сериализует Decimal('10.00') как string '10.00' — verified в тесте (D-19, A4)"
    - "amount='10.123' → ValidationError type=decimal_max_places; amount='-1' → type=greater_than; amount='10' → quantized to Decimal('10.00')"
    - "mypy strict + ruff check проходят на всех новых файлах"
  artifacts:
    - path: "src/bet_maker/schemas/__init__.py"
      provides: "Schema package marker"
      contains: ""
    - path: "src/bet_maker/schemas/events.py"
      provides: "EventState (str, Enum) — duplicated from line_provider per D-12"
      contains: "class EventState(str, Enum)"
      min_lines: 8
    - path: "src/bet_maker/schemas/bets.py"
      provides: "BetStatus, Amount type alias, BetCreate, BetRead"
      contains: "class BetCreate(BaseModel)"
      min_lines: 40
    - path: "src/bet_maker/helpers/__init__.py"
      provides: "Helpers package marker"
      contains: ""
    - path: "src/bet_maker/helpers/money.py"
      provides: "quantize_amount(Decimal) -> Decimal (ROUND_HALF_UP, 2dp)"
      contains: "def quantize_amount"
      min_lines: 6
    - path: "src/bet_maker/helpers/status.py"
      provides: "event_state_to_bet_status stub raising NotImplementedError для P5"
      contains: "raise NotImplementedError"
    - path: "tests/bet_maker/test_schemas.py"
      provides: "Schema unit tests"
      contains: "TestBetCreate"
      min_lines: 80
  key_links:
    - from: "src/bet_maker/schemas/bets.py"
      to: "src/bet_maker/helpers/money.quantize_amount"
      via: "AfterValidator(quantize_amount)"
      pattern: "AfterValidator\\(quantize_amount\\)"
    - from: "src/bet_maker/schemas/events.py EventState"
      to: "src/line_provider/schemas/events.py EventState"
      via: "intentional duplication per D-12 — value-parity test"
      pattern: "NEW.*FINISHED_WIN.*FINISHED_LOSE"
---

<objective>
Wave 1 plan #1 — Pydantic schemas + helpers. Domain primitives которые не зависят ни от DB-движка, ни от facades, ни от lifespan. Создаются параллельно с Plan 03-04 (model + migration); никакого общего файла, поэтому могут идти в одну волну.

Что делается:
1. **`schemas/events.py`** — `EventState(str, Enum)` дублирован из line_provider per D-12 (intentional service-boundary isolation). EventSnapshot живёт в `facades/event_lookup.py`, не здесь.
2. **`schemas/bets.py`** — `BetStatus(str, Enum)` ('PENDING'/'WON'/'LOST'); `Amount` type alias (`Annotated[Decimal, Field(gt=0, max_digits=12, decimal_places=2), AfterValidator(quantize_amount)]`); `BetCreate(BaseModel)` (`event_id: UUID, amount: Amount, extra='forbid'`); `BetRead(BaseModel)` (`id, event_id, amount, status, created_at, extra='forbid', from_attributes=True`).
3. **`helpers/money.py`** — `quantize_amount(Decimal) -> Decimal` (mirror P2 `quantize_coefficient` shape, ROUND_HALF_UP, `_TWO_PLACES = Decimal("0.01")`).
4. **`helpers/status.py`** — единственная функция-заглушка `event_state_to_bet_status(state: EventState) -> BetStatus: raise NotImplementedError("Implemented in P5")` per D-30. Этот файл существует чтобы P5 не имел SOC-нарушения; никаких других обязанностей в P3.
5. **`tests/bet_maker/test_schemas.py`** — раскрытие Wave 0 stub'а (не существовал в Wave 0; добавляется ad-hoc этим планом). Тесты в 6 классах: TestQuantize (3), TestBetCreate (8 — happy + 7 rejection cases), TestBetRead (2 — from_attributes + Decimal string serialization), TestEnums (3 — value-parity, member count), TestStatusStub (1 — NotImplementedError), TestExtraForbid (1 — schema-wide invariant).

Purpose: Без Pydantic-схем взаимозависимые планы 03-04 (model.amount тип), 03-05 (StubEventLookup использует EventSnapshot + EventState), 03-06 (interactor использует BetCreate+BetRead+EventState), 03-07 (routes — body=BetCreate, response_model=BetRead) — все blocked. После этого плана domain primitive слой готов.

Output: 7 файлов; mypy strict + ruff чисто; полная test-coverage на schema validation (18+ тестов); все 97 baseline + 18 новых = 115+ passed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/03-bet-maker-domain-db/03-CONTEXT.md
@.planning/phases/03-bet-maker-domain-db/03-RESEARCH.md
@.planning/phases/03-bet-maker-domain-db/03-PATTERNS.md
@.planning/phases/03-bet-maker-domain-db/03-VALIDATION.md
@src/line_provider/schemas/events.py
@src/line_provider/helpers/money.py
@src/line_provider/helpers/state_machine.py
@tests/line_provider/test_schemas.py
@CLAUDE.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create helpers/money.py + helpers/status.py + package __init__.py</name>
  <files>
    src/bet_maker/helpers/__init__.py,
    src/bet_maker/helpers/money.py,
    src/bet_maker/helpers/status.py
  </files>
  <read_first>
    - src/line_provider/helpers/money.py (analog — exact shape, 16 строк)
    - src/line_provider/helpers/state_machine.py (analog для status.py stub)
    - .planning/phases/03-bet-maker-domain-db/03-PATTERNS.md §«src/bet_maker/helpers/money.py» (lines 305–333) и §«src/bet_maker/helpers/status.py» (lines 336–359)
    - .planning/phases/03-bet-maker-domain-db/03-CONTEXT.md §D-30 (helpers/status.py — заглушка для P5)
  </read_first>
  <behavior>
    - quantize_amount(Decimal("10")) == Decimal("10.00") (string form '10.00')
    - quantize_amount(Decimal("10.123")) == Decimal("10.12") (ROUND_HALF_UP cuts at 2dp)
    - quantize_amount(Decimal("10.125")) == Decimal("10.13") (ROUND_HALF_UP rounds UP at half)
    - quantize_amount(Decimal("10.005")) == Decimal("10.01") (ROUND_HALF_UP, NOT banker's rounding)
    - quantize_amount(Decimal("0.001")) == Decimal("0.00") (rounds down — boundary)
    - event_state_to_bet_status — invocation raises NotImplementedError("Implemented in P5")
  </behavior>
  <action>
    Создать 3 файла.

    **`src/bet_maker/helpers/__init__.py`** — пустой файл (package marker; mirror line_provider/helpers/__init__.py).

    **`src/bet_maker/helpers/money.py`** — exact mirror P2 line_provider/helpers/money.py с rename:

    ```python
    from __future__ import annotations

    from decimal import ROUND_HALF_UP, Decimal

    _TWO_PLACES = Decimal("0.01")


    def quantize_amount(value: Decimal) -> Decimal:
        """Normalise bet amount to exactly two decimal places.

        Pydantic v2 `decimal_places=2` validates upper bound only — `Decimal("10")`
        passes but serialises as `"10"` not `"10.00"`. The spec (D-19) requires
        exactly two decimal places in BetRead.amount; we accept `"10"` and
        quantize-on-input via AfterValidator on BetCreate.amount.

        ROUND_HALF_UP (not banker's rounding): `Decimal("10.005")` becomes
        `Decimal("10.01")`. This matches the user's monetary intuition.
        """
        return value.quantize(_TWO_PLACES, rounding=ROUND_HALF_UP)
    ```

    **`src/bet_maker/helpers/status.py`** — stub для P5 (D-30):

    ```python
    from __future__ import annotations

    from bet_maker.schemas.bets import BetStatus
    from bet_maker.schemas.events import EventState


    def event_state_to_bet_status(state: EventState) -> BetStatus:
        """Map terminal event state to bet outcome status.

        Stub for P5: when the consumer settles bets after EventFinished arrives,
        it maps EventState.FINISHED_WIN → BetStatus.WON and FINISHED_LOSE → LOST.
        Calling this in P3 raises — there is no settlement path yet.

        D-30: helpers/status.py created here so P5 can fill it in without an
        SOC violation (single file ownership, no cross-phase edits to other
        modules).
        """
        raise NotImplementedError("Implemented in P5 (settle_bets_for_event interactor)")
    ```

    Не создавать никакие другие helper-функции. Не добавлять никаких импортов сверх показанных. Не создавать circular imports — `helpers/status.py` импортирует `BetStatus` и `EventState` из `schemas/` — это разрешено потому что `helpers/` зависит от `schemas/` (один направленный поток).
  </action>
  <verify>
    <automated>test -f src/bet_maker/helpers/__init__.py</automated>
    <automated>test -f src/bet_maker/helpers/money.py</automated>
    <automated>test -f src/bet_maker/helpers/status.py</automated>
    <automated>grep -q "from __future__ import annotations" src/bet_maker/helpers/money.py</automated>
    <automated>grep -q "ROUND_HALF_UP" src/bet_maker/helpers/money.py</automated>
    <automated>grep -q "def quantize_amount" src/bet_maker/helpers/money.py</automated>
    <automated>grep -q "raise NotImplementedError" src/bet_maker/helpers/status.py</automated>
    <automated>uv run python -c "from bet_maker.helpers.money import quantize_amount; from decimal import Decimal; assert quantize_amount(Decimal('10')) == Decimal('10.00'); assert quantize_amount(Decimal('10.125')) == Decimal('10.13'); assert quantize_amount(Decimal('10.005')) == Decimal('10.01'); print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `src/bet_maker/helpers/__init__.py` существует (может быть пустым)
    - `src/bet_maker/helpers/money.py` содержит `_TWO_PLACES = Decimal("0.01")` и `def quantize_amount(value: Decimal) -> Decimal:` и `value.quantize(_TWO_PLACES, rounding=ROUND_HALF_UP)`
    - `src/bet_maker/helpers/status.py` содержит `from bet_maker.schemas.bets import BetStatus` и `from bet_maker.schemas.events import EventState` и `raise NotImplementedError("Implemented in P5 ...")`
    - `quantize_amount(Decimal("10"))` возвращает `Decimal("10.00")` (verified inline Python check)
    - `quantize_amount(Decimal("10.005"))` возвращает `Decimal("10.01")` (ROUND_HALF_UP, not banker's)
    - Каждый файл начинается с `from __future__ import annotations`
    - **NOTE:** Импорт `status.py` пока упадёт ImportError потому что `schemas/bets.py` и `schemas/events.py` ещё не существуют (создаются в Task 2). Это ожидаемо — `helpers/status.py` будет проверен после Task 2. Inline Python check в Task 1 verify импортирует только `money.py` (нет cross-deps).
  </acceptance_criteria>
  <done>helpers/money.py и helpers/status.py созданы. quantize_amount работает корректно. status.py готов к импорту после Task 2 schemas создания.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create schemas/events.py + schemas/bets.py + package __init__.py</name>
  <files>
    src/bet_maker/schemas/__init__.py,
    src/bet_maker/schemas/events.py,
    src/bet_maker/schemas/bets.py
  </files>
  <read_first>
    - src/line_provider/schemas/events.py (analog — exact shape для EventState class + Coefficient Annotated alias)
    - .planning/phases/03-bet-maker-domain-db/03-CONTEXT.md §D-04 (BetCreate.amount Annotated Decimal recipe) + §D-19 (Decimal as string) + §D-20 ((str, Enum)) + §D-12 (EventState duplication)
    - .planning/phases/03-bet-maker-domain-db/03-RESEARCH.md §«Code Examples» Pydantic BetCreate (lines 745–767) — verified recipe
    - .planning/phases/03-bet-maker-domain-db/03-PATTERNS.md §«src/bet_maker/schemas/bets.py» (lines 53–96) и §«src/bet_maker/schemas/events.py» (lines 99–115)
  </read_first>
  <behavior>
    - EventState имеет ровно 3 члена: NEW, FINISHED_WIN, FINISHED_LOSE (value-parity с line_provider)
    - BetStatus имеет ровно 3 члена: PENDING, WON, LOST
    - BetCreate(event_id=UUID, amount='10') → amount внутри model становится Decimal('10.00') (quantized via AfterValidator)
    - BetCreate(event_id=UUID, amount='10.123') → ValidationError, type='decimal_max_places'
    - BetCreate(event_id=UUID, amount='-5') → ValidationError, type='greater_than'
    - BetCreate(event_id=UUID, amount='0') → ValidationError (gt=0)
    - BetCreate(event_id=UUID, amount='1.50', coefficient=Decimal('1.5')) → ValidationError extra='forbid'
    - BetCreate(event_id=UUID) → ValidationError, missing amount
    - BetCreate.model_dump_json() возвращает amount как строку "10.00"
    - BetRead from_attributes=True работает: BetRead.model_validate(orm_like_obj, from_attributes=True) принимает объект с attributes id, event_id, amount, status, created_at
  </behavior>
  <action>
    Создать 3 файла.

    **`src/bet_maker/schemas/__init__.py`** — пустой (package marker).

    **`src/bet_maker/schemas/events.py`** — duplicate EventState из line_provider per D-12:

    ```python
    """EventState duplicated from line_provider.schemas.events — intentional
    service-boundary isolation per D-12 (mirror of P2 D-13 EventFinishedMessage
    intentional duplication). Value-parity test in test_schemas.py prevents drift.
    """

    from __future__ import annotations

    from enum import Enum


    class EventState(str, Enum):
        NEW = "NEW"
        FINISHED_WIN = "FINISHED_WIN"
        FINISHED_LOSE = "FINISHED_LOSE"
    ```

    Никаких других определений в этом файле. EventSnapshot живёт в `facades/event_lookup.py` (Plan 03-05), не здесь.

    **`src/bet_maker/schemas/bets.py`** — основной schema-модуль:

    ```python
    from __future__ import annotations

    from datetime import datetime
    from decimal import Decimal
    from enum import Enum
    from typing import Annotated
    from uuid import UUID

    from pydantic import AfterValidator, BaseModel, ConfigDict, Field

    from bet_maker.helpers.money import quantize_amount


    class BetStatus(str, Enum):
        """Bet lifecycle status.

        PENDING — bet placed, event not yet finished.
        WON — event finished with the outcome the user bet on.
        LOST — event finished with the opposite outcome.

        Settled by the RabbitMQ consumer (P5) or reconciliation job (P6).
        Python 3.10: `(str, Enum)` instead of `StrEnum` (3.11+); P2 D-20 locked.
        """

        PENDING = "PENDING"
        WON = "WON"
        LOST = "LOST"


    Amount = Annotated[
        Decimal,
        Field(gt=Decimal("0"), max_digits=12, decimal_places=2),
        AfterValidator(quantize_amount),
    ]
    """Validated bet amount.

    D-04: gt=0, max_digits=12 (caps at 999,999,999,999.99 — Pitfall A4 mitigation
    for oversized payloads), decimal_places=2. AfterValidator normalises `"10"` →
    `Decimal("10.00")` so BetRead serialises consistently (D-19).
    """


    class BetCreate(BaseModel):
        """POST /bet body. D-04: event_id + amount only; no coefficient (D-01)."""

        model_config = ConfigDict(extra="forbid")

        event_id: UUID
        amount: Amount


    class BetRead(BaseModel):
        """Response shape for POST /bet 201, GET /bets items, GET /bet/{id} 200.

        D-05: superset of TZ-minimum (TZ requires only id + status; we include
        event_id + amount + created_at for curl-demo UX in README).
        from_attributes=True enables model_validate(orm_bet, from_attributes=True)
        inside the session (A1 / Anti-Pattern 5 mitigation: never return raw ORM).
        """

        model_config = ConfigDict(extra="forbid", from_attributes=True)

        id: UUID
        event_id: UUID
        amount: Decimal
        status: BetStatus
        created_at: datetime
    ```

    Ключевые детали (НЕ ОТКЛОНЯТЬСЯ):
    - `Field(gt=Decimal("0"), max_digits=12, decimal_places=2)` — точное число digits и places per D-04 / D-09
    - `AfterValidator(quantize_amount)` — quantize ПОСЛЕ Field validation (Field отвергает 3dp; quantize нормализует 0-2dp до 2)
    - `model_config = ConfigDict(extra="forbid")` на BetCreate и BetRead — T-03-3 input mitigation
    - `from_attributes=True` ТОЛЬКО на BetRead (interactor вызывает `BetRead.model_validate(bet, from_attributes=True)` per D-14)
    - `amount: Decimal` в BetRead (без Annotated/Amount alias) — на выходе мы доверяем DB-значению; Pydantic v2 сериализует Decimal как string "10.00" (D-19) автоматически

    Не добавлять никаких других fields. Не добавлять `model_serializer` или `field_validator` — `decimal_places=2` + `AfterValidator(quantize_amount)` достаточно. Не определять `Amount` alias за пределами `schemas/bets.py`.
  </action>
  <verify>
    <automated>test -f src/bet_maker/schemas/__init__.py</automated>
    <automated>test -f src/bet_maker/schemas/events.py</automated>
    <automated>test -f src/bet_maker/schemas/bets.py</automated>
    <automated>grep -q "from __future__ import annotations" src/bet_maker/schemas/events.py</automated>
    <automated>grep -q "from __future__ import annotations" src/bet_maker/schemas/bets.py</automated>
    <automated>grep -q "class EventState(str, Enum):" src/bet_maker/schemas/events.py</automated>
    <automated>grep -c '"NEW"\|"FINISHED_WIN"\|"FINISHED_LOSE"' src/bet_maker/schemas/events.py</automated>
    <automated>grep -q "class BetStatus(str, Enum):" src/bet_maker/schemas/bets.py</automated>
    <automated>grep -q 'PENDING = "PENDING"' src/bet_maker/schemas/bets.py</automated>
    <automated>grep -q 'WON = "WON"' src/bet_maker/schemas/bets.py</automated>
    <automated>grep -q 'LOST = "LOST"' src/bet_maker/schemas/bets.py</automated>
    <automated>grep -q "gt=Decimal" src/bet_maker/schemas/bets.py</automated>
    <automated>grep -q "max_digits=12" src/bet_maker/schemas/bets.py</automated>
    <automated>grep -q "decimal_places=2" src/bet_maker/schemas/bets.py</automated>
    <automated>grep -q "AfterValidator(quantize_amount)" src/bet_maker/schemas/bets.py</automated>
    <automated>grep -q 'extra="forbid"' src/bet_maker/schemas/bets.py</automated>
    <automated>grep -q "from_attributes=True" src/bet_maker/schemas/bets.py</automated>
    <automated>uv run python -c "from bet_maker.schemas.bets import BetCreate, BetRead, BetStatus, Amount; from bet_maker.schemas.events import EventState; from uuid import uuid4; b = BetCreate(event_id=uuid4(), amount='10'); assert str(b.amount) == '10.00', b.amount; print('OK')"</automated>
    <automated>uv run python -c "from bet_maker.schemas.bets import BetCreate; from uuid import uuid4; from pydantic import ValidationError; ok=False; \nimport sys\ntry: BetCreate(event_id=uuid4(), amount='10.123')\nexcept ValidationError as e: ok='decimal_max_places' in str(e)\nassert ok, 'expected decimal_max_places ValidationError'\nprint('OK')"</automated>
    <automated>uv run python -c "from bet_maker.helpers.status import event_state_to_bet_status; from bet_maker.schemas.events import EventState; \ntry:\n    event_state_to_bet_status(EventState.FINISHED_WIN)\n    raise AssertionError('expected NotImplementedError')\nexcept NotImplementedError as e:\n    assert 'P5' in str(e)\n    print('OK')"</automated>
    <automated>uv run mypy --strict src/bet_maker</automated>
    <automated>uv run ruff check src/bet_maker</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^class EventState(str, Enum):" src/bet_maker/schemas/events.py` == 1
    - `grep -cE '^\s+(NEW|FINISHED_WIN|FINISHED_LOSE) = "' src/bet_maker/schemas/events.py` == 3
    - `grep -c "^class BetStatus(str, Enum):" src/bet_maker/schemas/bets.py` == 1
    - `grep -cE '^\s+(PENDING|WON|LOST) = "' src/bet_maker/schemas/bets.py` == 3
    - `grep -c "^class BetCreate(BaseModel):" src/bet_maker/schemas/bets.py` == 1
    - `grep -c "^class BetRead(BaseModel):" src/bet_maker/schemas/bets.py` == 1
    - `grep -q "Amount = Annotated\[" src/bet_maker/schemas/bets.py`
    - `grep -q "AfterValidator(quantize_amount)" src/bet_maker/schemas/bets.py`
    - `grep -q "from_attributes=True" src/bet_maker/schemas/bets.py`
    - Inline test: `BetCreate(event_id=uuid4(), amount='10')` → `b.amount == Decimal('10.00')`
    - Inline test: `BetCreate(event_id=uuid4(), amount='10.123')` → `ValidationError` с `type='decimal_max_places'`
    - Inline test: `event_state_to_bet_status(EventState.FINISHED_WIN)` → `NotImplementedError("Implemented in P5 ...")`
    - `uv run mypy --strict src/bet_maker` exit 0
    - `uv run ruff check src/bet_maker` → All checks passed
  </acceptance_criteria>
  <done>Schemas + status stub созданы. BetCreate валидация работает (gt=0, max_digits=12, decimal_places=2, AfterValidator quantize, extra='forbid'). BetRead имеет from_attributes=True. mypy strict + ruff чисто.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Create tests/bet_maker/test_schemas.py — replace Wave 0 stub absent / add full schema test suite</name>
  <files>tests/bet_maker/test_schemas.py</files>
  <read_first>
    - tests/line_provider/test_schemas.py (P2 analog — 22 tests in 6 classes, REQ-ID docstrings convention)
    - src/bet_maker/schemas/bets.py (только что созданный — для актуальных fields и validators)
    - src/bet_maker/schemas/events.py (только что созданный)
    - .planning/phases/03-bet-maker-domain-db/03-PATTERNS.md §«REQ-ID docstrings in tests» (lines 592–602)
    - .planning/phases/03-bet-maker-domain-db/03-CONTEXT.md §D-19 (Decimal as string serialization)
  </read_first>
  <action>
    Создать `tests/bet_maker/test_schemas.py` (новый файл, в Wave 0 не создавался):

    ```python
    """Unit tests for bet_maker schemas + helpers/money.

    BM-05 / D-04: BetCreate.amount = Annotated[Decimal, Field(gt=0, max_digits=12,
    decimal_places=2), AfterValidator(quantize_amount)].
    BM-05 / D-19: Decimal serialization as string "10.00" (Pydantic v2 default,
    Pitfall A4 mitigation: tests compare strings, never floats).
    BM-06 / D-12: EventState duplicated from line_provider (value-parity).
    D-20 / P2 D-13: BetStatus is (str, Enum), not StrEnum (Python 3.10).
    D-30: helpers/status.event_state_to_bet_status is a P5 stub.
    """

    from __future__ import annotations

    from decimal import Decimal
    from types import SimpleNamespace
    from uuid import UUID, uuid4

    import pytest
    from pydantic import ValidationError

    from bet_maker.helpers.money import quantize_amount
    from bet_maker.helpers.status import event_state_to_bet_status
    from bet_maker.schemas.bets import BetCreate, BetRead, BetStatus
    from bet_maker.schemas.events import EventState


    class TestQuantize:
        """D-04 / D-19: quantize_amount normalises amounts to 2dp ROUND_HALF_UP."""

        def test_pad_zeros(self) -> None:
            """`Decimal('10')` → `Decimal('10.00')`."""
            assert quantize_amount(Decimal("10")) == Decimal("10.00")
            assert str(quantize_amount(Decimal("10"))) == "10.00"

        def test_keep_two_places(self) -> None:
            """Already-2dp value passes through unchanged."""
            assert quantize_amount(Decimal("10.42")) == Decimal("10.42")

        def test_round_half_up(self) -> None:
            """ROUND_HALF_UP, not banker's: `10.005` → `10.01`, `10.125` → `10.13`."""
            assert quantize_amount(Decimal("10.005")) == Decimal("10.01")
            assert quantize_amount(Decimal("10.125")) == Decimal("10.13")


    class TestBetCreate:
        """BM-05 / D-04: BetCreate validates amount and forbids extra fields."""

        def test_happy_path_quantizes(self) -> None:
            """BM-05: `amount='10'` is accepted and quantized to `Decimal('10.00')`."""
            body = BetCreate(event_id=uuid4(), amount=Decimal("10"))  # type: ignore[arg-type]
            assert body.amount == Decimal("10.00")

        def test_string_input_quantizes(self) -> None:
            """BM-05: JSON-like `amount='10'` (string) coerces to Decimal then quantizes."""
            body = BetCreate.model_validate({"event_id": str(uuid4()), "amount": "10"})
            assert body.amount == Decimal("10.00")

        def test_three_decimal_places_rejected(self) -> None:
            """D-04: `amount='10.123'` → ValidationError type=decimal_max_places."""
            with pytest.raises(ValidationError) as exc:
                BetCreate.model_validate({"event_id": str(uuid4()), "amount": "10.123"})
            assert "decimal_max_places" in str(exc.value)

        def test_zero_rejected(self) -> None:
            """D-04: `amount='0'` → ValidationError type=greater_than (gt=0, not ge)."""
            with pytest.raises(ValidationError) as exc:
                BetCreate.model_validate({"event_id": str(uuid4()), "amount": "0"})
            assert "greater_than" in str(exc.value)

        def test_negative_rejected(self) -> None:
            """D-04: `amount='-5'` → ValidationError type=greater_than."""
            with pytest.raises(ValidationError) as exc:
                BetCreate.model_validate({"event_id": str(uuid4()), "amount": "-5"})
            assert "greater_than" in str(exc.value)

        def test_invalid_uuid_rejected(self) -> None:
            """T-03-3: malformed event_id rejected before reaching SQL layer."""
            with pytest.raises(ValidationError):
                BetCreate.model_validate({"event_id": "not-a-uuid", "amount": "10"})

        def test_extra_field_rejected(self) -> None:
            """D-01: coefficient is NOT a field on BetCreate; extra='forbid' rejects it."""
            with pytest.raises(ValidationError) as exc:
                BetCreate.model_validate({
                    "event_id": str(uuid4()),
                    "amount": "10",
                    "coefficient": "1.5",
                })
            assert "Extra inputs are not permitted" in str(exc.value) or "extra_forbidden" in str(exc.value)

        def test_missing_amount_rejected(self) -> None:
            """D-04: amount is required."""
            with pytest.raises(ValidationError):
                BetCreate.model_validate({"event_id": str(uuid4())})

        def test_oversized_amount_rejected(self) -> None:
            """T-03-1 / D-04: max_digits=12 caps at 999_999_999_999.99."""
            with pytest.raises(ValidationError) as exc:
                BetCreate.model_validate({"event_id": str(uuid4()), "amount": "1000000000000.00"})
            # Either decimal_max_digits or string_too_long — both are 422 type
            assert "decimal" in str(exc.value).lower() or "max_digits" in str(exc.value).lower()


    class TestBetRead:
        """BM-05 / D-19 / D-05: BetRead from_attributes + Decimal as JSON string."""

        def test_from_attributes_accepts_orm_like(self) -> None:
            """D-14: BetRead.model_validate(orm_obj, from_attributes=True) works."""
            orm_like = SimpleNamespace(
                id=uuid4(),
                event_id=uuid4(),
                amount=Decimal("10.00"),
                status=BetStatus.PENDING,
                created_at=__import__("datetime").datetime.now(),
            )
            read = BetRead.model_validate(orm_like, from_attributes=True)
            assert isinstance(read.id, UUID)
            assert read.status == BetStatus.PENDING
            assert read.amount == Decimal("10.00")

        def test_decimal_serializes_as_string(self) -> None:
            """D-19 / Pitfall A4: amount serialises as string '10.00', not float."""
            import json

            orm_like = SimpleNamespace(
                id=uuid4(),
                event_id=uuid4(),
                amount=Decimal("10.00"),
                status=BetStatus.PENDING,
                created_at=__import__("datetime").datetime.now(),
            )
            read = BetRead.model_validate(orm_like, from_attributes=True)
            payload = json.loads(read.model_dump_json())
            assert payload["amount"] == "10.00", f"expected string '10.00', got {payload['amount']!r}"
            assert isinstance(payload["amount"], str)


    class TestEnums:
        """D-12 / D-20: EventState (duplicated) and BetStatus enum invariants."""

        def test_eventstate_has_three_members(self) -> None:
            """D-12: EventState parity with line_provider — 3 members, exact names."""
            members = {m.name: m.value for m in EventState}
            assert members == {
                "NEW": "NEW",
                "FINISHED_WIN": "FINISHED_WIN",
                "FINISHED_LOSE": "FINISHED_LOSE",
            }

        def test_eventstate_value_parity_with_line_provider(self) -> None:
            """D-12: bet_maker EventState values match line_provider exactly.

            P5 e2e tests assert byte-for-byte parity in EventFinishedMessage.
            Here we only assert the same string values exist on both sides.
            """
            from line_provider.schemas.events import EventState as LpEventState

            assert {m.value for m in EventState} == {m.value for m in LpEventState}

        def test_betstatus_has_three_members(self) -> None:
            """D-20: BetStatus = PENDING/WON/LOST as (str, Enum)."""
            members = {m.name: m.value for m in BetStatus}
            assert members == {"PENDING": "PENDING", "WON": "WON", "LOST": "LOST"}
            assert all(isinstance(m.value, str) for m in BetStatus)


    class TestStatusStub:
        """D-30: helpers/status.event_state_to_bet_status raises NotImplementedError."""

        def test_raises_for_p5(self) -> None:
            """D-30: P3 stub; P5 fills in real mapping."""
            with pytest.raises(NotImplementedError) as exc:
                event_state_to_bet_status(EventState.FINISHED_WIN)
            assert "P5" in str(exc.value)


    class TestExtraForbid:
        """Schema-wide invariant: BetCreate AND BetRead both forbid extras (T-03-3)."""

        def test_betcreate_extra_forbid(self) -> None:
            assert BetCreate.model_config.get("extra") == "forbid"

        def test_betread_extra_forbid(self) -> None:
            assert BetRead.model_config.get("extra") == "forbid"
    ```

    Не добавлять интеграционных HTTP-тестов — это unit-уровень, без `client` fixture. Не использовать testcontainers — schemas pure-domain, без БД.

    Каждый test method содержит docstring со ссылкой на минимум один (BM-XX / D-XX / T-03-X) — project convention.
  </action>
  <verify>
    <automated>uv run pytest tests/bet_maker/test_schemas.py -q --no-cov</automated>
    <automated>grep -c "^class Test" tests/bet_maker/test_schemas.py</automated>
    <automated>grep -c "def test_" tests/bet_maker/test_schemas.py</automated>
    <automated>uv run mypy --strict tests/bet_maker/test_schemas.py</automated>
    <automated>uv run ruff check tests/bet_maker/test_schemas.py</automated>
    <automated>uv run pytest -q 2>&1 | grep -E "passed|skipped"</automated>
  </verify>
  <acceptance_criteria>
    - `tests/bet_maker/test_schemas.py` существует
    - `grep -c "^class Test" tests/bet_maker/test_schemas.py` == 6 (TestQuantize, TestBetCreate, TestBetRead, TestEnums, TestStatusStub, TestExtraForbid)
    - `grep -c "def test_" tests/bet_maker/test_schemas.py` >= 18
    - `uv run pytest tests/bet_maker/test_schemas.py -q --no-cov` exit 0; все тесты passed (не skipped — этот файл НЕ Wave 0 stub)
    - `uv run pytest -q 2>&1 | grep -E "[0-9]+ passed"` показывает >= 115 passed (97 baseline + 18 новых)
    - `uv run mypy --strict tests/bet_maker/test_schemas.py` exit 0
    - `uv run ruff check tests/bet_maker/test_schemas.py` → All checks passed
    - Каждый test method содержит docstring с REQ-ID (`BM-XX`, `D-XX`, `T-03-X`, `Pitfall`, или phrase `D-19/D-30/etc`): `grep -c "BM-\|D-0\|D-1\|D-2\|D-3\|T-03\|Pitfall" tests/bet_maker/test_schemas.py` >= 18
  </acceptance_criteria>
  <done>18+ schema tests passed. Pydantic validation matrix покрыт. Decimal string serialization подтверждён. EventState parity test добавлен. mypy strict + ruff чисто.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HTTP client → BetCreate Pydantic validation | первая линия защиты от untrusted input на POST /bet (Plan 03-07 wired) |
| bet_maker.schemas.events.EventState → line_provider.schemas.events.EventState | service-boundary; intentional duplication (D-12) для test parity, без cross-import |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-1 | Tampering | BetCreate.amount Decimal coercion attack (`"1e2"`, `"0.0000001"`, `"1.005"`) | mitigate | `Field(gt=Decimal("0"), max_digits=12, decimal_places=2)` отвергает >2dp с `decimal_max_places`; AfterValidator(quantize_amount) нормализует 0-2dp до 2 с ROUND_HALF_UP. Test_three_decimal_places_rejected + test_oversized_amount_rejected + TestQuantize covering. |
| T-03-3 | Tampering | extra-field injection в BetCreate body (`coefficient`, `user_id`, etc.) | mitigate | `model_config = ConfigDict(extra="forbid")` на BetCreate и BetRead. test_extra_field_rejected подтверждает. |
| T-03-3-uuid | Tampering | SQL injection через event_id path/body | mitigate-deferred | Pydantic UUID4 coercion отвергает не-UUID строки; test_invalid_uuid_rejected. SQL parameterization подтвердится в Plan 03-04/03-06 (SQLAlchemy + Mapped[UUID] mapped_column(PgUUID)). |
| T-03-1-zero | Tampering | `amount="0"` или `amount="-1"` (zero/negative monetary) | mitigate | `Field(gt=Decimal("0"))` (gt=, not ge=) отвергает с `greater_than`. test_zero_rejected + test_negative_rejected. |
</threat_model>

<verification>
После завершения всех 3 задач:
- `uv run pytest tests/bet_maker -q --no-cov` → 18+ новых schemas tests passed; 2 P1 health passed; 9 Wave 0 stubs skipped (НЕ failed)
- `uv run pytest -q` → 115+ total passed
- `uv run mypy --strict src/bet_maker tests/bet_maker/test_schemas.py` exit 0
- `uv run ruff check src/bet_maker tests/bet_maker` exit 0
- Все артефакты существуют (см. acceptance_criteria каждого task)
</verification>

<success_criteria>
- src/bet_maker/schemas/events.py содержит EventState (3 значения, value-parity с line_provider)
- src/bet_maker/schemas/bets.py содержит BetStatus (3 значения), Amount Annotated alias, BetCreate (extra='forbid'), BetRead (extra='forbid' + from_attributes=True)
- src/bet_maker/helpers/money.py содержит quantize_amount (ROUND_HALF_UP, 2dp)
- src/bet_maker/helpers/status.py содержит event_state_to_bet_status raising NotImplementedError("P5")
- tests/bet_maker/test_schemas.py содержит 18+ тестов в 6 классах с REQ-ID docstrings
- Все тесты зелёные: quantize, BetCreate validation (gt=0, max_digits, decimal_places, extra='forbid'), BetRead from_attributes + Decimal-as-string, EventState parity, BetStatus shape, status stub raises
- mypy strict + ruff check + ruff format чисто на всех новых файлах
- Existing 97 baseline + 9 skipped + 18 new = 115+ pytest passed/skipped
</success_criteria>

<output>
After completion, create `.planning/phases/03-bet-maker-domain-db/03-03-SUMMARY.md` following `@$HOME/.claude/get-shit-done/templates/summary.md`.

SUMMARY должен включать:
- Точное число тестов в test_schemas.py (>= 18) и их распределение по 6 классам
- Подтверждение что EventState value-parity test действительно проходит (импорт из line_provider удался)
- pytest output line с total passed count
</output>
