---
phase: 03-bet-maker-domain-db
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/phases/03-bet-maker-domain-db/03-VALIDATION.md
autonomous: true
requirements: [BM-01, BM-05]
tags: [bet-maker, requirements-sync, doc-only, tz-compliance]

must_haves:
  truths:
    - "REQUIREMENTS.md BM-01 не содержит `coefficient Decimal 6.2`"
    - "REQUIREMENTS.md BM-05 не содержит «снимок coefficient на момент создания»"
    - "REQUIREMENTS.md содержит новую строку BM-13 для `GET /bet/{bet_id}` (200 + BetRead или 404)"
    - "REQUIREMENTS.md Traceability table содержит BM-13 → Phase 3"
    - "Per-phase distribution: Phase 3 — 9 requirements (BM-01..03, BM-05..08, BM-13, QA-07)"
    - "Coverage total updated to 43/43"
    - "Каждое изменение содержит inline ссылку `(per D-01 Phase 3 CONTEXT.md)` или `(per D-02)` для grep-traceability — зеркало P2 Plan 02-01"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "ТЗ-compliant BM-01/BM-05 без coefficient, новый BM-13"
      contains: "BM-13"
  key_links:
    - from: ".planning/REQUIREMENTS.md (BM-01/BM-05/BM-13)"
      to: ".planning/phases/03-bet-maker-domain-db/03-CONTEXT.md (D-01/D-02)"
      via: "inline citation `per D-01` / `per D-02`"
      pattern: "per D-0[12]"
---

<objective>
Sync `.planning/REQUIREMENTS.md` против первоисточника ТЗ (`./Тестовое задание Middle Python developer.pdf`) и Phase 3 CONTEXT.md (D-01, D-02). Это doc-only plan: ни строки кода, единственное назначение — устранить drift между REQUIREMENTS.md и ТЗ ДО того, как Wave 1 начнёт писать модель/схемы.

Drift, который устраняется:
1. **BM-01** содержит «coefficient Decimal 6.2» — ТЗ (стр. 3) определяет POST /bet body как `{идентификатор события, сумма ставки}`. Coefficient — атрибут события, живёт в line-provider, не Bet. Per D-01.
2. **BM-05** содержит «снимок coefficient на момент создания» — та же причина. Per D-01.
3. **BM-13** (новое) — `GET /bet/{bet_id}` есть на диаграмме ТЗ (стр. 3), отсутствует в текстовом описании. Решение D-02 — реализовать в P3.

Plan mirrors P2 Plan 02-01 (LP-02 sync `str → UUID4`). Структурно идентичный по форме: edit-only, no code, no tests, single commit.

Purpose: Без sync'а downstream planner / executor может уверенно создать `coefficient: Decimal` поле в Bet model, заставляя в P5 пилить compensating migration. Это блокирующий plan для всего Phase 3 — Wave 1 не должен запускаться, пока drift не закрыт.

Output: `.planning/REQUIREMENTS.md` (BM-01 и BM-05 переписаны, BM-13 добавлена, Traceability table расширена, per-phase distribution counters обновлены, total coverage обновлён); `03-VALIDATION.md` (статус `nyquist_compliant: true` и `wave_0_complete` будут обновлены позже в Plan 03-02 — здесь только Plan 03-01 row помечается `pending`).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/PROJECT.md
@.planning/phases/03-bet-maker-domain-db/03-CONTEXT.md
@.planning/phases/03-bet-maker-domain-db/03-RESEARCH.md
@.planning/phases/03-bet-maker-domain-db/03-VALIDATION.md
@.planning/phases/02-line-provider-domain/02-01-PLAN.md
@./Тестовое задание Middle Python developer.pdf
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify drift against TZ first source</name>
  <files>(none — verification only)</files>
  <read_first>
    - ./Тестовое задание Middle Python developer.pdf (стр. 1–4) — первоисточник
    - .planning/REQUIREMENTS.md (строки 32–38 — текущие BM-01 и BM-05)
    - .planning/phases/03-bet-maker-domain-db/03-CONTEXT.md §«ТЗ-compliance (отличие от REQUIREMENTS.md)» (lines 43–46 D-01, D-02, D-03)
  </read_first>
  <action>
    Прочитать ТЗ PDF (страницы 1–4 — pages 1-4). Подтвердить три факта:

    1. **POST /bet body на странице 3 ТЗ — `{идентификатор события, сумма ставки}` без coefficient.** Цитата для commit message: `«идентификатор события, сумма ставки»` (стр. 3 ТЗ).
    2. **GET /bets на странице 3 ТЗ — «массив JSON-объектов, содержащих информацию о ставках: их идентификаторы и текущие статусы».** Coefficient не упомянут.
    3. **GET /bet/{bet_id} на странице 3 ТЗ — присутствует на диаграмме (последний эндпоинт списка), отсутствует в текстовом описании.** D-02 интерпретирует как «реализовать».

    Если хотя бы одно из этих утверждений неверно (например, ТЗ изменили, и coefficient появился в BM-payload) — остановиться, не выполнять Task 2, и поднять blocker в SUMMARY с цитатой страницы.

    Если все три факта подтверждены — продолжить к Task 2. **Никаких правок файлов в Task 1.** Это verification-only step, нужный потому что user memory `feedback_verify_against_tz` явно требует verify против первоисточника перед фиксированием решений.
  </action>
  <verify>
    <automated>test -f "Тестовое задание Middle Python developer.pdf" && echo "OK"</automated>
  </verify>
  <acceptance_criteria>
    - PDF файл `./Тестовое задание Middle Python developer.pdf` существует и доступен
    - Три факта (POST/bet body без coefficient; GET /bets без coefficient; GET /bet/{bet_id} на диаграмме) явно подтверждены в Task SUMMARY (cite stage + page number)
    - Если любой факт опровергнут — Task 2 не выполняется, в SUMMARY raised blocker
  </acceptance_criteria>
  <done>Drift verified against TZ first source. Все 3 факта совпадают с D-01 и D-02.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite BM-01 (remove coefficient) per D-01</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (строка 32 — текущая BM-01)
    - .planning/phases/03-bet-maker-domain-db/03-CONTEXT.md §«ТЗ-compliance» D-01 + §«Bet модель / DB-схема» D-09
    - .planning/phases/02-line-provider-domain/02-01-PLAN.md Task 2 (mirror — LP-02 str→UUID4 sync)
  </read_first>
  <action>
    Открыть `.planning/REQUIREMENTS.md`. Найти строку 32:
    ```
    - [ ] **BM-01**: SQLAlchemy 2.0 async модели для ставок (id UUID, event_id, amount Decimal 12.2, coefficient Decimal 6.2, status enum, created_at, updated_at)
    ```

    Заменить ровно на:
    ```
    - [ ] **BM-01**: SQLAlchemy 2.0 async модели для ставок (id UUID, event_id UUID, amount Decimal 12.2, status enum (PENDING/WON/LOST), created_at, updated_at). Per D-01 (Phase 3 CONTEXT.md): coefficient НЕ хранится в Bet — это атрибут события, живёт в line-provider; ТЗ стр. 3 `POST /bet` body = `{идентификатор события, сумма ставки}` без coefficient.
    ```

    Никаких других правок в файле в рамках этого таска. Не трогать BM-02..BM-12, не трогать секции `## v2 Requirements`, `## Out of Scope`, `## Traceability` (Traceability обновляется в Task 4).

    `coefficient Decimal 6.2` НЕ должно остаться в строке BM-01 — это grep-able acceptance criterion.
  </action>
  <verify>
    <automated>grep -q "BM-01.*Per D-01" .planning/REQUIREMENTS.md</automated>
    <automated>bash -c "! grep -q 'BM-01.*coefficient Decimal 6.2' .planning/REQUIREMENTS.md"</automated>
    <automated>grep -c "^- \[ \] \*\*BM-01\*\*" .planning/REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "^- \[ \] \*\*BM-01\*\*.*Per D-01 (Phase 3 CONTEXT.md)" .planning/REQUIREMENTS.md`
    - `grep -q "BM-01.*status enum (PENDING/WON/LOST)" .planning/REQUIREMENTS.md`
    - `grep -c "BM-01.*coefficient Decimal 6.2" .planning/REQUIREMENTS.md` == 0
    - `grep -c "^- \[ \] \*\*BM-01\*\*" .planning/REQUIREMENTS.md` == 1 (ровно одна строка BM-01)
    - `grep -q "ТЗ стр. 3" .planning/REQUIREMENTS.md` (TЗ citation present)
  </acceptance_criteria>
  <done>BM-01 синхронизирован с D-01 + ТЗ. Coefficient удалён из payload-описания Bet модели.</done>
</task>

<task type="auto">
  <name>Task 3: Rewrite BM-05 (remove coefficient snapshot) per D-01 + add BM-13 (GET /bet/{bet_id}) per D-02</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (строка 36 — текущая BM-05; строки 32–43 — секция BM полностью)
    - .planning/phases/03-bet-maker-domain-db/03-CONTEXT.md §«ТЗ-compliance» D-01 + D-02
    - .planning/phases/03-bet-maker-domain-db/03-CONTEXT.md §«GET /bets / GET /bet/{bet_id}» D-08
  </read_first>
  <action>
    Открыть `.planning/REQUIREMENTS.md`.

    **Шаг 1.** Найти строку 36:
    ```
    - [ ] **BM-05**: `POST /bet` — приём ставки; в теле `{event_id, amount}` (amount > 0, 2 знака); ответ — id созданной ставки; снимок coefficient на момент создания
    ```

    Заменить ровно на:
    ```
    - [ ] **BM-05**: `POST /bet` — приём ставки; в теле `{event_id, amount}` (amount > 0, ровно 2 знака после запятой); ответ — 201 с BetRead `{id, event_id, amount, status, created_at}`; status=PENDING при создании. Per D-01 (Phase 3 CONTEXT.md): coefficient snapshot НЕ хранится — coefficient остаётся в line-provider; ТЗ стр. 3 не требует coefficient в Bet payload.
    ```

    **Шаг 2.** Между строкой BM-12 (последняя строка секции `### bet-maker (BM)`) и началом следующей секции `### Quality (QA)` (строка 46) вставить новую строку BM-13. Точное место вставки: сразу после строки `- [ ] **BM-12**:` (одна пустая строка не нужна — формат списка).

    Новая строка:
    ```
    - [ ] **BM-13**: `GET /bet/{bet_id}` — получение ставки по id; 200 + BetRead `{id, event_id, amount, status, created_at}` или 404 `{"detail":"bet {id} not found"}`. Per D-02 (Phase 3 CONTEXT.md): эндпоинт присутствует на диаграмме ТЗ стр. 3 (отсутствует в текстовом описании); реализуется в P3.
    ```

    Другие строки секции `### bet-maker (BM)` не трогать. Не менять BM-02, BM-03, BM-04, BM-06..BM-12.

    `снимок coefficient` НЕ должно остаться в BM-05 — grep-able criterion.
  </action>
  <verify>
    <automated>grep -q "BM-05.*Per D-01" .planning/REQUIREMENTS.md</automated>
    <automated>bash -c "! grep -q 'снимок coefficient' .planning/REQUIREMENTS.md"</automated>
    <automated>grep -q "BM-13.*GET /bet/{bet_id}" .planning/REQUIREMENTS.md</automated>
    <automated>grep -q "BM-13.*Per D-02" .planning/REQUIREMENTS.md</automated>
    <automated>grep -c "^- \[ \] \*\*BM-13\*\*" .planning/REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "BM-05.*status=PENDING" .planning/REQUIREMENTS.md`
    - `grep -q "BM-05.*Per D-01" .planning/REQUIREMENTS.md`
    - `grep -c "снимок coefficient" .planning/REQUIREMENTS.md` == 0
    - `grep -q "BM-13.*GET /bet/{bet_id}" .planning/REQUIREMENTS.md`
    - `grep -q "BM-13.*Per D-02" .planning/REQUIREMENTS.md`
    - `grep -c "^- \[ \] \*\*BM-13\*\*" .planning/REQUIREMENTS.md` == 1
    - BM-13 вставлена ПОСЛЕ BM-12 и ДО начала `### Quality (QA)`:
      `awk '/^- \[ \] \*\*BM-12\*\*/{p=NR} /^### Quality/{q=NR} /^- \[ \] \*\*BM-13\*\*/{r=NR} END{exit !(p<r && r<q)}' .planning/REQUIREMENTS.md`
  </acceptance_criteria>
  <done>BM-05 синхронизирован с D-01 (нет coefficient). BM-13 добавлена per D-02 (GET /bet/{bet_id} реализуется в P3).</done>
</task>

<task type="auto">
  <name>Task 4: Update Traceability table + Coverage counters for BM-13</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md секция `## Traceability` (строки 113–158) — текущая таблица
    - .planning/REQUIREMENTS.md секция `**Coverage:**` и `**Per-phase distribution:**` (строки 160–175)
  </read_first>
  <action>
    Открыть `.planning/REQUIREMENTS.md`.

    **Шаг 1.** В таблице `## Traceability` (`| Requirement | Phase | Status |`) после строки `| BM-12 | Phase 6 | Pending |` (строка ~144) вставить новую строку:
    ```
    | BM-13 | Phase 3 | Pending |
    ```

    Никакие другие строки таблицы не трогать (включая порядок).

    **Шаг 2.** В блоке `**Coverage:**` (строки ~160–164):
    - Заменить `- v1 requirements: 42 total` → `- v1 requirements: 43 total`
    - Заменить `- Mapped to phases: 42 (100%)` → `- Mapped to phases: 43 (100%)`

    Строку `- Unmapped: 0` оставить без изменений.

    **Шаг 3.** В блоке `**Per-phase distribution:**` (строки ~166–172):
    - Заменить строку Phase 3:
      `- Phase 3 (bet-maker DB): 8 requirements (BM-01..03, BM-05..08, QA-07)`
      на
      `- Phase 3 (bet-maker DB): 9 requirements (BM-01..03, BM-05..08, BM-13, QA-07)`

    Остальные `Phase X` строки не трогать.

    Footer (`*Last updated: 2026-05-13 ...*`) НЕ трогать — он обновляется в Plan 03-08 (phase-gate). В рамках 03-01 footer остаётся 2026-05-13.
  </action>
  <verify>
    <automated>grep -q "| BM-13 | Phase 3 | Pending |" .planning/REQUIREMENTS.md</automated>
    <automated>grep -q "v1 requirements: 43 total" .planning/REQUIREMENTS.md</automated>
    <automated>grep -q "Mapped to phases: 43" .planning/REQUIREMENTS.md</automated>
    <automated>grep -q "Phase 3 (bet-maker DB): 9 requirements" .planning/REQUIREMENTS.md</automated>
    <automated>bash -c "! grep -q 'Phase 3 (bet-maker DB): 8 requirements' .planning/REQUIREMENTS.md"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "| BM-13 | Phase 3 | Pending |" .planning/REQUIREMENTS.md` == 1
    - `grep -q "v1 requirements: 43 total" .planning/REQUIREMENTS.md`
    - `grep -q "Mapped to phases: 43 (100%)" .planning/REQUIREMENTS.md`
    - `grep -q "Phase 3 (bet-maker DB): 9 requirements (BM-01..03, BM-05..08, BM-13, QA-07)" .planning/REQUIREMENTS.md`
    - `grep -c "Phase 3 (bet-maker DB): 8 requirements" .planning/REQUIREMENTS.md` == 0
    - BM-13 строка в Traceability таблице расположена между BM-12 и QA-01:
      `awk '/^\| BM-12 \|/{p=NR} /^\| QA-01 \|/{q=NR} /^\| BM-13 \|/{r=NR} END{exit !(p<r && r<q)}' .planning/REQUIREMENTS.md`
  </acceptance_criteria>
  <done>Traceability таблица расширена BM-13, coverage counters обновлены (43 total / Phase 3 = 9 requirements).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GSD planner → planning documents | REQUIREMENTS.md используется как source of truth downstream агентами (executor, checker, P4/P5 planner) — drift с ТЗ приводит к compensating миграциям в будущем |
| ТЗ.pdf → REQUIREMENTS.md | ТЗ — первоисточник; REQUIREMENTS — наша трактовка; при конфликте побеждает ТЗ (user memory `feedback_verify_against_tz`) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-1 | — | not applicable | — | doc-only plan; no Decimal/SQL/network surface introduced |
| T-03-2 | — | not applicable | — | doc-only plan |
| T-03-3 | — | not applicable | — | doc-only plan |
| T-03-4 | — | not applicable | — | doc-only plan |
| T-03-5 | — | not applicable | — | doc-only plan |
| T-03-6 | — | not applicable | — | doc-only plan |
| T-03-1-DOC | Repudiation | REQUIREMENTS.md changes без traceability | mitigate | Каждое изменение содержит inline ссылку `(per D-01 Phase 3 CONTEXT.md)` или `(per D-02)` для grep-traceability — зеркало P2 Plan 02-01 |
| T-03-2-DOC | Tampering | BM-13 row inserted в неверном месте Traceability table | mitigate | awk-based acceptance criterion проверяет ordering (BM-12 < BM-13 < QA-01) |

Threat IDs T-03-1..T-03-6 (Decimal coercion, mass bet, SQL injection, replay, DSN leak, alembic backup) — out of scope для doc-only plan; они закрываются в кодовых планах 03-03..03-07.
</threat_model>

<verification>
После завершения всех 4 задач:
- `grep -q "BM-13" .planning/REQUIREMENTS.md` (новая строка существует)
- `grep -c "coefficient Decimal 6.2\|снимок coefficient" .planning/REQUIREMENTS.md` == 0 (стейл-формулировки устранены)
- `grep -q "v1 requirements: 43 total" .planning/REQUIREMENTS.md`
- `grep -q "Phase 3 (bet-maker DB): 9 requirements" .planning/REQUIREMENTS.md`
- `grep -c "per D-0[12]" .planning/REQUIREMENTS.md` >= 3 (минимум 3 inline ссылки: BM-01, BM-05, BM-13)
- Существующие тесты не задеты: `uv run pytest -q` exit 0 (baseline 97 passed)
</verification>

<success_criteria>
- BM-01 не упоминает coefficient (D-01 enforced; ТЗ-aligned)
- BM-05 не упоминает «снимок coefficient на момент создания» (D-01 enforced)
- BM-13 добавлен как новое требование `GET /bet/{bet_id}` (D-02 enforced)
- Traceability table содержит BM-13 → Phase 3 → Pending
- Coverage counters обновлены: 43 total, Phase 3 = 9 requirements
- Каждое изменение содержит TZ citation или D-XX backreference
- Все существующие 97 P1+P2 тестов остаются зелёными (REQUIREMENTS.md doc-only — не задевает код)
</success_criteria>

<output>
After completion, create `.planning/phases/03-bet-maker-domain-db/03-01-SUMMARY.md` следуя `@$HOME/.claude/get-shit-done/templates/summary.md`.

SUMMARY должен явно содержать TZ-citations (page numbers) подтверждающие drift, и обновить статус всех 4 task'ов на `complete`.
</output>
