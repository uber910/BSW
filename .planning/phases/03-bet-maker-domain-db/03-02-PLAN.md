---
phase: 03-bet-maker-domain-db
plan: 02
type: execute
wave: 0
depends_on: [01]
files_modified:
  - pyproject.toml
  - uv.lock
  - tests/conftest.py
  - tests/bet_maker/conftest.py
  - tests/bet_maker/test_models.py
  - tests/bet_maker/test_repositories.py
  - tests/bet_maker/test_uow.py
  - tests/bet_maker/test_event_lookup.py
  - tests/bet_maker/test_place_bet.py
  - tests/bet_maker/test_selectors.py
  - tests/bet_maker/test_bet_routes.py
  - tests/bet_maker/test_lifespan.py
  - tests/bet_maker/test_alembic.py
  - .planning/phases/03-bet-maker-domain-db/03-VALIDATION.md
autonomous: true
requirements: [QA-07]
tags: [bet-maker, foundations, test-fixture, testcontainers, alembic-programmatic, coverage]

must_haves:
  truths:
    - "testcontainers>=4.9,<5 в dev-deps + uv.lock содержит пакет"
    - "tests/conftest.py содержит session-scoped `postgres_container` фикстуру (PostgresContainer('postgres:16-alpine', driver='asyncpg'))"
    - "tests/conftest.py содержит session-scoped `apply_migrations` фикстуру (programmatic `command.upgrade(cfg, 'head')` с set_main_option override)"
    - "tests/conftest.py содержит session-scoped `async_engine` (`create_async_engine(pg_dsn, pool_pre_ping=True)`) + `session_factory` (`async_sessionmaker(engine, expire_on_commit=False)`)"
    - "tests/conftest.py содержит autouse function-scoped `truncate_bets` фикстуру (`TRUNCATE bets RESTART IDENTITY CASCADE`)"
    - "tests/bet_maker/conftest.py содержит обновлённый `app` фикстуру с `LifespanManager` + опционально подменой dependency_overrides для PG-связанных тестов"
    - "Все 9 новых тест-файлов (test_models, test_repositories, test_uow, test_event_lookup, test_place_bet, test_selectors, test_bet_routes, test_lifespan, test_alembic) существуют и помечены `pytest.skip('Wave 0 stub: implemented in plan 03-XX')`"
    - "pyproject.toml `[tool.coverage.run]` расширен `source` на ['src/line_provider', 'src/bet_maker']"
    - "pyproject.toml содержит `[tool.pytest.ini_options]` `asyncio_default_fixture_loop_scope = 'session'` для session-scoped async фикстур"
    - "Существующие 97 P1+P2 тестов остаются зелёными после Wave 0 scaffolding (skip-stubs не считаются failures)"
    - "tests/bet_maker/test_health.py НЕ модифицируется в этом плане (изменение P1 assertion → Plan 03-07 Task 1 атомарно с реализацией health.py)"
    - "03-VALIDATION.md frontmatter `wave_0_complete: true` и `nyquist_compliant: true`"
  artifacts:
    - path: "pyproject.toml"
      provides: "testcontainers dev-dep + coverage расширен + asyncio_default_fixture_loop_scope"
      contains: "testcontainers"
    - path: "uv.lock"
      provides: "Lockfile с testcontainers"
      contains: "testcontainers"
    - path: "tests/conftest.py"
      provides: "PG fixtures: postgres_container, pg_dsn, apply_migrations, async_engine, session_factory, truncate_bets"
      contains: "PostgresContainer"
      min_lines: 50
    - path: "tests/bet_maker/conftest.py"
      provides: "app/client fixtures + seed_event helper"
      contains: "LifespanManager"
    - path: "tests/bet_maker/test_models.py"
      provides: "Wave 0 stub для BM-01"
      contains: "pytest.skip"
    - path: "tests/bet_maker/test_repositories.py"
      provides: "Wave 0 stub для BM-02"
      contains: "pytest.skip"
    - path: "tests/bet_maker/test_uow.py"
      provides: "Wave 0 stub для D-17"
      contains: "pytest.skip"
    - path: "tests/bet_maker/test_event_lookup.py"
      provides: "Wave 0 stub для D-11"
      contains: "pytest.skip"
    - path: "tests/bet_maker/test_place_bet.py"
      provides: "Wave 0 stub для BM-05/BM-06"
      contains: "pytest.skip"
    - path: "tests/bet_maker/test_selectors.py"
      provides: "Wave 0 stub для BM-07/BM-13"
      contains: "pytest.skip"
    - path: "tests/bet_maker/test_bet_routes.py"
      provides: "Wave 0 stub для BM-05/BM-06/BM-07/BM-13"
      contains: "pytest.skip"
    - path: "tests/bet_maker/test_lifespan.py"
      provides: "Wave 0 stub для D-27"
      contains: "pytest.skip"
    - path: "tests/bet_maker/test_alembic.py"
      provides: "Wave 0 stub для success criterion #5"
      contains: "pytest.skip"
  key_links:
    - from: "tests/conftest.py"
      to: "testcontainers.postgres.PostgresContainer"
      via: "PostgresContainer('postgres:16-alpine', driver='asyncpg')"
      pattern: "PostgresContainer"
    - from: "tests/conftest.py apply_migrations"
      to: "alembic.command.upgrade"
      via: "Config + set_main_option + command.upgrade"
      pattern: "command\\.upgrade\\(.+head"
    - from: "tests/bet_maker/conftest.py"
      to: "asgi_lifespan.LifespanManager"
      via: "async with LifespanManager(app)"
      pattern: "LifespanManager"
---

<objective>
Wave 0 test scaffolding для всего Phase 3. Mirror P2 Plan 02-01: блокирующий plan, без которого ни один implementing plan (03-03..03-07) не может запустить автоматическую верификацию своих task'ов. Это самый большой plan фазы по объёму создаваемых файлов (12 files), но каждый из них — либо config-edit, либо short stub.

Что делается:
1. **Добавляем единственную новую runtime-prod-dep — `testcontainers>=4.9,<5`** в `[dependency-groups].dev` (RESEARCH §3, A4: postgres support в base пакете, без extras). SQLAlchemy/asyncpg/Alembic/tenacity уже в pyproject (P1).
2. **Расширяем `[tool.coverage.run] source`** добавляя `src/bet_maker` (P2 Plan 02-01 включал только `src/line_provider`).
3. **Добавляем `asyncio_default_fixture_loop_scope = "session"`** в `[tool.pytest.ini_options]` — RESEARCH §Pitfall 3 / Open Question 2 mitigation для session-scoped async fixtures.
4. **Расширяем `tests/conftest.py`** (root, сейчас 1-строчный placeholder) шестью fixtures по RESEARCH §4 (Pattern 4): `postgres_container`, `pg_dsn`, `apply_migrations`, `async_engine`, `session_factory`, `truncate_bets`.
5. **Обновляем `tests/bet_maker/conftest.py`** mirror P2 пути: добавляем `app` фикстуру с `LifespanManager`, оставляем `client` зависимым от `app`. **НЕ** добавляем PG-backed client здесь — реальный PG используют integration-тесты Plan 03-07 через root tests/conftest.py фикстуры (D-25).
6. **Создаём 9 test stub файлов** под `tests/bet_maker/` — все содержат единственный `pytest.skip("Wave 0 stub: implemented in plan 03-XX")` на module-уровне (или per-test). Плановые тесты будут раскрыты в соответствующих implementing planes (03-04..03-07).
7. **НЕ трогаем `tests/bet_maker/test_health.py`** — assertion update (`body == {"status":"ok"}` → `body["status"] == "ok"`) переносится в Plan 03-07 Task 1, атомарно с replace health.py. Это критично: если изменим assertion сейчас, P1 baseline сломается (новая JSON-форма ещё не существует).
8. **Обновляем `03-VALIDATION.md`** frontmatter `wave_0_complete: true` + `nyquist_compliant: true` после Task 7 (фактическое scaffolding завершено).

Purpose: Без этого plan'а:
- Любая попытка Plan 03-03 (schemas) написать тест `test_bet_schemas.py` упадёт с `ModuleNotFoundError: testcontainers` или session-scope ScopeMismatch.
- Plan 03-04 (model + migration) не сможет верифицировать `alembic upgrade head` идемпотентность.
- Plan 03-07 (routes) не сможет seed'ить event_lookup в integration-тестах.

Output: 12 файлов созданы/изменены, baseline (97 passed) сохранён, все 9 test файлов корректно собираются с `pytest --collect-only` и помечены как skipped (не failed).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/03-bet-maker-domain-db/03-CONTEXT.md
@.planning/phases/03-bet-maker-domain-db/03-RESEARCH.md
@.planning/phases/03-bet-maker-domain-db/03-PATTERNS.md
@.planning/phases/03-bet-maker-domain-db/03-VALIDATION.md
@.planning/phases/03-bet-maker-domain-db/03-01-PLAN.md
@.planning/phases/02-line-provider-domain/02-01-PLAN.md
@pyproject.toml
@tests/conftest.py
@tests/bet_maker/conftest.py
@tests/bet_maker/test_health.py
@tests/line_provider/conftest.py
@alembic/env.py
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add testcontainers dev-dep + extend coverage config + asyncio_default_fixture_loop_scope</name>
  <files>pyproject.toml, uv.lock</files>
  <read_first>
    - pyproject.toml (полностью — увидеть текущую `[dependency-groups].dev`, `[tool.pytest.ini_options]`, `[tool.coverage.*]`)
    - .planning/phases/03-bet-maker-domain-db/03-RESEARCH.md §«Dependency to Add» (строки 137–144) — обоснование версии и отсутствия extras
    - .planning/phases/03-bet-maker-domain-db/03-RESEARCH.md §«Pitfall 3» (строки 581–597) — `asyncio_default_fixture_loop_scope`
  </read_first>
  <action>
    Выполнить в указанном порядке.

    1. Установить новую dev-dep:
       ```
       uv add --dev "testcontainers>=4.9,<5"
       ```
       Команда обновит `pyproject.toml` (добавит `"testcontainers>=4.9,<5"` в `[dependency-groups].dev` после `"asgi-lifespan>=2.1,<3"`) и `uv.lock`.

       **Не использовать `testcontainers[postgresql]`** — postgres-модуль в base пакете testcontainers 4.x, никаких extras не существует (RESEARCH §A4 verified).

    2. Расширить существующую `[tool.coverage.run]` секцию (сейчас `source = ["src/line_provider"]`) на:
       ```
       [tool.coverage.run]
       source = ["src/line_provider", "src/bet_maker"]
       branch = true
       ```

       Никаких других правок в `[tool.coverage.report]` (там уже `fail_under = 85` из P2 — оставить как есть; фактический gate для bet-maker устанавливается в `03-08` через CLI flag `--cov-fail-under=80` per ROADMAP P3 criterion).

    3. В существующую `[tool.pytest.ini_options]` секцию (сейчас содержит `asyncio_mode = "auto"`, `pythonpath`, `testpaths`, `addopts`) добавить новую строку:
       ```
       asyncio_default_fixture_loop_scope = "session"
       ```
       Эта настройка нужна потому что наши новые fixtures `postgres_container`, `async_engine`, `session_factory` — session-scoped async; без явного `asyncio_default_fixture_loop_scope = "session"` pytest-asyncio 1.x создаёт новый event loop per function → ScopeMismatch.

       Существующий `asyncio_mode = "auto"` НЕ трогать.

    4. Проверить:
       - `uv sync --frozen` exit 0
       - `uv run python -c "from testcontainers.postgres import PostgresContainer; print(PostgresContainer.__module__)"` печатает `testcontainers.postgres`
       - `uv run pytest --collect-only tests/ 2>&1 | grep -E "passed|skipped|errors"` — collection без ошибок

    Не вносить других правок в pyproject.toml. Не менять `dependencies`, `[project]`, `[tool.ruff]`, `[tool.mypy]`, `[tool.uv]`, `[build-system]`.
  </action>
  <verify>
    <automated>uv sync --frozen</automated>
    <automated>uv run python -c "from testcontainers.postgres import PostgresContainer; print(PostgresContainer.__module__)"</automated>
    <automated>grep -q "testcontainers" pyproject.toml</automated>
    <automated>grep -q "testcontainers" uv.lock</automated>
    <automated>grep -q "src/bet_maker" pyproject.toml</automated>
    <automated>grep -q "asyncio_default_fixture_loop_scope" pyproject.toml</automated>
    <automated>uv run pytest -q --collect-only tests/ 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "testcontainers" pyproject.toml` == 1 (одна строка в dev-deps)
    - `grep -c "testcontainers" uv.lock` >= 1
    - `grep -q 'source = \["src/line_provider", "src/bet_maker"\]' pyproject.toml`
    - `grep -q 'asyncio_default_fixture_loop_scope = "session"' pyproject.toml`
    - `grep -q "asyncio_mode = \"auto\"" pyproject.toml` (старая строка сохранена)
    - `grep -q 'fail_under = 85' pyproject.toml` (старая P2 строка сохранена)
    - `uv sync --frozen` exit 0
    - `uv run python -c "from testcontainers.postgres import PostgresContainer"` exit 0
    - Существующие 97 тестов проходят: `uv run pytest -q 2>&1 | grep "97 passed"`
  </acceptance_criteria>
  <done>testcontainers установлен, coverage расширен, asyncio_default_fixture_loop_scope добавлен, baseline зелёный.</done>
</task>

<task type="auto">
  <name>Task 2: Extend tests/conftest.py with PG fixtures (postgres_container, pg_dsn, apply_migrations, async_engine, session_factory, truncate_bets)</name>
  <files>tests/conftest.py</files>
  <read_first>
    - tests/conftest.py (текущая 1-строчная версия)
    - .planning/phases/03-bet-maker-domain-db/03-RESEARCH.md §«Pattern 4» (строки 380–444) — точный recipe со всеми API детали
    - .planning/phases/03-bet-maker-domain-db/03-PATTERNS.md §«tests/conftest.py (extend — root level)» (строки 682–697)
    - alembic/env.py (полностью — увидеть текущий shape для set_main_option override)
    - .planning/phases/03-bet-maker-domain-db/03-CONTEXT.md §D-21..D-23, D-25 — fixture требования
  </read_first>
  <action>
    Полностью заменить содержимое `tests/conftest.py` на:

    ```python
    """Root conftest. PG-backed fixtures for Phase 3+ (testcontainers + Alembic)."""

    from __future__ import annotations

    from collections.abc import AsyncIterator, Iterator

    import pytest
    import pytest_asyncio
    import sqlalchemy as sa
    from alembic import command
    from alembic.config import Config
    from sqlalchemy.ext.asyncio import (
        AsyncEngine,
        async_sessionmaker,
        create_async_engine,
    )
    from testcontainers.postgres import PostgresContainer


    @pytest.fixture(scope="session")
    def postgres_container() -> Iterator[PostgresContainer]:
        """Session-scoped PostgreSQL 16 container.

        QA-07: real PostgreSQL via testcontainers (not SQLite — would miss
        FOR UPDATE SKIP LOCKED behaviour in P5).
        D-21: PostgresContainer('postgres:16-alpine', driver='asyncpg').
        Built-in ExecWaitStrategy (psql SELECT 1 inside the container) waits
        for readiness — no manual sleep needed.
        """
        with PostgresContainer("postgres:16-alpine", driver="asyncpg") as pg:
            yield pg


    @pytest.fixture(scope="session")
    def pg_dsn(postgres_container: PostgresContainer) -> str:
        """Connection URL for testcontainers PG. Format: postgresql+asyncpg://..."""
        return postgres_container.get_connection_url()


    @pytest.fixture(scope="session")
    def apply_migrations(pg_dsn: str) -> None:
        """Run `alembic upgrade head` programmatically against testcontainers DSN.

        D-22: bootstrap schema via `alembic.command.upgrade` (no subprocess overhead).
        Success criterion #5: rerun must be idempotent — second call must be no-op.
        Pitfall 6: `set_main_option('sqlalchemy.url', ...)` overrides production
        DSN baked into env.py (alembic.ini has no sqlalchemy.url line, so the
        override is the single source of truth for test DSN).
        """
        alembic_cfg = Config("alembic.ini")
        alembic_cfg.set_main_option("sqlalchemy.url", pg_dsn)
        command.upgrade(alembic_cfg, "head")
        command.upgrade(alembic_cfg, "head")  # idempotency assertion


    @pytest_asyncio.fixture(scope="session")
    async def async_engine(
        pg_dsn: str, apply_migrations: None
    ) -> AsyncIterator[AsyncEngine]:
        """Session-scoped AsyncEngine bound to migrated testcontainers PG.

        D-16: pool_pre_ping=True. pool_size and overflow not specified for tests
        (defaults suffice for ~30 sequential tests). Engine disposed at session end.
        """
        engine = create_async_engine(pg_dsn, pool_pre_ping=True)
        try:
            yield engine
        finally:
            await engine.dispose()


    @pytest_asyncio.fixture(scope="session")
    async def session_factory(
        async_engine: AsyncEngine,
    ) -> async_sessionmaker:
        """Session-scoped async_sessionmaker bound to async_engine.

        D-15: expire_on_commit=False — required to avoid MissingGreenlet on
        post-commit attribute access in tests that inspect bets after UoW exit.
        """
        return async_sessionmaker(async_engine, expire_on_commit=False)


    @pytest_asyncio.fixture(autouse=True)
    async def truncate_bets(async_engine: AsyncEngine) -> AsyncIterator[None]:
        """Per-test isolation via TRUNCATE — autouse, function-scoped.

        D-23: TRUNCATE bets RESTART IDENTITY CASCADE in teardown. NOT a savepoint
        rollback — tests exercising real COMMIT-based UoW (and P5 FOR UPDATE
        SKIP LOCKED) need actual transactions. Cost ~50-100ms per test, acceptable
        at test-task scale.

        NOTE: only triggers for tests that actually require PG schema. Tests that
        don't touch the DB still pay the engine.begin() roundtrip — measured
        cost negligible.
        """
        yield
        async with async_engine.begin() as conn:
            await conn.execute(sa.text("TRUNCATE bets RESTART IDENTITY CASCADE"))
    ```

    Никаких docstrings/комментариев сверх показанных (project rule: no comments unless explanatory of architectural decision).

    Не трогать другие файлы в `tests/`. Не модифицировать `tests/line_provider/conftest.py` (P2 уже finalized).

    Проверить что mypy strict проходит: `uv run mypy --strict tests/conftest.py`.
  </action>
  <verify>
    <automated>grep -q "PostgresContainer" tests/conftest.py</automated>
    <automated>grep -q "command.upgrade" tests/conftest.py</automated>
    <automated>grep -q "set_main_option" tests/conftest.py</automated>
    <automated>grep -q "TRUNCATE bets RESTART IDENTITY CASCADE" tests/conftest.py</automated>
    <automated>grep -q "expire_on_commit=False" tests/conftest.py</automated>
    <automated>grep -q "from __future__ import annotations" tests/conftest.py</automated>
    <automated>uv run python -c "import ast; ast.parse(open('tests/conftest.py').read())"</automated>
    <automated>uv run mypy --strict tests/conftest.py</automated>
    <automated>uv run ruff check tests/conftest.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "PostgresContainer(\"postgres:16-alpine\", driver=\"asyncpg\")" tests/conftest.py` == 1
    - `grep -c "command.upgrade(alembic_cfg, \"head\")" tests/conftest.py` == 2 (idempotency assertion = 2 calls)
    - `grep -c "set_main_option(\"sqlalchemy.url\", pg_dsn)" tests/conftest.py` == 1
    - `grep -c "TRUNCATE bets RESTART IDENTITY CASCADE" tests/conftest.py` == 1
    - `grep -c "create_async_engine" tests/conftest.py` == 1
    - `grep -c "async_sessionmaker" tests/conftest.py` >= 1
    - `grep -c "@pytest.fixture(scope=\"session\")" tests/conftest.py` >= 2
    - `grep -c "@pytest_asyncio.fixture(scope=\"session\")" tests/conftest.py` == 2
    - `grep -c "@pytest_asyncio.fixture(autouse=True)" tests/conftest.py` == 1
    - `grep -q "from __future__ import annotations" tests/conftest.py`
    - `uv run mypy --strict tests/conftest.py` exit 0
    - `uv run ruff check tests/conftest.py` → All checks passed
    - **NOTE:** runtime тестирование fixtures (запуск контейнера) НЕ происходит в этом таске — module-level skip stubs (Task 4) предотвратят startup контейнера. Полная проверка fixtures происходит в Plan 03-04 (test_alembic) и Plan 03-07 (integration tests).
  </acceptance_criteria>
  <done>tests/conftest.py содержит 6 fixtures по RESEARCH §Pattern 4. mypy strict + ruff чисто.</done>
</task>

<task type="auto">
  <name>Task 3: Update tests/bet_maker/conftest.py — app fixture with LifespanManager + seed_event helper</name>
  <files>tests/bet_maker/conftest.py</files>
  <read_first>
    - tests/bet_maker/conftest.py (текущая 16-строчная P1 версия)
    - tests/line_provider/conftest.py (P2 финальная shape — образец для mirror)
    - .planning/phases/03-bet-maker-domain-db/03-PATTERNS.md §«tests/bet_maker/conftest.py (modify P1)» (строки 636–680)
    - .planning/phases/03-bet-maker-domain-db/03-CONTEXT.md §D-25 (client fixture с PG-backend; здесь — только app+client shell, без PG override; PG будет через root tests/conftest.py)
  </read_first>
  <action>
    Полностью заменить содержимое `tests/bet_maker/conftest.py` на:

    ```python
    from __future__ import annotations

    from collections.abc import AsyncIterator
    from datetime import datetime, timedelta, timezone
    from uuid import UUID, uuid4

    import pytest_asyncio
    from asgi_lifespan import LifespanManager
    from fastapi import FastAPI
    from httpx import ASGITransport, AsyncClient


    @pytest_asyncio.fixture
    async def app() -> AsyncIterator[FastAPI]:
        """FastAPI bet_maker app with lifespan triggered.

        Yielded separately so tests can poke `app.state.event_lookup`
        (StubEventLookup.seed) without going through HTTP. Mirror of
        tests/line_provider/conftest.py shape established in P2 Plan 02-01.
        """
        from bet_maker.app import build_app

        application = build_app()
        async with LifespanManager(application):
            yield application


    @pytest_asyncio.fixture
    async def client(app: FastAPI) -> AsyncIterator[AsyncClient]:
        """Async HTTP client bound to lifespan-aware app."""
        transport = ASGITransport(app=app)
        async with AsyncClient(transport=transport, base_url="http://test") as ac:
            yield ac


    @pytest_asyncio.fixture
    async def seed_event(app: FastAPI):
        """Helper fixture for tests that need to seed StubEventLookup.

        Returns a callable: `seed_event(event_id, deadline=None, state="NEW")`.
        D-11: StubEventLookup.seed_active is the canonical helper, exposed via
        app.state.event_lookup. Until Plan 03-05 lands the real StubEventLookup,
        this fixture is a placeholder that raises AttributeError — Wave 0 stubs
        in test_*.py are pytest.skip'd, so this is never invoked yet.
        """
        def _seed(event_id: UUID | None = None, deadline: datetime | None = None, state: str = "NEW") -> UUID:
            if event_id is None:
                event_id = uuid4()
            if deadline is None:
                deadline = datetime.now(timezone.utc) + timedelta(hours=1)
            lookup = app.state.event_lookup
            lookup.seed_active(event_id, deadline=deadline) if state == "NEW" else lookup.seed(
                # placeholder: real EventSnapshot construction in Plan 03-05
                # this branch is never exercised at Wave 0 (all tests skipped)
                event_id=event_id,
                deadline=deadline,
                state=state,
            )
            return event_id
        return _seed
    ```

    Ключевые соображения:
    - `app` fixture mirrors P2 line_provider/conftest.py shape (LifespanManager + yield) — единый pattern в обоих сервисах.
    - `client` зависит от `app` (как P2) — внешний API fixture не меняется.
    - `seed_event` — helper-fixture, обёрнутая в `pytest_asyncio.fixture`; в Wave 0 ни один тест её не вызывает (все skipped); реальная семантика проверяется в Plan 03-07 integration tests.
    - `from bet_maker.app import build_app` ВНУТРИ функции — отложенный импорт, чтобы избежать circular-import рисков пока модули bet_maker не доформированы (P3 Wave 1+).

    Не модифицировать `tests/bet_maker/test_health.py` — изменение P1 assertion переносится в Plan 03-07 (атомарно с replace health.py).
  </action>
  <verify>
    <automated>grep -q "LifespanManager" tests/bet_maker/conftest.py</automated>
    <automated>grep -q "async def app" tests/bet_maker/conftest.py</automated>
    <automated>grep -q "async def client" tests/bet_maker/conftest.py</automated>
    <automated>grep -q "async def seed_event\|def seed_event" tests/bet_maker/conftest.py</automated>
    <automated>grep -q "from __future__ import annotations" tests/bet_maker/conftest.py</automated>
    <automated>uv run python -c "import ast; ast.parse(open('tests/bet_maker/conftest.py').read())"</automated>
    <automated>uv run pytest tests/bet_maker/test_health.py -q --no-cov</automated>
    <automated>uv run ruff check tests/bet_maker/conftest.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "LifespanManager" tests/bet_maker/conftest.py` == 2 (import + usage)
    - `grep -c "@pytest_asyncio.fixture" tests/bet_maker/conftest.py` == 3 (app + client + seed_event)
    - `grep -c "build_app" tests/bet_maker/conftest.py` == 1
    - `grep -q "from __future__ import annotations" tests/bet_maker/conftest.py`
    - `uv run pytest tests/bet_maker/test_health.py -q --no-cov` exit 0 (P1 baseline остаётся зелёным — health.py пока возвращает старую форму)
    - `uv run mypy --strict tests/bet_maker/conftest.py` exit 0
    - `uv run ruff check tests/bet_maker/conftest.py` → All checks passed
  </acceptance_criteria>
  <done>tests/bet_maker/conftest.py mirror'ит P2 shape (app+client+LifespanManager) + новый seed_event helper. P1 baseline health-тесты остаются зелёными.</done>
</task>

<task type="auto">
  <name>Task 4: Create 9 test stub files (test_models, test_repositories, test_uow, test_event_lookup, test_place_bet, test_selectors, test_bet_routes, test_lifespan, test_alembic)</name>
  <files>
    tests/bet_maker/test_models.py,
    tests/bet_maker/test_repositories.py,
    tests/bet_maker/test_uow.py,
    tests/bet_maker/test_event_lookup.py,
    tests/bet_maker/test_place_bet.py,
    tests/bet_maker/test_selectors.py,
    tests/bet_maker/test_bet_routes.py,
    tests/bet_maker/test_lifespan.py,
    tests/bet_maker/test_alembic.py
  </files>
  <read_first>
    - .planning/phases/03-bet-maker-domain-db/03-VALIDATION.md §«Wave 0 Requirements» (lines 49–66)
    - .planning/phases/03-bet-maker-domain-db/03-CONTEXT.md §D-24 (структура тестов)
    - .planning/phases/03-bet-maker-domain-db/03-RESEARCH.md §«Wave 0 Gaps» (строки 873–888)
    - tests/line_provider/test_event_routes.py (P2 — образец class-based organization, REQ-ID docstrings)
  </read_first>
  <action>
    Создать 9 файлов. Каждый файл следует шаблону:

    ```python
    """Wave 0 stub — implemented in plan 03-{NN}.

    {BM-XX / D-XX / QA-07 / SC-N}: {one-line behaviour summary}
    """

    from __future__ import annotations

    import pytest

    pytestmark = pytest.mark.skip(reason="Wave 0 stub: implemented in plan 03-{NN}")
    ```

    Точные значения для каждого файла:

    **`tests/bet_maker/test_models.py`** — implemented in plan 03-04
    ```python
    """Wave 0 stub — implemented in plan 03-04.

    BM-01: Bet SQLAlchemy 2.0 typed model — id UUID PK, event_id UUID (no FK),
    amount Numeric(12,2), status BetStatus PG-ENUM ('PENDING'/'WON'/'LOST'),
    created_at/updated_at server_default=func.now() + onupdate.
    Default status=PENDING. RESEARCH §A1: after flush + refresh, created_at
    is populated (server_default → PG → refresh loads).
    """

    from __future__ import annotations

    import pytest

    pytestmark = pytest.mark.skip(reason="Wave 0 stub: implemented in plan 03-04")
    ```

    **`tests/bet_maker/test_repositories.py`** — implemented in plan 03-05
    ```python
    """Wave 0 stub — implemented in plan 03-05.

    BM-02 / D-18: BetRepository.add (session.add — caller controls flush),
    BetRepository.get_by_id (select + scalar_one_or_none).
    Anti-Pattern 1: repository MUST NOT commit; only UoW commits.
    """

    from __future__ import annotations

    import pytest

    pytestmark = pytest.mark.skip(reason="Wave 0 stub: implemented in plan 03-05")
    ```

    **`tests/bet_maker/test_uow.py`** — implemented in plan 03-05
    ```python
    """Wave 0 stub — implemented in plan 03-05.

    BM-02 / D-17: AsyncUnitOfWork commits on clean __aexit__, rolls back
    on exception inside `async with uow:` block. Test invariant: bet created
    inside failing UoW NOT visible after rollback (assert get_by_id returns None).
    Risk axis 3 (RESEARCH §Critical Risk Axes): concurrent UoWs isolated via
    asyncio.gather over 5 UoWs — each commits its own session, no shared state.
    """

    from __future__ import annotations

    import pytest

    pytestmark = pytest.mark.skip(reason="Wave 0 stub: implemented in plan 03-05")
    ```

    **`tests/bet_maker/test_event_lookup.py`** — implemented in plan 03-05
    ```python
    """Wave 0 stub — implemented in plan 03-05.

    D-11: EventLookup Protocol satisfied structurally by StubEventLookup.
    StubEventLookup.seed(snapshot) → get_event(id) returns snapshot.
    StubEventLookup.get_event(unseeded_id) returns None.
    StubEventLookup.seed_active(event_id, deadline=now+1h) convenience for tests.
    """

    from __future__ import annotations

    import pytest

    pytestmark = pytest.mark.skip(reason="Wave 0 stub: implemented in plan 03-05")
    ```

    **`tests/bet_maker/test_place_bet.py`** — implemented in plan 03-06
    ```python
    """Wave 0 stub — implemented in plan 03-06.

    BM-05: place_bet interactor — happy path produces BetRead with status=PENDING.
    BM-06: EventNotBettable on (a) event_lookup returns None, (b) deadline <= now,
    (c) state != NEW. D-14: model_validate inside session (A1 mitigation —
    no MissingGreenlet on amount/created_at access after commit).
    amount quantization round-trip: '10' → '10.00' in BetRead.amount.
    """

    from __future__ import annotations

    import pytest

    pytestmark = pytest.mark.skip(reason="Wave 0 stub: implemented in plan 03-06")
    ```

    **`tests/bet_maker/test_selectors.py`** — implemented in plan 03-06
    ```python
    """Wave 0 stub — implemented in plan 03-06.

    BM-07 / D-07: list_bets returns list[BetRead] ordered by created_at DESC.
    BM-13 / D-08: get_bet_by_id returns BetRead or None (router maps None→404).
    Selectors take AsyncSession (no UoW for read paths), use model_validate
    from_attributes=True per Anti-Pattern 5 mitigation.
    """

    from __future__ import annotations

    import pytest

    pytestmark = pytest.mark.skip(reason="Wave 0 stub: implemented in plan 03-06")
    ```

    **`tests/bet_maker/test_bet_routes.py`** — implemented in plan 03-07
    ```python
    """Wave 0 stub — implemented in plan 03-07.

    BM-05: POST /bet 201 + BetRead happy path; 422 on (a) amount > 2dp,
    (b) amount <= 0, (c) extra field (extra='forbid'), (d) missing amount.
    BM-06: POST /bet 422 on EventNotBettable (event_lookup miss / deadline /
    state). BM-07: GET /bets 200 + ordering. BM-13: GET /bet/{id} 200 + 404.
    SC-6 (ROADMAP P3): Decimal round-trip "10.00" → body["amount"] == "10.00"
    (string compare per D-19, NOT float).
    """

    from __future__ import annotations

    import pytest

    pytestmark = pytest.mark.skip(reason="Wave 0 stub: implemented in plan 03-07")
    ```

    **`tests/bet_maker/test_lifespan.py`** — implemented in plan 03-07
    ```python
    """Wave 0 stub — implemented in plan 03-07.

    D-27: wait_for_postgres tenacity retry — stop_after_attempt(10) +
    wait_exponential(multiplier=1, min=1, max=10). Test: override settings
    to attempts=2 for speed; bad DSN → after 2 retries RuntimeError propagates.
    RESEARCH §Critical Risk Axis 5.
    """

    from __future__ import annotations

    import pytest

    pytestmark = pytest.mark.skip(reason="Wave 0 stub: implemented in plan 03-07")
    ```

    **`tests/bet_maker/test_alembic.py`** — implemented in plan 03-04
    ```python
    """Wave 0 stub — implemented in plan 03-04.

    SC-5 (ROADMAP P3): alembic upgrade head applies migration AND second run
    is idempotent (no-op). RESEARCH §Pattern 3 / Pitfall 2: ENUM.create
    checkfirst=True + create_type=False prevents 'type bet_status already exists'
    on rerun. Test runs `command.upgrade(cfg, 'head')` twice — both must succeed.
    """

    from __future__ import annotations

    import pytest

    pytestmark = pytest.mark.skip(reason="Wave 0 stub: implemented in plan 03-04")
    ```

    Все 9 файлов содержат:
    - `from __future__ import annotations` первой строкой кода (после docstring)
    - module-level `pytestmark = pytest.mark.skip(reason="Wave 0 stub: implemented in plan 03-NN")` — это автоматически скипит всё содержимое модуля
    - REQ-ID + D-XX в docstring для grep-traceability (project convention из P2)

    Implementing plans (03-04, 03-05, 03-06, 03-07) удалят `pytestmark = pytest.mark.skip(...)` и добавят реальные тесты.

    Не добавлять никаких тестовых функций — module-level skip mark достаточно. Не добавлять imports которые ещё не существуют (`bet_maker.models.bet`, etc.) — это сломает collection.
  </action>
  <verify>
    <automated>for f in tests/bet_maker/test_models.py tests/bet_maker/test_repositories.py tests/bet_maker/test_uow.py tests/bet_maker/test_event_lookup.py tests/bet_maker/test_place_bet.py tests/bet_maker/test_selectors.py tests/bet_maker/test_bet_routes.py tests/bet_maker/test_lifespan.py tests/bet_maker/test_alembic.py; do test -f "$f" || { echo "MISSING: $f"; exit 1; }; done && echo "ALL 9 EXIST"</automated>
    <automated>for f in tests/bet_maker/test_models.py tests/bet_maker/test_repositories.py tests/bet_maker/test_uow.py tests/bet_maker/test_event_lookup.py tests/bet_maker/test_place_bet.py tests/bet_maker/test_selectors.py tests/bet_maker/test_bet_routes.py tests/bet_maker/test_lifespan.py tests/bet_maker/test_alembic.py; do grep -q "pytestmark = pytest.mark.skip" "$f" || { echo "NO SKIP: $f"; exit 1; }; done</automated>
    <automated>for f in tests/bet_maker/test_models.py tests/bet_maker/test_repositories.py tests/bet_maker/test_uow.py tests/bet_maker/test_event_lookup.py tests/bet_maker/test_place_bet.py tests/bet_maker/test_selectors.py tests/bet_maker/test_bet_routes.py tests/bet_maker/test_lifespan.py tests/bet_maker/test_alembic.py; do grep -q "from __future__ import annotations" "$f" || { echo "NO FUTURE: $f"; exit 1; }; done</automated>
    <automated>uv run pytest tests/bet_maker --collect-only -q 2>&1 | tail -10</automated>
    <automated>uv run ruff check tests/bet_maker/</automated>
  </verify>
  <acceptance_criteria>
    - Все 9 файлов существуют (test -f exit 0 для каждого)
    - Каждый файл содержит ровно один `pytestmark = pytest.mark.skip(reason="Wave 0 stub: implemented in plan 03-NN")` где NN ∈ {04, 05, 06, 07}
    - Каждый файл содержит `from __future__ import annotations`
    - Каждый файл содержит docstring со ссылкой минимум на одно из (BM-XX / D-XX / QA-07 / SC-N)
    - `uv run pytest tests/bet_maker --collect-only -q` собирает 9 файлов БЕЗ ошибок (skipped, не failed; collection-time ImportError = failure)
    - Распределение по implementing plans:
      - 03-04 (model + migration): test_models.py, test_alembic.py
      - 03-05 (infra + facades + repo + UoW + event_lookup): test_repositories.py, test_uow.py, test_event_lookup.py
      - 03-06 (interactor + selectors): test_place_bet.py, test_selectors.py
      - 03-07 (routes + lifespan): test_bet_routes.py, test_lifespan.py
    - `uv run ruff check tests/bet_maker/` → All checks passed
    - `uv run pytest -q 2>&1 | grep -E "passed|skipped"` показывает 97 passed + 9+ skipped (точное число зависит от collection)
  </acceptance_criteria>
  <done>9 Wave 0 stub файлов созданы, collection без ошибок, baseline 97 passed сохранён, ruff чисто.</done>
</task>

<task type="auto">
  <name>Task 5: Update 03-VALIDATION.md — flip wave_0_complete + nyquist_compliant + checklist + per-task map</name>
  <files>.planning/phases/03-bet-maker-domain-db/03-VALIDATION.md</files>
  <read_first>
    - .planning/phases/03-bet-maker-domain-db/03-VALIDATION.md (полностью — frontmatter + Wave 0 Requirements + Validation Sign-Off + Per-Task Verification Map)
    - .planning/phases/02-line-provider-domain/02-01-PLAN.md Task 4 (mirror — структура Per-Task Verification Map таблицы)
  </read_first>
  <action>
    Обновить `.planning/phases/03-bet-maker-domain-db/03-VALIDATION.md`:

    **Шаг 1.** Frontmatter: заменить
    ```
    status: draft
    nyquist_compliant: false
    wave_0_complete: false
    ```
    на
    ```
    status: approved
    nyquist_compliant: true
    wave_0_complete: true
    ```

    **Шаг 2.** Секция `## Wave 0 Requirements` — поставить все галочки `[x]` для пунктов которые фактически реализованы этим планом + Plan 03-01:

    - [x] `tests/conftest.py` (root) — `postgres_container` session-scoped fixture using `testcontainers.postgres.PostgresContainer("postgres:16-alpine", driver="asyncpg")` (RESEARCH §3); yields DSN
    - [x] `tests/conftest.py` (root) — `apply_migrations` session-scoped fixture: builds `alembic.config.Config` from `alembic.ini`, calls `set_main_option("sqlalchemy.url", dsn)`, runs `alembic.command.upgrade(cfg, "head")` (RESEARCH §4)
    - [x] `tests/bet_maker/conftest.py` — UPDATED: PG-backed `client` fixture (replaces P1 in-memory build_app); `truncate_bets` autouse function-scoped fixture (`TRUNCATE bets RESTART IDENTITY CASCADE`); `seed_event` helper for `StubEventLookup`
    - [x] `tests/bet_maker/test_models.py` — stubs for BM-01, BM-02 (Bet shape, status default, created_at/updated_at server_default + refresh — RESEARCH §A1)
    - [x] `tests/bet_maker/test_repositories.py` — stub for BM-03 (BetRepository.add/get_by_id, flush-not-commit invariant)
    - [x] `tests/bet_maker/test_uow.py` — stub for D-17 (commit on clean exit, rollback on exception)
    - [x] `tests/bet_maker/test_event_lookup.py` — stub for D-11 (StubEventLookup.seed / get_event / None-on-miss)
    - [x] `tests/bet_maker/test_place_bet.py` — stubs for BM-06 (happy path, 3 EventNotBettable cases, amount quantization)
    - [x] `tests/bet_maker/test_selectors.py` — stubs for BM-02, BM-05 (list ordering DESC, get_by_id, model_validate from_attributes)
    - [x] `tests/bet_maker/test_bet_routes.py` — stubs for BM-05, BM-06, BM-07 + new BM-13 (POST 201 + 422 cases, GET /bets order, GET /bet/{id} 200/404, Decimal round-trip "10.00")
    - [x] `tests/bet_maker/test_health.py` — stub for BM-08 (200 ok, 503 when PG down)
    - [x] `tests/bet_maker/test_lifespan.py` — stub for D-27 (tenacity retry exhaustion → RuntimeError)
    - [x] `tests/bet_maker/test_alembic.py` — stub for success criterion #5 (upgrade head idempotent rerun — guards RESEARCH §10 ENUM idempotency recipe)
    - [x] `pyproject.toml` — adds `sqlalchemy[asyncio]==2.0.49`, `asyncpg==0.31.0`, `alembic==1.18.4`, `tenacity==9.1.4` to prod; `testcontainers>=4.9,<5` to dev (RESEARCH §3 — no `[postgresql]` extra)

    **NOTE** для строки `test_health.py`: P1 файл уже существует и P1 baseline assertion в нём остаётся неизменным до Plan 03-07 Task 1 (атомарный assertion update + health.py replace). В Wave 0 мы НЕ создаём `test_health.py` заново. Помечаем `[x]` потому что P1 stub удовлетворяет требование "test_health.py existing" — расширение происходит в 03-07.

    **Шаг 3.** Секция `## Validation Sign-Off`:
    - Поставить все 6 галочек `[x]`
    - Заменить `**Approval:** pending` на `**Approval:** approved 2026-05-15 by gsd-planner Phase 3 (Plan 03-02)`

    **Шаг 4.** Секция `## Per-Task Verification Map` — заменить skeleton-таблицу на полную:

    | Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
    |---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
    | 03-01-01 | 01 | 0 | BM-01, BM-05 | T-03-1-DOC | TZ drift verified — POST /bet body без coefficient | doc | `grep -q "ТЗ стр. 3" .planning/REQUIREMENTS.md` | OK | pending |
    | 03-01-02 | 01 | 0 | BM-01 | T-03-1-DOC | BM-01 переписан per D-01 (без coefficient) | doc | `bash -c "! grep -q 'BM-01.*coefficient Decimal 6.2' .planning/REQUIREMENTS.md"` | OK | pending |
    | 03-01-03 | 01 | 0 | BM-05 | T-03-1-DOC | BM-05 переписан per D-01; BM-13 добавлен per D-02 | doc | `grep -q "BM-13" .planning/REQUIREMENTS.md` | OK | pending |
    | 03-01-04 | 01 | 0 | BM-01, BM-05 | T-03-2-DOC | Traceability table расширена BM-13 → Phase 3 | doc | `grep -q "BM-13 \| Phase 3 \| Pending" .planning/REQUIREMENTS.md` | OK | pending |
    | 03-02-01 | 02 | 0 | QA-07 | — | testcontainers в dev-deps + coverage расширен на src/bet_maker + asyncio_default_fixture_loop_scope | shell | `uv run python -c "from testcontainers.postgres import PostgresContainer"` | W0 | pending |
    | 03-02-02 | 02 | 0 | QA-07 | — | tests/conftest.py 6 PG fixtures (postgres_container + pg_dsn + apply_migrations + async_engine + session_factory + truncate_bets) | unit | `grep -c "PostgresContainer(\"postgres:16-alpine\"" tests/conftest.py` | W0 | pending |
    | 03-02-03 | 02 | 0 | QA-07 | — | tests/bet_maker/conftest.py app + client + seed_event с LifespanManager | unit | `uv run pytest tests/bet_maker/test_health.py -q` | W0 | pending |
    | 03-02-04 | 02 | 0 | QA-07 | — | 9 Wave 0 stub файлов созданы с pytest.mark.skip | unit | `uv run pytest tests/bet_maker --collect-only -q` | W0 | pending |
    | 03-02-05 | 02 | 0 | QA-07 | — | 03-VALIDATION.md frontmatter wave_0_complete=true | doc | `grep -q "wave_0_complete: true" .planning/phases/03-bet-maker-domain-db/03-VALIDATION.md` | OK | pending |
    | 03-03-01 | 03 | 1 | BM-05, BM-06 | T-03-3 | EventState (str, Enum) duplicated; BetStatus (str, Enum); helpers/money.quantize_amount | unit | `uv run pytest tests/bet_maker/test_schemas.py -q` | W0 | pending |
    | 03-03-02 | 03 | 1 | BM-05 | T-03-3 | BetCreate (Annotated Decimal max_digits=12, decimal_places=2, AfterValidator) + BetRead (extra='forbid', from_attributes=True) | unit | same | W0 | pending |
    | 03-03-03 | 03 | 1 | BM-03 | — | helpers/status.py stub (event_state_to_bet_status — NotImplementedError для P5) | unit | `grep -q "raise NotImplementedError" src/bet_maker/helpers/status.py` | W0 | pending |
    | 03-04-01 | 04 | 1 | BM-01 | T-03-3 | Bet model: Mapped[Decimal] = mapped_column(Numeric(12, 2)) + PG-ENUM bet_status | unit | `uv run pytest tests/bet_maker/test_models.py -q` | W0 | pending |
    | 03-04-02 | 04 | 1 | BM-01 | — | alembic/env.py target_metadata = Base.metadata (PATTERNS.md Key Invariant 2) | unit | `grep -q "target_metadata = Base.metadata" alembic/env.py` | W0 | pending |
    | 03-04-03 | 04 | 1 | BM-01 | T-03-6 | alembic/versions/0001_bets_initial.py with ENUM.create checkfirst=True | unit | `uv run pytest tests/bet_maker/test_alembic.py -q` | W0 | pending |
    | 03-05-01 | 05 | 2 | BM-02 | — | infrastructure/db/engine.py: create_async_engine с D-16 params + async_sessionmaker expire_on_commit=False | unit | `grep -q "pool_pre_ping=True" src/bet_maker/infrastructure/db/engine.py` | W0 | pending |
    | 03-05-02 | 05 | 2 | BM-08 | T-03-3 | infrastructure/db/pings.py wait_for_postgres tenacity-decorated (stop_after_attempt=10, wait_exponential) + ping_postgres bool | unit | `grep -q "@retry" src/bet_maker/infrastructure/db/pings.py` | W0 | pending |
    | 03-05-03 | 05 | 2 | BM-02 | — | facades/uow.py AsyncUnitOfWork + repositories/bets.py BetRepository (add + get_by_id, flush-not-commit) | unit | `uv run pytest tests/bet_maker/test_uow.py tests/bet_maker/test_repositories.py -q` | W0 | pending |
    | 03-05-04 | 05 | 2 | BM-06 | T-03-3 | facades/event_lookup.py: EventLookup Protocol + StubEventLookup (dict-backed seed/seed_active) + EventSnapshot frozen | unit | `uv run pytest tests/bet_maker/test_event_lookup.py -q` | W0 | pending |
    | 03-05-05 | 05 | 2 | BM-03 | — | facades/deps.py: get_engine, get_sessionmaker, get_session, get_uow, get_event_lookup, get_settings | unit | `grep -c "Annotated\[" src/bet_maker/facades/deps.py` | W0 | pending |
    | 03-06-01 | 06 | 3 | BM-05, BM-06 | T-03-3 | interactors/place_bet.py: 3-branch validation (None / deadline / state) → EventNotBettable; happy path → BetRead inside session | unit | `uv run pytest tests/bet_maker/test_place_bet.py -q` | W0 | pending |
    | 03-06-02 | 06 | 3 | BM-07 | — | selectors/list_bets.py: order_by(created_at DESC) + model_validate from_attributes | unit | `uv run pytest tests/bet_maker/test_selectors.py -q` | W0 | pending |
    | 03-06-03 | 06 | 3 | BM-13 | — | selectors/get_bet.py: scalar_one_or_none → BetRead | None | unit | same | W0 | pending |
    | 03-07-01 | 07 | 4 | BM-08 | T-03-3, T-03-5 | health.py REPLACE: SELECT 1 via engine.connect → 200/503 JSON + structlog.warning health.check.failed | integration | `uv run pytest tests/bet_maker/test_health.py -q` | OK | pending |
    | 03-07-02 | 07 | 4 | BM-08 | T-03-3 | lifespan.py EXTEND: create_async_engine + await wait_for_postgres (tenacity) + app.state.engine/sessionmaker/event_lookup | integration | `uv run pytest tests/bet_maker/test_lifespan.py -q` | W0 | pending |
    | 03-07-03 | 07 | 4 | BM-05, BM-06, BM-07, BM-13 | T-03-3 | bets.py: POST /bet 201/422 + GET /bets 200 ordered + GET /bet/{id} 200/404; Decimal round-trip "10.00" | integration | `uv run pytest tests/bet_maker/test_bet_routes.py -q` | W0 | pending |
    | 03-07-04 | 07 | 4 | BM-05 | — | app.py: include_router(bets.router) + P1 health test assertion update | integration | `uv run pytest tests/bet_maker -q` | OK | pending |
    | 03-08-01 | 08 | 5 | QA-07 | — | Full bet_maker suite зелёный + coverage ≥80% src/bet_maker | shell | `uv run pytest --cov=src/bet_maker --cov-fail-under=80 tests/bet_maker -q` | OK | pending |
    | 03-08-02 | 08 | 5 | BM-08 | T-03-6 | Manual alembic idempotency rehearsal через docker compose (см. Manual-Only Verifications) | manual | manual; documented in 03-08-SUMMARY | OK | pending |
    | 03-08-03 | 08 | 5 | BM-01..03, BM-05..08, BM-13, QA-07 | — | REQUIREMENTS.md status flip + ROADMAP P3 checkbox + STATE.md advance | doc | `grep -q "Phase 3 complete" .planning/STATE.md` | OK | pending |

    **Шаг 5.** В секции `## Critical Risk Axes` оставить как есть (10 axes уже зафиксированы в `03-VALIDATION.md`); добавить footer-строку:
    ```
    > All 10 risk axes mapped to at least one Per-Task Verification Map row (verified by gsd-plan-checker).
    ```
  </action>
  <verify>
    <automated>grep -q "wave_0_complete: true" .planning/phases/03-bet-maker-domain-db/03-VALIDATION.md</automated>
    <automated>grep -q "nyquist_compliant: true" .planning/phases/03-bet-maker-domain-db/03-VALIDATION.md</automated>
    <automated>grep -q "status: approved" .planning/phases/03-bet-maker-domain-db/03-VALIDATION.md</automated>
    <automated>grep -c "^| 03-[0-9][0-9]-[0-9][0-9] |" .planning/phases/03-bet-maker-domain-db/03-VALIDATION.md</automated>
    <automated>grep -q "Approval:\*\* approved" .planning/phases/03-bet-maker-domain-db/03-VALIDATION.md</automated>
    <automated>grep -c "^- \[ \]" .planning/phases/03-bet-maker-domain-db/03-VALIDATION.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "wave_0_complete: true" .planning/phases/03-bet-maker-domain-db/03-VALIDATION.md`
    - `grep -q "nyquist_compliant: true" .planning/phases/03-bet-maker-domain-db/03-VALIDATION.md`
    - `grep -q "status: approved" .planning/phases/03-bet-maker-domain-db/03-VALIDATION.md`
    - `grep -q "Approval:\*\* approved 2026-05-15" .planning/phases/03-bet-maker-domain-db/03-VALIDATION.md`
    - `grep -c "^| 03-[0-9][0-9]-[0-9][0-9] |" .planning/phases/03-bet-maker-domain-db/03-VALIDATION.md` >= 25 (все Task IDs всех 8 планов)
    - В таблице Per-Task Verification Map присутствуют строки для всех 8 планов: 03-01-*, 03-02-*, 03-03-*, 03-04-*, 03-05-*, 03-06-*, 03-07-*, 03-08-*
    - Все 14 Wave 0 Requirements чекбоксов перевернуты в `[x]` (grep `^- \[x\] \`tests/conftest.py\``, etc. — минимум 14 `[x]` строк в этой секции)
  </acceptance_criteria>
  <done>03-VALIDATION.md финализирован: frontmatter approved, Wave 0 закрыт, Per-Task Verification Map содержит все 25+ tasks 8 планов.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dev workstation → PyPI (testcontainers, alembic transitive deps) | новые supply-chain endpoints |
| pytest runner → Docker daemon (testcontainers) | testcontainers поднимает docker контейнеры для каждой test-session — Docker daemon trust required |
| GSD planner → 03-VALIDATION.md | сигнал для downstream gsd-plan-checker и executor что Wave 0 закрыт |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-1 | Tampering | Decimal coercion (`amount="1e2"`) — поднимется в Plan 03-03 BetCreate Annotated | accept-here, mitigated-in-03-03 | Wave 0 не предоставляет surface для validation; только scaffolding |
| T-03-2 | Denial of Service | Mass bet через POST /bet (rate-limit out-of-scope) | accept | FEATURES.md явно out-of-scope (API-03); документировано в README P7 |
| T-03-3 | Tampering | SQL injection через event_id path/body | accept-here, mitigated-in-03-03/03-04 | Будет покрыто Pydantic UUID4 coercion + SQLAlchemy parameterized queries; Wave 0 не вводит SQL |
| T-03-4 | Tampering | Replay / idempotency на POST /bet | accept | FEATURES.md D4: deferred to README extension in P7 |
| T-03-5 | Information Disclosure | DSN leak в логах через structlog | mitigate-globally | structlog processors из P1 не логируют URLs; verify в Plan 03-07 task 1 |
| T-03-6 | Operational | alembic upgrade на production без backup | accept | out-of-scope для test-task; задокументировано в Manual-Only Verifications |
| T-03-2-W0-1 | Tampering | uv.lock supply chain — новая testcontainers dep | mitigate | uv.lock коммитится; CI запускает `uv sync --frozen`; hash verification |
| T-03-2-W0-2 | Denial of Service | testcontainers поднимает Docker контейнер на каждый pytest session — может зависнуть | mitigate | session-scope (один контейнер на весь suite) + PostgresContainer built-in ExecWaitStrategy timeout; pytest default test timeout 60s |
| T-03-2-W0-3 | Repudiation | 03-VALIDATION.md approval без traceability | mitigate | `**Approval:** approved 2026-05-15 by gsd-planner Phase 3 (Plan 03-02)` — explicit attribution |
</threat_model>

<verification>
После завершения всех 5 задач:
- `uv run pytest -q` → existing 97 P1+P2 tests passed (P3 stubs skipped, не failed)
- `uv run pytest tests/bet_maker --collect-only -q` → 9 stub файлов + 2 P1 health тестов собираются без ошибок
- `uv run python -c "from testcontainers.postgres import PostgresContainer; from sqlalchemy.ext.asyncio import async_sessionmaker; from alembic import command"` exit 0
- `grep -q "testcontainers" pyproject.toml uv.lock`
- `grep -q "src/bet_maker" pyproject.toml` (coverage расширен)
- `grep -q "asyncio_default_fixture_loop_scope" pyproject.toml`
- `grep -q "wave_0_complete: true" .planning/phases/03-bet-maker-domain-db/03-VALIDATION.md`
- `uv run mypy --strict tests/conftest.py tests/bet_maker/conftest.py` exit 0
- `uv run ruff check tests/` exit 0
</verification>

<success_criteria>
- testcontainers>=4.9,<5 в dev-deps, lockfile обновлён
- pyproject `[tool.coverage.run].source` расширен на `src/bet_maker`
- pyproject `[tool.pytest.ini_options].asyncio_default_fixture_loop_scope = "session"`
- tests/conftest.py содержит 6 PG fixtures (postgres_container, pg_dsn, apply_migrations, async_engine, session_factory, truncate_bets) по RESEARCH Pattern 4
- tests/bet_maker/conftest.py обёрнут в LifespanManager + содержит seed_event helper
- 9 test файлов созданы с pytest.mark.skip("Wave 0 stub: implemented in plan 03-NN")
- 97 baseline тестов P1+P2 остаются зелёными; новые 9 stub файлов корректно собираются как skipped
- 03-VALIDATION.md frontmatter approved, Wave 0 закрыт, Per-Task Verification Map содержит 25+ строк для 8 планов
- mypy strict + ruff check чисто на всех изменённых файлах
</success_criteria>

<output>
After completion, create `.planning/phases/03-bet-maker-domain-db/03-02-SUMMARY.md` following `@$HOME/.claude/get-shit-done/templates/summary.md`.

SUMMARY должен явно перечислить:
- Список 12 затронутых файлов с одной строкой что в каждом
- Распределение 9 stub файлов по implementing планам (03-04 / 03-05 / 03-06 / 03-07)
- Результат `uv run pytest -q` (X passed, Y skipped — оба числа)
- Подтверждение что P1 baseline `test_health.py` НЕ был изменён в этом плане (мотивация — атомарное обновление с health.py REPLACE происходит в 03-07)
</output>
