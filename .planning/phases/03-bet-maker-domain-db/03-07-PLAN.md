---
phase: 03-bet-maker-domain-db
plan: 07
type: execute
wave: 4
depends_on: [02, 03, 04, 05, 06]
files_modified:
  - src/bet_maker/interactors/__init__.py
  - src/bet_maker/interactors/place_bet.py
  - src/bet_maker/selectors/__init__.py
  - src/bet_maker/selectors/list_bets.py
  - src/bet_maker/selectors/get_bet.py
  - tests/bet_maker/test_place_bet.py
  - tests/bet_maker/test_selectors.py
autonomous: true
requirements: [BM-05, BM-06, BM-07, BM-13]
tags: [bet-maker, interactor, selectors, business-logic, wave-4]

must_haves:
  truths:
    - "interactors/place_bet.py содержит async def place_bet(uow, *, event_id, amount, event_lookup) -> BetRead с 3-branch validation (lookup None / deadline <= now / state != NEW)"
    - "interactors/place_bet.py содержит class EventNotBettable(Exception) с .reason attribute"
    - "place_bet вызывает model_validate(bet, from_attributes=True) ВНУТРИ session (A1 mitigation — никаких MissingGreenlet)"
    - "place_bet делает session.flush + session.refresh после bets.add (для server_default created_at/updated_at)"
    - "selectors/list_bets.py содержит async def list_bets(session) -> list[BetRead] с order_by(Bet.created_at.desc()) + model_validate from_attributes"
    - "selectors/get_bet.py содержит async def get_bet_by_id(session, bet_id) -> BetRead | None"
    - "tests/bet_maker/test_place_bet.py: 7+ тестов: happy + 3 EventNotBettable cases + amount quantization roundtrip + UoW commit-on-success + UoW rollback-on-EventNotBettable (interactor raises BEFORE UoW enter — no DB writes)"
    - "tests/bet_maker/test_selectors.py: 6+ тестов: list_bets order DESC + empty + model_validate from_attributes; get_bet_by_id happy + miss-returns-None + BetRead shape"
    - "Никакой interactor НЕ возвращает raw ORM объект — все возвраты типизированы BetRead (Anti-Pattern 5 mitigation)"
    - "EventNotBettable.reason содержит 'event not found' / 'deadline passed' / 'event not active' — три точные строки"
  artifacts:
    - path: "src/bet_maker/interactors/place_bet.py"
      provides: "place_bet interactor + EventNotBettable"
      contains: "class EventNotBettable"
      min_lines: 35
    - path: "src/bet_maker/selectors/list_bets.py"
      provides: "list_bets selector (DESC ordering, BetRead list)"
      contains: "Bet.created_at.desc()"
      min_lines: 10
    - path: "src/bet_maker/selectors/get_bet.py"
      provides: "get_bet_by_id selector (BetRead | None)"
      contains: "scalar_one_or_none"
      min_lines: 10
    - path: "tests/bet_maker/test_place_bet.py"
      provides: "place_bet interactor tests"
      contains: "test_event_not_found_raises"
    - path: "tests/bet_maker/test_selectors.py"
      provides: "list_bets + get_bet_by_id tests"
      contains: "test_list_bets_orders_by_created_at_desc"
  key_links:
    - from: "src/bet_maker/interactors/place_bet.py model_validate"
      to: "src/bet_maker/schemas/bets.BetRead.model_validate(bet, from_attributes=True)"
      via: "inside async with uow: block (A1 mitigation)"
      pattern: "model_validate\\(bet, from_attributes=True\\)"
    - from: "src/bet_maker/interactors/place_bet.py validation"
      to: "EventNotBettable raised before UoW entered"
      via: "early-return guard before async with uow:"
      pattern: "raise EventNotBettable"
---

<objective>
Wave 4 — business logic слой. Interactor (write path) + selectors (read paths). Все 3 модуля чистая Python-логика поверх схем/UoW/event_lookup; никаких HTTP routes или lifespan здесь. Routes пользуются этими модулями в Plan 03-08.

Что делается:
1. **`interactors/place_bet.py`** — единственный write use-case в P3. Точная shape per CONTEXT D-14 + RESEARCH §«Code Examples» (lines 645–696). 3-branch validation: `event_lookup.get_event(id) is None` → EventNotBettable("event not found"); `snapshot.deadline <= utc_now()` → "deadline passed"; `snapshot.state != EventState.NEW` → "event not active". На happy path — внутри UoW: `uow.bets.add(bet); await uow.session.flush(); await uow.session.refresh(bet); return BetRead.model_validate(bet, from_attributes=True)`. `refresh` критичен для загрузки server_default created_at (Pitfall 1).
2. **`selectors/list_bets.py`** — pure read через session: `select(Bet).order_by(Bet.created_at.desc())` → BetRead list.
3. **`selectors/get_bet.py`** — pure read: `select(Bet).where(Bet.id == bet_id)` → BetRead | None.
4. **`tests/bet_maker/test_place_bet.py`** — раскрытие Wave 0 stub. 7+ тестов: happy path + 3 EventNotBettable branches + quantization roundtrip + UoW-commit-on-success + no-DB-write-on-validation-fail.
5. **`tests/bet_maker/test_selectors.py`** — раскрытие Wave 0 stub. 6+ тестов: list_bets ordering DESC + empty + DESC across N bets; get_bet_by_id happy + miss returns None + BetRead.amount string serialization.

Purpose: Routes (03-08) не могут существовать без interactor (POST /bet body handler) и selectors (GET /bets + GET /bet/{id} body handlers). Все три модуля — fan-in для FastAPI routes.

Output: 7 файлов; mypy strict + ruff чисто; Critical Risk Axis 9 (GET /bets ordering under server_default) явно покрыт; baseline + 18 + 12 + 4 + 5 + 5 + 9 + 13 = 165+ total passed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/03-bet-maker-domain-db/03-CONTEXT.md
@.planning/phases/03-bet-maker-domain-db/03-RESEARCH.md
@.planning/phases/03-bet-maker-domain-db/03-PATTERNS.md
@src/bet_maker/models/bet.py
@src/bet_maker/schemas/bets.py
@src/bet_maker/schemas/events.py
@src/bet_maker/helpers/money.py
@src/bet_maker/facades/uow.py
@src/bet_maker/facades/event_lookup.py
@src/bet_maker/repositories/bets.py
@src/line_provider/interactors/set_event_state.py
@CLAUDE.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create interactors/place_bet.py — EventNotBettable + place_bet function</name>
  <files>
    src/bet_maker/interactors/__init__.py,
    src/bet_maker/interactors/place_bet.py
  </files>
  <read_first>
    - .planning/phases/03-bet-maker-domain-db/03-CONTEXT.md §D-14 (полный interactor pseudocode)
    - .planning/phases/03-bet-maker-domain-db/03-RESEARCH.md §«Code Examples» Interactor place_bet (lines 645–696) — verified shape
    - .planning/phases/03-bet-maker-domain-db/03-PATTERNS.md §«src/bet_maker/interactors/place_bet.py» (lines 207–245)
    - src/line_provider/interactors/set_event_state.py (analog — early-return validation pattern)
    - src/bet_maker/schemas/bets.py + helpers/money.py + facades/uow.py + facades/event_lookup.py (только что созданные)
    - src/config/time.py (utc_now — single source of "now")
  </read_first>
  <behavior>
    - `place_bet(uow, event_id=ID, amount=Decimal("10"), event_lookup=stub_with_active_event)` → возвращает BetRead с status=PENDING, amount=Decimal('10.00') (quantized)
    - `place_bet(...)` с unseeded event_id → `EventNotBettable(reason="event not found")` raised
    - `place_bet(...)` с deadline в прошлом → `EventNotBettable(reason="deadline passed")` raised
    - `place_bet(...)` с state=FINISHED_WIN → `EventNotBettable(reason="event not active")` raised
    - На fail-branch UoW НЕ открывается → DB пустая (verified тестом)
    - На happy path BetRead.created_at не None (refresh загрузил server_default)
    - На happy path BetRead.amount = Decimal('10.00') даже если input был '10' (quantization работает в BetCreate AfterValidator, передаётся в Bet)
  </behavior>
  <action>
    Создать 2 файла.

    **`src/bet_maker/interactors/__init__.py`** — пустой package marker.

    **`src/bet_maker/interactors/place_bet.py`** — per RESEARCH §«Code Examples» (lines 645–696):

    ```python
    from __future__ import annotations

    from decimal import Decimal
    from uuid import UUID

    import structlog

    from bet_maker.facades.event_lookup import EventLookup
    from bet_maker.facades.uow import AsyncUnitOfWork
    from bet_maker.helpers.money import quantize_amount
    from bet_maker.models.bet import Bet
    from bet_maker.schemas.bets import BetRead
    from bet_maker.schemas.events import EventState
    from config.time import utc_now

    log = structlog.get_logger()


    class EventNotBettable(Exception):
        """Raised by place_bet when event_id is not bettable.

        Route layer (Plan 03-08) catches and maps to HTTPException(422,
        detail=f"event {event_id} is not bettable: {e.reason}").

        D-06: three exact reason strings — "event not found", "deadline passed",
        "event not active" — so tests can assert on .reason without coupling
        to the full HTTP detail string.
        """

        def __init__(self, reason: str) -> None:
            self.reason = reason
            super().__init__(reason)


    async def place_bet(
        uow: AsyncUnitOfWork,
        *,
        event_id: UUID,
        amount: Decimal,
        event_lookup: EventLookup,
    ) -> BetRead:
        """Place a single bet on an event.

        D-14 / BM-05 / BM-06: validate (event_lookup snapshot exists, deadline
        in future, state == NEW) THEN insert. Validation MUST happen BEFORE
        entering the UoW context — failed validation must not trigger any
        DB transaction (verified by test_no_db_write_on_validation_fail).

        Anti-Pattern 5 / Pitfall A1 mitigation: BetRead.model_validate(bet,
        from_attributes=True) is called INSIDE `async with uow:` — after
        flush+refresh, before commit. This loads server_default fields
        (created_at, updated_at) under an open session, eliminating
        MissingGreenlet on attribute access from the route.
        """
        snapshot = await event_lookup.get_event(event_id)
        if snapshot is None:
            log.info("place_bet.rejected", event_id=str(event_id), reason="event not found")
            raise EventNotBettable("event not found")
        if snapshot.deadline <= utc_now():
            log.info(
                "place_bet.rejected",
                event_id=str(event_id),
                reason="deadline passed",
                deadline=snapshot.deadline.isoformat(),
            )
            raise EventNotBettable("deadline passed")
        if snapshot.state != EventState.NEW:
            log.info(
                "place_bet.rejected",
                event_id=str(event_id),
                reason="event not active",
                state=snapshot.state.value,
            )
            raise EventNotBettable("event not active")

        async with uow:
            bet = Bet(event_id=event_id, amount=quantize_amount(amount))
            uow.bets.add(bet)
            await uow.session.flush()
            await uow.session.refresh(bet)
            log.info(
                "place_bet.created",
                bet_id=str(bet.id),
                event_id=str(event_id),
                amount=str(bet.amount),
            )
            return BetRead.model_validate(bet, from_attributes=True)
    ```

    Ключевые non-negotiable детали:
    - **Validation ДО UoW open** — `if snapshot is None: raise EventNotBettable(...)` ДО `async with uow:`. Это означает что fail-branch не trigger`ит begin → autorollback. Тест test_no_db_write_on_validation_fail подтверждает.
    - **`snapshot.deadline <= utc_now()`** — строгое `<=` (boundary case: deadline == now НЕ позволяет — bet on event с дедлайном "прямо сейчас" rejected). Mirror P2 list_active_events `e.deadline > now` (boundary case "deadline == now is NOT active") — consistent semantics.
    - **`quantize_amount(amount)`** — ещё один quantize на input. BetCreate AfterValidator уже квантизовал на HTTP layer, но interactor может быть вызван из других мест (P5/P6 fix-up paths) с raw Decimal — defense in depth.
    - **`model_validate(bet, from_attributes=True)` ВНУТРИ session** — must be inside `async with uow:` block, before `__aexit__` commits. После commit + expire_on_commit=False атрибуты остаются доступны, но Anti-Pattern 5 советует не полагаться на это; делаем сериализацию пока session открыта.
    - **structlog.info** — структурированные логи (D-31 middleware bind request_id auto-propagates).
  </action>
  <verify>
    <automated>test -f src/bet_maker/interactors/__init__.py</automated>
    <automated>test -f src/bet_maker/interactors/place_bet.py</automated>
    <automated>grep -q "from __future__ import annotations" src/bet_maker/interactors/place_bet.py</automated>
    <automated>grep -q "class EventNotBettable(Exception):" src/bet_maker/interactors/place_bet.py</automated>
    <automated>grep -q "async def place_bet" src/bet_maker/interactors/place_bet.py</automated>
    <automated>grep -c "raise EventNotBettable" src/bet_maker/interactors/place_bet.py</automated>
    <automated>grep -q "event not found" src/bet_maker/interactors/place_bet.py</automated>
    <automated>grep -q "deadline passed" src/bet_maker/interactors/place_bet.py</automated>
    <automated>grep -q "event not active" src/bet_maker/interactors/place_bet.py</automated>
    <automated>grep -q "snapshot.deadline <= utc_now()" src/bet_maker/interactors/place_bet.py</automated>
    <automated>grep -q "snapshot.state != EventState.NEW" src/bet_maker/interactors/place_bet.py</automated>
    <automated>grep -q "model_validate(bet, from_attributes=True)" src/bet_maker/interactors/place_bet.py</automated>
    <automated>grep -q "session.refresh(bet)" src/bet_maker/interactors/place_bet.py</automated>
    <automated>uv run python -c "from bet_maker.interactors.place_bet import place_bet, EventNotBettable; e = EventNotBettable('event not found'); assert e.reason == 'event not found'; assert isinstance(e, Exception); print('OK')"</automated>
    <automated>uv run mypy --strict src/bet_maker/interactors/</automated>
    <automated>uv run ruff check src/bet_maker/interactors/</automated>
  </verify>
  <acceptance_criteria>
    - 2 файла существуют
    - `EventNotBettable` экспонирует `.reason` attribute (verified inline)
    - `place_bet` сигнатура: `(uow, *, event_id, amount, event_lookup) -> BetRead` (keyword-only после uow)
    - `grep -c "raise EventNotBettable" src/bet_maker/interactors/place_bet.py` == 3 (три rejection branches)
    - Validation happens BEFORE `async with uow:` (grep: `raise EventNotBettable` line numbers all < line number of `async with uow:`)
    - `model_validate(bet, from_attributes=True)` ВНУТРИ async with uow: (after refresh)
    - Inline import succeeds
    - `uv run mypy --strict src/bet_maker/interactors/` exit 0
    - `uv run ruff check src/bet_maker/interactors/` → All checks passed
  </acceptance_criteria>
  <done>place_bet interactor + EventNotBettable созданы. 3-branch validation outside UoW. A1 mitigation: model_validate inside session after refresh. mypy strict + ruff чисто.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create selectors/list_bets.py + selectors/get_bet.py</name>
  <files>
    src/bet_maker/selectors/__init__.py,
    src/bet_maker/selectors/list_bets.py,
    src/bet_maker/selectors/get_bet.py
  </files>
  <read_first>
    - .planning/phases/03-bet-maker-domain-db/03-RESEARCH.md §«Selectors» (lines 727–740)
    - .planning/phases/03-bet-maker-domain-db/03-CONTEXT.md §D-07 (list_bets ORDER BY created_at DESC) + §D-08 (get_bet → None|BetRead)
    - .planning/phases/03-bet-maker-domain-db/03-PATTERNS.md §«selectors/list_bets.py» + §«selectors/get_bet.py»
    - src/line_provider/selectors/list_active_events.py + get_event_by_id.py (analog — pure read pattern)
  </read_first>
  <behavior>
    - `list_bets(session)` возвращает [] для пустой таблицы
    - `list_bets(session)` возвращает list[BetRead] для N bets, отсортированных по created_at DESC (newest first)
    - `get_bet_by_id(session, uuid4())` для несуществующего id возвращает None
    - `get_bet_by_id(session, existing_bet.id)` возвращает BetRead с правильными id, event_id, amount, status, created_at
  </behavior>
  <action>
    Создать 3 файла.

    **`src/bet_maker/selectors/__init__.py`** — пустой package marker.

    **`src/bet_maker/selectors/list_bets.py`**:

    ```python
    from __future__ import annotations

    from sqlalchemy import select
    from sqlalchemy.ext.asyncio import AsyncSession

    from bet_maker.models.bet import Bet
    from bet_maker.schemas.bets import BetRead


    async def list_bets(session: AsyncSession) -> list[BetRead]:
        """Return all bets, newest first.

        D-07 / BM-07: ROADMAP P3 success criterion #3 — `GET /bets` ordered by
        created_at DESC. No pagination, no filtering at test-task scale.
        Pure read — no UoW (D-25), session is one-shot via get_session DI.

        Anti-Pattern 5 mitigation: BetRead.model_validate(row, from_attributes=True)
        inside the iteration — caller receives DTOs, never ORM instances.
        """
        stmt = select(Bet).order_by(Bet.created_at.desc())
        result = await session.execute(stmt)
        return [BetRead.model_validate(row, from_attributes=True) for row in result.scalars()]
    ```

    **`src/bet_maker/selectors/get_bet.py`**:

    ```python
    from __future__ import annotations

    from uuid import UUID

    from sqlalchemy import select
    from sqlalchemy.ext.asyncio import AsyncSession

    from bet_maker.models.bet import Bet
    from bet_maker.schemas.bets import BetRead


    async def get_bet_by_id(session: AsyncSession, bet_id: UUID) -> BetRead | None:
        """SELECT bet by id; return BetRead or None.

        D-08 / BM-13: `GET /bet/{bet_id}` route maps None → HTTPException(404).
        Anti-Pattern 5 mitigation: returns DTO (BetRead), never raw Bet ORM.
        """
        result = await session.execute(select(Bet).where(Bet.id == bet_id))
        row = result.scalar_one_or_none()
        if row is None:
            return None
        return BetRead.model_validate(row, from_attributes=True)
    ```

    Не определять каких-либо дополнительных селекторов. `list_pending_event_ids` и `list_active_events` (P5/P6) живут в отдельных будущих файлах.
  </action>
  <verify>
    <automated>test -f src/bet_maker/selectors/__init__.py</automated>
    <automated>test -f src/bet_maker/selectors/list_bets.py</automated>
    <automated>test -f src/bet_maker/selectors/get_bet.py</automated>
    <automated>grep -q "async def list_bets" src/bet_maker/selectors/list_bets.py</automated>
    <automated>grep -q "async def get_bet_by_id" src/bet_maker/selectors/get_bet.py</automated>
    <automated>grep -q "Bet.created_at.desc()" src/bet_maker/selectors/list_bets.py</automated>
    <automated>grep -q "scalar_one_or_none" src/bet_maker/selectors/get_bet.py</automated>
    <automated>grep -c "model_validate.*from_attributes=True" src/bet_maker/selectors/list_bets.py src/bet_maker/selectors/get_bet.py</automated>
    <automated>uv run python -c "from bet_maker.selectors.list_bets import list_bets; from bet_maker.selectors.get_bet import get_bet_by_id; print('OK')"</automated>
    <automated>uv run mypy --strict src/bet_maker/selectors/</automated>
    <automated>uv run ruff check src/bet_maker/selectors/</automated>
  </verify>
  <acceptance_criteria>
    - 3 файла существуют
    - list_bets.py содержит `select(Bet).order_by(Bet.created_at.desc())` и возвращает `list[BetRead]`
    - get_bet.py содержит `select(Bet).where(Bet.id == bet_id)` и `scalar_one_or_none()`
    - Оба selectors используют `BetRead.model_validate(row, from_attributes=True)` (Anti-Pattern 5)
    - `uv run mypy --strict src/bet_maker/selectors/` exit 0
    - `uv run ruff check src/bet_maker/selectors/` → All checks passed
  </acceptance_criteria>
  <done>list_bets (DESC) + get_bet_by_id (None|BetRead) selectors созданы. Anti-Pattern 5 mitigation in both. mypy strict + ruff чисто.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Replace test_place_bet.py Wave 0 stub — happy + 3 rejection + UoW commit/rollback behaviour</name>
  <files>tests/bet_maker/test_place_bet.py</files>
  <read_first>
    - tests/bet_maker/test_place_bet.py (Wave 0 stub из Plan 03-02)
    - src/bet_maker/interactors/place_bet.py (Task 1)
    - src/bet_maker/facades/event_lookup.py (для StubEventLookup в фикстурах)
    - .planning/phases/03-bet-maker-domain-db/03-VALIDATION.md §«Critical Risk Axes» #1 + #6 + #7 (Decimal + 422 specificity)
  </read_first>
  <action>
    Полностью заменить `tests/bet_maker/test_place_bet.py`:

    ```python
    """Unit tests for bet_maker.interactors.place_bet.

    BM-05: happy path inserts Bet, returns BetRead with PENDING status.
    BM-06 / D-14: three EventNotBettable branches (event not found / deadline /
    state != NEW).
    BM-05 / D-04 / D-19: amount quantization round-trip (e.g., '10' → '10.00').
    D-14 / Anti-Pattern 5: model_validate inside session (A1 mitigation —
    no MissingGreenlet on returned BetRead).
    Critical Risk Axis 1 (Decimal precision) + Axis 6 (health liveness —
    out of scope here) + Axis 9 (ordering — verified in test_selectors).
    Validation MUST happen BEFORE UoW open — test_no_db_write_on_validation_fail
    verifies failed validation produces zero DB writes.
    """

    from __future__ import annotations

    from datetime import datetime, timedelta, timezone
    from decimal import Decimal
    from uuid import UUID, uuid4

    import pytest
    from sqlalchemy import func, select
    from sqlalchemy.ext.asyncio import async_sessionmaker

    from bet_maker.facades.event_lookup import EventSnapshot, StubEventLookup
    from bet_maker.facades.uow import AsyncUnitOfWork
    from bet_maker.interactors.place_bet import EventNotBettable, place_bet
    from bet_maker.models.bet import Bet
    from bet_maker.schemas.bets import BetRead, BetStatus
    from bet_maker.schemas.events import EventState


    class TestHappyPath:
        """BM-05: happy path inserts bet with PENDING status."""

        async def test_returns_betread_with_pending_status(
            self, session_factory: async_sessionmaker
        ) -> None:
            """BM-05 / D-14: place_bet returns BetRead, status=PENDING, server_default
            created_at populated via refresh."""
            event_id = uuid4()
            lookup = StubEventLookup()
            lookup.seed_active(event_id)
            uow = AsyncUnitOfWork(session_factory)

            read = await place_bet(
                uow,
                event_id=event_id,
                amount=Decimal("10.00"),
                event_lookup=lookup,
            )

            assert isinstance(read, BetRead)
            assert read.event_id == event_id
            assert read.status == BetStatus.PENDING
            assert read.amount == Decimal("10.00")
            assert isinstance(read.id, UUID)
            assert read.created_at is not None  # refresh loaded server_default

        async def test_amount_quantized_to_two_places(
            self, session_factory: async_sessionmaker
        ) -> None:
            """D-04 / D-19: input Decimal('10') stored as '10.00' (quantize_amount)."""
            event_id = uuid4()
            lookup = StubEventLookup()
            lookup.seed_active(event_id)
            uow = AsyncUnitOfWork(session_factory)

            read = await place_bet(
                uow,
                event_id=event_id,
                amount=Decimal("10"),  # not yet 2dp
                event_lookup=lookup,
            )

            assert read.amount == Decimal("10.00")
            assert str(read.amount) == "10.00"

        async def test_persists_to_db(
            self, session_factory: async_sessionmaker
        ) -> None:
            """BM-05: UoW commits on clean exit — second session sees the bet."""
            event_id = uuid4()
            lookup = StubEventLookup()
            lookup.seed_active(event_id)
            uow = AsyncUnitOfWork(session_factory)

            read = await place_bet(
                uow,
                event_id=event_id,
                amount=Decimal("5.00"),
                event_lookup=lookup,
            )

            async with session_factory() as session:
                fetched = (await session.execute(select(Bet).where(Bet.id == read.id))).scalar_one_or_none()
                assert fetched is not None
                assert fetched.event_id == event_id


    class TestRejections:
        """BM-06 / D-14: three EventNotBettable branches."""

        async def test_event_not_found_raises(
            self, session_factory: async_sessionmaker
        ) -> None:
            """D-06: event_lookup.get_event returns None → 'event not found'."""
            lookup = StubEventLookup()  # nothing seeded
            uow = AsyncUnitOfWork(session_factory)

            with pytest.raises(EventNotBettable) as exc:
                await place_bet(
                    uow,
                    event_id=uuid4(),
                    amount=Decimal("10.00"),
                    event_lookup=lookup,
                )
            assert exc.value.reason == "event not found"

        async def test_deadline_passed_raises(
            self, session_factory: async_sessionmaker
        ) -> None:
            """D-14: deadline <= utc_now() → 'deadline passed'."""
            event_id = uuid4()
            lookup = StubEventLookup()
            past = datetime.now(timezone.utc) - timedelta(hours=1)
            lookup.seed(EventSnapshot(event_id=event_id, deadline=past, state=EventState.NEW))
            uow = AsyncUnitOfWork(session_factory)

            with pytest.raises(EventNotBettable) as exc:
                await place_bet(
                    uow,
                    event_id=event_id,
                    amount=Decimal("10.00"),
                    event_lookup=lookup,
                )
            assert exc.value.reason == "deadline passed"

        async def test_state_not_new_raises(
            self, session_factory: async_sessionmaker
        ) -> None:
            """D-14: state != NEW (FINISHED_WIN/FINISHED_LOSE) → 'event not active'."""
            event_id = uuid4()
            lookup = StubEventLookup()
            future = datetime.now(timezone.utc) + timedelta(hours=1)
            lookup.seed(
                EventSnapshot(event_id=event_id, deadline=future, state=EventState.FINISHED_WIN)
            )
            uow = AsyncUnitOfWork(session_factory)

            with pytest.raises(EventNotBettable) as exc:
                await place_bet(
                    uow,
                    event_id=event_id,
                    amount=Decimal("10.00"),
                    event_lookup=lookup,
                )
            assert exc.value.reason == "event not active"

        async def test_no_db_write_on_validation_fail(
            self, session_factory: async_sessionmaker
        ) -> None:
            """D-14: validation raises BEFORE UoW open — DB stays empty.

            If validation were INSIDE async with uow:, async_sessionmaker.begin()
            would auto-rollback on exception — same observable end-state, but
            the begin/rollback cycle would still acquire a connection from the
            pool. The test guards: no row appeared even transiently.
            """
            lookup = StubEventLookup()  # unseeded — will trigger 'event not found'
            uow = AsyncUnitOfWork(session_factory)

            with pytest.raises(EventNotBettable):
                await place_bet(
                    uow,
                    event_id=uuid4(),
                    amount=Decimal("10.00"),
                    event_lookup=lookup,
                )

            async with session_factory() as session:
                count = (await session.execute(select(func.count()).select_from(Bet))).scalar_one()
                assert count == 0
    ```

    Не использовать `app` fixture или HTTP client — это unit-уровень interactor против реального PG через session_factory.
    Не дублировать testcontainers fixtures.
  </action>
  <verify>
    <automated>bash -c "! grep -q 'pytestmark = pytest.mark.skip' tests/bet_maker/test_place_bet.py"</automated>
    <automated>uv run pytest tests/bet_maker/test_place_bet.py -q --no-cov</automated>
    <automated>grep -c "def test_" tests/bet_maker/test_place_bet.py</automated>
    <automated>grep -c "EventNotBettable" tests/bet_maker/test_place_bet.py</automated>
    <automated>grep -q '"event not found"' tests/bet_maker/test_place_bet.py</automated>
    <automated>grep -q '"deadline passed"' tests/bet_maker/test_place_bet.py</automated>
    <automated>grep -q '"event not active"' tests/bet_maker/test_place_bet.py</automated>
    <automated>uv run mypy --strict tests/bet_maker/test_place_bet.py</automated>
    <automated>uv run ruff check tests/bet_maker/test_place_bet.py</automated>
  </verify>
  <acceptance_criteria>
    - Wave 0 stub skip mark удалён
    - `grep -c "def test_" tests/bet_maker/test_place_bet.py` >= 7 (TestHappyPath: 3, TestRejections: 4)
    - Все 3 EventNotBettable reason strings явно проверяются ("event not found", "deadline passed", "event not active")
    - test_no_db_write_on_validation_fail явно делает `SELECT count(*) FROM bets == 0` после failed validation
    - test_amount_quantized_to_two_places покрывает Critical Risk Axis 1
    - `uv run pytest tests/bet_maker/test_place_bet.py -q --no-cov` — все passed
    - `uv run mypy --strict tests/bet_maker/test_place_bet.py` exit 0
    - `uv run ruff check tests/bet_maker/test_place_bet.py` → All checks passed
  </acceptance_criteria>
  <done>test_place_bet.py: 7+ тестов passed. Все 3 rejection branches + happy path + quantization + no-DB-write на validation fail. Risk Axis 1 covered.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Replace test_selectors.py Wave 0 stub — list_bets ordering + get_bet None/happy</name>
  <files>tests/bet_maker/test_selectors.py</files>
  <read_first>
    - tests/bet_maker/test_selectors.py (Wave 0 stub из Plan 03-02)
    - src/bet_maker/selectors/list_bets.py + get_bet.py (Task 2)
    - .planning/phases/03-bet-maker-domain-db/03-VALIDATION.md §«Critical Risk Axes» #9 (GET /bets ordering under server_default created_at)
  </read_first>
  <action>
    Полностью заменить `tests/bet_maker/test_selectors.py`:

    ```python
    """Unit tests for bet_maker.selectors.list_bets + get_bet.

    BM-07 / D-07: list_bets returns list[BetRead] sorted by created_at DESC.
    BM-13 / D-08: get_bet_by_id returns BetRead or None (router maps None→404).
    Anti-Pattern 5: selectors return DTOs (BetRead), never ORM instances.
    Critical Risk Axis 9 (RESEARCH §Validation): GET /bets ordering under
    server_default created_at — verified via 3 bets inserted in known order,
    list_bets returns them in DESC order.
    """

    from __future__ import annotations

    import asyncio
    from decimal import Decimal
    from uuid import uuid4

    from sqlalchemy.ext.asyncio import async_sessionmaker

    from bet_maker.models.bet import Bet
    from bet_maker.schemas.bets import BetRead, BetStatus
    from bet_maker.selectors.get_bet import get_bet_by_id
    from bet_maker.selectors.list_bets import list_bets


    class TestListBets:
        """BM-07 / Risk Axis 9: list_bets ordering and shape invariants."""

        async def test_returns_empty_list_when_no_bets(
            self, session_factory: async_sessionmaker
        ) -> None:
            """Empty table → []."""
            async with session_factory() as session:
                bets = await list_bets(session)
                assert bets == []

        async def test_orders_by_created_at_desc(
            self, session_factory: async_sessionmaker
        ) -> None:
            """D-07 / Risk Axis 9: newest first.

            Insert 3 bets with small sleeps between flushes — server_default
            func.now() assigns increasing timestamps. list_bets returns them
            with the newest (last inserted) first.
            """
            inserted_ids = []
            for i in range(3):
                async with session_factory.begin() as session:
                    bet = Bet(event_id=uuid4(), amount=Decimal(f"{i + 1}.00"))
                    session.add(bet)
                    await session.flush()
                    await session.refresh(bet)
                    inserted_ids.append(bet.id)
                # Small sleep so created_at differs at sub-second granularity
                await asyncio.sleep(0.01)

            async with session_factory() as session:
                bets = await list_bets(session)

            assert len(bets) == 3
            # Newest first — reverse of insertion order
            assert [b.id for b in bets] == list(reversed(inserted_ids))

        async def test_returns_betread_dto_not_orm(
            self, session_factory: async_sessionmaker
        ) -> None:
            """Anti-Pattern 5: list_bets returns BetRead, not ORM Bet instances."""
            async with session_factory.begin() as session:
                bet = Bet(event_id=uuid4(), amount=Decimal("1.00"))
                session.add(bet)
                await session.flush()

            async with session_factory() as session:
                bets = await list_bets(session)
                assert all(isinstance(b, BetRead) for b in bets)
                assert not any(isinstance(b, Bet) for b in bets)


    class TestGetBetById:
        """BM-13 / D-08: get_bet_by_id happy/miss/dto-shape."""

        async def test_returns_betread_for_existing(
            self, session_factory: async_sessionmaker
        ) -> None:
            """BM-13: existing bet → BetRead with correct fields."""
            event_id = uuid4()
            async with session_factory.begin() as session:
                bet = Bet(event_id=event_id, amount=Decimal("5.50"))
                session.add(bet)
                await session.flush()
                await session.refresh(bet)
                bet_id = bet.id

            async with session_factory() as session:
                read = await get_bet_by_id(session, bet_id)

            assert read is not None
            assert isinstance(read, BetRead)
            assert read.id == bet_id
            assert read.event_id == event_id
            assert read.amount == Decimal("5.50")
            assert read.status == BetStatus.PENDING

        async def test_returns_none_for_missing(
            self, session_factory: async_sessionmaker
        ) -> None:
            """D-08: unknown id → None (router maps to 404)."""
            async with session_factory() as session:
                read = await get_bet_by_id(session, uuid4())
                assert read is None

        async def test_amount_is_decimal_with_two_places(
            self, session_factory: async_sessionmaker
        ) -> None:
            """D-19 / Risk Axis 1: BetRead.amount preserves '10.00' string form."""
            async with session_factory.begin() as session:
                bet = Bet(event_id=uuid4(), amount=Decimal("10.00"))
                session.add(bet)
                await session.flush()
                bet_id = bet.id

            async with session_factory() as session:
                read = await get_bet_by_id(session, bet_id)

            assert read is not None
            assert str(read.amount) == "10.00"
    ```
  </action>
  <verify>
    <automated>bash -c "! grep -q 'pytestmark = pytest.mark.skip' tests/bet_maker/test_selectors.py"</automated>
    <automated>uv run pytest tests/bet_maker/test_selectors.py -q --no-cov</automated>
    <automated>grep -c "def test_" tests/bet_maker/test_selectors.py</automated>
    <automated>grep -q "orders_by_created_at_desc" tests/bet_maker/test_selectors.py</automated>
    <automated>uv run mypy --strict tests/bet_maker/test_selectors.py</automated>
    <automated>uv run ruff check tests/bet_maker/test_selectors.py</automated>
  </verify>
  <acceptance_criteria>
    - Wave 0 stub skip mark удалён
    - `grep -c "def test_" tests/bet_maker/test_selectors.py` >= 6 (TestListBets: 3, TestGetBetById: 3)
    - test_orders_by_created_at_desc явно вставляет N bets с asyncio.sleep между ними и проверяет reverse order
    - test_returns_none_for_missing явно покрывает 404 mapping path
    - test_returns_betread_dto_not_orm покрывает Anti-Pattern 5
    - `uv run pytest tests/bet_maker/test_selectors.py -q --no-cov` — все passed
    - `uv run mypy --strict tests/bet_maker/test_selectors.py` exit 0
    - `uv run ruff check tests/bet_maker/test_selectors.py` → All checks passed
  </acceptance_criteria>
  <done>test_selectors.py: 6+ тестов passed. Risk Axis 9 (ordering DESC) explicitly covered. Anti-Pattern 5 verified. mypy strict + ruff чисто.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Route (Plan 03-08) → interactor place_bet | BetCreate Pydantic-validated input enters here; interactor trusts schema validation already happened |
| interactor place_bet → event_lookup.get_event | service-boundary — in P3 stubbed (StubEventLookup), in P4 real httpx call to line-provider |
| interactor → UoW.session.flush/refresh | transactional gate — server_default created_at loaded under open session (A1 mitigation) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-1 | Tampering | Decimal precision drift if interactor bypasses quantize_amount | mitigate | `quantize_amount(amount)` called inside place_bet даже если BetCreate AfterValidator уже квантизовал — defense in depth. test_amount_quantized_to_two_places verifies '10' → '10.00'. |
| T-03-3 | Tampering | place_bet returns ORM Bet, route serializes raw object (MissingGreenlet, info leak) | mitigate | Anti-Pattern 5: `BetRead.model_validate(bet, from_attributes=True)` INSIDE `async with uow:` before commit. Route never sees ORM instance. test_persists_to_db separately verifies persistence; happy-path test asserts `isinstance(read, BetRead)`. |
| T-03-3-rejection | Tampering | EventNotBettable .reason exposes internal state (state machine details) | mitigate | Three exact `.reason` strings: "event not found" / "deadline passed" / "event not active" — no event_id, no deadline value, no state value leaked through .reason; structlog binds these in logs separately for ops, but HTTP detail will include only the .reason string + event_id (route-level wrapping in 03-07). |
| T-03-4 | Tampering | Duplicate POST /bet with same event_id replays — same bet twice | accept | FEATURES.md D4: Idempotency-Key out-of-scope для P3, README extension в P7. Multiple bets on same event_id is valid (one user can place multiple). |
| T-03-1-Anti5 | Information Disclosure | Future contributor returns `Bet` instance from interactor | mitigate | test_returns_betread_with_pending_status asserts `isinstance(read, BetRead)` — typing AND runtime check. PATTERNS.md §key invariants documented. |
| T-03-9-ordering | Tampering | list_bets returns bets out of order under concurrent INSERTs | accept | server_default func.now() has microsecond resolution; в test-task scale конкуррентные inserts с identical microsecond timestamps крайне маловероятны. Real production system would add monotonic seq column — out-of-scope для P3. test_orders_by_created_at_desc вставляет с явным `asyncio.sleep(0.01)` для детерминизма теста. |
</threat_model>

<verification>
После завершения всех 4 задач:
- `uv run pytest tests/bet_maker -q --no-cov` → все Wave 1+ test файлы passed (test_schemas + test_models + test_alembic + test_repositories + test_uow + test_event_lookup + test_place_bet + test_selectors + test_health P1); remaining stubs (test_bet_routes + test_lifespan) skipped
- `uv run pytest -q` → 165+ total passed
- `uv run mypy --strict src/bet_maker tests/bet_maker` exit 0
- `uv run ruff check src/bet_maker tests/bet_maker` exit 0
- Critical Risk Axis 1 (Decimal precision) covered: test_amount_quantized_to_two_places + test_amount_is_decimal_with_two_places
- Critical Risk Axis 9 (ordering) covered: test_orders_by_created_at_desc
- All 3 EventNotBettable reason strings grep-verified
</verification>

<success_criteria>
- src/bet_maker/interactors/place_bet.py: place_bet + EventNotBettable; 3-branch validation OUTSIDE UoW; A1 mitigation (model_validate inside session after refresh)
- src/bet_maker/selectors/list_bets.py: DESC ordering, returns list[BetRead]
- src/bet_maker/selectors/get_bet.py: returns BetRead | None
- tests/bet_maker/test_place_bet.py: 7+ tests (happy + 3 rejections + quantize + no-db-write + persists)
- tests/bet_maker/test_selectors.py: 6+ tests (list empty/ordering/dto + get happy/miss/decimal)
- Все 3 EventNotBettable.reason точно "event not found" / "deadline passed" / "event not active"
- Anti-Pattern 5 verified во всех 3 production файлах (model_validate from_attributes=True)
- Critical Risk Axes 1, 9 покрыты
- mypy strict + ruff check + ruff format чисто
- pytest 165+ passed
</success_criteria>

<output>
After completion, create `.planning/phases/03-bet-maker-domain-db/03-07-SUMMARY.md` following `@$HOME/.claude/get-shit-done/templates/summary.md`.

SUMMARY должен включать:
- Test counts: test_place_bet.py (≥7), test_selectors.py (≥6)
- Total pytest passed count
- Подтверждение что 3 EventNotBettable reason strings exact match ("event not found" / "deadline passed" / "event not active")
- Подтверждение что Risk Axes 1 + 9 покрыты по test name
</output>
