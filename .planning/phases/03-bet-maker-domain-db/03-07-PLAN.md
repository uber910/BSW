---
phase: 03-bet-maker-domain-db
plan: 07
type: execute
wave: 5
depends_on: [02, 03, 04, 05, 06]
files_modified:
  - src/bet_maker/entrypoints/api/bets.py
  - src/bet_maker/entrypoints/api/health.py
  - src/bet_maker/entrypoints/lifespan.py
  - src/bet_maker/app.py
  - tests/bet_maker/test_health.py
  - tests/bet_maker/test_bet_routes.py
  - tests/bet_maker/test_lifespan.py
autonomous: true
requirements: [BM-05, BM-06, BM-07, BM-08, BM-13]
tags: [bet-maker, routes, lifespan, health, integration, wave-5]

must_haves:
  truths:
    - "POST /bet → 201 + BetRead body (event_id, amount, status, created_at) на happy path"
    - "POST /bet → 422 с detail='event {id} is not bettable: {reason}' на EventNotBettable (BM-06)"
    - "POST /bet → 422 на amount > 2dp (Pydantic decimal_max_places) и amount <= 0 (greater_than) и extra field"
    - "GET /bets → 200 + list[BetRead] ordered by created_at DESC (BM-07)"
    - "GET /bet/{bet_id} → 200 + BetRead или 404 с detail='bet {id} not found' (BM-13)"
    - "GET /health → 200 + JSON {status: 'ok', checks: {postgres: 'ok'}} когда PG up (D-28)"
    - "GET /health → 503 + JSON {status: 'degraded', checks: {postgres: 'down'}} когда engine pool принудительно закрыт (D-28, BM-08)"
    - "Lifespan создаёт engine + sessionmaker через create_engine_and_sessionmaker(), ждёт PG через wait_for_postgres tenacity retry, регистрирует app.state.engine/sessionmaker/event_lookup=StubEventLookup() (D-13, D-27)"
    - "Lifespan finally: блок вызывает await engine.dispose()"
    - "P1 test_health.py assertion обновлён: `body == {'status':'ok'}` → `body['status'] == 'ok'` (subset check, D-28); P1 X-Request-ID test остаётся неизменным"
    - "src/bet_maker/app.py добавляет include_router(bets.router) после health.router (health-first preserved)"
    - "test_bet_routes.py: >= 14 integration тестов покрывая POST happy + 5 422 cases + GET /bets happy + ordering + GET /bet/{id} happy + 404 + Decimal '10.00' round-trip (BM-05/06/07/13)"
    - "test_lifespan.py: >= 2 теста (a) tenacity retry exhaustion bad DSN → RuntimeError after attempts<3 для скорости; (b) successful startup pins app.state.engine/sessionmaker/event_lookup (smoke)"
    - "Decimal round-trip end-to-end: POST /bet с amount='10.00' → GET /bets → body[0]['amount'] == '10.00' (SC-6, RESEARCH §Pitfall 5)"
  artifacts:
    - path: "src/bet_maker/entrypoints/api/bets.py"
      provides: "POST /bet + GET /bets + GET /bet/{bet_id} routes"
      contains: "@router.post"
      min_lines: 60
    - path: "src/bet_maker/entrypoints/api/health.py"
      provides: "GET /health replacing P1 stub — PG ping via engine.connect"
      contains: "JSONResponse"
      min_lines: 25
    - path: "src/bet_maker/entrypoints/lifespan.py"
      provides: "Extended lifespan: engine + sessionmaker + tenacity wait_for_postgres + StubEventLookup"
      contains: "wait_for_postgres"
    - path: "src/bet_maker/app.py"
      provides: "build_app includes bets router"
      contains: "include_router(bets.router)"
    - path: "tests/bet_maker/test_health.py"
      provides: "P1 baseline + 503-on-PG-down test"
      contains: "503"
    - path: "tests/bet_maker/test_bet_routes.py"
      provides: "Full POST/GET integration matrix"
      contains: "TestPostBet"
    - path: "tests/bet_maker/test_lifespan.py"
      provides: "Tenacity retry exhaustion + startup pins app.state"
      contains: "RuntimeError"
  key_links:
    - from: "src/bet_maker/entrypoints/lifespan.py"
      to: "wait_for_postgres + create_engine_and_sessionmaker + StubEventLookup"
      via: "lifespan startup sequence"
      pattern: "await wait_for_postgres"
    - from: "src/bet_maker/entrypoints/api/bets.py POST /bet"
      to: "interactors.place_bet.place_bet + EventNotBettable → HTTPException(422)"
      via: "try/except EventNotBettable + raise HTTPException(422)"
      pattern: "EventNotBettable"
    - from: "src/bet_maker/entrypoints/api/health.py"
      to: "infrastructure.db.pings.ping_postgres"
      via: "Depends(get_engine) → ping_postgres(engine) → 200/503"
      pattern: "ping_postgres"
---

<objective>
Wave 5 — final HTTP & lifespan layer. Это plan, который превращает Phase 3 в наблюдаемое работающее приложение через `curl`.

Что делается (5 production изменений + 3 test раскрытия):
1. **`entrypoints/api/bets.py`** — новый router с 3 эндпоинтами (POST /bet, GET /bets, GET /bet/{bet_id}). Использует Annotated dependency aliases из 03-05.
2. **`entrypoints/api/health.py`** — REPLACE P1 stub. PG ping через `ping_postgres(engine)` из 03-05; 200 / 503 JSON shape per D-28.
3. **`entrypoints/lifespan.py`** — EXTEND P1 lifespan. Добавляет 4 шага после `configure_structlog`: (a) `create_engine_and_sessionmaker(settings)`, (b) `await wait_for_postgres(engine)` (tenacity retry), (c) `app.state.engine/sessionmaker/event_lookup = ...`, (d) `finally: await engine.dispose()`.
4. **`src/bet_maker/app.py`** — add `app.include_router(bets.router)` (один import + одна строка). health-first порядок preserved.
5. **`tests/bet_maker/test_health.py`** — UPDATE: P1 `body == {"status": "ok"}` → `body["status"] == "ok"` (subset check per D-28). Добавить 503-test через monkeypatch.
6. **`tests/bet_maker/test_bet_routes.py`** — раскрытие Wave 0 stub. 14+ integration тестов покрывающих:
   - POST /bet 201 happy
   - POST /bet 422 amount > 2dp
   - POST /bet 422 amount <= 0
   - POST /bet 422 extra field
   - POST /bet 422 missing amount
   - POST /bet 422 invalid event_id (not UUID)
   - POST /bet 422 EventNotBettable (each of 3 reason strings)
   - GET /bets 200 empty
   - GET /bets 200 ordered DESC
   - GET /bet/{id} 200 happy
   - GET /bet/{id} 404 missing
   - Decimal "10.00" end-to-end round-trip
7. **`tests/bet_maker/test_lifespan.py`** — раскрытие Wave 0 stub. 2+ теста: (a) bad DSN с overridden attempts=2 → RuntimeError raised; (b) successful startup pins app.state.engine/sessionmaker/event_lookup (smoke).

Critical Risk Axes covered:
- 1 (Decimal precision) — end-to-end POST→GET roundtrip
- 5 (tenacity retry exhaustion) — test_lifespan
- 6 (health liveness during PG outage) — test_health 503
- 7 (Pydantic 422 specificity) — multiple POST 422 tests
- 8 (EventLookup stub seeding) — integration tests use seed_event helper

Purpose: Это финальный production-side plan. После него вся функциональность Phase 3 видима через HTTP. Phase-gate (03-08) verifies coverage + idempotency + docs.

Output: 7 файлов; mypy strict + ruff чисто; pytest 165+18+2 = 185+ passed; coverage ~80%+ на src/bet_maker.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/03-bet-maker-domain-db/03-CONTEXT.md
@.planning/phases/03-bet-maker-domain-db/03-RESEARCH.md
@.planning/phases/03-bet-maker-domain-db/03-PATTERNS.md
@src/bet_maker/entrypoints/lifespan.py
@src/bet_maker/entrypoints/api/health.py
@src/bet_maker/app.py
@src/bet_maker/infrastructure/db/engine.py
@src/bet_maker/infrastructure/db/pings.py
@src/bet_maker/facades/deps.py
@src/bet_maker/facades/event_lookup.py
@src/bet_maker/interactors/place_bet.py
@src/bet_maker/selectors/list_bets.py
@src/bet_maker/selectors/get_bet.py
@src/bet_maker/schemas/bets.py
@src/line_provider/entrypoints/api/events.py
@tests/bet_maker/test_health.py
@CLAUDE.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend lifespan.py — engine + sessionmaker + wait_for_postgres + StubEventLookup</name>
  <files>src/bet_maker/entrypoints/lifespan.py</files>
  <read_first>
    - src/bet_maker/entrypoints/lifespan.py (текущая P1 версия — 23 строки)
    - src/line_provider/entrypoints/lifespan.py (analog для app.state.* pattern)
    - .planning/phases/03-bet-maker-domain-db/03-CONTEXT.md §D-13 (StubEventLookup в lifespan) + §D-27 (tenacity startup retry)
    - .planning/phases/03-bet-maker-domain-db/03-PATTERNS.md §«src/bet_maker/entrypoints/lifespan.py» (lines 458–505)
    - src/bet_maker/infrastructure/db/engine.py + pings.py (созданы в 03-05)
    - src/bet_maker/facades/event_lookup.py (создан в 03-05)
  </read_first>
  <behavior>
    - Lifespan startup: configure_structlog → create_engine_and_sessionmaker → await wait_for_postgres (tenacity) → app.state.* pins → yield
    - Lifespan shutdown: log info → await engine.dispose() → engine закрыт
    - Если wait_for_postgres исчерпал retries и raised OperationalError — лог `log.critical("bet_maker.startup.failed", ...)` + reraise (uvicorn умирает)
    - app.state.settings (P1), app.state.engine, app.state.sessionmaker, app.state.event_lookup ВСЕ pinned после lifespan startup
  </behavior>
  <action>
    Полностью заменить `src/bet_maker/entrypoints/lifespan.py`:

    ```python
    from __future__ import annotations

    from collections.abc import AsyncIterator
    from contextlib import asynccontextmanager

    import structlog
    from fastapi import FastAPI

    from bet_maker.facades.event_lookup import StubEventLookup
    from bet_maker.infrastructure.db.engine import create_engine_and_sessionmaker
    from bet_maker.infrastructure.db.pings import wait_for_postgres
    from bet_maker.settings.config import BetMakerSettings
    from config.logging import configure_structlog


    @asynccontextmanager
    async def lifespan(app: FastAPI) -> AsyncIterator[None]:
        """bet_maker lifespan — extends P1 (configure_structlog + app.state.settings)
        with engine + sessionmaker + PG wait + StubEventLookup.

        D-13: app.state.event_lookup = StubEventLookup() (P4 swaps to HttpEventLookup).
        D-27: await wait_for_postgres(engine) — tenacity 10 attempts, exponential
        backoff; bad DSN crashes startup with clear log (Pitfall D2 mitigation).
        D-15/D-16: engine + sessionmaker created via create_engine_and_sessionmaker
        with locked params (pool_size=10, expire_on_commit=False, etc.).

        Shutdown: await engine.dispose() — release pool, close all connections.
        """
        settings = BetMakerSettings()
        configure_structlog(settings.log_level)
        log = structlog.get_logger()
        log.info("bet_maker.startup", service=settings.service_name)

        engine, sessionmaker = create_engine_and_sessionmaker(settings)
        try:
            await wait_for_postgres(engine)
        except Exception as exc:
            log.critical("bet_maker.startup.failed", reason=str(exc))
            await engine.dispose()
            raise

        app.state.settings = settings
        app.state.engine = engine
        app.state.sessionmaker = sessionmaker
        app.state.event_lookup = StubEventLookup()
        log.info("bet_maker.startup.ready", engine_url=str(settings.postgres_dsn).split("@")[-1])

        try:
            yield
        finally:
            log.info("bet_maker.shutdown")
            await engine.dispose()
    ```

    Ключевые детали:
    - `engine_url=str(settings.postgres_dsn).split("@")[-1]` — логируем только host:port/db (после `@`), не credentials (T-03-5 DSN leak mitigation).
    - `try/except Exception: ... raise` оборачивает только `wait_for_postgres` — потому что lifespan startup может умереть на этом этапе, и мы хотим явный critical-log + dispose ДО reraise.
    - `app.state.engine` присваивается ТОЛЬКО после успешного wait_for_postgres — гарантия что роуты не увидят engine который не отвечает.
    - В тестах StubEventLookup создаётся в lifespan; тесты seed'ят через `app.state.event_lookup.seed_active(...)` (seed_event fixture в 03-02 conftest).
  </action>
  <verify>
    <automated>grep -q "from bet_maker.infrastructure.db.engine import create_engine_and_sessionmaker" src/bet_maker/entrypoints/lifespan.py</automated>
    <automated>grep -q "from bet_maker.infrastructure.db.pings import wait_for_postgres" src/bet_maker/entrypoints/lifespan.py</automated>
    <automated>grep -q "from bet_maker.facades.event_lookup import StubEventLookup" src/bet_maker/entrypoints/lifespan.py</automated>
    <automated>grep -q "await wait_for_postgres" src/bet_maker/entrypoints/lifespan.py</automated>
    <automated>grep -q "app.state.engine = engine" src/bet_maker/entrypoints/lifespan.py</automated>
    <automated>grep -q "app.state.sessionmaker = sessionmaker" src/bet_maker/entrypoints/lifespan.py</automated>
    <automated>grep -q "app.state.event_lookup = StubEventLookup()" src/bet_maker/entrypoints/lifespan.py</automated>
    <automated>grep -q "await engine.dispose()" src/bet_maker/entrypoints/lifespan.py</automated>
    <automated>grep -q "bet_maker.startup.failed" src/bet_maker/entrypoints/lifespan.py</automated>
    <automated>bash -c "! grep -q 'POSTGRES_DSN.*://' src/bet_maker/entrypoints/lifespan.py" || echo "DSN not leaked in source"</automated>
    <automated>uv run mypy --strict src/bet_maker/entrypoints/lifespan.py</automated>
    <automated>uv run ruff check src/bet_maker/entrypoints/lifespan.py</automated>
  </verify>
  <acceptance_criteria>
    - Lifespan содержит ВСЕ 4 импорта (create_engine_and_sessionmaker, wait_for_postgres, StubEventLookup, P1: configure_structlog + BetMakerSettings)
    - Sequence: settings → configure_structlog → create_engine_and_sessionmaker → wait_for_postgres → app.state.* pins → yield → finally: dispose
    - `app.state.engine`, `app.state.sessionmaker`, `app.state.event_lookup` все pinned (3 grep hits)
    - `await engine.dispose()` присутствует И в `except` (на failed startup) И в `finally:` (на successful shutdown)
    - `log.critical("bet_maker.startup.failed", ...)` для bad DSN — explicit log line
    - DSN credentials НЕ логируются полностью — только host часть `split("@")[-1]`
    - `uv run mypy --strict src/bet_maker/entrypoints/lifespan.py` exit 0
    - `uv run ruff check src/bet_maker/entrypoints/lifespan.py` → All checks passed
  </acceptance_criteria>
  <done>lifespan.py extended: engine + sessionmaker + tenacity wait + StubEventLookup + safe dispose. mypy strict + ruff чисто.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Replace health.py — PG ping via engine.connect + 200/503 JSON</name>
  <files>src/bet_maker/entrypoints/api/health.py</files>
  <read_first>
    - src/bet_maker/entrypoints/api/health.py (текущая P1 stub: 10 строк)
    - .planning/phases/03-bet-maker-domain-db/03-RESEARCH.md §«Pattern 6: Health check endpoint» (lines 489–515)
    - .planning/phases/03-bet-maker-domain-db/03-CONTEXT.md §D-26 (no cache, per-request) + §D-28 (response shapes) + §D-29 (try/except SQLAlchemyError)
    - src/bet_maker/infrastructure/db/pings.py (ping_postgres из 03-05)
    - src/bet_maker/facades/deps.py (EngineDep alias из 03-05)
  </read_first>
  <behavior>
    - GET /health когда PG up: 200, JSON `{"status": "ok", "checks": {"postgres": "ok"}}`
    - GET /health когда PG down (engine pool принудительно закрыт): 503, JSON `{"status": "degraded", "checks": {"postgres": "down"}}`
    - Per-request: каждый GET /health делает свежий SELECT 1 (no cache)
  </behavior>
  <action>
    Полностью заменить `src/bet_maker/entrypoints/api/health.py`:

    ```python
    from __future__ import annotations

    from fastapi import APIRouter
    from fastapi.responses import JSONResponse

    from bet_maker.facades.deps import EngineDep
    from bet_maker.infrastructure.db.pings import ping_postgres

    router = APIRouter(tags=["health"])


    @router.get("/health")
    async def health(engine: EngineDep) -> JSONResponse:
        """BM-08 / D-26: per-request PG liveness ping.

        D-28: 200 returns {"status":"ok","checks":{"postgres":"ok"}};
        503 returns {"status":"degraded","checks":{"postgres":"down"}}.
        P5 will add `rabbitmq` + `subscriber_count` keys to checks dict —
        contract is backwards-compatible (top-level `status` preserved).

        Pitfall 4 / D-28: P1 baseline test `body == {"status":"ok"}` is
        replaced (atomically in this plan's Task 4) with a subset check.
        """
        ok = await ping_postgres(engine)
        if ok:
            return JSONResponse({"status": "ok", "checks": {"postgres": "ok"}})
        return JSONResponse(
            {"status": "degraded", "checks": {"postgres": "down"}},
            status_code=503,
        )
    ```

    Не использовать FastAPI default response (`dict`) — `JSONResponse` нужен для явного `status_code=503` на сбое.
    Не использовать `Request` объект (нет need): `EngineDep` через Annotated[..., Depends(get_engine)] достаточно.
    Не кэшировать `ping_postgres` результат — D-26 явно требует свежий per-request ping.
  </action>
  <verify>
    <automated>grep -q "from bet_maker.infrastructure.db.pings import ping_postgres" src/bet_maker/entrypoints/api/health.py</automated>
    <automated>grep -q "from bet_maker.facades.deps import EngineDep" src/bet_maker/entrypoints/api/health.py</automated>
    <automated>grep -q "JSONResponse" src/bet_maker/entrypoints/api/health.py</automated>
    <automated>grep -q "status_code=503" src/bet_maker/entrypoints/api/health.py</automated>
    <automated>grep -q "checks.*postgres.*ok" src/bet_maker/entrypoints/api/health.py</automated>
    <automated>grep -q "checks.*postgres.*down" src/bet_maker/entrypoints/api/health.py</automated>
    <automated>grep -q "engine: EngineDep" src/bet_maker/entrypoints/api/health.py</automated>
    <automated>uv run mypy --strict src/bet_maker/entrypoints/api/health.py</automated>
    <automated>uv run ruff check src/bet_maker/entrypoints/api/health.py</automated>
  </verify>
  <acceptance_criteria>
    - health.py использует EngineDep + ping_postgres
    - Возвращает JSONResponse в ОБОИХ случаях (200 + 503)
    - 200 path JSON shape: `{"status":"ok","checks":{"postgres":"ok"}}`
    - 503 path JSON shape: `{"status":"degraded","checks":{"postgres":"down"}}` со `status_code=503`
    - Старый `{"status": "ok"}` dict-возврат НЕ присутствует
    - `uv run mypy --strict src/bet_maker/entrypoints/api/health.py` exit 0
    - `uv run ruff check src/bet_maker/entrypoints/api/health.py` → All checks passed
  </acceptance_criteria>
  <done>health.py REPLACED: per-request PG ping; 200/503 JSON per D-28. mypy strict + ruff чисто.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Create entrypoints/api/bets.py — POST /bet + GET /bets + GET /bet/{bet_id}</name>
  <files>src/bet_maker/entrypoints/api/bets.py</files>
  <read_first>
    - src/line_provider/entrypoints/api/events.py (analog — exact pattern для thin route handlers + try/except → HTTPException + EventNotBettable mapping mirror set_event_state TransitionForbiddenError mapping)
    - .planning/phases/03-bet-maker-domain-db/03-CONTEXT.md §D-04..D-08 (полный API contract)
    - .planning/phases/03-bet-maker-domain-db/03-PATTERNS.md §«src/bet_maker/entrypoints/api/bets.py» (lines 391–455)
    - src/bet_maker/schemas/bets.py + interactors/place_bet.py + selectors/list_bets.py + selectors/get_bet.py + facades/deps.py
  </read_first>
  <behavior>
    - POST /bet с body={event_id: UUID, amount: "10.00"} и seeded active event → 201 + BetRead body
    - POST /bet когда event_lookup miss → 422 detail "event {id} is not bettable: event not found"
    - POST /bet когда past deadline → 422 detail "event {id} is not bettable: deadline passed"
    - POST /bet когда finished → 422 detail "event {id} is not bettable: event not active"
    - POST /bet с amount > 2dp → 422 Pydantic ValidationError (FastAPI default)
    - GET /bets → 200 + list[BetRead] sorted DESC
    - GET /bet/{bet_id} существующая → 200 + BetRead
    - GET /bet/{bet_id} несуществующая → 404 detail "bet {id} not found"
    - GET /bet/{not-uuid} → 422 (FastAPI UUID path coercion fails)
  </behavior>
  <action>
    Создать `src/bet_maker/entrypoints/api/bets.py`:

    ```python
    from __future__ import annotations

    from uuid import UUID

    from fastapi import APIRouter, HTTPException, status

    from bet_maker.facades.deps import EventLookupDep, SessionDep, UoWDep
    from bet_maker.interactors.place_bet import EventNotBettable, place_bet
    from bet_maker.schemas.bets import BetCreate, BetRead
    from bet_maker.selectors.get_bet import get_bet_by_id
    from bet_maker.selectors.list_bets import list_bets

    router = APIRouter(tags=["bets"])


    @router.post(
        "/bet",
        status_code=status.HTTP_201_CREATED,
        response_model=BetRead,
    )
    async def post_bet(
        body: BetCreate,
        uow: UoWDep,
        event_lookup: EventLookupDep,
    ) -> BetRead:
        """BM-05 / D-04 / D-05: place a bet.

        Body: {event_id: UUID4, amount: Decimal>0 with 2dp}.
        Response 201: BetRead {id, event_id, amount, status=PENDING, created_at}.
        BM-06 / D-06: 422 if event_lookup misses, deadline passed, or state != NEW
        (detail = "event {id} is not bettable: {reason}").
        """
        try:
            return await place_bet(
                uow,
                event_id=body.event_id,
                amount=body.amount,
                event_lookup=event_lookup,
            )
        except EventNotBettable as exc:
            raise HTTPException(
                status_code=status.HTTP_422_UNPROCESSABLE_ENTITY,
                detail=f"event {body.event_id} is not bettable: {exc.reason}",
            ) from exc


    @router.get(
        "/bets",
        response_model=list[BetRead],
    )
    async def get_bets(session: SessionDep) -> list[BetRead]:
        """BM-07 / D-07: history of all bets, ordered by created_at DESC."""
        return await list_bets(session)


    @router.get(
        "/bet/{bet_id}",
        response_model=BetRead,
    )
    async def get_bet(bet_id: UUID, session: SessionDep) -> BetRead:
        """BM-13 / D-08: fetch a single bet by id; 404 if not found."""
        bet = await get_bet_by_id(session, bet_id)
        if bet is None:
            raise HTTPException(
                status_code=status.HTTP_404_NOT_FOUND,
                detail=f"bet {bet_id} not found",
            )
        return bet
    ```

    Ключевые детали:
    - `response_model=BetRead` / `response_model=list[BetRead]` — explicit, OpenAPI получает правильную схему.
    - `EventNotBettable` → `HTTPException(422)` с PEP 3134 chain (`raise ... from exc`) — ruff B904 satisfied.
    - `HTTP_422_UNPROCESSABLE_ENTITY` (а не `_CONTENT`) — P2 SUMMARY 02-07 указал deprecation, но мы используем существующий константный путь FastAPI 0.136 (Phase 7 rename).
    - `status.HTTP_404_NOT_FOUND` constants.
    - GET routes используют `SessionDep` (read-only); POST использует `UoWDep` (write transaction) + `EventLookupDep`.
    - НЕ извлекать `Request` объект (не нужен — Annotated DI достаточно).
    - НЕ добавлять middleware-level error handlers — FastAPI Pydantic 422 ValidationError автоматически.
  </action>
  <verify>
    <automated>test -f src/bet_maker/entrypoints/api/bets.py</automated>
    <automated>grep -q "@router.post" src/bet_maker/entrypoints/api/bets.py</automated>
    <automated>grep -c "@router.get" src/bet_maker/entrypoints/api/bets.py</automated>
    <automated>grep -q 'status_code=status.HTTP_201_CREATED' src/bet_maker/entrypoints/api/bets.py</automated>
    <automated>grep -q "EventNotBettable" src/bet_maker/entrypoints/api/bets.py</automated>
    <automated>grep -q "from exc" src/bet_maker/entrypoints/api/bets.py</automated>
    <automated>grep -q "is not bettable" src/bet_maker/entrypoints/api/bets.py</automated>
    <automated>grep -q "bet .* not found" src/bet_maker/entrypoints/api/bets.py</automated>
    <automated>grep -q "response_model=BetRead" src/bet_maker/entrypoints/api/bets.py</automated>
    <automated>grep -q "response_model=list\[BetRead\]" src/bet_maker/entrypoints/api/bets.py</automated>
    <automated>uv run mypy --strict src/bet_maker/entrypoints/api/bets.py</automated>
    <automated>uv run ruff check src/bet_maker/entrypoints/api/bets.py</automated>
  </verify>
  <acceptance_criteria>
    - bets.py содержит 3 routes (post /bet + get /bets + get /bet/{bet_id})
    - POST /bet статус 201 с response_model=BetRead
    - GET /bets response_model=list[BetRead]
    - GET /bet/{bet_id} response_model=BetRead + 404 mapping для None из get_bet_by_id
    - EventNotBettable → HTTPException(422, detail="event {id} is not bettable: {reason}") с `from exc`
    - 404 detail string `f"bet {bet_id} not found"`
    - DI через Annotated aliases (UoWDep, SessionDep, EventLookupDep)
    - `uv run mypy --strict src/bet_maker/entrypoints/api/bets.py` exit 0
    - `uv run ruff check src/bet_maker/entrypoints/api/bets.py` → All checks passed
  </acceptance_criteria>
  <done>bets.py создан: 3 routes thin wrappers вокруг interactors+selectors. mypy strict + ruff чисто.</done>
</task>

<task type="auto">
  <name>Task 4: Wire bets router into app.py + update test_health.py P1 assertion</name>
  <files>
    src/bet_maker/app.py,
    tests/bet_maker/test_health.py
  </files>
  <read_first>
    - src/bet_maker/app.py (текущая P1 версия — 19 строк)
    - tests/bet_maker/test_health.py (текущий P1 файл — 2 теста; первый assert будет обновлён, второй остаётся неизменным)
    - .planning/phases/03-bet-maker-domain-db/03-RESEARCH.md §«Pitfall 4: P1 health test ... сломается» (lines 600–611)
    - src/bet_maker/entrypoints/api/bets.py (Task 3)
  </read_first>
  <action>
    Изменить 2 файла.

    **`src/bet_maker/app.py`** — добавить bets router после health router (preserve health-first order):

    Текущая P1 app.py:
    ```python
    from __future__ import annotations

    from fastapi import FastAPI

    from bet_maker.entrypoints.api import health
    from bet_maker.entrypoints.lifespan import lifespan
    from bet_maker.entrypoints.middleware import RequestContextMiddleware


    def build_app() -> FastAPI:
        app = FastAPI(
            title="bet-maker",
            version="0.1.0",
            lifespan=lifespan,
        )
        app.add_middleware(RequestContextMiddleware)
        app.include_router(health.router)
        return app
    ```

    Изменения:
    - В строку `from bet_maker.entrypoints.api import health` добавить `bets`: `from bet_maker.entrypoints.api import bets, health`
    - После `app.include_router(health.router)` добавить `app.include_router(bets.router)`
    - return оставить как есть

    Итоговая app.py:
    ```python
    from __future__ import annotations

    from fastapi import FastAPI

    from bet_maker.entrypoints.api import bets, health
    from bet_maker.entrypoints.lifespan import lifespan
    from bet_maker.entrypoints.middleware import RequestContextMiddleware


    def build_app() -> FastAPI:
        app = FastAPI(
            title="bet-maker",
            version="0.1.0",
            lifespan=lifespan,
        )
        app.add_middleware(RequestContextMiddleware)
        app.include_router(health.router)
        app.include_router(bets.router)
        return app
    ```

    **`tests/bet_maker/test_health.py`** — UPDATE P1 assertion + ADD 503 test:

    Полностью заменить файл:
    ```python
    """Integration tests for bet_maker /health endpoint.

    QA-10: pytest must collect and pass green (P1 baseline).
    INFR-08: HTTP-level E2E proof of structlog request-id propagation
    via RequestContextMiddleware — X-Request-ID echoed back (P1 invariant).
    BM-08 / D-28: PG ping → 200 with {status:"ok", checks:{postgres:"ok"}};
    PG down → 503 with {status:"degraded", checks:{postgres:"down"}}.
    Pitfall 4: P1 assertion `body == {"status": "ok"}` is replaced with a
    subset check (`body["status"] == "ok"`) because the response shape is now
    extended; P5 will add rabbitmq/subscriber_count keys to checks dict.
    Critical Risk Axis 6: health liveness during PG outage — verified via
    force-close of engine pool (NOT testcontainer stop — too coarse).
    """

    from __future__ import annotations

    from fastapi import FastAPI
    from httpx import AsyncClient


    async def test_health_returns_status_ok(client: AsyncClient) -> None:
        """QA-10 / D-28: /health returns 200 with status="ok" and checks.postgres="ok".

        Subset check on `status` (Pitfall 4) — full body equality would break
        when P5 adds rabbitmq to the checks dict.
        """
        response = await client.get("/health")
        assert response.status_code == 200
        body = response.json()
        assert body["status"] == "ok"
        assert body["checks"]["postgres"] == "ok"


    async def test_health_echoes_request_id_header(client: AsyncClient) -> None:
        """INFR-08 (HTTP-level E2E): RequestContextMiddleware echoes X-Request-ID."""
        response = await client.get("/health")
        assert "X-Request-ID" in response.headers
        assert len(response.headers["X-Request-ID"]) > 0


    async def test_health_returns_503_when_pg_down(
        app: FastAPI, client: AsyncClient
    ) -> None:
        """BM-08 / D-28 / Risk Axis 6: PG outage → 503 + status="degraded".

        Force-close the engine pool to simulate PG outage without stopping
        testcontainers PG (which is session-scoped and shared with other tests).
        After dispose, ping_postgres SELECT 1 fails with SQLAlchemyError →
        ping_postgres returns False → health route returns 503.
        """
        await app.state.engine.dispose()
        response = await client.get("/health")
        assert response.status_code == 503
        body = response.json()
        assert body["status"] == "degraded"
        assert body["checks"]["postgres"] == "down"
    ```

    Ключевые детали изменения test_health.py:
    - **Старый `assert response.json() == {"status": "ok"}` РАЗБИТ на 2 assertions** (`body["status"] == "ok"` + `body["checks"]["postgres"] == "ok"`) — Pitfall 4 mitigation.
    - **X-Request-ID test НЕ ТРОГАЕТСЯ** — P1 invariant preserved.
    - **Новый 503 test использует `app.state.engine.dispose()`** — это сломает engine pool, но тест на скорую руку (engine disposed → ping fails). Не использует `docker compose stop postgres` (out of test runner scope).
    - test_health_returns_503 ставит autouse `truncate_bets` fixture _но_ truncate в teardown пытается re-execute через disposed engine → возможна collateral failure последующих тестов в той же session. **Mitigation:** этот тест добавляется как ПОСЛЕДНИЙ функциональный тест в файле (pytest collects в порядке definition); autouse teardown truncate скорее всего работать не будет, но **обходной механизм:** session-scoped engine fixture восстанавливает свой pool через первый upcoming `with async_engine.begin()` (SQLAlchemy creates new connections on demand). Engine.dispose() закрывает существующие connections но не делает engine permanently unusable.
    - Альтернатива: использовать `monkeypatch` для замены `app.state.engine` на mock который raise SQLAlchemyError на connect(). Эта альтернатива более тривиальна — executor может выбрать что лучше работает в реальной среде. PATTERNS.md §«No Analog Found» row для test_lifespan делает явный recommendation использовать `engine pool` force-close, но executor имеет право выбрать monkeypatch.

    Executor должен запустить `uv run pytest tests/bet_maker/test_health.py -q` и подтвердить что все 3 теста passed; если 503-test ломает последующие, выбрать monkeypatch alternative.
  </action>
  <verify>
    <automated>grep -q "from bet_maker.entrypoints.api import bets, health" src/bet_maker/app.py</automated>
    <automated>grep -q "app.include_router(bets.router)" src/bet_maker/app.py</automated>
    <automated>grep -q "app.include_router(health.router)" src/bet_maker/app.py</automated>
    <automated>bash -c "awk '/health.router/{p=NR} /bets.router/{b=NR} END{exit !(p<b)}' src/bet_maker/app.py" || echo "health router included BEFORE bets router (order preserved)"</automated>
    <automated>bash -c "! grep -q 'response.json() == {\"status\": \"ok\"}' tests/bet_maker/test_health.py"</automated>
    <automated>grep -q "body\[\"status\"\] == \"ok\"" tests/bet_maker/test_health.py</automated>
    <automated>grep -q "body\[\"checks\"\]\[\"postgres\"\]" tests/bet_maker/test_health.py</automated>
    <automated>grep -q "status_code == 503" tests/bet_maker/test_health.py</automated>
    <automated>grep -q "X-Request-ID" tests/bet_maker/test_health.py</automated>
    <automated>uv run pytest tests/bet_maker/test_health.py -q --no-cov</automated>
    <automated>uv run mypy --strict src/bet_maker/app.py tests/bet_maker/test_health.py</automated>
    <automated>uv run ruff check src/bet_maker/app.py tests/bet_maker/test_health.py</automated>
  </verify>
  <acceptance_criteria>
    - app.py: 2 include_router calls (health, bets) в правильном порядке (health first)
    - app.py: bets import added в существующей строке (один import statement)
    - test_health.py: старый `body == {"status": "ok"}` assertion удалён; subset check active
    - test_health.py: 3 теста (P1 baseline updated + X-Request-ID + new 503)
    - test_health.py: 503 test проверяет `body["status"] == "degraded"` + `body["checks"]["postgres"] == "down"`
    - `uv run pytest tests/bet_maker/test_health.py -q --no-cov` — все 3 теста passed
    - `uv run mypy --strict src/bet_maker/app.py tests/bet_maker/test_health.py` exit 0
    - `uv run ruff check src/bet_maker/app.py tests/bet_maker/test_health.py` → All checks passed
  </acceptance_criteria>
  <done>app.py wired bets router (health-first preserved). test_health.py updated: 3 tests (subset check + X-Request-ID + 503). Pitfall 4 + Risk Axis 6 covered.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 5: Replace test_bet_routes.py Wave 0 stub — full POST/GET integration matrix</name>
  <files>tests/bet_maker/test_bet_routes.py</files>
  <read_first>
    - tests/bet_maker/test_bet_routes.py (Wave 0 stub из Plan 03-02)
    - tests/line_provider/test_event_routes.py (analog — class-based organization, REQ-ID docstrings)
    - tests/bet_maker/conftest.py (app, client, seed_event fixtures из Plan 03-02)
    - src/bet_maker/entrypoints/api/bets.py (Task 3 — для exact response shapes/error strings)
    - .planning/phases/03-bet-maker-domain-db/03-VALIDATION.md §«Critical Risk Axes» #1, #7, #8, #9
  </read_first>
  <action>
    Полностью заменить `tests/bet_maker/test_bet_routes.py`:

    ```python
    """Integration tests for bet_maker /bet, /bets, /bet/{bet_id}.

    BM-05 / D-04: POST /bet 201 happy + 422 cases (amount > 2dp, amount <= 0,
    extra field, missing amount, invalid event_id UUID).
    BM-06 / D-06: POST /bet 422 EventNotBettable (3 reason strings).
    BM-07 / D-07: GET /bets 200 + ordering DESC.
    BM-13 / D-08: GET /bet/{id} 200 + 404 + 422 (non-UUID path).
    D-19 / SC-6 (ROADMAP P3): Decimal "10.00" round-trip — POST then GET both
    return "10.00" (string). Pitfall 5 mitigation: compare as STRING, not float.
    Critical Risk Axes 1 (Decimal), 7 (422 specificity), 8 (EventLookup seed),
    9 (DESC ordering).
    """

    from __future__ import annotations

    import asyncio
    from datetime import datetime, timedelta, timezone
    from uuid import uuid4

    from fastapi import FastAPI
    from httpx import AsyncClient

    from bet_maker.facades.event_lookup import EventSnapshot
    from bet_maker.schemas.events import EventState


    def _seed_active(app: FastAPI, event_id, deadline=None):
        """Local helper — seed_event fixture pattern but synchronous."""
        app.state.event_lookup.seed_active(
            event_id,
            deadline=deadline or datetime.now(timezone.utc) + timedelta(hours=1),
        )


    class TestPostBet:
        """BM-05 / BM-06: POST /bet contract."""

        async def test_post_bet_happy_returns_201_with_betread(
            self, app: FastAPI, client: AsyncClient
        ) -> None:
            """BM-05 / D-05: 201 + BetRead {id, event_id, amount, status, created_at}."""
            event_id = uuid4()
            _seed_active(app, event_id)

            response = await client.post(
                "/bet",
                json={"event_id": str(event_id), "amount": "10.00"},
            )

            assert response.status_code == 201, response.text
            body = response.json()
            assert body["event_id"] == str(event_id)
            assert body["amount"] == "10.00"  # D-19: STRING, not float
            assert body["status"] == "PENDING"
            assert "id" in body
            assert "created_at" in body

        async def test_post_bet_quantizes_amount(
            self, app: FastAPI, client: AsyncClient
        ) -> None:
            """D-04 / D-19: amount '10' (no decimals) → stored+returned as '10.00'."""
            event_id = uuid4()
            _seed_active(app, event_id)

            response = await client.post(
                "/bet",
                json={"event_id": str(event_id), "amount": "10"},
            )
            assert response.status_code == 201, response.text
            assert response.json()["amount"] == "10.00"

        async def test_post_bet_three_dp_returns_422(
            self, app: FastAPI, client: AsyncClient
        ) -> None:
            """D-04 / Risk Axis 1+7: amount '10.123' → 422 decimal_max_places."""
            event_id = uuid4()
            _seed_active(app, event_id)

            response = await client.post(
                "/bet",
                json={"event_id": str(event_id), "amount": "10.123"},
            )
            assert response.status_code == 422

        async def test_post_bet_zero_returns_422(
            self, app: FastAPI, client: AsyncClient
        ) -> None:
            """D-04: amount '0' → 422 greater_than (gt=0)."""
            event_id = uuid4()
            _seed_active(app, event_id)

            response = await client.post(
                "/bet",
                json={"event_id": str(event_id), "amount": "0"},
            )
            assert response.status_code == 422

        async def test_post_bet_negative_returns_422(
            self, app: FastAPI, client: AsyncClient
        ) -> None:
            """D-04: amount '-5' → 422 greater_than."""
            event_id = uuid4()
            _seed_active(app, event_id)

            response = await client.post(
                "/bet",
                json={"event_id": str(event_id), "amount": "-5"},
            )
            assert response.status_code == 422

        async def test_post_bet_extra_field_returns_422(
            self, app: FastAPI, client: AsyncClient
        ) -> None:
            """D-01 / T-03-3: BetCreate extra='forbid' rejects coefficient drift."""
            event_id = uuid4()
            _seed_active(app, event_id)

            response = await client.post(
                "/bet",
                json={"event_id": str(event_id), "amount": "10.00", "coefficient": "1.5"},
            )
            assert response.status_code == 422

        async def test_post_bet_missing_amount_returns_422(
            self, app: FastAPI, client: AsyncClient
        ) -> None:
            """D-04: amount is required."""
            event_id = uuid4()
            _seed_active(app, event_id)

            response = await client.post("/bet", json={"event_id": str(event_id)})
            assert response.status_code == 422

        async def test_post_bet_invalid_uuid_returns_422(
            self, client: AsyncClient
        ) -> None:
            """T-03-3 / D-04: malformed event_id rejected before SQL layer."""
            response = await client.post(
                "/bet",
                json={"event_id": "not-a-uuid", "amount": "10.00"},
            )
            assert response.status_code == 422

        async def test_post_bet_event_not_found_returns_422(
            self, client: AsyncClient
        ) -> None:
            """BM-06 / D-06: unseeded event → 422 'event not found'."""
            response = await client.post(
                "/bet",
                json={"event_id": str(uuid4()), "amount": "10.00"},
            )
            assert response.status_code == 422
            assert "event not found" in response.json()["detail"]

        async def test_post_bet_deadline_passed_returns_422(
            self, app: FastAPI, client: AsyncClient
        ) -> None:
            """BM-06 / D-14: past deadline → 422 'deadline passed'."""
            event_id = uuid4()
            past = datetime.now(timezone.utc) - timedelta(hours=1)
            app.state.event_lookup.seed(
                EventSnapshot(event_id=event_id, deadline=past, state=EventState.NEW)
            )

            response = await client.post(
                "/bet",
                json={"event_id": str(event_id), "amount": "10.00"},
            )
            assert response.status_code == 422
            assert "deadline passed" in response.json()["detail"]

        async def test_post_bet_event_not_active_returns_422(
            self, app: FastAPI, client: AsyncClient
        ) -> None:
            """BM-06 / D-14: state=FINISHED_WIN → 422 'event not active'."""
            event_id = uuid4()
            future = datetime.now(timezone.utc) + timedelta(hours=1)
            app.state.event_lookup.seed(
                EventSnapshot(event_id=event_id, deadline=future, state=EventState.FINISHED_WIN)
            )

            response = await client.post(
                "/bet",
                json={"event_id": str(event_id), "amount": "10.00"},
            )
            assert response.status_code == 422
            assert "event not active" in response.json()["detail"]


    class TestGetBets:
        """BM-07 / D-07 / Risk Axis 9: GET /bets ordering."""

        async def test_get_bets_empty_returns_empty_list(
            self, client: AsyncClient
        ) -> None:
            """Empty DB → 200 + []."""
            response = await client.get("/bets")
            assert response.status_code == 200
            assert response.json() == []

        async def test_get_bets_returns_desc_order(
            self, app: FastAPI, client: AsyncClient
        ) -> None:
            """D-07 / SC-3: newest first.

            Place 3 bets sequentially with small sleeps; GET /bets returns them
            with the newest first.
            """
            ids = []
            for i in range(3):
                event_id = uuid4()
                _seed_active(app, event_id)
                resp = await client.post(
                    "/bet",
                    json={"event_id": str(event_id), "amount": f"{i + 1}.00"},
                )
                assert resp.status_code == 201
                ids.append(resp.json()["id"])
                await asyncio.sleep(0.01)

            response = await client.get("/bets")
            assert response.status_code == 200
            body = response.json()
            assert len(body) == 3
            assert [b["id"] for b in body] == list(reversed(ids))


    class TestGetBetById:
        """BM-13 / D-08: GET /bet/{bet_id} 200/404/422."""

        async def test_get_bet_happy_returns_betread(
            self, app: FastAPI, client: AsyncClient
        ) -> None:
            """BM-13: existing bet → 200 + BetRead."""
            event_id = uuid4()
            _seed_active(app, event_id)
            post_resp = await client.post(
                "/bet",
                json={"event_id": str(event_id), "amount": "5.50"},
            )
            assert post_resp.status_code == 201
            bet_id = post_resp.json()["id"]

            response = await client.get(f"/bet/{bet_id}")
            assert response.status_code == 200
            body = response.json()
            assert body["id"] == bet_id
            assert body["amount"] == "5.50"
            assert body["status"] == "PENDING"

        async def test_get_bet_missing_returns_404(self, client: AsyncClient) -> None:
            """D-08: unknown id → 404 + detail 'bet {id} not found'."""
            missing = uuid4()
            response = await client.get(f"/bet/{missing}")
            assert response.status_code == 404
            assert "not found" in response.json()["detail"].lower()
            assert str(missing) in response.json()["detail"]

        async def test_get_bet_invalid_uuid_returns_422(self, client: AsyncClient) -> None:
            """T-03-3: malformed path UUID → 422 before reaching DB."""
            response = await client.get("/bet/not-a-uuid")
            assert response.status_code == 422


    class TestDecimalRoundtrip:
        """SC-6 (ROADMAP P3) / D-19: '10.00' → POST → GET → '10.00' (STRING)."""

        async def test_decimal_string_preserved_end_to_end(
            self, app: FastAPI, client: AsyncClient
        ) -> None:
            """Risk Axis 1 / Pitfall 5: amount stays exactly '10.00' through the
            full pipeline (Pydantic → SQLAlchemy → asyncpg → PG Numeric(12,2) →
            asyncpg → SQLAlchemy → Pydantic). Tests compare STRING not float.
            """
            event_id = uuid4()
            _seed_active(app, event_id)
            post_resp = await client.post(
                "/bet",
                json={"event_id": str(event_id), "amount": "10.00"},
            )
            assert post_resp.status_code == 201
            assert post_resp.json()["amount"] == "10.00"
            assert isinstance(post_resp.json()["amount"], str)

            get_resp = await client.get("/bets")
            assert get_resp.status_code == 200
            assert get_resp.json()[0]["amount"] == "10.00"
            assert isinstance(get_resp.json()[0]["amount"], str)
    ```
  </action>
  <verify>
    <automated>bash -c "! grep -q 'pytestmark = pytest.mark.skip' tests/bet_maker/test_bet_routes.py"</automated>
    <automated>uv run pytest tests/bet_maker/test_bet_routes.py -q --no-cov</automated>
    <automated>grep -c "def test_" tests/bet_maker/test_bet_routes.py</automated>
    <automated>grep -c "^class Test" tests/bet_maker/test_bet_routes.py</automated>
    <automated>grep -q '"10.00"' tests/bet_maker/test_bet_routes.py</automated>
    <automated>grep -q "isinstance.*str" tests/bet_maker/test_bet_routes.py</automated>
    <automated>uv run mypy --strict tests/bet_maker/test_bet_routes.py</automated>
    <automated>uv run ruff check tests/bet_maker/test_bet_routes.py</automated>
  </verify>
  <acceptance_criteria>
    - Wave 0 stub skip mark удалён
    - `grep -c "def test_" tests/bet_maker/test_bet_routes.py` >= 14 (TestPostBet: 11, TestGetBets: 2, TestGetBetById: 3, TestDecimalRoundtrip: 1 = 17; minimum 14)
    - `grep -c "^class Test" tests/bet_maker/test_bet_routes.py` == 4
    - Все 3 EventNotBettable reason strings проверяются ("event not found", "deadline passed", "event not active")
    - Decimal "10.00" round-trip явно verified (POST returns "10.00", GET returns "10.00", isinstance str)
    - `uv run pytest tests/bet_maker/test_bet_routes.py -q --no-cov` — все passed
    - `uv run mypy --strict tests/bet_maker/test_bet_routes.py` exit 0
    - `uv run ruff check tests/bet_maker/test_bet_routes.py` → All checks passed
  </acceptance_criteria>
  <done>test_bet_routes.py: 14+ integration тестов passed. Risk Axes 1, 7, 8, 9 covered. SC-6 (Decimal round-trip end-to-end) verified.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 6: Replace test_lifespan.py Wave 0 stub — bad-DSN retry exhaustion + successful startup smoke</name>
  <files>tests/bet_maker/test_lifespan.py</files>
  <read_first>
    - tests/bet_maker/test_lifespan.py (Wave 0 stub из Plan 03-02)
    - src/bet_maker/entrypoints/lifespan.py (Task 1)
    - src/bet_maker/infrastructure/db/pings.py (wait_for_postgres @retry)
    - .planning/phases/03-bet-maker-domain-db/03-VALIDATION.md §«Critical Risk Axes» #5
  </read_first>
  <action>
    Полностью заменить `tests/bet_maker/test_lifespan.py`:

    ```python
    """Integration tests for bet_maker lifespan.

    D-27 / Risk Axis 5: wait_for_postgres tenacity retry exhaustion → RuntimeError
    after exhausted attempts. Override settings to attempts=2 for speed.
    D-13: successful startup pins app.state.engine/sessionmaker/event_lookup.
    """

    from __future__ import annotations

    from sqlalchemy.exc import OperationalError
    from sqlalchemy.ext.asyncio import AsyncEngine, async_sessionmaker

    from bet_maker.facades.event_lookup import StubEventLookup


    class TestStartupSmoke:
        """D-13: successful startup pins app.state.* singletons."""

        async def test_lifespan_pins_engine_sessionmaker_event_lookup(
            self, app
        ) -> None:
            """D-13: after lifespan startup, app.state.* are all bound and typed."""
            assert isinstance(app.state.engine, AsyncEngine)
            assert isinstance(app.state.sessionmaker, async_sessionmaker)
            assert isinstance(app.state.event_lookup, StubEventLookup)

        async def test_lifespan_engine_responds_to_select_one(self, app) -> None:
            """Successful startup proved wait_for_postgres passed — engine usable."""
            from sqlalchemy import text

            async with app.state.engine.connect() as conn:
                result = await conn.scalar(text("SELECT 1"))
                assert result == 1


    class TestStartupRetryExhaustion:
        """D-27 / Risk Axis 5: bad DSN → tenacity retries exhausted → reraise."""

        async def test_wait_for_postgres_raises_on_bad_dsn_after_retries(
            self, monkeypatch
        ) -> None:
            """D-27: wait_for_postgres with bad DSN raises OperationalError after
            stop_after_attempt cycles.

            We override `stop_after_attempt(10)` to `stop_after_attempt(2)` for
            test speed by monkeypatching the decorator's stop condition. The
            recommended approach: import wait_for_postgres and inspect its
            __wrapped__ retry config OR rebuild the function with a tighter
            retry budget. Simplest: call the underlying coroutine directly with
            a bad engine and catch the SQLAlchemyError without retry — that
            tests engine.connect() failure but NOT the retry contract.

            Compromise: we test the retry contract via a custom decorated
            function (mirror of wait_for_postgres but attempts=2), proving
            the recipe (stop_after_attempt + wait_exponential + reraise=True)
            reraises after exhaustion.
            """
            import logging

            from sqlalchemy.ext.asyncio import create_async_engine
            from tenacity import (
                before_sleep_log,
                retry,
                stop_after_attempt,
                wait_exponential,
            )
            from sqlalchemy import text

            calls = {"count": 0}

            @retry(
                stop=stop_after_attempt(2),
                wait=wait_exponential(multiplier=0.01, min=0.01, max=0.05),
                reraise=True,
                before_sleep=before_sleep_log(logging.getLogger(__name__), logging.DEBUG),
            )
            async def _wait_for_bad_pg(engine: AsyncEngine) -> None:
                calls["count"] += 1
                async with engine.connect() as conn:
                    await conn.scalar(text("SELECT 1"))

            # Bad DSN — non-routable port
            bad_engine = create_async_engine("postgresql+asyncpg://postgres:postgres@127.0.0.1:1/postgres")
            try:
                raised = False
                try:
                    await _wait_for_bad_pg(bad_engine)
                except Exception:
                    raised = True
                assert raised, "wait_for_postgres-like retry must reraise after exhaustion"
                assert calls["count"] == 2, f"expected 2 attempts, got {calls['count']}"
            finally:
                await bad_engine.dispose()
    ```

    Note: первый test использует `app` fixture (lifespan startup уже завершён успешно) — smoke что app.state pinned.
    Второй test НЕ использует `app` fixture (не запускает lifespan), а напрямую конструирует `bad_engine` и `@retry`-decorated function. Это тестирует **the retry contract** (stop_after_attempt + reraise), без mock'инга tenacity globals.

    `monkeypatch` параметр в первом test необязателен — может быть удалён (он нужен только во втором, но во втором мы не патчим — мы строим параллельную функцию). Оставить параметр или убрать — на executor.
  </action>
  <verify>
    <automated>bash -c "! grep -q 'pytestmark = pytest.mark.skip' tests/bet_maker/test_lifespan.py"</automated>
    <automated>uv run pytest tests/bet_maker/test_lifespan.py -q --no-cov</automated>
    <automated>grep -c "def test_" tests/bet_maker/test_lifespan.py</automated>
    <automated>grep -q "stop_after_attempt(2)" tests/bet_maker/test_lifespan.py</automated>
    <automated>grep -q "reraise=True" tests/bet_maker/test_lifespan.py</automated>
    <automated>uv run mypy --strict tests/bet_maker/test_lifespan.py</automated>
    <automated>uv run ruff check tests/bet_maker/test_lifespan.py</automated>
  </verify>
  <acceptance_criteria>
    - Wave 0 stub skip mark удалён
    - `grep -c "def test_" tests/bet_maker/test_lifespan.py` >= 3 (TestStartupSmoke: 2, TestStartupRetryExhaustion: 1)
    - test_wait_for_postgres_raises_on_bad_dsn_after_retries использует stop_after_attempt(2) + reraise=True для скорости
    - test_lifespan_pins_engine_sessionmaker_event_lookup проверяет ВСЕ ТРИ singletons (isinstance AsyncEngine, async_sessionmaker, StubEventLookup)
    - `uv run pytest tests/bet_maker/test_lifespan.py -q --no-cov` — все passed
    - `uv run mypy --strict tests/bet_maker/test_lifespan.py` exit 0
    - `uv run ruff check tests/bet_maker/test_lifespan.py` → All checks passed
  </acceptance_criteria>
  <done>test_lifespan.py: 3+ тестов passed. Risk Axis 5 covered (retry exhaustion reraise). app.state.* pinned smoke verified.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HTTP client → POST /bet body | Pydantic BetCreate первая линия защиты от untrusted input |
| route layer → interactor place_bet → UoW.session.flush | transactional gate; validation rejects ДО UoW open |
| lifespan startup → engine.connect() | критический момент для bad DSN detection — tenacity retry exhaustion → loud crash |
| /health route → engine.connect() | per-request liveness; SQLAlchemyError → 503 (no caching of bad state) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-1 | Tampering | Decimal serialization drift between POST and GET | mitigate | TestDecimalRoundtrip.test_decimal_string_preserved_end_to_end — POST + GET both return string "10.00", isinstance(str). |
| T-03-2 | Denial of Service | Mass POST /bet flood (rate-limit out-of-scope) | accept | FEATURES.md API-03 explicit out-of-scope; README extension в P7. |
| T-03-3 | Tampering | SQL injection through path parameter `/bet/{bet_id}` | mitigate | FastAPI Pydantic UUID path coercion отвергает "not-a-uuid" → 422 ДО SQL слоя. test_get_bet_invalid_uuid_returns_422 verifies. |
| T-03-3-extra | Tampering | extra-field injection в POST /bet body (coefficient drift) | mitigate | BetCreate `extra='forbid'` + test_post_bet_extra_field_returns_422. |
| T-03-5 | Information Disclosure | DSN leak в lifespan logs | mitigate | `log.info("bet_maker.startup.ready", engine_url=str(settings.postgres_dsn).split("@")[-1])` — credentials исключены; host:port/db only. Failure log `bet_maker.startup.failed` — содержит class+message exception, не raw DSN. |
| T-03-6 | Operational | health 503 во время transient PG blip создаёт false-positive в docker-compose | accept | D-26 явно требует live SELECT 1; cache не приемлем для compose healthcheck. Если PG flaps на network glitch, healthcheck перезапустит контейнер — это правильное поведение. |
| T-03-2-DoS | Denial of Service | infinite startup retry storm если PG долго недоступен | mitigate | wait_for_postgres `stop_after_attempt(10)` + cumulative ~75s → reraise. test_wait_for_postgres_raises_on_bad_dsn_after_retries proves retry exhaustion semantics. |
| T-03-3-event | Tampering | EventNotBettable .reason leakage to HTTP client | mitigate | Three exact reason strings — no event_id details / deadline value / state value leaked through reason. HTTP detail includes event_id (path-derived, client-known) + reason; no DB-internal information. |
| T-03-1-amount-leak | Information Disclosure | OpenAPI schema discloses Numeric(12,2) limits | accept | max_digits=12 (999,999,999,999.99 cap) public via OpenAPI — known by design for client validation. Not a secret. |
</threat_model>

<verification>
После завершения всех 6 задач:
- `uv run pytest tests/bet_maker -q --no-cov` → все P3 test файлы passed (>=180 в bet_maker tree)
- `uv run pytest -q` → >= 200 total passed (97 baseline + ~100 P3 new)
- `uv run mypy --strict src/bet_maker tests/bet_maker` exit 0
- `uv run ruff check src/bet_maker tests/bet_maker` exit 0
- Critical Risk Axes 1, 5, 6, 7, 8, 9 — ВСЕ покрыты as documented в `03-VALIDATION.md`
- Decimal "10.00" end-to-end roundtrip (POST → GET) explicit verified
- /health 200/503 both verified
- POST /bet 11 different 422 cases verified
- GET /bet/{id} 200/404/422 all three status codes verified
</verification>

<success_criteria>
- src/bet_maker/entrypoints/api/bets.py: 3 routes (POST/bet 201, GET /bets 200, GET /bet/{id} 200|404)
- src/bet_maker/entrypoints/api/health.py: REPLACED P1 stub; per-request PG ping; 200/503 JSON per D-28
- src/bet_maker/entrypoints/lifespan.py: EXTENDED; create_engine_and_sessionmaker + wait_for_postgres + StubEventLookup + safe dispose
- src/bet_maker/app.py: include_router(bets.router) added (health-first preserved)
- tests/bet_maker/test_health.py: 3 tests (P1 baseline subset-updated + X-Request-ID + 503)
- tests/bet_maker/test_bet_routes.py: 14+ integration тестов (4 classes)
- tests/bet_maker/test_lifespan.py: 3 tests (smoke + retry exhaustion)
- Все Critical Risk Axes 1, 5, 6, 7, 8, 9 покрыты
- mypy strict + ruff check + ruff format чисто
- pytest 200+ total passed
</success_criteria>

<output>
After completion, create `.planning/phases/03-bet-maker-domain-db/03-07-SUMMARY.md` following `@$HOME/.claude/get-shit-done/templates/summary.md`.

SUMMARY должен включать:
- Per-file test count: test_health (3), test_bet_routes (≥14), test_lifespan (3)
- Total pytest passed count
- Coverage report headline (full coverage будет в Plan 03-08 phase-gate)
- Подтверждение что Decimal "10.00" end-to-end verified
- Подтверждение что Pitfall 4 (P1 health assertion) updated AND P1 X-Request-ID test preserved
</output>
