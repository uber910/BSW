---
phase: 03-bet-maker-domain-db
plan: 05
type: execute
wave: 3
depends_on: [02, 03, 04]
files_modified:
  - src/bet_maker/infrastructure/__init__.py
  - src/bet_maker/infrastructure/db/__init__.py
  - src/bet_maker/infrastructure/db/engine.py
  - src/bet_maker/infrastructure/db/pings.py
  - src/bet_maker/repositories/__init__.py
  - src/bet_maker/repositories/bets.py
  - src/bet_maker/facades/__init__.py
  - src/bet_maker/facades/uow.py
  - src/bet_maker/facades/event_lookup.py
  - src/bet_maker/facades/deps.py
  - tests/bet_maker/test_repositories.py
  - tests/bet_maker/test_uow.py
  - tests/bet_maker/test_event_lookup.py
autonomous: true
requirements: [BM-02, BM-03, BM-06, BM-08]
tags: [bet-maker, infrastructure, facades, uow, repository, event-lookup, wave-3]

must_haves:
  truths:
    - "infrastructure/db/engine.py содержит create_engine_and_sessionmaker(settings) -> (AsyncEngine, async_sessionmaker) с D-16 params (pool_size=10, max_overflow=20, pool_pre_ping=True, pool_recycle=1800) + expire_on_commit=False"
    - "infrastructure/db/pings.py содержит wait_for_postgres декорированную @retry(stop_after_attempt=10, wait_exponential=multiplier=1,min=1,max=10, reraise=True) + ping_postgres -> bool без retry"
    - "facades/uow.py содержит AsyncUnitOfWork с __aenter__ через sessionmaker.begin(), exposes self.session + self.bets, без manual commit в __aexit__"
    - "repositories/bets.py содержит BetRepository: add() (session.add — no flush, caller controls; per PATTERNS.md), get_by_id() через select + scalar_one_or_none; никакого commit/rollback в репозитории (Anti-Pattern 1)"
    - "facades/event_lookup.py содержит EventLookup Protocol + EventSnapshot(BaseModel, frozen=True) + StubEventLookup с dict[UUID, EventSnapshot] + seed/seed_active методами"
    - "facades/deps.py содержит get_engine/get_sessionmaker/get_session/get_uow/get_event_lookup/get_settings провайдеры + соответствующие Annotated aliases"
    - "tests/bet_maker/test_repositories.py: 4+ тестов: add+flush в session, get_by_id happy + miss, NO commit в repo, BetRepository(session) shape"
    - "tests/bet_maker/test_uow.py: 5+ тестов: commit on clean exit, rollback on exception, isolated concurrent UoWs (asyncio.gather), uow.session + uow.bets exposed"
    - "tests/bet_maker/test_event_lookup.py: 6+ тестов: Protocol structural typing, seed + get_event roundtrip, get_event миссы None, seed_active default deadline, EventSnapshot frozen"
    - "Anti-Pattern 1 enforced: `grep -c 'commit\\|rollback' src/bet_maker/repositories/bets.py == 0` (репозитории не управляют транзакциями)"
    - "Concurrent UoW isolation тест явно использует asyncio.gather с 5 параллельными UoWs (RESEARCH §Critical Risk Axis 3)"
  artifacts:
    - path: "src/bet_maker/infrastructure/db/engine.py"
      provides: "create_engine_and_sessionmaker (with D-16 params)"
      contains: "pool_pre_ping=True"
      min_lines: 25
    - path: "src/bet_maker/infrastructure/db/pings.py"
      provides: "wait_for_postgres (tenacity) + ping_postgres (no retry, bool)"
      contains: "@retry"
      min_lines: 30
    - path: "src/bet_maker/facades/uow.py"
      provides: "AsyncUnitOfWork — async_sessionmaker.begin() based"
      contains: "class AsyncUnitOfWork"
      min_lines: 25
    - path: "src/bet_maker/repositories/bets.py"
      provides: "BetRepository.add + get_by_id (no commit)"
      contains: "class BetRepository"
      min_lines: 18
    - path: "src/bet_maker/facades/event_lookup.py"
      provides: "EventLookup Protocol + EventSnapshot + StubEventLookup"
      contains: "class EventLookup(Protocol)"
      min_lines: 45
    - path: "src/bet_maker/facades/deps.py"
      provides: "DI providers + Annotated aliases for routes"
      contains: "EngineDep"
      min_lines: 30
    - path: "tests/bet_maker/test_repositories.py"
      provides: "BetRepository tests"
      contains: "test_add_no_commit"
    - path: "tests/bet_maker/test_uow.py"
      provides: "AsyncUnitOfWork tests + concurrent isolation"
      contains: "test_concurrent_uows_isolated"
    - path: "tests/bet_maker/test_event_lookup.py"
      provides: "StubEventLookup tests"
      contains: "test_seed_and_get"
  key_links:
    - from: "src/bet_maker/facades/uow.py AsyncUnitOfWork"
      to: "async_sessionmaker.begin"
      via: "self._cm = self._sessionmaker.begin(); await self._cm.__aenter__()"
      pattern: "sessionmaker.*begin"
    - from: "src/bet_maker/infrastructure/db/pings.py wait_for_postgres"
      to: "tenacity.retry"
      via: "@retry(stop=stop_after_attempt(10), wait=wait_exponential(min=1, max=10))"
      pattern: "stop_after_attempt\\(10\\)"
    - from: "src/bet_maker/facades/event_lookup.py StubEventLookup"
      to: "dict[UUID, EventSnapshot] in-process backing store"
      via: "self._events: dict[UUID, EventSnapshot] = {}"
      pattern: "dict\\[UUID, EventSnapshot\\]"
---

<objective>
Wave 3 — infrastructure + repository + facades. После этого плана все building blocks готовы для interactor (03-06) и routes (03-07).

Что делается (большой plan, но всё тесно связано — split на 3 плана уменьшил бы файлы каждого до 2-3, но increase context cost экстремально из-за дублирующих imports/fixtures; consolidated один план под ~50% context):
1. **`infrastructure/db/engine.py`** — `create_engine_and_sessionmaker(settings)` factory: возвращает кортеж `(AsyncEngine, async_sessionmaker)` с D-16 параметрами + `expire_on_commit=False`.
2. **`infrastructure/db/pings.py`** — `wait_for_postgres(engine)` декорированную `@retry(stop_after_attempt(10), wait_exponential(multiplier=1, min=1, max=10), reraise=True)` + `ping_postgres(engine) -> bool` (без retry, для /health per-request).
3. **`repositories/bets.py`** — `BetRepository(session)`: `.add(bet)` (session.add only — no flush; caller controls; per PATTERNS.md лучше чем CONTEXT D-18 потому что interactor получает control over flush timing для refresh) + `.get_by_id(bet_id)` (scalar_one_or_none).
4. **`facades/uow.py`** — `AsyncUnitOfWork(sessionmaker)`: точная shape per RESEARCH §Pattern 2 verified recipe (lines 301–328). Под `async with uow:` exposes `uow.session` + `uow.bets`. Commit/rollback делегирован `async_sessionmaker.begin()` context manager.
5. **`facades/event_lookup.py`** — `EventLookup(Protocol)` + `EventSnapshot(BaseModel, frozen=True)` + `StubEventLookup` (`dict[UUID, EventSnapshot]` + `seed(snapshot)` + `seed_active(event_id, deadline=now+1h)`). P4 заменит на `HttpEventLookup` без правки Protocol/Snapshot.
6. **`facades/deps.py`** — DI providers + Annotated aliases per PATTERNS.md §«src/bet_maker/facades/deps.py» (lines 169–204).
7. **Раскрытие 3 Wave 0 stubs:**
   - `tests/bet_maker/test_repositories.py` — 4+ тестов (BetRepository.add + get_by_id; flush invariant; no commit)
   - `tests/bet_maker/test_uow.py` — 5+ тестов (commit/rollback semantics + Critical Risk Axis 3 concurrent isolation via asyncio.gather)
   - `tests/bet_maker/test_event_lookup.py` — 6+ тестов (Protocol structural typing, EventSnapshot frozen, seed/seed_active/get_event)

Purpose: Без infrastructure + facades слоя:
- Lifespan (03-07) не сможет создать engine + sessionmaker + StubEventLookup
- Interactor place_bet (03-06) не имеет UoW для транзакции и event_lookup для валидации
- /health endpoint (03-07) не может пинговать PG

Output: 10 production files + 3 test files updated; mypy strict + ruff чисто; Critical Risk Axes 3 (concurrent UoW isolation) и 8 (EventLookup stub seeding) явно покрыты.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/03-bet-maker-domain-db/03-CONTEXT.md
@.planning/phases/03-bet-maker-domain-db/03-RESEARCH.md
@.planning/phases/03-bet-maker-domain-db/03-PATTERNS.md
@.planning/phases/03-bet-maker-domain-db/03-VALIDATION.md
@src/bet_maker/models/bet.py
@src/bet_maker/schemas/bets.py
@src/bet_maker/schemas/events.py
@src/bet_maker/settings/config.py
@src/line_provider/facades/event_bus.py
@src/line_provider/facades/deps.py
@CLAUDE.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create infrastructure/db/engine.py + infrastructure/db/pings.py + package markers</name>
  <files>
    src/bet_maker/infrastructure/__init__.py,
    src/bet_maker/infrastructure/db/__init__.py,
    src/bet_maker/infrastructure/db/engine.py,
    src/bet_maker/infrastructure/db/pings.py
  </files>
  <read_first>
    - .planning/phases/03-bet-maker-domain-db/03-RESEARCH.md §«Pattern 5: tenacity async retry для wait_for_postgres» (lines 446–487) — verified recipe
    - .planning/phases/03-bet-maker-domain-db/03-CONTEXT.md §D-15 (expire_on_commit=False) + §D-16 (pool params) + §D-27 (tenacity retry)
    - .planning/phases/03-bet-maker-domain-db/03-PATTERNS.md §«infrastructure/db/engine.py» + §«infrastructure/db/pings.py» (no analogs — use RESEARCH)
    - src/bet_maker/settings/config.py (BetMakerSettings — для типа аргумента)
  </read_first>
  <behavior>
    - `create_engine_and_sessionmaker(settings)` возвращает `(AsyncEngine, async_sessionmaker)`, engine с pool_size=10, max_overflow=20, pool_pre_ping=True, pool_recycle=1800; sessionmaker с expire_on_commit=False
    - `wait_for_postgres(engine)` (async) при недоступной БД делает до 10 попыток с exponential backoff (min=1, max=10), потом reraise OperationalError
    - `ping_postgres(engine)` (async) возвращает True если SELECT 1 успешен, False при SQLAlchemyError; без retry
  </behavior>
  <action>
    Создать 4 файла.

    **`src/bet_maker/infrastructure/__init__.py`** — пустой package marker.

    **`src/bet_maker/infrastructure/db/__init__.py`** — пустой package marker.

    **`src/bet_maker/infrastructure/db/engine.py`** (D-15, D-16):

    ```python
    from __future__ import annotations

    from sqlalchemy.ext.asyncio import (
        AsyncEngine,
        AsyncSession,
        async_sessionmaker,
        create_async_engine,
    )

    from bet_maker.settings.config import BetMakerSettings


    def create_engine_and_sessionmaker(
        settings: BetMakerSettings,
    ) -> tuple[AsyncEngine, async_sessionmaker[AsyncSession]]:
        """Build the AsyncEngine + async_sessionmaker singletons for bet_maker.

        D-16 (Pitfall A3 mitigation): pool_size=10, max_overflow=20 — enough
        headroom for the test-task workload without exhausting PG.
        pool_pre_ping=True — issues SELECT 1 on checkout, kills stale connections
        in idle pools (avoids "connection closed" after PG restart).
        pool_recycle=1800 — refresh connections every 30 min (defensive against
        cloud PG forcing disconnects on long-idle clients).

        D-15 (Pitfall A1 mitigation): expire_on_commit=False — without this,
        any access to ORM attributes after commit raises MissingGreenlet
        (the async session would try to lazy-reload, which requires greenlet).
        Tests + interactor place_bet (D-14) rely on this contract.
        """
        engine = create_async_engine(
            str(settings.postgres_dsn),
            pool_size=10,
            max_overflow=20,
            pool_pre_ping=True,
            pool_recycle=1800,
        )
        sessionmaker = async_sessionmaker(engine, expire_on_commit=False)
        return engine, sessionmaker
    ```

    **`src/bet_maker/infrastructure/db/pings.py`** (D-27, D-29):

    ```python
    from __future__ import annotations

    import logging

    import structlog
    from sqlalchemy import text
    from sqlalchemy.exc import SQLAlchemyError
    from sqlalchemy.ext.asyncio import AsyncEngine
    from tenacity import (
        before_sleep_log,
        retry,
        stop_after_attempt,
        wait_exponential,
    )

    log = structlog.get_logger()
    _stdlib_log = logging.getLogger(__name__)


    @retry(
        stop=stop_after_attempt(10),
        wait=wait_exponential(multiplier=1, min=1, max=10),
        reraise=True,
        before_sleep=before_sleep_log(_stdlib_log, logging.WARNING),
    )
    async def wait_for_postgres(engine: AsyncEngine) -> None:
        """Block until PG accepts SELECT 1, or give up after 10 attempts.

        D-27: tenacity @retry automatically uses AsyncRetrying for coroutines.
        Cumulative wait ≈ 1+2+4+8+10+10+10+10+10+10 = ~75s worst case before
        reraise. Lifespan (Plan 03-07) calls this — if all 10 attempts fail,
        startup crashes with a clear `OperationalError` and structlog log.

        Pitfall D2 mitigation: surfaces bad DSN at startup, not at first request.
        """
        async with engine.connect() as conn:
            await conn.scalar(text("SELECT 1"))


    async def ping_postgres(engine: AsyncEngine) -> bool:
        """Single SELECT 1 ping for /health — no retry, returns True/False.

        D-26: per-request check (no caching at test-task scale). ~5ms overhead
        is fine; live 503 is critical for docker-compose healthcheck.
        D-29: SQLAlchemyError caught → return False → route emits 503.
        """
        try:
            async with engine.connect() as conn:
                await conn.scalar(text("SELECT 1"))
            return True
        except SQLAlchemyError as exc:
            log.warning("health.check.failed", check="postgres", error=str(exc))
            return False
    ```

    Не определять `dispose_engine` функцию — `await engine.dispose()` вызывается напрямую в lifespan `finally:` block.
    Не использовать `from typing import TypeAlias` — `tuple[AsyncEngine, async_sessionmaker[...]]` достаточно с `from __future__ import annotations`.
  </action>
  <verify>
    <automated>test -f src/bet_maker/infrastructure/__init__.py</automated>
    <automated>test -f src/bet_maker/infrastructure/db/__init__.py</automated>
    <automated>test -f src/bet_maker/infrastructure/db/engine.py</automated>
    <automated>test -f src/bet_maker/infrastructure/db/pings.py</automated>
    <automated>grep -q "pool_size=10" src/bet_maker/infrastructure/db/engine.py</automated>
    <automated>grep -q "max_overflow=20" src/bet_maker/infrastructure/db/engine.py</automated>
    <automated>grep -q "pool_pre_ping=True" src/bet_maker/infrastructure/db/engine.py</automated>
    <automated>grep -q "pool_recycle=1800" src/bet_maker/infrastructure/db/engine.py</automated>
    <automated>grep -q "expire_on_commit=False" src/bet_maker/infrastructure/db/engine.py</automated>
    <automated>grep -q "@retry" src/bet_maker/infrastructure/db/pings.py</automated>
    <automated>grep -q "stop_after_attempt(10)" src/bet_maker/infrastructure/db/pings.py</automated>
    <automated>grep -q "wait_exponential(multiplier=1, min=1, max=10)" src/bet_maker/infrastructure/db/pings.py</automated>
    <automated>grep -q "reraise=True" src/bet_maker/infrastructure/db/pings.py</automated>
    <automated>grep -q "async def wait_for_postgres" src/bet_maker/infrastructure/db/pings.py</automated>
    <automated>grep -q "async def ping_postgres" src/bet_maker/infrastructure/db/pings.py</automated>
    <automated>uv run mypy --strict src/bet_maker/infrastructure/</automated>
    <automated>uv run ruff check src/bet_maker/infrastructure/</automated>
  </verify>
  <acceptance_criteria>
    - 4 файла существуют
    - engine.py содержит `pool_size=10, max_overflow=20, pool_pre_ping=True, pool_recycle=1800` (все 4 params)
    - engine.py содержит `async_sessionmaker(engine, expire_on_commit=False)`
    - pings.py содержит `@retry(...)` decorator с `stop=stop_after_attempt(10)`, `wait=wait_exponential(multiplier=1, min=1, max=10)`, `reraise=True`
    - pings.py содержит ОБА `async def wait_for_postgres` И `async def ping_postgres`
    - `wait_for_postgres` декорирована `@retry`; `ping_postgres` НЕ декорирована (single-shot)
    - `uv run mypy --strict src/bet_maker/infrastructure/` exit 0
    - `uv run ruff check src/bet_maker/infrastructure/` → All checks passed
  </acceptance_criteria>
  <done>infrastructure/db/engine.py и pings.py созданы с верифицированными RESEARCH §Pattern 5 рецептами. mypy strict + ruff чисто.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create repositories/bets.py + facades/uow.py</name>
  <files>
    src/bet_maker/repositories/__init__.py,
    src/bet_maker/repositories/bets.py,
    src/bet_maker/facades/__init__.py,
    src/bet_maker/facades/uow.py
  </files>
  <read_first>
    - .planning/phases/03-bet-maker-domain-db/03-RESEARCH.md §«Pattern 2: AsyncUnitOfWork (verified code shape)» (lines 299–328) + §«BetRepository» (lines 700–723)
    - .planning/phases/03-bet-maker-domain-db/03-CONTEXT.md §D-17 (UoW shape) + §D-18 (BetRepository add+get_by_id, NO commit)
    - .planning/phases/03-bet-maker-domain-db/03-PATTERNS.md §«src/bet_maker/repositories/bets.py» + Anti-Pattern 1
    - src/bet_maker/models/bet.py (для импорта Bet)
  </read_first>
  <behavior>
    - `BetRepository(session).add(bet)` — добавляет bet в session, НЕ flush, НЕ commit; вызов на `add` детерминирован (не возвращает Future)
    - `BetRepository(session).get_by_id(bet_id)` — возвращает `Bet | None` через `select.where(Bet.id == bet_id)` + `scalar_one_or_none`
    - `async with AsyncUnitOfWork(sessionmaker) as uow:` exposes `uow.session: AsyncSession` и `uow.bets: BetRepository`
    - `async with uow:` clean exit → commit; exception inside → rollback (delegated to async_sessionmaker.begin() semantics)
  </behavior>
  <action>
    Создать 4 файла.

    **`src/bet_maker/repositories/__init__.py`** — пустой package marker.

    **`src/bet_maker/repositories/bets.py`** — per RESEARCH §«BetRepository» (lines 700–723) + PATTERNS.md (no commit):

    ```python
    from __future__ import annotations

    from uuid import UUID

    from sqlalchemy import select
    from sqlalchemy.ext.asyncio import AsyncSession

    from bet_maker.models.bet import Bet


    class BetRepository:
        """Repository for Bet entity. UoW owns transactions — no commit here.

        D-18 / Anti-Pattern 1 (ARCHITECTURE.md): repositories MUST NOT commit
        or rollback. Only `.add()` (queue an INSERT) and `.get_by_id()`
        (issue a SELECT). The enclosing UoW calls commit on clean exit and
        rollback on exception.

        `add()` does NOT flush — the caller (interactor place_bet) controls
        flush timing because it needs `session.refresh(bet)` immediately
        after to load server_default created_at/updated_at (RESEARCH §Pitfall 1).
        Plan 03-06 interactor calls `uow.bets.add(bet); await uow.session.flush()`
        explicitly.
        """

        def __init__(self, session: AsyncSession) -> None:
            self._session = session

        def add(self, bet: Bet) -> None:
            """Stage the bet for INSERT in the next flush."""
            self._session.add(bet)

        async def get_by_id(self, bet_id: UUID) -> Bet | None:
            """SELECT * FROM bets WHERE id = :bet_id."""
            result = await self._session.execute(select(Bet).where(Bet.id == bet_id))
            return result.scalar_one_or_none()
    ```

    **`src/bet_maker/facades/__init__.py`** — пустой package marker.

    **`src/bet_maker/facades/uow.py`** — точно по RESEARCH §Pattern 2 (lines 301–328):

    ```python
    from __future__ import annotations

    from typing import Self

    from sqlalchemy.ext.asyncio import AsyncSession, async_sessionmaker

    from bet_maker.repositories.bets import BetRepository


    class AsyncUnitOfWork:
        """Async UoW over `async_sessionmaker.begin()`.

        D-17 (RESEARCH §Pattern 2): __aenter__ enters `sessionmaker.begin()`
        context — SQLAlchemy auto-commits on clean __aexit__ and auto-rollback
        on exception. No manual `session.commit()` / `session.rollback()` calls
        anywhere in bet_maker code — UoW owns the transaction.

        Usage (Plan 03-06 interactor):
            async with AsyncUnitOfWork(sessionmaker) as uow:
                uow.bets.add(bet)
                await uow.session.flush()
                await uow.session.refresh(bet)
                # exit → commit
        """

        bets: BetRepository
        session: AsyncSession

        def __init__(self, sessionmaker: async_sessionmaker[AsyncSession]) -> None:
            self._sessionmaker = sessionmaker
            self._cm: object | None = None

        async def __aenter__(self) -> Self:
            self._cm = self._sessionmaker.begin()
            self.session = await self._cm.__aenter__()  # type: ignore[union-attr]
            self.bets = BetRepository(self.session)
            return self

        async def __aexit__(
            self,
            exc_type: object,
            exc: object,
            tb: object,
        ) -> None:
            assert self._cm is not None
            await self._cm.__aexit__(exc_type, exc, tb)  # type: ignore[union-attr]
            self._cm = None
    ```

    **Заметки про mypy strict:**
    - `_cm: object | None` — namespaced так широко потому что `async_sessionmaker.begin()` returns `AsyncSessionTransaction` typed как private generic; `# type: ignore[union-attr]` на `.__aenter__` / `.__aexit__` приемлемый — это документированная SQLAlchemy idiom.
    - Альтернатива: импортировать `AsyncSessionTransaction` из `sqlalchemy.ext.asyncio` (public с 2.0.x). Если такой тип публичный — использовать строгий тип; если SQLAlchemy >= 2.0.49 экспортирует — заменить `object | None` на `AsyncSessionTransaction | None`. Executor выбирает по результату `uv run python -c "from sqlalchemy.ext.asyncio import AsyncSessionTransaction; print('ok')"`; если import работает — strong-type, иначе keep `object | None` с ignore.

    Не добавлять методов кроме `__aenter__`, `__aexit__`, `__init__`. Не делать UoW pickleable/serializable. UoW — короткоживущий объект, один на одну транзакцию.
  </action>
  <verify>
    <automated>test -f src/bet_maker/repositories/__init__.py</automated>
    <automated>test -f src/bet_maker/repositories/bets.py</automated>
    <automated>test -f src/bet_maker/facades/__init__.py</automated>
    <automated>test -f src/bet_maker/facades/uow.py</automated>
    <automated>grep -q "class BetRepository:" src/bet_maker/repositories/bets.py</automated>
    <automated>grep -q "def add(self, bet: Bet) -> None:" src/bet_maker/repositories/bets.py</automated>
    <automated>grep -q "async def get_by_id" src/bet_maker/repositories/bets.py</automated>
    <automated>grep -q "scalar_one_or_none" src/bet_maker/repositories/bets.py</automated>
    <automated>bash -c "! grep -E 'session.commit|session.rollback' src/bet_maker/repositories/bets.py"</automated>
    <automated>grep -q "class AsyncUnitOfWork:" src/bet_maker/facades/uow.py</automated>
    <automated>grep -q "self._sessionmaker.begin" src/bet_maker/facades/uow.py</automated>
    <automated>grep -q "async def __aenter__" src/bet_maker/facades/uow.py</automated>
    <automated>grep -q "async def __aexit__" src/bet_maker/facades/uow.py</automated>
    <automated>uv run python -c "from bet_maker.facades.uow import AsyncUnitOfWork; from bet_maker.repositories.bets import BetRepository; print('OK')"</automated>
    <automated>uv run mypy --strict src/bet_maker/repositories/ src/bet_maker/facades/uow.py</automated>
    <automated>uv run ruff check src/bet_maker/repositories/ src/bet_maker/facades/uow.py</automated>
  </verify>
  <acceptance_criteria>
    - repositories/bets.py содержит BetRepository с add(bet) -> None и async get_by_id(bet_id) -> Bet | None
    - **Anti-Pattern 1 enforced:** `grep -cE 'session.commit|session.rollback' src/bet_maker/repositories/bets.py` == 0
    - facades/uow.py содержит AsyncUnitOfWork с __aenter__/__aexit__ через `self._sessionmaker.begin()`
    - UoW не делает manual commit (commit делается через `_cm.__aexit__` который делегирует async_sessionmaker.begin context manager)
    - Inline import succeeds
    - `uv run mypy --strict src/bet_maker/repositories/ src/bet_maker/facades/uow.py` exit 0
    - `uv run ruff check src/bet_maker/repositories/ src/bet_maker/facades/uow.py` → All checks passed
  </acceptance_criteria>
  <done>BetRepository (add/get_by_id, no commit) + AsyncUnitOfWork (async_sessionmaker.begin) созданы. Anti-Pattern 1 grep-verified. mypy strict + ruff чисто.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Create facades/event_lookup.py — EventLookup Protocol + EventSnapshot + StubEventLookup</name>
  <files>src/bet_maker/facades/event_lookup.py</files>
  <read_first>
    - src/line_provider/facades/event_bus.py (analog — Protocol + Stub pattern)
    - .planning/phases/03-bet-maker-domain-db/03-CONTEXT.md §D-11 (full shape: EventSnapshot, StubEventLookup with seed/seed_active, EventLookupDep)
    - .planning/phases/03-bet-maker-domain-db/03-PATTERNS.md §«src/bet_maker/facades/event_lookup.py» (lines 119–166)
    - src/bet_maker/schemas/events.py (EventState для импорта в EventSnapshot)
    - src/config/time.py (utc_now для seed_active default)
  </read_first>
  <behavior>
    - `EventSnapshot(event_id=UUID, deadline=datetime, state=EventState.NEW)` создаёт frozen Pydantic model
    - `EventSnapshot` запрещает mutation: `snap.deadline = newdate` raises ValidationError или AttributeError
    - `EventSnapshot` запрещает extra fields: `EventSnapshot(event_id=..., extra="x")` ValidationError
    - `StubEventLookup()` — empty dict initially
    - `await lookup.get_event(uuid4())` — None для unseeded UUID
    - `lookup.seed(snapshot)` + `await lookup.get_event(snapshot.event_id)` — возвращает snapshot
    - `lookup.seed_active(event_id)` — заполняет dict с deadline=now+1h и state=NEW; default deadline через utc_now()
    - `lookup.seed_active(event_id, deadline=custom)` использует custom deadline
    - `isinstance(StubEventLookup(), EventLookup)` через runtime_checkable Protocol (если decorated) или duck-type structural check
  </behavior>
  <action>
    Создать `src/bet_maker/facades/event_lookup.py`:

    ```python
    from __future__ import annotations

    from datetime import datetime, timedelta
    from typing import Protocol
    from uuid import UUID

    from pydantic import BaseModel, ConfigDict

    from bet_maker.schemas.events import EventState
    from config.time import utc_now


    class EventSnapshot(BaseModel):
        """Frozen snapshot of an event as observed by line-provider.

        D-11: Returned by EventLookup.get_event. interactor place_bet (D-14)
        validates: snapshot is not None, deadline > now, state == NEW.
        frozen=True ensures the snapshot can't be mutated between validation
        steps. extra='forbid' guards against drift if line-provider adds new
        fields (P4 HttpEventLookup must explicitly handle them).
        """

        model_config = ConfigDict(frozen=True, extra="forbid")

        event_id: UUID
        deadline: datetime
        state: EventState


    class EventLookup(Protocol):
        """Service-boundary facade for resolving an event_id to a snapshot.

        D-11 / D-13: P3 implementation = StubEventLookup (in-process dict).
        P4 implementation = HttpEventLookup (httpx → line-provider GET /event/{id}).
        Both satisfy the same Protocol structurally — no inheritance needed.
        """

        async def get_event(self, event_id: UUID) -> EventSnapshot | None: ...


    class StubEventLookup:
        """In-process dict-backed EventLookup for P3 tests + dev environments.

        D-11: Tests use `app.state.event_lookup.seed_active(event_id)` to
        register a bettable event before POSTing to /bet. seed_active is the
        common-case convenience method (state=NEW + deadline=now+1h); seed()
        accepts a full EventSnapshot for edge cases (past deadline / finished
        state).
        """

        def __init__(self) -> None:
            self._events: dict[UUID, EventSnapshot] = {}

        def seed(self, snapshot: EventSnapshot) -> None:
            """Register an arbitrary snapshot — used by tests for edge cases."""
            self._events[snapshot.event_id] = snapshot

        def seed_active(
            self,
            event_id: UUID,
            *,
            deadline: datetime | None = None,
        ) -> None:
            """Register an active (state=NEW, deadline in future) event.

            Default deadline = utc_now() + 1 hour. Custom deadline allowed for
            boundary tests (e.g., deadline exactly now → must be rejected by
            interactor with `deadline <= now()` check, D-14).
            """
            if deadline is None:
                deadline = utc_now() + timedelta(hours=1)
            self._events[event_id] = EventSnapshot(
                event_id=event_id,
                deadline=deadline,
                state=EventState.NEW,
            )

        async def get_event(self, event_id: UUID) -> EventSnapshot | None:
            """Return the seeded snapshot or None — interactor maps None to 422."""
            return self._events.get(event_id)
    ```

    Ключевые детали:
    - `Protocol` НЕ декорирован `@runtime_checkable` потому что нам не нужны `isinstance(stub, EventLookup)` runtime checks; tests используют structural typing через variable annotation `lookup: EventLookup = StubEventLookup()` который mypy strict проверит.
    - `EventSnapshot.model_config = ConfigDict(frozen=True, extra="forbid")` — frozen+forbid invariants.
    - `seed_active` принимает `deadline: datetime | None = None` keyword-only (`*,`) — explicit args, harder to misuse.
    - `_events: dict[UUID, EventSnapshot] = {}` instance attribute (НЕ class-level — caused mutation leak across test instances).
    - НЕ использовать `asyncio.Lock` — StubEventLookup только тесты, тесты per-function fixture (новый instance каждый тест); concurrent mutation не возможен.

    EventLookupDep aliases добавляются в Task 4 (facades/deps.py) — не здесь.
  </action>
  <verify>
    <automated>test -f src/bet_maker/facades/event_lookup.py</automated>
    <automated>grep -q "class EventLookup(Protocol):" src/bet_maker/facades/event_lookup.py</automated>
    <automated>grep -q "class EventSnapshot(BaseModel):" src/bet_maker/facades/event_lookup.py</automated>
    <automated>grep -q "class StubEventLookup:" src/bet_maker/facades/event_lookup.py</automated>
    <automated>grep -q "frozen=True" src/bet_maker/facades/event_lookup.py</automated>
    <automated>grep -q 'extra="forbid"' src/bet_maker/facades/event_lookup.py</automated>
    <automated>grep -q "def seed(self, snapshot: EventSnapshot)" src/bet_maker/facades/event_lookup.py</automated>
    <automated>grep -q "def seed_active" src/bet_maker/facades/event_lookup.py</automated>
    <automated>grep -q "async def get_event" src/bet_maker/facades/event_lookup.py</automated>
    <automated>grep -q "dict\[UUID, EventSnapshot\]" src/bet_maker/facades/event_lookup.py</automated>
    <automated>uv run python -c "import asyncio; from bet_maker.facades.event_lookup import EventLookup, EventSnapshot, StubEventLookup; from bet_maker.schemas.events import EventState; from uuid import uuid4; from datetime import datetime, timezone; lookup: EventLookup = StubEventLookup(); eid = uuid4(); snap = EventSnapshot(event_id=eid, deadline=datetime.now(timezone.utc), state=EventState.NEW); lookup.seed(snap); got = asyncio.run(lookup.get_event(eid)); assert got == snap, got; assert asyncio.run(lookup.get_event(uuid4())) is None; print('OK')"</automated>
    <automated>uv run mypy --strict src/bet_maker/facades/event_lookup.py</automated>
    <automated>uv run ruff check src/bet_maker/facades/event_lookup.py</automated>
  </verify>
  <acceptance_criteria>
    - event_lookup.py содержит ВСЕ ТРИ: `class EventLookup(Protocol):`, `class EventSnapshot(BaseModel):`, `class StubEventLookup:`
    - EventSnapshot имеет `frozen=True` и `extra="forbid"`
    - EventSnapshot fields: `event_id: UUID, deadline: datetime, state: EventState` (точно 3 fields)
    - StubEventLookup имеет: `seed(snapshot)`, `seed_active(event_id, *, deadline=None)`, `async get_event(event_id)`
    - StubEventLookup._events: dict[UUID, EventSnapshot] instance attribute
    - Inline Python check: `StubEventLookup()` → `seed(snapshot)` → `get_event(snapshot.event_id)` returns snapshot
    - Inline Python check: typing annotation `lookup: EventLookup = StubEventLookup()` mypy-strict проходит
    - `uv run mypy --strict src/bet_maker/facades/event_lookup.py` exit 0
    - `uv run ruff check src/bet_maker/facades/event_lookup.py` → All checks passed
  </acceptance_criteria>
  <done>EventLookup Protocol + EventSnapshot frozen + StubEventLookup с dict backend и seed/seed_active методами созданы. Structural typing работает. mypy strict + ruff чисто.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Create facades/deps.py — DI providers + Annotated aliases</name>
  <files>src/bet_maker/facades/deps.py</files>
  <read_first>
    - src/line_provider/facades/deps.py (analog — exact shape для StoreDep / EventBusDep aliases)
    - .planning/phases/03-bet-maker-domain-db/03-PATTERNS.md §«src/bet_maker/facades/deps.py» (lines 169–204)
    - src/bet_maker/settings/config.py (BetMakerSettings)
    - src/bet_maker/facades/uow.py + repositories/bets.py + facades/event_lookup.py (только что созданные)
  </read_first>
  <behavior>
    - `get_engine(request)` возвращает `request.app.state.engine` typed как AsyncEngine
    - `get_sessionmaker(request)` возвращает `request.app.state.sessionmaker` typed как `async_sessionmaker[AsyncSession]`
    - `get_session(request)` async generator yielding `AsyncSession` (для read-only selectors)
    - `get_uow(request)` возвращает `AsyncUnitOfWork(sessionmaker)` (для write взаимодействий)
    - `get_event_lookup(request)` возвращает `request.app.state.event_lookup` typed как `EventLookup`
    - `get_settings(request)` возвращает `request.app.state.settings` typed как `BetMakerSettings`
    - Все `Annotated[T, Depends(get_x)]` aliases экспортируются: EngineDep, SessionmakerDep, SessionDep, UoWDep, EventLookupDep, SettingsDep
  </behavior>
  <action>
    Создать `src/bet_maker/facades/deps.py`:

    ```python
    from __future__ import annotations

    from collections.abc import AsyncIterator
    from typing import Annotated, cast

    from fastapi import Depends, Request
    from sqlalchemy.ext.asyncio import AsyncEngine, AsyncSession, async_sessionmaker

    from bet_maker.facades.event_lookup import EventLookup
    from bet_maker.facades.uow import AsyncUnitOfWork
    from bet_maker.settings.config import BetMakerSettings


    def get_settings(request: Request) -> BetMakerSettings:
        """Read settings — pinned to app.state by lifespan (Plan 03-07)."""
        return cast(BetMakerSettings, request.app.state.settings)


    def get_engine(request: Request) -> AsyncEngine:
        """Read the AsyncEngine — pinned to app.state by lifespan."""
        return cast(AsyncEngine, request.app.state.engine)


    def get_sessionmaker(request: Request) -> async_sessionmaker[AsyncSession]:
        """Read the async_sessionmaker — pinned to app.state by lifespan."""
        return cast(async_sessionmaker[AsyncSession], request.app.state.sessionmaker)


    async def get_session(request: Request) -> AsyncIterator[AsyncSession]:
        """Yield a one-shot AsyncSession for read-only selectors (no UoW).

        D-25: GET /bets and GET /bet/{id} are pure reads — selectors take a
        session directly. Session is auto-closed when the request exits.
        """
        sessionmaker = get_sessionmaker(request)
        async with sessionmaker() as session:
            yield session


    def get_uow(request: Request) -> AsyncUnitOfWork:
        """Construct a fresh UoW for the current request (Pitfall A2 mitigation).

        Each request gets its own UoW → its own AsyncSession → its own
        transaction. Sessions are NEVER shared across requests.
        """
        sessionmaker = get_sessionmaker(request)
        return AsyncUnitOfWork(sessionmaker)


    def get_event_lookup(request: Request) -> EventLookup:
        """Read the EventLookup — pinned to app.state by lifespan.

        D-13: P3 StubEventLookup; P4 swaps to HttpEventLookup without touching
        this provider — same Protocol satisfied structurally.
        """
        return cast(EventLookup, request.app.state.event_lookup)


    SettingsDep = Annotated[BetMakerSettings, Depends(get_settings)]
    EngineDep = Annotated[AsyncEngine, Depends(get_engine)]
    SessionmakerDep = Annotated[async_sessionmaker[AsyncSession], Depends(get_sessionmaker)]
    SessionDep = Annotated[AsyncSession, Depends(get_session)]
    UoWDep = Annotated[AsyncUnitOfWork, Depends(get_uow)]
    EventLookupDep = Annotated[EventLookup, Depends(get_event_lookup)]
    ```

    Не добавлять никаких других providers. Не добавлять `get_bet_repository` — interactor получает repository через `uow.bets` (UoW pattern).
  </action>
  <verify>
    <automated>test -f src/bet_maker/facades/deps.py</automated>
    <automated>grep -q "def get_settings" src/bet_maker/facades/deps.py</automated>
    <automated>grep -q "def get_engine" src/bet_maker/facades/deps.py</automated>
    <automated>grep -q "def get_sessionmaker" src/bet_maker/facades/deps.py</automated>
    <automated>grep -q "async def get_session" src/bet_maker/facades/deps.py</automated>
    <automated>grep -q "def get_uow" src/bet_maker/facades/deps.py</automated>
    <automated>grep -q "def get_event_lookup" src/bet_maker/facades/deps.py</automated>
    <automated>grep -q "SettingsDep = Annotated" src/bet_maker/facades/deps.py</automated>
    <automated>grep -q "EngineDep = Annotated" src/bet_maker/facades/deps.py</automated>
    <automated>grep -q "SessionDep = Annotated" src/bet_maker/facades/deps.py</automated>
    <automated>grep -q "UoWDep = Annotated" src/bet_maker/facades/deps.py</automated>
    <automated>grep -q "EventLookupDep = Annotated" src/bet_maker/facades/deps.py</automated>
    <automated>uv run python -c "from bet_maker.facades.deps import get_engine, get_uow, get_event_lookup, EngineDep, UoWDep, EventLookupDep; print('OK')"</automated>
    <automated>uv run mypy --strict src/bet_maker/facades/deps.py</automated>
    <automated>uv run ruff check src/bet_maker/facades/deps.py</automated>
  </verify>
  <acceptance_criteria>
    - deps.py содержит 6 providers (get_settings, get_engine, get_sessionmaker, get_session, get_uow, get_event_lookup)
    - deps.py содержит 6 Annotated aliases (SettingsDep, EngineDep, SessionmakerDep, SessionDep, UoWDep, EventLookupDep)
    - `get_session` async generator (yields one-shot session); другие — sync providers
    - Все impl используют `cast(T, request.app.state.x)` pattern (per PATTERNS.md app.state DI pattern)
    - Inline import succeeds
    - `uv run mypy --strict src/bet_maker/facades/deps.py` exit 0
    - `uv run ruff check src/bet_maker/facades/deps.py` → All checks passed
  </acceptance_criteria>
  <done>facades/deps.py содержит 6 providers + 6 Annotated aliases — готовы для consumer'ов в Plan 03-07. mypy strict + ruff чисто.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 5: Replace test_repositories.py + test_uow.py Wave 0 stubs</name>
  <files>
    tests/bet_maker/test_repositories.py,
    tests/bet_maker/test_uow.py
  </files>
  <read_first>
    - tests/bet_maker/test_repositories.py + test_uow.py (Wave 0 stubs из Plan 03-02)
    - src/bet_maker/repositories/bets.py + facades/uow.py (созданные в Task 2)
    - .planning/phases/03-bet-maker-domain-db/03-VALIDATION.md §«Critical Risk Axes» #3 (concurrent UoW isolation)
    - .planning/phases/03-bet-maker-domain-db/03-RESEARCH.md §«Pattern 2» (UoW shape)
  </read_first>
  <action>
    Полностью заменить 2 файла.

    **`tests/bet_maker/test_repositories.py`**:

    ```python
    """Unit tests for bet_maker.repositories.bets.

    BM-02 / D-18: BetRepository.add(bet) — session.add only (no flush, no commit).
    BM-02 / D-18: BetRepository.get_by_id(bet_id) — select + scalar_one_or_none.
    Anti-Pattern 1: repository MUST NOT commit/rollback (UoW owns transactions).
    """

    from __future__ import annotations

    import inspect
    from decimal import Decimal
    from uuid import uuid4

    from sqlalchemy.ext.asyncio import async_sessionmaker

    from bet_maker.models.bet import Bet
    from bet_maker.repositories.bets import BetRepository


    class TestAntiPattern1:
        """Anti-Pattern 1: repository MUST NOT commit/rollback.

        Grep-level guard plus introspection — if a future contributor adds
        `await session.commit()` to add/get_by_id, this catches it.
        """

        def test_source_has_no_commit(self) -> None:
            """No `session.commit` / `session.rollback` in repository source."""
            source = inspect.getsource(BetRepository)
            assert "commit" not in source.lower(), source
            assert "rollback" not in source.lower(), source


    class TestRuntime:
        """BM-02: BetRepository against testcontainers PG via session_factory."""

        async def test_add_stages_bet_in_session(
            self, session_factory: async_sessionmaker
        ) -> None:
            """D-18: .add() calls session.add — does NOT flush. The bet is
            in `session.new` but not yet persisted."""
            async with session_factory() as session:
                repo = BetRepository(session)
                bet = Bet(event_id=uuid4(), amount=Decimal("10.00"))
                repo.add(bet)
                assert bet in session.new

        async def test_add_then_flush_then_get_by_id_roundtrip(
            self, session_factory: async_sessionmaker
        ) -> None:
            """BM-02: add → flush → get_by_id returns the same bet inside session."""
            async with session_factory.begin() as session:
                repo = BetRepository(session)
                bet = Bet(event_id=uuid4(), amount=Decimal("5.50"))
                repo.add(bet)
                await session.flush()
                fetched = await repo.get_by_id(bet.id)
                assert fetched is not None
                assert fetched.id == bet.id

        async def test_get_by_id_returns_none_for_unknown(
            self, session_factory: async_sessionmaker
        ) -> None:
            """D-18: missing bet → None (not exception)."""
            async with session_factory() as session:
                repo = BetRepository(session)
                fetched = await repo.get_by_id(uuid4())
                assert fetched is None

        async def test_add_does_not_flush_implicitly(
            self, session_factory: async_sessionmaker
        ) -> None:
            """D-18 / PATTERNS.md: .add() does NOT flush; caller controls flush
            timing. Verified by checking the bet is still in session.new (not
            session.persistent) after .add() but before .flush()."""
            async with session_factory() as session:
                repo = BetRepository(session)
                bet = Bet(event_id=uuid4(), amount=Decimal("1.00"))
                repo.add(bet)
                assert bet in session.new
                assert bet not in session.identity_map.values()
    ```

    **`tests/bet_maker/test_uow.py`**:

    ```python
    """Unit tests for bet_maker.facades.uow.AsyncUnitOfWork.

    BM-02 / D-17: AsyncUnitOfWork over async_sessionmaker.begin().
    Commit on clean __aexit__; rollback on exception.
    Critical Risk Axis 3 (RESEARCH §Validation): per-request UoW isolation
    under concurrency — verified via asyncio.gather over 5 UoWs.
    """

    from __future__ import annotations

    import asyncio
    from decimal import Decimal
    from uuid import uuid4

    import pytest
    from sqlalchemy.ext.asyncio import async_sessionmaker

    from bet_maker.facades.uow import AsyncUnitOfWork
    from bet_maker.models.bet import Bet


    class TestShape:
        """D-17: UoW exposes session + bets, no manual commit method."""

        async def test_aenter_exposes_session_and_bets(
            self, session_factory: async_sessionmaker
        ) -> None:
            """D-17: `async with uow:` → uow.session + uow.bets available."""
            async with AsyncUnitOfWork(session_factory) as uow:
                assert uow.session is not None
                assert uow.bets is not None
                assert uow.bets._session is uow.session  # type: ignore[attr-defined]

        async def test_uow_has_no_public_commit_or_rollback(self) -> None:
            """D-17 / Anti-Pattern 1: no manual commit/rollback methods exposed."""
            for forbidden in ("commit", "rollback"):
                assert not hasattr(AsyncUnitOfWork, forbidden), forbidden


    class TestTransactionSemantics:
        """BM-02 / D-17: commit on clean exit, rollback on exception."""

        async def test_commit_on_clean_exit(
            self, session_factory: async_sessionmaker
        ) -> None:
            """Clean exit → INSERT committed → second UoW sees the bet."""
            event_id = uuid4()
            async with AsyncUnitOfWork(session_factory) as uow:
                bet = Bet(event_id=event_id, amount=Decimal("10.00"))
                uow.bets.add(bet)
                await uow.session.flush()
                bet_id = bet.id

            # New UoW — must see committed bet
            async with AsyncUnitOfWork(session_factory) as uow2:
                fetched = await uow2.bets.get_by_id(bet_id)
                assert fetched is not None
                assert fetched.event_id == event_id

        async def test_rollback_on_exception(
            self, session_factory: async_sessionmaker
        ) -> None:
            """Exception inside `async with uow:` → rollback → second UoW sees nothing."""
            event_id = uuid4()
            captured_id = None
            with pytest.raises(RuntimeError, match="forced rollback"):
                async with AsyncUnitOfWork(session_factory) as uow:
                    bet = Bet(event_id=event_id, amount=Decimal("10.00"))
                    uow.bets.add(bet)
                    await uow.session.flush()
                    captured_id = bet.id
                    raise RuntimeError("forced rollback")

            assert captured_id is not None
            async with AsyncUnitOfWork(session_factory) as uow2:
                fetched = await uow2.bets.get_by_id(captured_id)
                assert fetched is None  # rolled back


    class TestConcurrency:
        """Critical Risk Axis 3: per-request UoW isolation."""

        async def test_concurrent_uows_isolated(
            self, session_factory: async_sessionmaker
        ) -> None:
            """Pitfall A2: 5 parallel UoWs each commit their own session
            independently. asyncio.gather over independent UoWs must not
            interfere or deadlock."""
            async def place_one(value: str) -> Bet:
                async with AsyncUnitOfWork(session_factory) as uow:
                    bet = Bet(event_id=uuid4(), amount=Decimal(value))
                    uow.bets.add(bet)
                    await uow.session.flush()
                    await uow.session.refresh(bet)
                    return bet

            results = await asyncio.gather(*[
                place_one(f"{i + 1}.00") for i in range(5)
            ])
            assert len({b.id for b in results}) == 5, "all UoWs must produce distinct bets"
            assert {str(b.amount) for b in results} == {"1.00", "2.00", "3.00", "4.00", "5.00"}
    ```

    Не использовать pytest.mark.xfail. Не оборачивать concurrent test в `pytest.mark.timeout` — default 60s достаточно.
  </action>
  <verify>
    <automated>bash -c "! grep -q 'pytestmark = pytest.mark.skip' tests/bet_maker/test_repositories.py"</automated>
    <automated>bash -c "! grep -q 'pytestmark = pytest.mark.skip' tests/bet_maker/test_uow.py"</automated>
    <automated>uv run pytest tests/bet_maker/test_repositories.py tests/bet_maker/test_uow.py -q --no-cov</automated>
    <automated>grep -c "def test_" tests/bet_maker/test_repositories.py</automated>
    <automated>grep -c "def test_" tests/bet_maker/test_uow.py</automated>
    <automated>grep -q "asyncio.gather" tests/bet_maker/test_uow.py</automated>
    <automated>uv run mypy --strict tests/bet_maker/test_repositories.py tests/bet_maker/test_uow.py</automated>
    <automated>uv run ruff check tests/bet_maker/test_repositories.py tests/bet_maker/test_uow.py</automated>
  </verify>
  <acceptance_criteria>
    - Wave 0 stub skip mark удалён из обоих файлов
    - test_repositories.py: >= 5 тестов (Anti-Pattern 1 source-grep + 4 runtime)
    - test_uow.py: >= 5 тестов (TestShape: 2, TestTransactionSemantics: 2, TestConcurrency: 1)
    - `grep -q "asyncio.gather" tests/bet_maker/test_uow.py` — Critical Risk Axis 3 покрыт
    - `uv run pytest tests/bet_maker/test_repositories.py tests/bet_maker/test_uow.py -q --no-cov` — все passed
    - `uv run mypy --strict tests/bet_maker/test_repositories.py tests/bet_maker/test_uow.py` exit 0
    - `uv run ruff check tests/bet_maker/test_repositories.py tests/bet_maker/test_uow.py` → All checks passed
  </acceptance_criteria>
  <done>test_repositories.py (5+ тестов) + test_uow.py (5+ тестов incl asyncio.gather concurrent isolation) passed. Anti-Pattern 1 grep-verified. Risk Axis 3 покрыт.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 6: Replace test_event_lookup.py Wave 0 stub</name>
  <files>tests/bet_maker/test_event_lookup.py</files>
  <read_first>
    - tests/bet_maker/test_event_lookup.py (Wave 0 stub из Plan 03-02)
    - tests/line_provider/test_facades.py (P2 analog — Protocol+Stub test pattern)
    - src/bet_maker/facades/event_lookup.py (Task 3)
    - .planning/phases/03-bet-maker-domain-db/03-VALIDATION.md §«Critical Risk Axes» #8 (EventLookup stub seeding)
  </read_first>
  <action>
    Полностью заменить `tests/bet_maker/test_event_lookup.py`:

    ```python
    """Unit tests for bet_maker.facades.event_lookup.

    D-11: EventLookup Protocol satisfied by StubEventLookup structurally.
    D-11: EventSnapshot is frozen (Pydantic v2 frozen=True) + extra='forbid'.
    D-11: StubEventLookup.seed() / .seed_active() / .get_event() contract.
    Risk Axis 8 (RESEARCH §Validation): per-test seeding works for tests that
    later call /bet — verified here at the unit level.
    """

    from __future__ import annotations

    from datetime import datetime, timedelta, timezone
    from uuid import uuid4

    import pytest
    from pydantic import ValidationError

    from bet_maker.facades.event_lookup import (
        EventLookup,
        EventSnapshot,
        StubEventLookup,
    )
    from bet_maker.schemas.events import EventState


    class TestEventSnapshot:
        """D-11: EventSnapshot is frozen and forbids extras."""

        def test_construction_happy(self) -> None:
            """Happy path — 3 required fields, no extras."""
            event_id = uuid4()
            deadline = datetime.now(timezone.utc) + timedelta(hours=1)
            snap = EventSnapshot(event_id=event_id, deadline=deadline, state=EventState.NEW)
            assert snap.event_id == event_id
            assert snap.deadline == deadline
            assert snap.state == EventState.NEW

        def test_frozen_rejects_mutation(self) -> None:
            """D-11: frozen=True — attribute assignment raises ValidationError."""
            snap = EventSnapshot(
                event_id=uuid4(),
                deadline=datetime.now(timezone.utc),
                state=EventState.NEW,
            )
            with pytest.raises(ValidationError):
                snap.state = EventState.FINISHED_WIN  # type: ignore[misc]

        def test_extra_forbid(self) -> None:
            """D-11: extra='forbid' rejects unknown fields (e.g., coefficient drift)."""
            with pytest.raises(ValidationError) as exc:
                EventSnapshot(
                    event_id=uuid4(),
                    deadline=datetime.now(timezone.utc),
                    state=EventState.NEW,
                    coefficient="1.50",  # type: ignore[call-arg]
                )
            assert "extra_forbidden" in str(exc.value) or "Extra inputs" in str(exc.value)


    class TestStubEventLookup:
        """D-11 / Risk Axis 8: StubEventLookup seed + get_event contract."""

        async def test_protocol_structural_typing(self) -> None:
            """D-11: StubEventLookup satisfies EventLookup Protocol structurally."""
            lookup: EventLookup = StubEventLookup()
            assert hasattr(lookup, "get_event")

        async def test_seed_then_get_event_returns_snapshot(self) -> None:
            """D-11: seed(snapshot) → get_event(id) returns the same snapshot."""
            lookup = StubEventLookup()
            event_id = uuid4()
            snap = EventSnapshot(
                event_id=event_id,
                deadline=datetime.now(timezone.utc) + timedelta(hours=1),
                state=EventState.NEW,
            )
            lookup.seed(snap)
            got = await lookup.get_event(event_id)
            assert got == snap

        async def test_get_event_returns_none_for_unseeded(self) -> None:
            """D-11: unseeded id → None (interactor maps to 422 EventNotBettable)."""
            lookup = StubEventLookup()
            got = await lookup.get_event(uuid4())
            assert got is None

        async def test_seed_active_default_deadline_future_state_new(self) -> None:
            """D-11: seed_active(id) defaults to deadline=now+1h, state=NEW."""
            lookup = StubEventLookup()
            event_id = uuid4()
            before = datetime.now(timezone.utc)
            lookup.seed_active(event_id)
            got = await lookup.get_event(event_id)
            assert got is not None
            assert got.state == EventState.NEW
            # deadline approx now + 1h
            assert got.deadline > before
            assert got.deadline > before + timedelta(minutes=50)
            assert got.deadline < before + timedelta(minutes=70)

        async def test_seed_active_custom_deadline(self) -> None:
            """D-11: seed_active accepts custom deadline (boundary tests)."""
            lookup = StubEventLookup()
            event_id = uuid4()
            custom = datetime(2030, 1, 1, tzinfo=timezone.utc)
            lookup.seed_active(event_id, deadline=custom)
            got = await lookup.get_event(event_id)
            assert got is not None
            assert got.deadline == custom

        async def test_instance_isolation(self) -> None:
            """D-11: each StubEventLookup instance has its own dict — no class
            attribute mutation leak (production safety + test isolation)."""
            lookup_a = StubEventLookup()
            lookup_b = StubEventLookup()
            event_id = uuid4()
            lookup_a.seed_active(event_id)
            assert await lookup_a.get_event(event_id) is not None
            assert await lookup_b.get_event(event_id) is None
    ```
  </action>
  <verify>
    <automated>bash -c "! grep -q 'pytestmark = pytest.mark.skip' tests/bet_maker/test_event_lookup.py"</automated>
    <automated>uv run pytest tests/bet_maker/test_event_lookup.py -q --no-cov</automated>
    <automated>grep -c "def test_" tests/bet_maker/test_event_lookup.py</automated>
    <automated>uv run mypy --strict tests/bet_maker/test_event_lookup.py</automated>
    <automated>uv run ruff check tests/bet_maker/test_event_lookup.py</automated>
  </verify>
  <acceptance_criteria>
    - Wave 0 stub skip mark удалён
    - `grep -c "def test_" tests/bet_maker/test_event_lookup.py` >= 9 (TestEventSnapshot: 3, TestStubEventLookup: 6)
    - `uv run pytest tests/bet_maker/test_event_lookup.py -q --no-cov` exit 0; все passed
    - `uv run mypy --strict tests/bet_maker/test_event_lookup.py` exit 0
    - `uv run ruff check tests/bet_maker/test_event_lookup.py` → All checks passed
  </acceptance_criteria>
  <done>test_event_lookup.py: 9+ тестов passed; EventSnapshot frozen + StubEventLookup seed/seed_active/get_event invariants verified. Risk Axis 8 покрыт.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| FastAPI request → app.state.* via Depends(get_x) | DI providers — sole entrypoint to runtime singletons (engine, sessionmaker, event_lookup) |
| Lifespan (Plan 03-07) → tenacity retry → PG container | startup gate: bad DSN must surface here, not at first request |
| Per-request UoW → AsyncSession → PG transaction | Critical Risk Axis 3: concurrent UoWs MUST NOT share sessions (Pitfall A2) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-1 | Tampering | Decimal coercion в DB layer | mitigate (already at schema layer + verified at model layer in 03-04) | No new surface in 03-05; UoW + Repository pass `Bet.amount` through unchanged. |
| T-03-3 | Tampering | SQL injection через `get_by_id(bet_id)` | mitigate | SQLAlchemy `select(Bet).where(Bet.id == bet_id)` parameterizes; bet_id типизирован как UUID (Pydantic UUID4 coercion в Plan 03-07 route layer). No raw SQL in repository. |
| T-03-5 | Information Disclosure | DSN leak в logs через SQLAlchemyError | mitigate | `log.warning("health.check.failed", check="postgres", error=str(exc))` логирует только class+message, не DSN. `wait_for_postgres` использует `before_sleep_log` standard logging which doesn't dump engine repr. |
| T-03-2-DoS | Denial of Service | tenacity infinite retry storm | mitigate | `stop_after_attempt(10)` bounded; cumulative wait ~75s worst case → reraise OperationalError → lifespan crashes с clear log. RESEARCH §Pattern 5 verified. |
| T-03-3-Anti1 | Repudiation | Future contributor adds `await session.commit()` в repository | mitigate | TestAntiPattern1.test_source_has_no_commit grep-introspects BetRepository source code. |
| T-03-3-A2 | Tampering | Shared AsyncSession across concurrent tasks (Pitfall A2) | mitigate | TestConcurrency.test_concurrent_uows_isolated — asyncio.gather over 5 UoWs, each получает свой sessionmaker.begin() → свой session. get_session async generator yields ONE-SHOT session per request. |
</threat_model>

<verification>
После завершения всех 6 задач:
- `uv run pytest tests/bet_maker -q --no-cov` → schemas (18+) + models (12+) + alembic (4+) + repositories (5+) + uow (5+) + event_lookup (9+) + health (2 P1) passed; remaining Wave 0 stubs skipped
- `uv run pytest -q` → 150+ total passed
- `uv run mypy --strict src/bet_maker tests/bet_maker` exit 0
- `uv run ruff check src/bet_maker tests/bet_maker` exit 0
- Anti-Pattern 1 grep-enforced: `grep -cE 'session.commit|session.rollback' src/bet_maker/repositories/bets.py` == 0
- Critical Risk Axis 3 covered: `grep -q "asyncio.gather" tests/bet_maker/test_uow.py`
- Critical Risk Axis 8 covered: test_event_lookup.py has seed_active + get_event miss tests
</verification>

<success_criteria>
- infrastructure/db/engine.py: create_engine_and_sessionmaker (D-16 + D-15 expire_on_commit=False)
- infrastructure/db/pings.py: wait_for_postgres (@retry 10 attempts, exponential 1..10) + ping_postgres (bool, no retry)
- repositories/bets.py: BetRepository.add (no flush) + get_by_id (scalar_one_or_none); NO commit/rollback
- facades/uow.py: AsyncUnitOfWork via async_sessionmaker.begin()
- facades/event_lookup.py: EventLookup Protocol + EventSnapshot (frozen+forbid) + StubEventLookup (dict, seed, seed_active)
- facades/deps.py: 6 providers + 6 Annotated aliases (Settings/Engine/Sessionmaker/Session/UoW/EventLookup)
- 3 test files updated (test_repositories: 5+, test_uow: 5+ incl asyncio.gather concurrency, test_event_lookup: 9+)
- Critical Risk Axes 3 (UoW concurrency) and 8 (EventLookup seed) covered
- Anti-Pattern 1 grep-verified (0 commits in repository)
- mypy strict + ruff check + ruff format чисто
- pytest 150+ passed
</success_criteria>

<output>
After completion, create `.planning/phases/03-bet-maker-domain-db/03-05-SUMMARY.md` following `@$HOME/.claude/get-shit-done/templates/summary.md`.

SUMMARY должен включать:
- Точное число тестов в каждом из 3 раскрытых test файлов
- Total pytest passed count
- Подтверждение что Critical Risk Axes 3 + 8 покрыты (явная ссылка на test names)
- Если mypy strict потребовал any `# type: ignore` — задокументировать каждое (особенно в uow.py for async_sessionmaker.begin() typing)
</output>
