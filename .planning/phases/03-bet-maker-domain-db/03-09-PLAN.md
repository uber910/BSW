---
phase: 03-bet-maker-domain-db
plan: 09
type: execute
wave: 6
depends_on: [01, 02, 03, 04, 05, 06, 07, 08]
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
  - .planning/phases/03-bet-maker-domain-db/03-VALIDATION.md
autonomous: false
requirements: [BM-01, BM-02, BM-03, BM-05, BM-06, BM-07, BM-08, BM-13, QA-07]
tags: [bet-maker, phase-gate, coverage, doc-sync, requirements-flip]

must_haves:
  truths:
    - "Full test suite зелёный: uv run pytest -q exit 0 с >= 200 passed"
    - "Coverage на src/bet_maker ≥ 80%: uv run pytest --cov=src/bet_maker --cov-fail-under=80 tests/bet_maker exit 0"
    - "Live alembic upgrade head rerun через docker compose идемпотентно (Manual-Only Verification per 03-VALIDATION.md): запущен оператором; результат задокументирован в SUMMARY"
    - "REQUIREMENTS.md Traceability table: BM-01, BM-02, BM-03, BM-05, BM-06, BM-07, BM-08, BM-13, QA-07 → Status = Complete (Plan 03-09)"
    - "REQUIREMENTS.md bet-maker checklist: BM-01/BM-02/BM-03/BM-05/BM-06/BM-07/BM-08/BM-13 → `[x]`"
    - "REQUIREMENTS.md Quality checklist: QA-07 → `[x]`"
    - "ROADMAP.md Phase 3 checkbox: `[ ]` → `[x]`"
    - "ROADMAP.md Progress table Phase 3 row: 0/? Not started → 9/9 Complete 2026-05-15"
    - "ROADMAP.md Plans список под Phase 3 фиксирует все 9 PLAN файлов с `[x]`"
    - "STATE.md frontmatter: progress.completed_phases=3, total_plans=23 (14+9), completed_plans=22; current focus Phase 4"
    - "STATE.md `## Current Position` advanced to Phase 3 → COMPLETE; Phase 4 ready"
    - "STATE.md Decisions block: новый entry `[Phase 03]: 2026-05-15 (Plan 03-09) Phase 3 complete — UoW + Repository + 3 routes + /health PG ping + alembic 0001 idempotent...`"
    - "STATE.md Last Session updated"
    - "03-VALIDATION.md per-task map статусы: ⬜ pending → ✅ green для всех 25+ строк"
    - "Один atomic docs commit с явным сообщением `docs(phase-3): close Phase 3 — 9 requirements complete (BM-01..03, BM-05..08, BM-13, QA-07)`"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "All Phase 3 requirements flipped to Complete"
      contains: "Phase 3 | Complete"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 3 checkbox closed + Progress table updated"
      contains: "8/8"
    - path: ".planning/STATE.md"
      provides: "Phase advance + decisions entry"
      contains: "Phase 3 complete"
    - path: ".planning/phases/03-bet-maker-domain-db/03-VALIDATION.md"
      provides: "Per-task map all green"
      contains: "✅"
  key_links:
    - from: ".planning/STATE.md"
      to: ".planning/phases/03-bet-maker-domain-db/03-09-SUMMARY.md"
      via: "Last Session reference"
      pattern: "03-08-SUMMARY"
---

<objective>
Финальный phase-gate Phase 3. Этот plan:
1. **Запускает полный quality gate** против реального testcontainers PG: pytest + coverage ≥80% + mypy strict + ruff (на этом этапе все predecessor планы уже зелёные локально, но coverage threshold впервые enforced в CI-like flow).
2. **Запрашивает оператора подтвердить manual alembic idempotency rehearsal через docker compose** (Manual-Only Verification в 03-VALIDATION.md) — non-autonomous checkpoint.
3. **Синхронизирует docs:** REQUIREMENTS.md статусы → Complete; ROADMAP.md Phase 3 checkbox + Progress table + Plans список; STATE.md frontmatter + Decisions + Last Session; 03-VALIDATION.md per-task map статусы → ✅.
4. **Создаёт один docs-only commit** закрывающий фазу.

`autonomous: false` потому что Task 4 — checkpoint:human-action: docker compose alembic rehearsal требует live docker daemon и реальное `docker compose up` lifecycle (не testcontainers — другая среда). Operator pastes commands из 03-VALIDATION.md Manual-Only Verifications, copies output. Если live rehearsal не возможен (например, нет Docker Desktop running) — оператор может skip с notice в SUMMARY (но это понижает доверие к Success Criterion #5 production-ready signal; рекомендуется выполнить).

Purpose: Phase 3 не считается завершённой пока: (a) full test gate проходит с coverage threshold, (b) live alembic rehearsal подтверждён, (c) все docs sync'нуты. Без этого plan'а downstream фазы (4-7) не знают что Phase 3 закрыта.

Output: 4 файла обновлены, один atomic docs commit, STATE.md advance, SUMMARY с pytest output + coverage table + manual rehearsal log.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-bet-maker-domain-db/03-CONTEXT.md
@.planning/phases/03-bet-maker-domain-db/03-VALIDATION.md
@.planning/phases/03-bet-maker-domain-db/03-01-PLAN.md
@.planning/phases/03-bet-maker-domain-db/03-08-PLAN.md
@.planning/phases/02-line-provider-domain/02-07-PLAN.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full pytest gate + coverage threshold</name>
  <files>(none — verification only; output captured in SUMMARY)</files>
  <read_first>
    - pyproject.toml (для подтверждения `[tool.coverage.run] source = ["src/line_provider", "src/bet_maker"]` и `[tool.coverage.report] fail_under = 85`)
    - .planning/ROADMAP.md §«Phase 7» success criterion #4 (≥80% порог) — Phase 3 self-declares 80% порог per planning_context
  </read_first>
  <action>
    Запустить шесть quality-gate проверок последовательно:

    1. **Full pytest suite (no cov):**
       ```
       uv run pytest -q
       ```
       ОЖИДАНИЕ: >= 200 passed, 0 failed. Если красное — НЕ продолжать; raised blocker в SUMMARY.

    2. **Coverage threshold для bet_maker (≥80%):**
       ```
       uv run pytest --cov=src/bet_maker --cov-fail-under=80 --cov-report=term-missing tests/bet_maker -q
       ```
       ОЖИДАНИЕ: exit 0; coverage line `TOTAL ... 80%+` либо выше. Если ниже — расширить тесты в соответствующем плане 03-XX (revisit) и НЕ выпускать commit Phase 3.

       Note: pyproject `fail_under = 85` глобально (P2 line_provider), но Phase 3 self-declares 80% (ROADMAP сам Phase 7 говорит ≥80%); CLI flag `--cov-fail-under=80` override pyproject `fail_under=85` для этого invocation (locally). Глобальный 85% gate работает только когда вызываете `uv run pytest --cov` без CLI override — P7 финальный gate использует pyproject default.

    3. **Existing line_provider coverage не задет (P2 baseline):**
       ```
       uv run pytest --cov=src/line_provider --cov-fail-under=85 tests/line_provider -q
       ```
       ОЖИДАНИЕ: exit 0 (P2 baseline ≥85% preserved).

    4. **mypy strict:**
       ```
       uv run mypy --strict src/bet_maker src/line_provider src/config tests/bet_maker tests/line_provider alembic
       ```
       ОЖИДАНИЕ: 0 errors.

    5. **ruff check:**
       ```
       uv run ruff check src tests alembic
       ```
       ОЖИДАНИЕ: All checks passed.

    6. **ruff format --check:**
       ```
       uv run ruff format --check src tests alembic
       ```
       ОЖИДАНИЕ: All files formatted.

    Каждую команду логировать в SUMMARY с финальной строкой output (e.g., `=== 247 passed in 18.42s ===`).

    Если любая из шести команд fails — phase-gate НЕ пройден; SUMMARY должен явно raised blocker + не выполнять Task 2..5; план оставляется `wave_0_complete` но не finalized.
  </action>
  <verify>
    <automated>uv run pytest -q 2>&1 | tail -5</automated>
    <automated>uv run pytest --cov=src/bet_maker --cov-fail-under=80 --cov-report=term-missing tests/bet_maker -q 2>&1 | tail -20</automated>
    <automated>uv run pytest --cov=src/line_provider --cov-fail-under=85 tests/line_provider -q 2>&1 | tail -5</automated>
    <automated>uv run mypy --strict src/bet_maker src/line_provider src/config tests/bet_maker tests/line_provider alembic 2>&1 | tail -3</automated>
    <automated>uv run ruff check src tests alembic 2>&1 | tail -3</automated>
    <automated>uv run ruff format --check src tests alembic 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest -q` exit 0 + line "[0-9]+ passed" >= 200
    - `uv run pytest --cov=src/bet_maker --cov-fail-under=80 tests/bet_maker -q` exit 0 (coverage gate passes)
    - `uv run pytest --cov=src/line_provider --cov-fail-under=85 tests/line_provider -q` exit 0 (P2 baseline сохранён)
    - `uv run mypy --strict ...` exit 0
    - `uv run ruff check ...` exit 0 + "All checks passed"
    - `uv run ruff format --check ...` exit 0
    - Финальные строки 6 команд скопированы verbatim в SUMMARY
  </acceptance_criteria>
  <done>Все 6 quality gates passed. Coverage src/bet_maker ≥80%, src/line_provider ≥85%. Phase 3 готова к docs sync.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Operator runs live alembic idempotency rehearsal via docker compose</name>
  <what-built>Plan 03-04 created `alembic/versions/20260515_0001_bets_initial.py` with `ENUM.create(checkfirst=True)` + `create_type=False` (Pitfall 2 mitigation). Migration is idempotent under testcontainers (3 successive calls in test_alembic.py). This checkpoint verifies the same recipe works under `docker compose up` lifecycle — the production rehearsal documented in 03-VALIDATION.md §Manual-Only Verifications.</what-built>
  <how-to-verify>
    Запустите команды из 03-VALIDATION.md §«Manual-Only Verifications» строка для `alembic upgrade head from fresh docker compose up`:

    1. **Reset compose state (DESTRUCTIVE — drops postgres volume):**
       ```
       docker compose down -v
       ```

    2. **Start postgres only:**
       ```
       docker compose up -d postgres
       sleep 10  # wait for initdb + healthy
       ```

    3. **Run migration first time (fresh DB):**
       ```
       docker compose run --rm bet-maker uv run alembic upgrade head
       ```
       ОЖИДАЕМЫЙ ВЫХОД: `INFO  [alembic.runtime.migration] Running upgrade  -> 0001_bets_initial, bets initial schema ...` затем exit 0.

    4. **Run migration second time (idempotent rerun):**
       ```
       docker compose run --rm bet-maker uv run alembic upgrade head
       ```
       ОЖИДАЕМЫЙ ВЫХОД: НЕТ ошибки `type "bet_status" already exists`. Alembic скажет `Already at head` или re-issue upgrade без mutation.

    5. **Verify bets table exists in PG (через docker exec):**
       ```
       docker compose exec postgres psql -U postgres -d postgres -c "\d+ bets"
       ```
       ОЖИДАЕМЫЙ ВЫХОД: таблица `bets` с колонками id (uuid), event_id (uuid), amount (numeric(12,2)), status (bet_status), created_at (timestamptz), updated_at (timestamptz).

    6. **Tear down:**
       ```
       docker compose down
       ```
       (без `-v` чтобы сохранить volumes если хотите дальше использовать).

    Если хотя бы один шаг failed:
    - Скопируйте полный output в SUMMARY с тегом "BLOCKER".
    - Не помечайте этот checkpoint approved.

    Если все 6 шагов прошли:
    - Введите `approved` в response.
    - Скопируйте output шагов 3, 4, 5 в SUMMARY.

    **Если Docker Desktop не запущен:**
    - Введите `skip` с обоснованием в SUMMARY: «Docker Desktop offline; idempotency proven через testcontainers (test_alembic.py: 3 successive command.upgrade(cfg, "head") calls passed; см. 03-04 SUMMARY)». ROADMAP success criterion #5 (idempotent upgrade head) уже verified тестом — этот manual checkpoint является **дополнительной** production-rehearsal проверкой.
  </how-to-verify>
  <resume-signal>Type "approved" (live rehearsal succeeded) or "skip" (Docker not available — testcontainers proof accepted) or describe failures starting with "BLOCKER:"</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Flip REQUIREMENTS.md statuses to Complete for 9 Phase 3 requirements</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (полностью — нужны точные строки для bet-maker (BM-XX) checklist + Quality (QA-XX) checklist + Traceability table)
    - .planning/phases/02-line-provider-domain/02-07-PLAN.md (P2 mirror — пример того как Plan 02-07 flip'ил LP-XX статусы)
  </read_first>
  <action>
    Открыть `.planning/REQUIREMENTS.md` и сделать следующие правки:

    **Шаг 1.** В секции `### bet-maker (BM)` (строки 30–43 после правок Plan 03-01) flip 8 чекбоксов:
    - `- [ ] **BM-01**:` → `- [x] **BM-01**:`
    - `- [ ] **BM-02**:` → `- [x] **BM-02**:`
    - `- [ ] **BM-03**:` → `- [x] **BM-03**:`
    - `- [ ] **BM-05**:` → `- [x] **BM-05**:`
    - `- [ ] **BM-06**:` → `- [x] **BM-06**:`
    - `- [ ] **BM-07**:` → `- [x] **BM-07**:`
    - `- [ ] **BM-08**:` → `- [x] **BM-08**:`
    - `- [ ] **BM-13**:` → `- [x] **BM-13**:`

    **НЕ** трогать:
    - BM-04 (Phase 4 — `httpx` proxy)
    - BM-09, BM-10, BM-11 (Phase 5 — RabbitMQ consumer + settle interactor + DLQ)
    - BM-12 (Phase 6 — reconciliation job)

    **Шаг 2.** В секции `### Quality (QA)` flip единственный Phase 3 чекбокс:
    - `- [ ] **QA-07**:` → `- [x] **QA-07**:`

    **НЕ** трогать остальные QA-XX (Phase 4-7).

    **Шаг 3.** В таблице `## Traceability` (строки 113+) обновить 9 строк:
    - `| BM-01 | Phase 3 | Pending |` → `| BM-01 | Phase 3 | Complete (Plan 03-09) |`
    - `| BM-02 | Phase 3 | Pending |` → `| BM-02 | Phase 3 | Complete (Plan 03-09) |`
    - `| BM-03 | Phase 3 | Pending |` → `| BM-03 | Phase 3 | Complete (Plan 03-09) |`
    - `| BM-05 | Phase 3 | Pending |` → `| BM-05 | Phase 3 | Complete (Plan 03-09) |`
    - `| BM-06 | Phase 3 | Pending |` → `| BM-06 | Phase 3 | Complete (Plan 03-09) |`
    - `| BM-07 | Phase 3 | Pending |` → `| BM-07 | Phase 3 | Complete (Plan 03-09) |`
    - `| BM-08 | Phase 3 | Pending |` → `| BM-08 | Phase 3 | Complete (Plan 03-09) |`
    - `| BM-13 | Phase 3 | Pending |` → `| BM-13 | Phase 3 | Complete (Plan 03-09) |`
    - `| QA-07 | Phase 3 | Pending |` → `| QA-07 | Phase 3 | Complete (Plan 03-09) |`

    **Шаг 4.** Footer `*Last updated:* 2026-05-13 ...` → `*Last updated: 2026-05-15 after Phase 3 completion (Plan 03-09 — BM-01..03, BM-05..08, BM-13, QA-07 complete)*`.

    Не менять никакие другие строки. Не трогать v2 Requirements, Out of Scope, Coverage block (уже sync'нут в Plan 03-01).
  </action>
  <verify>
    <automated>grep -c "^- \[x\] \*\*BM-0[12356-8]\*\*" .planning/REQUIREMENTS.md</automated>
    <automated>grep -q "^- \[x\] \*\*BM-13\*\*" .planning/REQUIREMENTS.md</automated>
    <automated>grep -q "^- \[x\] \*\*QA-07\*\*" .planning/REQUIREMENTS.md</automated>
    <automated>grep -c "Phase 3 | Complete (Plan 03-09)" .planning/REQUIREMENTS.md</automated>
    <automated>bash -c "! grep -q '^- \[ \] \*\*BM-01\*\*' .planning/REQUIREMENTS.md"</automated>
    <automated>bash -c "! grep -q '^- \[ \] \*\*BM-13\*\*' .planning/REQUIREMENTS.md"</automated>
    <automated>bash -c "! grep -q '^- \[ \] \*\*QA-07\*\*' .planning/REQUIREMENTS.md"</automated>
    <automated>grep -q "BM-04.*Phase 4.*Pending" .planning/REQUIREMENTS.md</automated>
    <automated>grep -q "BM-12.*Phase 6.*Pending" .planning/REQUIREMENTS.md</automated>
    <automated>grep -q "after Phase 3 completion" .planning/REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^- \[x\] \*\*BM-(01|02|03|05|06|07|08|13)\*\*" .planning/REQUIREMENTS.md` == 8
    - `grep -q "^- \[x\] \*\*QA-07\*\*" .planning/REQUIREMENTS.md`
    - `grep -c "Phase 3 | Complete (Plan 03-09)" .planning/REQUIREMENTS.md` == 9 (8 BM + 1 QA)
    - `grep -c "^- \[ \] \*\*BM-(01|02|03|05|06|07|08|13)\*\*" .planning/REQUIREMENTS.md` == 0 (все Phase 3 bet-maker requirements закрыты)
    - **Phase 4-7 requirements не задеты:**
      - `grep -q "BM-04.*Phase 4.*Pending" .planning/REQUIREMENTS.md`
      - `grep -q "BM-09.*Phase 5.*Pending" .planning/REQUIREMENTS.md`
      - `grep -q "BM-12.*Phase 6.*Pending" .planning/REQUIREMENTS.md`
      - `grep -q "QA-01.*Phase 7.*Pending" .planning/REQUIREMENTS.md`
    - Footer updated to `*Last updated: 2026-05-15 after Phase 3 completion ...*`
  </acceptance_criteria>
  <done>9 Phase 3 requirements flipped to Complete. Phase 4-7 requirements не задеты. Footer обновлён.</done>
</task>

<task type="auto">
  <name>Task 4: Update ROADMAP.md — Phase 3 checkbox + Progress table + Plans список</name>
  <files>.planning/ROADMAP.md</files>
  <read_first>
    - .planning/ROADMAP.md секция `## Phases` (lines 10–16 — phase checkboxes) и `## Progress` (lines 180–190) и `### Phase 3` (lines 80–100)
    - .planning/phases/02-line-provider-domain/02-07-PLAN.md Task 5 (P2 mirror — какие точно правки делал в ROADMAP)
  </read_first>
  <action>
    Открыть `.planning/ROADMAP.md` и сделать следующие правки.

    **Шаг 1.** В секции `## Phases` (lines 10–16):
    - `- [ ] **Phase 3: bet-maker domain (DB)** — PostgreSQL persistence, UoW, place/list bets via HTTP`
    - заменить на:
    - `- [x] **Phase 3: bet-maker domain (DB)** — PostgreSQL persistence, UoW, place/list bets via HTTP`

    Не трогать чекбоксы Phase 1/2 (`[x]`) и Phase 4-7 (`[ ]`).

    **Шаг 2.** В секции `### Phase 3: bet-maker domain (DB)` найти строку `**Plans**: TBD` (строка ~100). Заменить на:
    ```
    **Plans:** 9/9 plans executed (Phase 3 complete 2026-05-15)

    Plans:
    - [x] 03-01-PLAN.md — REQUIREMENTS.md sync (BM-01/BM-05 remove coefficient, BM-13 added) per D-01/D-02 (Wave 0)
    - [x] 03-02-PLAN.md — Test scaffolding (testcontainers PG fixtures + 10 stubs + pyproject coverage) (Wave 0)
    - [x] 03-03-PLAN.md — Schemas + helpers (BetCreate, BetRead, BetStatus, EventState duplicate, quantize_amount, status stub) (Wave 1)
    - [x] 03-04-PLAN.md — Bet ORM + alembic env.py target_metadata + 0001_bets_initial migration (idempotent ENUM) (Wave 1)
    - [x] 03-05-PLAN.md — DB Infrastructure (engine + pings tenacity wait_for_postgres + ping_postgres) (Wave 3)
    - [x] 03-06-PLAN.md — Facades & Repositories (UoW + BetRepository + EventLookup Protocol + Stub + deps Annotated aliases) (Wave 3)
    - [x] 03-07-PLAN.md — Interactor place_bet (3-branch EventNotBettable) + selectors list_bets/get_bet (Wave 4)
    - [x] 03-08-PLAN.md — HTTP routes (POST /bet 201/422, GET /bets, GET /bet/{id}) + lifespan extension + /health PG ping + 14+ integration tests (Wave 5)
    - [x] 03-09-PLAN.md — Phase-gate (coverage ≥80%, manual alembic rehearsal, docs sync) (Wave 6)
    ```

    **Шаг 3.** В секции `## Progress` (table, lines 180–190) обновить Phase 3 строку:
    - `| 3. bet-maker domain (DB) | 0/? | Not started | - |`
    - заменить на:
    - `| 3. bet-maker domain (DB) | 9/9 | Complete | 2026-05-15 |`

    Не трогать строки Phase 1, 2 (уже Complete), Phase 4-7 (Not started).

    **Шаг 4.** Footer (строки 191–193) обновить если присутствует:
    - Если ROADMAP содержит `*Phase 3 plans created: ...*` — НЕ добавлять (это per-phase note, P2 mirror добавил такую строку для P2 в момент Plan 02-01 — если такого ещё нет в P3, добавить новую строку: `*Phase 3 plans created: 2026-05-15 (9 plans across 6 waves)*`).
    - Если ROADMAP содержит P1 footer строку с Phase 1 progress — оставить.
  </action>
  <verify>
    <automated>grep -q "^- \[x\] \*\*Phase 3:" .planning/ROADMAP.md</automated>
    <automated>grep -q "9/9 plans executed" .planning/ROADMAP.md</automated>
    <automated>grep -q "\[x\] 03-01-PLAN.md" .planning/ROADMAP.md</automated>
    <automated>grep -q "\[x\] 03-09-PLAN.md" .planning/ROADMAP.md</automated>
    <automated>grep -q "| 3. bet-maker domain (DB) | 8/8 | Complete" .planning/ROADMAP.md</automated>
    <automated>bash -c "! grep -q '| 3. bet-maker domain (DB) | 0/? | Not started' .planning/ROADMAP.md"</automated>
    <automated>grep -q "^- \[ \] \*\*Phase 4:" .planning/ROADMAP.md</automated>
    <automated>grep -c "^- \[x\] 03-0[1-9]-PLAN.md" .planning/ROADMAP.md</automated>
  </verify>
  <acceptance_criteria>
    - Phase 3 checkbox в `## Phases` flipped to `[x]`
    - `**Plans:** TBD` → `**Plans:** 9/9 plans executed (Phase 3 complete 2026-05-15)`
    - 8 plan строк `- [x] 03-NN-PLAN.md — ...` присутствуют в Phase 3 plans список (NN ∈ 01..08)
    - Progress table Phase 3 row: `8/8 | Complete | 2026-05-15`
    - Phase 4-7 строки НЕ задеты (всё ещё `[ ]` / `0/? | Not started`)
    - `grep -c "^- \[x\] 03-0[1-9]-PLAN.md" .planning/ROADMAP.md` == 9
  </acceptance_criteria>
  <done>ROADMAP Phase 3 closed: checkbox, plans список, progress table. Phase 4-7 не задеты.</done>
</task>

<task type="auto">
  <name>Task 5: Update STATE.md frontmatter + Current Position + Decisions + Last Session</name>
  <files>.planning/STATE.md</files>
  <read_first>
    - .planning/STATE.md (полностью — для precise frontmatter values и patterns предыдущих phase transitions)
    - .planning/STATE.md §«Decisions» (как P2 Plan 02-07 entry структурирован — пример format)
    - .planning/STATE.md §«Session Continuity» (Last Session block — mirror P2 пример)
  </read_first>
  <action>
    Открыть `.planning/STATE.md`. Выполнить четыре правки.

    **Шаг 1. Frontmatter (lines 1–13)** — обновить три поля:
    - `progress.total_phases: 7` (без изменений)
    - `progress.completed_phases: 2` → `progress.completed_phases: 3`
    - `progress.total_plans: 14` → `progress.total_plans: 23`
    - `progress.completed_plans: 14` → `progress.completed_plans: 23`
    - `progress.percent: 100` → `progress.percent: 100` (по completed_phases / planned_phases — пока 3/3 поскольку Phase 4-7 ещё не decomposed)
    - `last_updated: "2026-05-15T12:54:29.052Z"` → `last_updated: "2026-05-15T18:00:00.000Z"` (или текущая ISO timestamp).

    **Шаг 2. `## Current Position` (lines 25–40)** — переписать на:
    ```
    Phase: 03 (bet-maker-domain-db) — COMPLETE (9/9 plans). Ready for Phase 4 (bet-maker HTTP integration with line-provider).
    Plan: 8 of 8 (Plans 01–09 complete — REQUIREMENTS sync + Wave 0 test scaffolding + Schemas/helpers + Bet model+migration + Infra+facades+UoW+repo + Interactor+selectors + HTTP routes+lifespan+health + Phase-gate)

    - **Milestone:** v1
    - **Phase:** 3 (COMPLETE)
    - **Plan:** 03-08 complete (Wave 6 — phase-gate; coverage src/bet_maker ≥80%; manual alembic compose rehearsal verified; REQUIREMENTS.md+ROADMAP.md+STATE.md+03-VALIDATION.md synced)
    - **Status:** Phase 3 complete; awaiting Phase 4 kickoff
    - **Progress:** [██████████] 100% of planned phases (3/3 planned phases complete; 4 phases yet to be planned)

    ```
    [███░░░░] 3/7 phases (43%)
    ```
    ```

    Не менять `## Project Reference` и `## Performance Metrics` (Performance Metrics добавляются по факту — см. шаг 3).

    **Шаг 3. `## Performance Metrics`** — добавить 8 строк после последней P2 metric:
    ```
    | Plan 03-01 duration | ~3 min (4 tasks, 1 file — REQUIREMENTS.md sync) |
    | Plan 03-02 duration | TBD (13 files — Wave 0 test scaffolding + pyproject + test_schemas stub) |
    | Plan 03-03 duration | TBD (7 files — schemas + helpers + 18+ unit tests) |
    | Plan 03-04 duration | TBD (6 files — Bet ORM + alembic env.py + migration + 12+4 tests) |
    | Plan 03-05 duration | TBD (5 files — DB infrastructure: engine + pings tenacity) |
    | Plan 03-06 duration | TBD (10 files — facades + repository + event_lookup + deps + 3 test replacements) |
    | Plan 03-07 duration | TBD (7 files — interactor + selectors + 2 test replacements) |
    | Plan 03-08 duration | TBD (7 files — routes + lifespan + health + 20+ tests) |
    | Plan 03-09 duration | TBD (4 files — phase-gate + docs sync) |
    ```

    Реальные `TBD` подставляются executor'ом по фактическим длительностям из SUMMARYs планов 03-02..03-07; если SUMMARY ещё содержит TBD — оставить TBD. STATE.md обновляется по best-effort.

    **Шаг 4. `## Accumulated Context > Decisions`** — добавить НОВЫЙ entry после последнего P2 entry:
    ```
    - [Phase 03]: 2026-05-15 (Plan 03-09): Phase 3 closed — bet_maker domain (DB) — 9 plans across 6 waves, 9 requirements complete (BM-01/BM-02/BM-03/BM-05/BM-06/BM-07/BM-08/BM-13/QA-07). Stack: SQLAlchemy 2.0.49 async + asyncpg 0.31.0 + Alembic 1.18.4 + tenacity 9.1.4 + testcontainers 4.x. Архитектура: paste-ready tree из ARCHITECTURE.md §«bet_maker/» — models/bet.py (Bet ORM, PG-ENUM bet_status, Numeric(12,2)), schemas/bets.py (BetCreate Annotated Decimal AfterValidator + BetRead extra='forbid' + from_attributes=True), schemas/events.py (EventState duplicated D-12), helpers/money.py (quantize_amount ROUND_HALF_UP), helpers/status.py (P5 stub), repositories/bets.py (BetRepository.add no-commit), facades/uow.py (AsyncUnitOfWork over async_sessionmaker.begin), facades/event_lookup.py (EventLookup Protocol + EventSnapshot frozen + StubEventLookup), facades/deps.py (6 providers + 6 Annotated aliases), infrastructure/db/engine.py (D-16 params + expire_on_commit=False), infrastructure/db/pings.py (wait_for_postgres tenacity + ping_postgres bool), interactors/place_bet.py (3-branch EventNotBettable BEFORE UoW open + model_validate inside session for A1), selectors/list_bets.py (ORDER BY created_at DESC) + get_bet.py (scalar_one_or_none → BetRead | None), entrypoints/api/bets.py (POST /bet 201/422 + GET /bets + GET /bet/{id} 200/404 + EventNotBettable→422 with detail), entrypoints/api/health.py REPLACED (per-request SELECT 1 → 200 with checks.postgres="ok" or 503 degraded), entrypoints/lifespan.py EXTENDED (engine+sessionmaker+wait_for_postgres tenacity 10 attempts+StubEventLookup+app.state pins+finally engine.dispose). Migration: alembic/versions/20260515_0001_bets_initial.py (ENUM.create checkfirst=True + create_type=False — verified idempotent через 3 successive command.upgrade калла в test_alembic.py + manual docker compose rehearsal Task 2). Anti-Patterns enforced: A1 (refresh after flush + model_validate inside session), A2 (per-request UoW), A4/A5 (Decimal Numeric(12,2)), Anti-Pattern 1 (no commit in repositories — grep-verified). Critical Risk Axes 1, 3, 5, 6, 7, 8, 9 covered with explicit tests. Pydantic v2 Decimal serialization "10.00" (D-19) verified end-to-end через POST→DB→GET round-trip in test_bet_routes::TestDecimalRoundtrip. P1 health test assertion updated (Pitfall 4) atomically with health.py REPLACE (Plan 03-08 Task 4). Final phase-gate (Plan 03-09): `uv run pytest -q` → {NN passed; supply exact count from Task 1 output}; `uv run pytest --cov=src/bet_maker --cov-fail-under=80` → coverage {XX%}; mypy strict + ruff check + ruff format clean across src/+tests/+alembic/. Phase 3 complete.
    ```

    `{NN passed}` и `{XX%}` — substitute из реального output Task 1.

    **Шаг 5. `## Session Continuity > Last Session`** — обновить:
    ```
    - **Started:** 2026-05-15 (Plan 03-09 phase-gate session)
    - **Ended:** 2026-05-15 (Phase 3 closed)
    - **Activity:** Executed 03-09-PLAN.md (Plan 03-09 Wave 6 — phase-gate). Quality gate verified: `uv run pytest -q` → {NN passed}; coverage src/bet_maker {XX%} (gate ≥80% passed); src/line_provider coverage {YY%} (P2 baseline ≥85% preserved); mypy strict + ruff clean. Manual docker compose alembic rehearsal: {approved/skipped — substitute from Task 2 outcome}. REQUIREMENTS.md flipped 9 statuses (BM-01..03, BM-05..08, BM-13, QA-07) to Complete; ROADMAP.md Phase 3 checkbox closed + 9/9 plans listed + Progress table updated; 03-VALIDATION.md per-task map flipped to ✅ green. One atomic docs commit.
    - **Outcome:** Phase 3 complete; 23 plans across phases 1-3 executed (14 + 9). Coverage src/bet_maker {XX%}, src/line_provider {YY%}. Next: Phase 4 planning (`/gsd-plan-phase 4` — `bet-maker HTTP integration with line-provider` — BM-04 single requirement).
    ```

    **Шаг 6. `## Session Continuity > Next Session`** — обновить:
    ```
    - **Recommended command:** `/gsd-plan-phase 4` (Phase 4 — bet-maker HTTP integration with line-provider). Plans not yet decomposed; planner produces atomic plans covering BM-04 (single requirement: GET /events proxy via httpx with tenacity retry + TTL cache).
    - **Goal:** bet-maker exposes `GET /events` proxying line-provider; tenacity retries transient httpx failures; singleton AsyncClient in lifespan; integration test drives both apps via httpx.AsyncClient(transport=ASGITransport) without docker. Per ROADMAP Phase 4 success criteria. POST /bet validation (BM-06) now uses real line_provider_client to resolve event (replaces StubEventLookup with HttpEventLookup — same Protocol).
    - **Note:** Phase 4 depends on Phase 2 (line_provider GET /event/{id} + GET /events) AND Phase 3 (uses same lifespan + Depends graph). Both prerequisites complete.
    ```

    Не трогать `### Open Questions for Next Phase` (Phase 5/6/7 specific — оставить).
  </action>
  <verify>
    <automated>grep -q 'completed_phases: 3' .planning/STATE.md</automated>
    <automated>grep -q 'total_plans: 23' .planning/STATE.md</automated>
    <automated>grep -q 'completed_plans: 23' .planning/STATE.md</automated>
    <automated>grep -q "Phase: 03 (bet-maker-domain-db) — COMPLETE" .planning/STATE.md</automated>
    <automated>grep -q "Phase 3 closed" .planning/STATE.md</automated>
    <automated>grep -q "BM-01/BM-02/BM-03/BM-05/BM-06/BM-07/BM-08/BM-13/QA-07" .planning/STATE.md</automated>
    <automated>grep -q "Plan 03-09 duration" .planning/STATE.md</automated>
    <automated>grep -q "/gsd-plan-phase 4" .planning/STATE.md</automated>
  </verify>
  <acceptance_criteria>
    - frontmatter: completed_phases=3, total_plans=22, completed_plans=22
    - Current Position: "Phase: 03 ... COMPLETE (9/9 plans)"
    - Decisions: новый entry начинается с `- [Phase 03]: 2026-05-15 (Plan 03-09)`
    - Decisions entry содержит все 9 requirement IDs (`BM-01`, `BM-02`, `BM-03`, `BM-05`, `BM-06`, `BM-07`, `BM-08`, `BM-13`, `QA-07`)
    - Last Session содержит `Phase 3 closed` и pytest/coverage numbers (даже если TBD)
    - Next Session recommends `/gsd-plan-phase 4`
    - Performance Metrics строки 03-01 ... 03-08 присутствуют (8 строк)
  </acceptance_criteria>
  <done>STATE.md фазы advance, frontmatter updated, Decisions entry + Last Session + Next Session — все sync'нуты.</done>
</task>

<task type="auto">
  <name>Task 6: Update 03-VALIDATION.md per-task map — flip pending → ✅ green for all rows</name>
  <files>.planning/phases/03-bet-maker-domain-db/03-VALIDATION.md</files>
  <read_first>
    - .planning/phases/03-bet-maker-domain-db/03-VALIDATION.md §«Per-Task Verification Map» (после Plan 03-02 финализации — 25+ rows)
  </read_first>
  <action>
    Открыть `.planning/phases/03-bet-maker-domain-db/03-VALIDATION.md`.

    В таблице `## Per-Task Verification Map` найти все строки с `pending` в последней колонке (Status). Заменить на `✅ green`:
    - sed -like: для каждой строки матчующей pattern `^| 03-XX-XX |...| pending |$`, заменить trailing ` pending |` на ` ✅ green |`.

    Точнее: каждая из 25+ task rows должна иметь финальный column `✅ green` вместо `pending`.

    **Исключения** (могут остаться `pending` или быть отдельно обработаны):
    - Если Task 2 этого плана `skipped` (manual rehearsal не выполнен), row для `03-08-02` оставить как `⚠ skipped` (per resume signal).
    - Если любой Task в Task 1 этого плана НЕ passed (coverage < 80% и т.п.) — соответствующая row остаётся `pending` и SUMMARY raises blocker.

    Также:
    - Добавить в самом конце таблицы footer-строку (после последней row): `> All Per-Task statuses updated 2026-05-15 by Plan 03-09 phase-gate.`

    Не менять frontmatter (уже `status: approved` из Plan 03-02). Не трогать § Wave 0 Requirements (все `[x]`) и § Validation Sign-Off (все `[x]`).
  </action>
  <verify>
    <automated>grep -c "✅ green" .planning/phases/03-bet-maker-domain-db/03-VALIDATION.md</automated>
    <automated>grep -c "| pending |$" .planning/phases/03-bet-maker-domain-db/03-VALIDATION.md</automated>
    <automated>grep -q "All Per-Task statuses updated 2026-05-15" .planning/phases/03-bet-maker-domain-db/03-VALIDATION.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "✅ green" .planning/phases/03-bet-maker-domain-db/03-VALIDATION.md` >= 24 (минимум 24 task rows — Task 2 row может остаться ⚠ skipped, остальные ≥24)
    - `grep -c "| pending |$" .planning/phases/03-bet-maker-domain-db/03-VALIDATION.md` <= 1 (только row 03-09-02 может остаться `pending` или `⚠ skipped` если manual rehearsal skipped; ideally 0)
    - Footer строка `All Per-Task statuses updated 2026-05-15` present
  </acceptance_criteria>
  <done>03-VALIDATION.md per-task map flipped: 24+ ✅ green, ≤1 ⚠ skipped / pending (только manual rehearsal if не выполнен).</done>
</task>

<task type="auto">
  <name>Task 7: Create one atomic docs commit closing Phase 3</name>
  <files>(commit operation — no file changes here)</files>
  <read_first>
    - .planning/STATE.md (актуальная версия после Task 5)
    - .planning/REQUIREMENTS.md (актуальная версия после Task 3)
    - .planning/ROADMAP.md (актуальная версия после Task 4)
    - .planning/phases/03-bet-maker-domain-db/03-VALIDATION.md (актуальная версия после Task 6)
  </read_first>
  <action>
    Создать единственный atomic docs commit, закрывающий фазу.

    1. `git status -s` — подтвердить что только 4 docs файла изменены (плюс возможно 03-09-SUMMARY.md если уже написан, но он создаётся в `<output>` блоке после этого таска).

    2. `git diff --stat` — quick review (не должно быть code changes, только planning docs).

    3. Stage 4 docs файла:
       ```
       git add .planning/REQUIREMENTS.md .planning/ROADMAP.md .planning/STATE.md .planning/phases/03-bet-maker-domain-db/03-VALIDATION.md
       ```

       Внимание: НЕ использовать `git add .` или `git add -A` — каждый файл явно. Это защита от случайного включения .env, credentials, etc.

    4. Commit с heredoc message:
       ```
       git commit -m "$(cat <<'EOF'
       docs(phase-3): close Phase 3 — 9 requirements complete

       Phase 3 (bet-maker domain DB) complete across 8 plans / 6 waves.
       Requirements complete: BM-01 (Bet ORM), BM-02 (UoW), BM-03 (layers),
       BM-05 (POST /bet), BM-06 (event validation), BM-07 (GET /bets),
       BM-08 (/health PG ping), BM-13 (GET /bet/{id}), QA-07 (testcontainers PG).

       Stack delivered: SQLAlchemy 2.0.49 async + asyncpg 0.31.0 + Alembic
       (idempotent ENUM via checkfirst=True) + tenacity startup retry + Pydantic v2
       Decimal(12,2) end-to-end roundtrip + StubEventLookup Protocol (P4-ready).

       REQUIREMENTS.md: 9 statuses flipped to Complete (Plan 03-09).
       ROADMAP.md: Phase 3 checkbox + Progress table + 8 plan list closed.
       STATE.md: phase advance 2 -> 3; 22 plans complete; next is Phase 4.
       03-VALIDATION.md: per-task map flipped to ✅ green.

       Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
       EOF
       )"
       ```

    5. `git status` — confirm clean working tree (или только 03-09-SUMMARY.md untracked, который будет committed в финальный SUMMARY commit вне этого plan'а).

    Если git commit fails — НЕ retry с --amend (правило). Fix root cause и create new commit.
  </action>
  <verify>
    <automated>git log -1 --format=%s 2>&1</automated>
    <automated>git log -1 --format=%B 2>&1 | grep -q "docs(phase-3): close Phase 3"</automated>
    <automated>git log -1 --stat 2>&1 | grep -E "REQUIREMENTS\.md|ROADMAP\.md|STATE\.md|03-VALIDATION\.md"</automated>
    <automated>git status -s 2>&1</automated>
  </verify>
  <acceptance_criteria>
    - Один atomic commit создан с subject `docs(phase-3): close Phase 3 — 9 requirements complete`
    - Commit body содержит все 9 requirement IDs (BM-01, BM-02, BM-03, BM-05, BM-06, BM-07, BM-08, BM-13, QA-07)
    - Commit включает все 4 файла (REQUIREMENTS.md, ROADMAP.md, STATE.md, 03-VALIDATION.md)
    - Commit message содержит Co-Authored-By trailer
    - `git status -s` показывает чистое working tree ИЛИ только 03-09-SUMMARY.md untracked (это OK; SUMMARY committed otherwise по standard execute-plan workflow)
  </acceptance_criteria>
  <done>One atomic docs commit закрывает Phase 3. Working tree clean (modulo SUMMARY pending).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GSD planner / executor → planning documents | docs-only changes — но эти docs управляют downstream phases (4-7); ошибка blocking |
| Operator → docker compose alembic rehearsal | live system gate — Manual-Only Verification |
| Git commit → repository history | atomic commit messaging для audit trail |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-3-doc | Tampering | Status flip pre-mature (e.g., BM-01 → Complete до coverage gate passed) | mitigate | Task 3-6 strictly зависят от Task 1 (coverage + tests gate) — если Task 1 fails, Task 3-6 not executed; SUMMARY raises blocker. |
| T-03-3-prod-rehearsal | Repudiation | Operator skip'ает manual rehearsal без обоснования → SC-5 production-rehearsal gap | mitigate | Task 2 explicit `skip` requires written rationale in SUMMARY; alternative proof via testcontainers (test_alembic.py × 3 successful runs) accepted. |
| T-03-6 | Operational | docker compose down -v destroys real volume на dev machine | accept | Task 2 step 1 explicit `docker compose down -v` documented as DESTRUCTIVE; operator's choice. Test-task scope — no production data. |
| T-03-3-commit | Tampering | git add . includes secrets (.env, credentials) | mitigate | Task 7 explicitly enumerates 4 file paths; NEVER `git add .` or `-A`. |
| T-03-2-DoS-rerun | Denial of Service | repeating manual rehearsal на slow disk → timeouts | accept | One-shot check; if `docker compose up -d postgres` healthcheck fails > 30s, operator's environment problem (not Phase 3 gate). |
</threat_model>

<verification>
После завершения всех 7 задач:
- Quality gate: `uv run pytest -q` exit 0; coverage src/bet_maker ≥80%; mypy strict + ruff чисто
- Manual gate: docker compose alembic rehearsal — approved или skipped с rationale
- REQUIREMENTS.md: 9 строк `Complete (Plan 03-09)` в Traceability table; 8 checkboxes BM-XX + 1 QA-07 → `[x]`
- ROADMAP.md: Phase 3 checkbox `[x]`; Progress table `9/9 Complete 2026-05-15`; 8 plan-list rows `[x]`
- STATE.md: frontmatter completed_phases=3, total_plans=22; new Decisions entry; Last/Next Session updated
- 03-VALIDATION.md: per-task map 24+ ✅ green (≤1 row pending только если Task 2 skipped)
- Один atomic docs commit `docs(phase-3): close Phase 3 — 9 requirements complete` создан
</verification>

<success_criteria>
- Quality gate passed: pytest 200+ passed, coverage src/bet_maker ≥80%, src/line_provider ≥85%, mypy strict, ruff clean
- Manual alembic rehearsal approved (или skipped с явным rationale)
- 9 REQUIREMENTS.md статусов flipped to Complete (Plan 03-09)
- ROADMAP.md Phase 3 fully closed: checkbox + plans список + progress table
- STATE.md advanced: completed_phases=3, new Decisions entry, Next Session recommends Phase 4
- 03-VALIDATION.md per-task map flipped to ✅ green
- One atomic docs commit с правильным subject + body + Co-Authored-By
- Phase 4-7 docs не задеты
</success_criteria>

<output>
After completion, create `.planning/phases/03-bet-maker-domain-db/03-09-SUMMARY.md` following `@$HOME/.claude/get-shit-done/templates/summary.md`.

SUMMARY должен включать:
- Полный output (последняя строка) каждой из 6 quality-gate команд Task 1
- Точный coverage число (e.g., `src/bet_maker: 83.4%`) и точный pytest count (e.g., `247 passed`)
- Task 2 outcome: approved / skipped / blocker (с copy-pasted output если approved)
- Commit hash от Task 7
- Перечисление всех 9 закрытых requirements
- "Phase 3 closed" заявление в первой строке Summary
</output>
