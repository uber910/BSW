---
phase: 02-line-provider-domain
plan: 03
type: execute
wave: 2
depends_on: [01, 02]
files_modified:
  - src/line_provider/helpers/state_machine.py
  - tests/line_provider/test_state_machine.py
autonomous: true
requirements: [LP-05, LP-08]
tags: [line-provider, state-machine, helper, pure-function]

must_haves:
  truths:
    - "is_transition_allowed возвращает True для (NEW, FINISHED_WIN), (NEW, FINISHED_LOSE) и любых (X, X) (no-op)"
    - "is_transition_allowed возвращает False для FINISHED_* → NEW, FINISHED_WIN ↔ FINISHED_LOSE"
    - "ALLOWED_TRANSITIONS — frozenset (immutable)"
    - "TransitionForbiddenError содержит current и new state и человекочитаемый message"
    - "Все 9 пар (3×3 декартово произведение) покрыты unit-тестом"
  artifacts:
    - path: "src/line_provider/helpers/state_machine.py"
      provides: "is_transition_allowed, TransitionForbiddenError, ALLOWED_TRANSITIONS"
      exports: ["is_transition_allowed", "TransitionForbiddenError", "ALLOWED_TRANSITIONS"]
    - path: "tests/line_provider/test_state_machine.py"
      provides: "Параметризованная таблица 9 переходов + TransitionForbiddenError тест"
  key_links:
    - from: "src/line_provider/helpers/state_machine.py"
      to: "src/line_provider/schemas/events.py"
      via: "import EventState (only)"
      pattern: "from line_provider.schemas.events import EventState"
---

<objective>
Чистая state-machine для line-provider events. Domain-логика «можно ли перейти из X в Y» вынесена в `helpers/state_machine.py` как pure function — без классов, без DI, без I/O. Используется Plan 02-05 (`interactors/set_event_state.py`) для решения «продолжить мутацию или поднять 422».

Purpose: По CONTEXT.md D-08 — state-machine это критический инвариант (LP-08 «запрет обратных переходов FINISHED → NEW»). Pure function отделена от interactor'а специально, чтобы её можно было протестировать таблицей переходов без поднятия store/event_bus. State-machine знает ТОЛЬКО про EventState — никаких dependencies на store, bus, time.

Output: 1 production helper + 1 unit test (параметризованный 9-case + TransitionForbiddenError shape).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-line-provider-domain/02-CONTEXT.md
@.planning/phases/02-line-provider-domain/02-RESEARCH.md
@.planning/phases/02-line-provider-domain/02-PATTERNS.md
@.planning/phases/02-line-provider-domain/02-02-SUMMARY.md
@src/config/time.py

<interfaces>
<!-- Exported contract — consumed by 02-05 interactors and 02-07 routes. -->
From src/line_provider/helpers/state_machine.py (this plan):
```python
ALLOWED_TRANSITIONS: frozenset[tuple[EventState, EventState]]

class TransitionForbiddenError(Exception):
    current: EventState
    new: EventState

def is_transition_allowed(current: EventState, new: EventState) -> bool: ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create helpers/state_machine.py (pure function + ALLOWED_TRANSITIONS + TransitionForbiddenError)</name>
  <files>src/line_provider/helpers/state_machine.py</files>
  <read_first>
    - src/config/time.py (стиль pure-function one-liner модуля — Excerpt F в PATTERNS.md)
    - src/line_provider/schemas/events.py (EventState — создан в Plan 02-02 Task 1)
    - .planning/phases/02-line-provider-domain/02-RESEARCH.md §«Pattern 2» (строки 336–367) — обоснование frozenset
    - .planning/phases/02-line-provider-domain/02-CONTEXT.md D-08 + D-09 (no-op state semantics)
    - .planning/phases/02-line-provider-domain/02-PATTERNS.md §«Excerpt F» + §«§3.E Pure-function helpers»
  </read_first>
  <behavior>
    is_transition_allowed:
    - (NEW, NEW) → True (no-op, D-09)
    - (NEW, FINISHED_WIN) → True
    - (NEW, FINISHED_LOSE) → True
    - (FINISHED_WIN, FINISHED_WIN) → True (no-op)
    - (FINISHED_LOSE, FINISHED_LOSE) → True (no-op)
    - (FINISHED_WIN, NEW) → False
    - (FINISHED_LOSE, NEW) → False
    - (FINISHED_WIN, FINISHED_LOSE) → False
    - (FINISHED_LOSE, FINISHED_WIN) → False

    TransitionForbiddenError:
    - Подкласс Exception
    - Принимает (current, new)
    - Хранит атрибуты `.current` и `.new` типов EventState
    - `str(err)` содержит "state transition NEW->FINISHED_WIN not allowed" формат (D-08 wording)
  </behavior>
  <action>
    Создать `src/line_provider/helpers/state_machine.py`:
    ```python
    from __future__ import annotations

    from line_provider.schemas.events import EventState


    ALLOWED_TRANSITIONS: frozenset[tuple[EventState, EventState]] = frozenset(
        {
            (EventState.NEW, EventState.FINISHED_WIN),
            (EventState.NEW, EventState.FINISHED_LOSE),
        }
    )


    class TransitionForbiddenError(Exception):
        def __init__(self, current: EventState, new: EventState) -> None:
            self.current = current
            self.new = new
            super().__init__(
                f"state transition {current.value}->{new.value} not allowed"
            )


    def is_transition_allowed(current: EventState, new: EventState) -> bool:
        if current == new:
            return True
        return (current, new) in ALLOWED_TRANSITIONS
    ```

    Замечания:
    - `ALLOWED_TRANSITIONS` — публичный module-level constant (uppercase): tesseля могут импортировать его для assert-based проверок («ровно 2 forward transitions»).
    - Никаких docstrings и комментариев в production-коде (project hard rule).
    - Никакого DI, никаких параметров `now`, `store`, `bus` — это чистая функция.
    - НЕ использовать `match`/`case` — frozenset lookup точнее и быстрее, и легче тестируется.
  </action>
  <verify>
    <automated>uv run python -c "from line_provider.helpers.state_machine import is_transition_allowed, ALLOWED_TRANSITIONS, TransitionForbiddenError; from line_provider.schemas.events import EventState; assert len(ALLOWED_TRANSITIONS) == 2; assert is_transition_allowed(EventState.NEW, EventState.FINISHED_WIN); assert not is_transition_allowed(EventState.FINISHED_WIN, EventState.NEW); err = TransitionForbiddenError(EventState.FINISHED_WIN, EventState.NEW); assert 'FINISHED_WIN->NEW' in str(err); assert err.current == EventState.FINISHED_WIN" &amp;&amp; uv run mypy --strict src/line_provider/helpers/state_machine.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "ALLOWED_TRANSITIONS: frozenset" src/line_provider/helpers/state_machine.py`
    - `grep -q "class TransitionForbiddenError" src/line_provider/helpers/state_machine.py`
    - `grep -q "def is_transition_allowed" src/line_provider/helpers/state_machine.py`
    - `grep -q "from line_provider.schemas.events import EventState" src/line_provider/helpers/state_machine.py`
    - `grep -c "EventState.NEW, EventState.FINISHED" src/line_provider/helpers/state_machine.py` == 2 (ровно 2 forward transitions в frozenset)
    - `grep -c "^# " src/line_provider/helpers/state_machine.py` == 0 (никаких комментариев)
    - `uv run mypy --strict src/line_provider/helpers/state_machine.py` exit 0
    - `uv run ruff check src/line_provider/helpers/state_machine.py` → All checks passed
  </acceptance_criteria>
  <done>helpers/state_machine.py содержит pure function + frozenset + TransitionForbiddenError exception.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Unit test — tests/line_provider/test_state_machine.py (parametrized 9-case table)</name>
  <files>tests/line_provider/test_state_machine.py</files>
  <read_first>
    - .planning/phases/02-line-provider-domain/02-RESEARCH.md §«Code Examples → state-machine test» (строки 874–898)
    - .planning/phases/02-line-provider-domain/02-PATTERNS.md §«Excerpt H» (REQ-ID docstring convention)
    - src/line_provider/helpers/state_machine.py (созданный в Task 1)
  </read_first>
  <behavior>
    Параметризованный тест 9 переходов (3×3) — каждый кейс проверяет точное значение True/False.
    Отдельный тест на TransitionForbiddenError:
    - конструируется с двумя EventState
    - атрибуты .current, .new выставлены
    - str(err) содержит обе values + слово "not allowed"
    Отдельный тест на ALLOWED_TRANSITIONS — frozenset (immutable), size == 2.
  </behavior>
  <action>
    Создать `tests/line_provider/test_state_machine.py`:
    ```python
    """Unit tests for line_provider.helpers.state_machine.

    LP-08: state-machine forbids reverse transitions FINISHED_* -> NEW and cross
    FINISHED transitions; allowed: NEW -> FINISHED_WIN | FINISHED_LOSE plus
    no-op (current == new).
    D-08: 422 wording carries "state transition X->Y not allowed".
    D-09: no-op state is allowed (publish skipped at interactor level).
    """

    from __future__ import annotations

    import pytest

    from line_provider.helpers.state_machine import (
        ALLOWED_TRANSITIONS,
        TransitionForbiddenError,
        is_transition_allowed,
    )
    from line_provider.schemas.events import EventState


    @pytest.mark.parametrize(
        ("current", "new", "allowed"),
        [
            (EventState.NEW, EventState.NEW, True),
            (EventState.NEW, EventState.FINISHED_WIN, True),
            (EventState.NEW, EventState.FINISHED_LOSE, True),
            (EventState.FINISHED_WIN, EventState.FINISHED_WIN, True),
            (EventState.FINISHED_WIN, EventState.NEW, False),
            (EventState.FINISHED_WIN, EventState.FINISHED_LOSE, False),
            (EventState.FINISHED_LOSE, EventState.FINISHED_LOSE, True),
            (EventState.FINISHED_LOSE, EventState.NEW, False),
            (EventState.FINISHED_LOSE, EventState.FINISHED_WIN, False),
        ],
    )
    def test_is_transition_allowed_table(
        current: EventState, new: EventState, allowed: bool
    ) -> None:
        """LP-08: state-machine 3x3 truth table."""
        assert is_transition_allowed(current, new) is allowed


    def test_allowed_transitions_is_frozenset_with_two_entries() -> None:
        """LP-08: ALLOWED_TRANSITIONS is immutable and contains exactly two forward transitions."""
        assert isinstance(ALLOWED_TRANSITIONS, frozenset)
        assert len(ALLOWED_TRANSITIONS) == 2
        assert (EventState.NEW, EventState.FINISHED_WIN) in ALLOWED_TRANSITIONS
        assert (EventState.NEW, EventState.FINISHED_LOSE) in ALLOWED_TRANSITIONS


    def test_transition_forbidden_error_carries_states_and_message() -> None:
        """D-08: TransitionForbiddenError preserves current/new + human-readable message."""
        err = TransitionForbiddenError(EventState.FINISHED_WIN, EventState.NEW)
        assert err.current == EventState.FINISHED_WIN
        assert err.new == EventState.NEW
        message = str(err)
        assert "FINISHED_WIN" in message
        assert "NEW" in message
        assert "not allowed" in message


    def test_transition_forbidden_error_is_exception_subclass() -> None:
        """TransitionForbiddenError must be a plain Exception subclass (raisable, catchable)."""
        with pytest.raises(TransitionForbiddenError):
            raise TransitionForbiddenError(EventState.FINISHED_WIN, EventState.NEW)
    ```
  </action>
  <verify>
    <automated>uv run pytest tests/line_provider/test_state_machine.py -q -x &amp;&amp; uv run mypy --strict tests/line_provider/test_state_machine.py &amp;&amp; uv run ruff check tests/line_provider/test_state_machine.py</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/line_provider/test_state_machine.py -q` → 12+ passed (9 parametrized + 3 standalone)
    - `grep -c "pytest.mark.parametrize" tests/line_provider/test_state_machine.py` == 1
    - `grep -c "(EventState.NEW, EventState." tests/line_provider/test_state_machine.py` >= 3 (NEW→NEW, NEW→WIN, NEW→LOSE)
    - `grep -c "(EventState.FINISHED" tests/line_provider/test_state_machine.py` >= 6 (FIN_WIN x 3 + FIN_LOSE x 3)
    - `grep -q "LP-08" tests/line_provider/test_state_machine.py` (REQ-ID traceability)
    - `uv run mypy --strict tests/line_provider/test_state_machine.py` exit 0
  </acceptance_criteria>
  <done>9-case truth table + ALLOWED_TRANSITIONS + TransitionForbiddenError всё покрыто; mypy strict.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| interactor → state-machine helper | Внутренний доменный вызов; нет untrusted input — current/new приходят уже валидированными Pydantic |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | ALLOWED_TRANSITIONS изменён в runtime | mitigate | `frozenset` — immutable; module-level constant. Атаки на module attribute через `setattr` — недоступны без code injection (вне scope). |
| T-03-02 | Repudiation | 422 message не отражает реальный переход | mitigate | TransitionForbiddenError форматирует `current.value->new.value` строго; unit-тест ассертит presence обоих значений в message. |
| T-03-03 | Information Disclosure | 422 leak detail внутреннего state | accept | Message содержит только enum names (NEW/FINISHED_WIN/FINISHED_LOSE) — это публичный домен contract, не PII. |
| T-03-04 | Denial of Service | Бесконечный цикл валидации | accept | Pure function, без I/O, без рекурсии — O(1) lookup в frozenset. |
</threat_model>

<verification>
- `uv run pytest tests/line_provider/test_state_machine.py -q` → 12 passed
- `uv run mypy --strict src/line_provider/helpers/state_machine.py tests/line_provider/test_state_machine.py` → 0 errors
- `uv run ruff check src/line_provider/helpers tests/line_provider/test_state_machine.py` → All checks passed
</verification>

<success_criteria>
- state_machine.py — pure function + frozenset + TransitionForbiddenError; никаких dependencies кроме EventState
- test_state_machine.py покрывает все 9 переходов + immutability ALLOWED_TRANSITIONS + Exception attributes
- mypy strict + ruff чисто
</success_criteria>

<output>
After completion, create `.planning/phases/02-line-provider-domain/02-03-SUMMARY.md`
</output>
