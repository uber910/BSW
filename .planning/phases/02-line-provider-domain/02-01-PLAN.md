---
phase: 02-line-provider-domain
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - pyproject.toml
  - uv.lock
  - .planning/REQUIREMENTS.md
  - tests/line_provider/conftest.py
  - .planning/phases/02-line-provider-domain/02-VALIDATION.md
autonomous: true
requirements: [QA-04, QA-05]
tags: [line-provider, foundations, test-fixture, asgi-lifespan, requirements-sync, coverage]

must_haves:
  truths:
    - "asgi-lifespan dev-dep установлен и зафиксирован в uv.lock"
    - "REQUIREMENTS.md LP-02 содержит UUID4 (а не str) для event_id"
    - "Existing P1 tests/line_provider/test_health.py остаются зелёными после upgrade fixture"
    - "Fixture client из conftest триггерит lifespan через LifespanManager"
    - "pyproject.toml содержит [tool.coverage.run] source=['src/line_provider'] и [tool.coverage.report] fail_under=85"
  artifacts:
    - path: "pyproject.toml"
      provides: "asgi-lifespan>=2.1,<3 в [dependency-groups].dev + [tool.coverage.*]"
      contains: "asgi-lifespan"
    - path: "uv.lock"
      provides: "Lockfile с asgi-lifespan"
      contains: "asgi-lifespan"
    - path: ".planning/REQUIREMENTS.md"
      provides: "LP-02 синхронизирован с D-05 (UUID4)"
      contains: "UUID4"
    - path: "tests/line_provider/conftest.py"
      provides: "client fixture обёрнут в LifespanManager + опционально app accessor"
      contains: "LifespanManager"
  key_links:
    - from: "tests/line_provider/conftest.py"
      to: "asgi_lifespan.LifespanManager"
      via: "async with LifespanManager(app)"
      pattern: "LifespanManager"
---

<objective>
Foundations Wave 0 для Phase 2. Готовим почву для остальных шести планов:
1. Устанавливаем единственную новую dev-dependency P2 — `asgi-lifespan>=2.1,<3` (RESEARCH.md Pitfall 1 / Pattern 7) — без неё все integration-тесты P2 упадут с `AttributeError: app.state.event_store`, потому что `httpx.ASGITransport` не триггерит ASGI lifespan.
2. Синхронизируем REQUIREMENTS.md LP-02 (`event_id: str` → `event_id: UUID4`) per CONTEXT.md D-05 — устраняем единственное расхождение между источниками правды.
3. Обновляем `tests/line_provider/conftest.py`: оборачиваем `build_app()` в `LifespanManager(app)`, меняем сигнатуру fixture на yield-кортеж `(client, app)` чтобы integration-тесты Wave 3 могли seed'ить store напрямую без `_transport` приватного доступа.
4. Добавляем `[tool.coverage.run]` и `[tool.coverage.report]` в pyproject — phase-gate Plan 02-07 будет требовать ≥85% на `src/line_provider/{helpers,interactors,selectors,infrastructure,facades}`.
5. Обновляем `02-VALIDATION.md`: реальные Task IDs (`2-01-01..2-07-NN`) синхронизированы с этим PLAN.

Purpose: Без Wave 0 любая попытка Wave 1 поднять схемы / store упадёт на этапе integration-тестов (Plan 02-07). Это блокирующий plan для всего Phase 2.
Output: pyproject.toml (4 секции изменены), uv.lock (обновлён), REQUIREMENTS.md (1 строка LP-02), conftest.py (yield-tuple shape), 02-VALIDATION.md (per-task map синхронизирован).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-line-provider-domain/02-CONTEXT.md
@.planning/phases/02-line-provider-domain/02-RESEARCH.md
@.planning/phases/02-line-provider-domain/02-PATTERNS.md
@.planning/phases/02-line-provider-domain/02-VALIDATION.md
@.planning/phases/01-skeleton-infrastructure/01-07-SUMMARY.md
@pyproject.toml
@tests/line_provider/conftest.py
@tests/line_provider/test_health.py
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add asgi-lifespan>=2.1,<3 dev-dep + coverage config + sync uv.lock</name>
  <files>pyproject.toml, uv.lock</files>
  <read_first>
    - pyproject.toml (полный файл — посмотреть текущую структуру `[dependency-groups].dev` и `[tool.pytest.ini_options]`; coverage секций пока нет — добавить рядом с pytest)
    - .planning/phases/02-line-provider-domain/02-RESEARCH.md §«НОВАЯ ЗАВИСИМОСТЬ» (строки 137–151) — обоснование версии `>=2.1,<3`
    - .planning/phases/02-line-provider-domain/02-VALIDATION.md §«Wave 0 Requirements» — coverage `[tool.coverage.run] source=["src/line_provider"]`, `branch=true`, `[tool.coverage.report] fail_under=85`
  </read_first>
  <action>
    Выполнить в указанном порядке:

    1. Запустить `uv add --dev "asgi-lifespan>=2.1,<3"` из корня репо (cwd `/Users/dmitrydankov/Personal/BSW`). Команда обновит и pyproject.toml (добавит строку `"asgi-lifespan>=2.1,<3"` в `[dependency-groups].dev` после `"pre-commit>=4.6,<5"`), и uv.lock.

    2. Добавить в `pyproject.toml` две новые секции сразу ПОСЛЕ существующей `[tool.uv]` (то есть в самом конце файла):
       ```
       [tool.coverage.run]
       source = ["src/line_provider"]
       branch = true

       [tool.coverage.report]
       fail_under = 85
       show_missing = true
       skip_covered = false
       exclude_lines = [
           "pragma: no cover",
           "raise NotImplementedError",
           "if TYPE_CHECKING:",
       ]
       ```
       Эти настройки используются Plan 02-07 для phase-gate (≥85% на line_provider domain). Bet-maker покрытие настраивается отдельно в P3.

    3. Проверить итог: `uv sync --frozen` должен пройти без ошибок (lockfile свежий), `uv run python -c "import asgi_lifespan; print(asgi_lifespan.__version__)"` должен напечатать версию >=2.1.0.

    Не вносить других изменений в pyproject. Не менять существующие зависимости. Не модифицировать `[tool.pytest.ini_options]`.
  </action>
  <verify>
    <automated>uv sync --frozen</automated>
    <automated>uv run python -c "import asgi_lifespan; v = asgi_lifespan.__version__; assert v.startswith('2.'), v; print(v)"</automated>
    <automated>grep -q "asgi-lifespan" pyproject.toml</automated>
    <automated>grep -q "asgi-lifespan" uv.lock</automated>
    <automated>grep -q "tool.coverage.run" pyproject.toml</automated>
    <automated>grep -q "fail_under = 85" pyproject.toml</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "asgi-lifespan" pyproject.toml` == 1 (одна строка в dev-deps)
    - `grep -c "asgi-lifespan" uv.lock` >= 1 (lockfile содержит распакованную информацию о пакете)
    - `grep -q "\[tool.coverage.run\]" pyproject.toml` && `grep -q "source = \[\"src/line_provider\"\]" pyproject.toml`
    - `grep -q "\[tool.coverage.report\]" pyproject.toml` && `grep -q "fail_under = 85" pyproject.toml`
    - `uv sync --frozen` exit code 0
    - `uv run python -c "import asgi_lifespan"` exit code 0
    - Существующие 4 P1-теста проходят: `uv run pytest -q` exit 0
  </acceptance_criteria>
  <done>asgi-lifespan установлен, lockfile обновлён, coverage конфиг присутствует, P1 baseline зелёный.</done>
</task>

<task type="auto">
  <name>Task 2: Sync REQUIREMENTS.md LP-02 (str → UUID4) per D-05</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (строки ~21–23 — текущая формулировка LP-02)
    - .planning/phases/02-line-provider-domain/02-CONTEXT.md §«event_id» D-05 — обоснование override
  </read_first>
  <action>
    Открыть `.planning/REQUIREMENTS.md`. Найти строку 22:
    ```
    - [ ] **LP-02**: Модель `Event`: event_id (str), coefficient (Decimal, ровно 2 знака после запятой, > 0), deadline (timestamp), state (NEW / FINISHED_WIN / FINISHED_LOSE)
    ```
    Заменить ровно на:
    ```
    - [ ] **LP-02**: Модель `Event`: event_id (UUID4, client-generated), coefficient (Decimal, ровно 2 знака после запятой, > 0), deadline (UTC-aware datetime), state (NEW / FINISHED_WIN / FINISHED_LOSE). Per D-05 (Phase 2 CONTEXT.md): event_id — UUID4, не str; согласовано через line-provider, AMQP `EventFinishedMessage.event_id` и `bet_maker.bets.event_id`.
    ```
    Другие строки файла не трогать. Не менять `## Traceability` и `## v2 Requirements`.

    Не делать никаких других правок в `.planning/REQUIREMENTS.md` в рамках этого таска (изменения статусов LP-* на «In progress/Complete» делаются позже на каждом завершении плана).
  </action>
  <verify>
    <automated>grep -q "LP-02.*UUID4" .planning/REQUIREMENTS.md</automated>
    <automated>grep -q "Per D-05" .planning/REQUIREMENTS.md</automated>
    <automated>bash -c "! grep -q 'event_id (str)' .planning/REQUIREMENTS.md"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "LP-02.*UUID4.*client-generated" .planning/REQUIREMENTS.md`
    - `grep -q "Per D-05" .planning/REQUIREMENTS.md`
    - `grep -c "event_id (str)" .planning/REQUIREMENTS.md` == 0 (старая формулировка устранена)
    - `grep -c "^- \[ \] \*\*LP-02\*\*" .planning/REQUIREMENTS.md` == 1 (ровно одна строка LP-02 в файле)
  </acceptance_criteria>
  <done>REQUIREMENTS.md LP-02 фиксирует UUID4 client-generated, ссылается на D-05.</done>
</task>

<task type="auto">
  <name>Task 3: Upgrade tests/line_provider/conftest.py to LifespanManager + yield (client, app) tuple</name>
  <files>tests/line_provider/conftest.py, tests/line_provider/test_health.py</files>
  <read_first>
    - tests/line_provider/conftest.py (текущая 16-строчная версия)
    - tests/line_provider/test_health.py (как использует client fixture — нужно не сломать)
    - .planning/phases/02-line-provider-domain/02-RESEARCH.md §«Pattern 7: Test fixture» (строки 698–736)
    - .planning/phases/02-line-provider-domain/02-PATTERNS.md §«Excerpt C» (строки 159–217)
  </read_first>
  <action>
    1. Заменить полностью содержимое `tests/line_provider/conftest.py` на:
       ```python
       from __future__ import annotations

       from collections.abc import AsyncIterator

       import pytest_asyncio
       from asgi_lifespan import LifespanManager
       from fastapi import FastAPI
       from httpx import ASGITransport, AsyncClient

       from line_provider.app import build_app


       @pytest_asyncio.fixture
       async def app() -> AsyncIterator[FastAPI]:
           """FastAPI instance with lifespan triggered.

           Yielded separately so integration tests in plan 02-07 can seed
           app.state.event_store directly without touching client._transport.
           """
           application = build_app()
           async with LifespanManager(application):
               yield application


       @pytest_asyncio.fixture
       async def client(app: FastAPI) -> AsyncIterator[AsyncClient]:
           """Async HTTP client bound to the lifespan-aware app fixture."""
           transport = ASGITransport(app=app)
           async with AsyncClient(transport=transport, base_url="http://test") as ac:
               yield ac
       ```
       Никаких комментариев в коде (project hard rule); docstrings разрешены (по соглашению P1-07).

    2. Проверить `tests/line_provider/test_health.py` — там два теста с сигнатурой `async def test_*(client: AsyncClient)`. Новая `client` fixture теперь зависит от `app`, но внешне API не меняется — сигнатура тестов остаётся прежней, никаких правок в test_health.py не требуется. ОДНАКО, перепроверить факт: если в test_health.py нет других fixture зависимостей, ничего не нужно. Если что-то найдено, что ломается — задокументировать в SUMMARY.

    3. Запустить `uv run pytest tests/line_provider -q` — должно остаться 4 passed (P1 smoke).
  </action>
  <verify>
    <automated>uv run pytest tests/line_provider -q</automated>
    <automated>grep -q "from asgi_lifespan import LifespanManager" tests/line_provider/conftest.py</automated>
    <automated>grep -q "async with LifespanManager" tests/line_provider/conftest.py</automated>
    <automated>grep -q "async def app" tests/line_provider/conftest.py</automated>
    <automated>grep -q "async def client" tests/line_provider/conftest.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "LifespanManager" tests/line_provider/conftest.py` == 2 (импорт + использование)
    - `grep -c "@pytest_asyncio.fixture" tests/line_provider/conftest.py` == 2 (app + client)
    - `grep -c "from line_provider.app import build_app" tests/line_provider/conftest.py` == 1
    - `grep -q "from __future__ import annotations" tests/line_provider/conftest.py`
    - `uv run pytest tests/line_provider -q` → `2 passed` (test_health.py)
    - `uv run mypy --strict tests/line_provider/conftest.py` exit 0
    - `uv run ruff check tests/line_provider/conftest.py` → "All checks passed"
  </acceptance_criteria>
  <done>conftest.py обёрнут в LifespanManager, P1 smoke tests зелёные, mypy strict проходит.</done>
</task>

<task type="auto">
  <name>Task 4: Sync 02-VALIDATION.md per-task map with this PLAN's task IDs</name>
  <files>.planning/phases/02-line-provider-domain/02-VALIDATION.md</files>
  <read_first>
    - .planning/phases/02-line-provider-domain/02-VALIDATION.md (всю секцию «Per-Task Verification Map» — там skeleton с placeholder IDs)
  </read_first>
  <action>
    Заменить в `.planning/phases/02-line-provider-domain/02-VALIDATION.md` существующую таблицу «Per-Task Verification Map» на актуальную, синхронизированную с реальными task IDs семи планов P2:

    | Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
    |---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
    | 2-01-01 | 01 | 0 | QA-04, QA-05 | — | asgi-lifespan>=2.1 in dev-deps + coverage config | shell | `uv run python -c "import asgi_lifespan"` && `grep -q "fail_under = 85" pyproject.toml` | W0 | pending |
    | 2-01-02 | 01 | 0 | QA-04 | — | REQUIREMENTS.md LP-02 = UUID4 per D-05 | shell | `grep -q "LP-02.*UUID4" .planning/REQUIREMENTS.md` | W0 | pending |
    | 2-01-03 | 01 | 0 | QA-05 | — | conftest client fixture wraps LifespanManager | unit | `uv run pytest tests/line_provider -q` | W0 | pending |
    | 2-01-04 | 01 | 0 | QA-04, QA-05 | — | This validation map synced | doc | `grep -q "2-07-" .planning/phases/02-line-provider-domain/02-VALIDATION.md` | W0 | pending |
    | 2-02-01 | 02 | 1 | LP-02 | T-02-INPUT | EventState enum, EventCreate/EventUpdate/Event/EventRead with frozen + extra="forbid" + AwareDatetime + Coefficient annotated | unit | `uv run pytest tests/line_provider/test_schemas.py -q` | W0 | pending |
    | 2-02-02 | 02 | 1 | LP-04 | T-02-INPUT | EventFinishedMessage schema (frozen, extra="forbid", schema_version, EventTerminalState, UUID event_id) | unit | `uv run pytest tests/line_provider/test_schemas.py::TestMessages -q` | W0 | pending |
    | 2-03-01 | 03 | 1 | LP-05, LP-08 | T-03-STATE | is_transition_allowed parametrized 9-case table | unit | `uv run pytest tests/line_provider/test_state_machine.py -q` | W0 | pending |
    | 2-04-01 | 04 | 1 | LP-01, LP-08 | T-04-RACE | InMemoryEventStore add/update/get/list under asyncio.Lock + concurrent gather safe | unit | `uv run pytest tests/line_provider/test_in_memory_store.py -q` | W0 | pending |
    | 2-04-02 | 04 | 1 | LP-01 | T-04-RACE | store.update returns (new, previous_state) tuple | unit | same | W0 | pending |
    | 2-05-01 | 05 | 2 | LP-01, LP-03, LP-08 | T-05-ORDER | create_event interactor builds frozen Event(state=NEW) + store.add | unit | `uv run pytest tests/line_provider/test_create_event.py -q` | W0 | pending |
    | 2-05-02 | 05 | 2 | LP-03, LP-08 | T-05-ORDER | set_event_state: commit BEFORE publish; reverse → TransitionForbiddenError; no-op state → no publish; FakeEventBus captures call order | unit | `uv run pytest tests/line_provider/test_set_event_state.py -q` | W0 | pending |
    | 2-05-03 | 05 | 2 | LP-01 | T-05-FACADE | EventBus Protocol + NoopEventBus + facades/deps DI providers | unit | `uv run pytest tests/line_provider/test_facades.py -q` | W0 | pending |
    | 2-06-01 | 06 | 2 | LP-04 | — | get_event_by_id selector (pure read) | unit | `uv run pytest tests/line_provider/test_selectors.py::test_get -q` | W0 | pending |
    | 2-06-02 | 06 | 2 | LP-05 | — | list_active_events filters deadline>now AND state==NEW (monkey-patched utc_now) | unit | `uv run pytest tests/line_provider/test_selectors.py::test_list -q` | W0 | pending |
    | 2-07-01 | 07 | 3 | LP-03 | T-07-INPUT | POST /event 201 happy + 409 dup + 422 coefficient/deadline | integration | `uv run pytest tests/line_provider/test_event_routes.py::TestCreate -q` | W0 | pending |
    | 2-07-02 | 07 | 3 | LP-03, LP-08 | T-07-STATE | PUT /event/{id} 200 update + 404 missing + 422 reverse + no-publish on no-op | integration | `uv run pytest tests/line_provider/test_event_routes.py::TestUpdate -q` | W0 | pending |
    | 2-07-03 | 07 | 3 | LP-04 | — | GET /event/{id} 200/404 | integration | `uv run pytest tests/line_provider/test_event_routes.py::TestGet -q` | W0 | pending |
    | 2-07-04 | 07 | 3 | LP-05 | — | GET /events filters active | integration | `uv run pytest tests/line_provider/test_event_routes.py::TestList -q` | W0 | pending |
    | 2-07-05 | 07 | 3 | LP-07 | — | GET /health 200 stub (deep-pings → P5) | integration | `uv run pytest tests/line_provider/test_health.py -q` | OK | pending |
    | 2-07-06 | 07 | 3 | QA-04, QA-05 | — | Full suite green + coverage ≥85% on line_provider domain | shell | `uv run pytest --cov=src/line_provider --cov-report=term-missing` | W0 | pending |

    Также:
    - В заголовке файла поставить `nyquist_compliant: true`, `wave_0_complete: true` (после успешного выполнения Task 1-3 настоящего плана это станет фактом).
    - В секции «Wave 0 Requirements» поставить все галочки `[x]`.
    - В секции «Validation Sign-Off» поставить все галочки `[x]`, `Approval: approved 2026-05-14 by gsd-planner Phase 2`.

    Удалить пункт «`src/bet_maker/.../messages.py` ... `event_id` field corrected to `UUID`» из Wave 0 Requirements — в репо bet_maker не имеет ещё `schemas/messages.py` (создаётся в P3/P5), поэтому правка преждевременна. Зафиксировать примечание: «Bet-maker EventFinishedMessage создаётся в P3/P5 — UUID уже зафиксирован в D-05, drift невозможен».
  </action>
  <verify>
    <automated>grep -q "2-07-06" .planning/phases/02-line-provider-domain/02-VALIDATION.md</automated>
    <automated>grep -q "2-05-02" .planning/phases/02-line-provider-domain/02-VALIDATION.md</automated>
    <automated>grep -q "nyquist_compliant: true" .planning/phases/02-line-provider-domain/02-VALIDATION.md</automated>
    <automated>bash -c "! grep -q 'src/bet_maker/.*event_id field' .planning/phases/02-line-provider-domain/02-VALIDATION.md"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^| 2-[0-9][0-9]-[0-9][0-9] |" .planning/phases/02-line-provider-domain/02-VALIDATION.md` >= 18 (все task IDs в таблице)
    - `grep -q "nyquist_compliant: true" .planning/phases/02-line-provider-domain/02-VALIDATION.md`
    - `grep -q "wave_0_complete: true" .planning/phases/02-line-provider-domain/02-VALIDATION.md`
    - `grep -q "Approval: approved" .planning/phases/02-line-provider-domain/02-VALIDATION.md`
    - `grep -c "src/bet_maker" .planning/phases/02-line-provider-domain/02-VALIDATION.md` == 0 (преждевременная правка убрана)
  </acceptance_criteria>
  <done>02-VALIDATION.md имеет финальную per-task verification map, frontmatter обновлён, Wave 0 закрыт.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| developer workstation → uv registry | Установка `asgi-lifespan` тянет код с PyPI; источник доверенный, но третья сторона |
| GSD planner → planning documents | Документы используются как source of truth для downstream агентов — нельзя оставить inconsistency |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | uv.lock (supply chain) | mitigate | uv.lock коммитится; CI запускает `uv sync --frozen` — любая подмена хэша → fail. Bandit/safety not in scope для тестового задания (RESEARCH.md). |
| T-01-02 | Information Disclosure | tests/line_provider/conftest.py логирует stacktraces | accept | structlog уже выводит JSON; никакого PII в test code. |
| T-01-03 | Repudiation | REQUIREMENTS.md LP-02 sync без traceability | mitigate | Текст явно цитирует D-05 (CONTEXT.md Phase 2); grep-able в acceptance_criteria. |
| T-01-04 | Denial of Service | LifespanManager hangs в тестах | accept | asgi-lifespan 2.x проверен на FastAPI 0.115+; если P2-route зависнет в lifespan — `pytest` timeout по умолчанию 60s. |
</threat_model>

<verification>
После завершения всех 4 задач:
- `uv run pytest tests/line_provider -q` → 2 passed (test_health.py остаётся зелёный)
- `uv run mypy --strict src/line_provider tests/line_provider` → 0 errors
- `uv run ruff check src/line_provider tests/line_provider` → All checks passed
- `grep -q "asgi-lifespan" pyproject.toml uv.lock` (оба файла)
- `grep -q "UUID4" .planning/REQUIREMENTS.md`
- `grep -q "fail_under = 85" pyproject.toml`
- `grep -q "wave_0_complete: true" .planning/phases/02-line-provider-domain/02-VALIDATION.md`
</verification>

<success_criteria>
- asgi-lifespan>=2.1,<3 в dev-deps, зафиксирован в uv.lock
- coverage config в pyproject (source=src/line_provider, fail_under=85)
- REQUIREMENTS.md LP-02 = UUID4 (per D-05)
- conftest.py обёрнут в LifespanManager, имеет fixture (app, client) yield-tuple shape
- 02-VALIDATION.md имеет финальную per-task map (18+ строк), frontmatter `wave_0_complete: true`
- Все 4 P1-теста остаются зелёными после upgrade fixture
- mypy strict + ruff check + ruff format чисто
</success_criteria>

<output>
After completion, create `.planning/phases/02-line-provider-domain/02-01-SUMMARY.md`
</output>
