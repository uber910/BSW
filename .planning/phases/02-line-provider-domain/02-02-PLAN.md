---
phase: 02-line-provider-domain
plan: 02
type: execute
wave: 1
depends_on: [01]
files_modified:
  - src/line_provider/schemas/__init__.py
  - src/line_provider/schemas/events.py
  - src/line_provider/schemas/messages.py
  - src/line_provider/helpers/__init__.py
  - src/line_provider/helpers/money.py
  - tests/line_provider/test_schemas.py
autonomous: true
requirements: [LP-01, LP-02, LP-04]
tags: [line-provider, schemas, pydantic, decimal, uuid, aware-datetime]

must_haves:
  truths:
    - "EventState StrEnum имеет ровно 3 члена (NEW, FINISHED_WIN, FINISHED_LOSE)"
    - "EventCreate валидирует event_id=UUID4, coefficient>0 (≤2dp), deadline в будущем UTC-aware"
    - "EventUpdate валидирует coefficient/deadline/state без event_id и без deadline>now"
    - "Event domain entity frozen (raise ValidationError на setattr)"
    - "EventFinishedMessage frozen + extra=forbid + schema_version=1; event_id: UUID"
    - "Decimal 'coefficient' нормализован к 2dp (quantize) — приём '10' возвращает '10.00'"
  artifacts:
    - path: "src/line_provider/schemas/events.py"
      provides: "EventState, EventCreate, EventUpdate, Event, EventRead, Coefficient, FutureDeadline"
      exports: ["EventState", "EventCreate", "EventUpdate", "Event", "EventRead", "Coefficient", "FutureDeadline"]
    - path: "src/line_provider/schemas/messages.py"
      provides: "EventTerminalState, EventFinishedMessage"
      exports: ["EventTerminalState", "EventFinishedMessage"]
    - path: "src/line_provider/helpers/money.py"
      provides: "quantize_coefficient helper (Decimal → 2dp)"
      exports: ["quantize_coefficient"]
    - path: "tests/line_provider/test_schemas.py"
      provides: "Unit tests на все валидаторы"
  key_links:
    - from: "src/line_provider/schemas/messages.py"
      to: "src/line_provider/schemas/events.py"
      via: "import EventState (через EventTerminalState соответствие)"
      pattern: "EventTerminalState"
    - from: "src/line_provider/schemas/events.py"
      to: "src/config/time.py"
      via: "utc_now() в FutureDeadline AfterValidator"
      pattern: "from config.time import utc_now"
---

<objective>
Pydantic v2 schema layer для line-provider domain: HTTP-схемы (`EventCreate`, `EventUpdate`, `EventRead`), доменная сущность (`Event` frozen), AMQP-схема (`EventFinishedMessage`) и единственный helper (`quantize_coefficient`) для Pitfall 3 (RESEARCH.md) — нормализация `Decimal("10")` → `Decimal("10.00")`.

Purpose: Schema-слой — фундамент для всех остальных модулей P2 (`store`, `interactors`, `selectors`, `routes`). Pydantic v2 = единственный источник правды для контрактов (D-10, D-13, D-17). Создаётся в Wave 1 (W-2 revision: единственный план в Wave 1). State_machine (Plan 02-03) и in-memory store (Plan 02-04) выполняются параллельно в Wave 2, оба depends_on=[01, 02] — у них нет cross-file конфликтов между собой.

Output: 4 production файла + 1 test файл; `tests/line_provider/test_schemas.py` зелёный.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-line-provider-domain/02-CONTEXT.md
@.planning/phases/02-line-provider-domain/02-RESEARCH.md
@.planning/phases/02-line-provider-domain/02-PATTERNS.md
@.planning/phases/02-line-provider-domain/02-01-SUMMARY.md
@src/line_provider/settings/config.py
@src/config/time.py
@CLAUDE.md

<interfaces>
<!-- Key contracts exported by this plan, consumed by 02-04 (store), 02-05 (interactors), 02-07 (routes). -->

From src/line_provider/schemas/events.py (this plan):
```python
class EventState(StrEnum):
    NEW = "NEW"
    FINISHED_WIN = "FINISHED_WIN"
    FINISHED_LOSE = "FINISHED_LOSE"

Coefficient = Annotated[Decimal, ...]  # gt=0, ≤2dp, quantized
FutureDeadline = Annotated[AwareDatetime, AfterValidator(_in_future)]

class EventCreate(BaseModel):
    model_config = ConfigDict(extra="forbid")
    event_id: UUID
    coefficient: Coefficient
    deadline: FutureDeadline

class EventUpdate(BaseModel):
    model_config = ConfigDict(extra="forbid")
    coefficient: Coefficient
    deadline: AwareDatetime  # no FutureDeadline per D-07
    state: EventState

class Event(BaseModel):
    model_config = ConfigDict(frozen=True, extra="forbid")
    event_id: UUID
    coefficient: Coefficient
    deadline: AwareDatetime
    state: EventState

class EventRead(BaseModel):
    model_config = ConfigDict(extra="forbid")
    event_id: UUID
    coefficient: Coefficient
    deadline: AwareDatetime
    state: EventState
```

From src/line_provider/schemas/messages.py (this plan):
```python
class EventTerminalState(StrEnum):
    FINISHED_WIN = "FINISHED_WIN"
    FINISHED_LOSE = "FINISHED_LOSE"

class EventFinishedMessage(BaseModel):
    model_config = ConfigDict(frozen=True, extra="forbid")
    schema_version: Annotated[int, Field(ge=1)] = 1
    event_id: UUID
    new_state: EventTerminalState
    coefficient: Annotated[Decimal, Field(gt=0, max_digits=8, decimal_places=2)]
    occurred_at: AwareDatetime
    correlation_id: str
```

From src/line_provider/helpers/money.py (this plan):
```python
def quantize_coefficient(value: Decimal) -> Decimal: ...  # always 2dp
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create schemas/events.py + helpers/money.py + package markers</name>
  <files>src/line_provider/schemas/__init__.py, src/line_provider/schemas/events.py, src/line_provider/helpers/__init__.py, src/line_provider/helpers/money.py</files>
  <read_first>
    - src/line_provider/settings/config.py (стиль Pydantic v2 ConfigDict layout)
    - src/config/time.py (`utc_now()` для FutureDeadline)
    - .planning/phases/02-line-provider-domain/02-RESEARCH.md §«Pattern 6: Pydantic v2 schemas» (строки 580–696)
    - .planning/phases/02-line-provider-domain/02-RESEARCH.md §«Pitfall 3: Decimal сериализуется как 10 вместо 10.00» (строки 782–787)
    - .planning/phases/02-line-provider-domain/02-PATTERNS.md §«Excerpt G» (строки 307–347)
  </read_first>
  <behavior>
    - EventState имеет ровно 3 значения NEW/FINISHED_WIN/FINISHED_LOSE как StrEnum
    - Coefficient принимает строку "10" и Decimal("10"), quantize'ит до Decimal("10.00")
    - Coefficient отвергает 0, отрицательные, "10.123" (>2dp)
    - FutureDeadline принимает aware-datetime в будущем, отвергает в прошлом и naive
    - EventCreate отвергает лишние поля (extra="forbid") и принимает state-less тело
    - Event frozen: setattr → ValidationError
    - quantize_coefficient(Decimal("10")) == Decimal("10.00") и .as_tuple().exponent == -2
  </behavior>
  <action>
    1. Создать пустой `src/line_provider/schemas/__init__.py` (1 строка — пустой; package marker).
    2. Создать пустой `src/line_provider/helpers/__init__.py` (1 строка — пустой; package marker).

    3. Создать `src/line_provider/helpers/money.py`:
       ```python
       from __future__ import annotations

       from decimal import ROUND_HALF_UP, Decimal

       _TWO_PLACES = Decimal("0.01")


       def quantize_coefficient(value: Decimal) -> Decimal:
           """Normalise coefficient to exactly two decimal places.

           Pydantic v2 `decimal_places=2` validates upper bound only — `Decimal("10")`
           passes but serialises as `"10"` not `"10.00"`. ТЗ requires «ровно 2 знака»;
           we accept `"10"` and quantize-on-input. Used by EventCreate.coefficient
           and EventUpdate.coefficient (after-validators).
           """
           return value.quantize(_TWO_PLACES, rounding=ROUND_HALF_UP)
       ```

    4. Создать `src/line_provider/schemas/events.py`:
       ```python
       from __future__ import annotations

       from datetime import datetime
       from decimal import Decimal
       from enum import StrEnum
       from typing import Annotated
       from uuid import UUID

       from pydantic import AfterValidator, AwareDatetime, BaseModel, ConfigDict, Field

       from config.time import utc_now
       from line_provider.helpers.money import quantize_coefficient


       class EventState(StrEnum):
           NEW = "NEW"
           FINISHED_WIN = "FINISHED_WIN"
           FINISHED_LOSE = "FINISHED_LOSE"


       def _quantize(value: Decimal) -> Decimal:
           return quantize_coefficient(value)


       def _deadline_in_future(value: datetime) -> datetime:
           if value <= utc_now():
               raise ValueError("deadline must be in the future")
           return value


       Coefficient = Annotated[
               Decimal,
               Field(gt=Decimal("0"), max_digits=8, decimal_places=2),
               AfterValidator(_quantize),
           ]

       FutureDeadline = Annotated[AwareDatetime, AfterValidator(_deadline_in_future)]


       class EventCreate(BaseModel):
           model_config = ConfigDict(extra="forbid")

           event_id: UUID
           coefficient: Coefficient
           deadline: FutureDeadline


       class EventUpdate(BaseModel):
           model_config = ConfigDict(extra="forbid")

           coefficient: Coefficient
           deadline: AwareDatetime
           state: EventState


       class Event(BaseModel):
           model_config = ConfigDict(frozen=True, extra="forbid")

           event_id: UUID
           coefficient: Coefficient
           deadline: AwareDatetime
           state: EventState


       class EventRead(BaseModel):
           model_config = ConfigDict(extra="forbid")

           event_id: UUID
           coefficient: Coefficient
           deadline: AwareDatetime
           state: EventState
       ```

       Замечания:
       - `Coefficient` AfterValidator выполняется ПОСЛЕ Pydantic-валидации (`gt=0`, `decimal_places=2`), поэтому `"10.123"` будет отвергнут до quantize'а как `decimal_places > 2`.
       - `EventCreate.deadline` — `FutureDeadline` (D-07); в `EventUpdate` — простой `AwareDatetime` (D-07: PUT принимает любой deadline).
       - `Event` и `EventRead` оба используют `AwareDatetime` (не `FutureDeadline`) — store хранит уже завершённые события с прошлым deadline после реального финиша.
       - Никаких комментариев в коде. Docstrings допустимы — но в этом плане без них (P1 convention в production-коде; docstrings — только тесты и helpers/money.py выше).
  </action>
  <verify>
    <automated>uv run python -c "from line_provider.schemas.events import EventState, EventCreate, EventUpdate, Event, EventRead, Coefficient, FutureDeadline; assert len(list(EventState)) == 3; from line_provider.helpers.money import quantize_coefficient; from decimal import Decimal; assert quantize_coefficient(Decimal('10')) == Decimal('10.00')" &amp;&amp; uv run mypy --strict src/line_provider/schemas src/line_provider/helpers</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "class EventState" src/line_provider/schemas/events.py` == 1
    - `grep -c "class EventCreate" src/line_provider/schemas/events.py` == 1
    - `grep -c "class EventUpdate" src/line_provider/schemas/events.py` == 1
    - `grep -c "class Event\b" src/line_provider/schemas/events.py` == 1
    - `grep -c "class EventRead" src/line_provider/schemas/events.py` == 1
    - `grep -q "frozen=True" src/line_provider/schemas/events.py` (Event имеет frozen)
    - `grep -c "extra=\"forbid\"" src/line_provider/schemas/events.py` >= 4 (на всех 4 моделях)
    - `grep -q "AfterValidator" src/line_provider/schemas/events.py`
    - `grep -q "from config.time import utc_now" src/line_provider/schemas/events.py`
    - `grep -q "from line_provider.helpers.money import quantize_coefficient" src/line_provider/schemas/events.py`
    - `grep -q "def quantize_coefficient" src/line_provider/helpers/money.py`
    - `grep -q "_TWO_PLACES = Decimal" src/line_provider/helpers/money.py`
    - `uv run mypy --strict src/line_provider/schemas src/line_provider/helpers` exit 0
    - `uv run ruff check src/line_provider/schemas src/line_provider/helpers` → All checks passed
  </acceptance_criteria>
  <done>schemas/events.py + helpers/money.py существуют, импортируются без ошибок, mypy strict проходит.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create schemas/messages.py (EventFinishedMessage + EventTerminalState)</name>
  <files>src/line_provider/schemas/messages.py</files>
  <read_first>
    - src/line_provider/schemas/events.py (создан в Task 1 этого плана — для согласования EventState ↔ EventTerminalState)
    - .planning/phases/02-line-provider-domain/02-RESEARCH.md §«Pattern 6 → schemas/messages.py» (строки 638–671)
    - .planning/phases/02-line-provider-domain/02-CONTEXT.md D-13 + D-05 (UUID, frozen, extra=forbid)
  </read_first>
  <behavior>
    - EventTerminalState имеет ровно 2 значения (FINISHED_WIN, FINISHED_LOSE)
    - EventFinishedMessage frozen: попытка setattr → ValidationError
    - EventFinishedMessage отвергает schema_version=0
    - EventFinishedMessage отвергает лишние поля (extra="forbid")
    - event_id: UUID (НЕ str, НЕ int) — D-05
    - EventTerminalState.value совместим со значениями EventState.FINISHED_*
  </behavior>
  <action>
    Создать `src/line_provider/schemas/messages.py`:
    ```python
    from __future__ import annotations

    from decimal import Decimal
    from enum import StrEnum
    from typing import Annotated
    from uuid import UUID

    from pydantic import AwareDatetime, BaseModel, ConfigDict, Field


    class EventTerminalState(StrEnum):
        FINISHED_WIN = "FINISHED_WIN"
        FINISHED_LOSE = "FINISHED_LOSE"


    class EventFinishedMessage(BaseModel):
        model_config = ConfigDict(frozen=True, extra="forbid")

        schema_version: Annotated[int, Field(ge=1)] = 1
        event_id: UUID
        new_state: EventTerminalState
        coefficient: Annotated[Decimal, Field(gt=Decimal("0"), max_digits=8, decimal_places=2)]
        occurred_at: AwareDatetime
        correlation_id: str
    ```

    Замечания:
    - `coefficient` здесь без AfterValidator quantize — AMQP-сообщение строится из уже-quantized `Event.coefficient`, повторный quantize избыточен. Plan 02-05 (interactor `set_event_state`) гарантирует это.
    - `EventTerminalState` значения текстуально равны значениям соответствующих `EventState.FINISHED_*` — это позволяет конвертацию `EventTerminalState(event.state.value)` в interactor.
    - Никаких комментариев. Docstrings нет — следуем стилю settings/config.py.
  </action>
  <verify>
    <automated>uv run python -c "from line_provider.schemas.messages import EventFinishedMessage, EventTerminalState; from line_provider.schemas.events import EventState; assert EventTerminalState.FINISHED_WIN.value == EventState.FINISHED_WIN.value; assert EventTerminalState.FINISHED_LOSE.value == EventState.FINISHED_LOSE.value; assert len(list(EventTerminalState)) == 2" &amp;&amp; uv run mypy --strict src/line_provider/schemas/messages.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "class EventTerminalState" src/line_provider/schemas/messages.py` == 1
    - `grep -c "class EventFinishedMessage" src/line_provider/schemas/messages.py` == 1
    - `grep -q "frozen=True" src/line_provider/schemas/messages.py`
    - `grep -q "extra=\"forbid\"" src/line_provider/schemas/messages.py`
    - `grep -q "event_id: UUID" src/line_provider/schemas/messages.py`
    - `grep -q "schema_version" src/line_provider/schemas/messages.py`
    - `grep -q "correlation_id: str" src/line_provider/schemas/messages.py`
    - `grep -c "FINISHED_WIN\|FINISHED_LOSE" src/line_provider/schemas/messages.py` == 2 (ровно 2 terminal values)
    - `uv run mypy --strict src/line_provider/schemas/messages.py` exit 0
  </acceptance_criteria>
  <done>schemas/messages.py содержит EventFinishedMessage + EventTerminalState, UUID для event_id, frozen + extra=forbid.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Unit tests — tests/line_provider/test_schemas.py</name>
  <files>tests/line_provider/test_schemas.py</files>
  <read_first>
    - tests/line_provider/test_health.py (Excerpt H — стиль REQ-ID в docstring, async signatures, asyncio_mode=auto)
    - .planning/phases/02-line-provider-domain/02-PATTERNS.md §«Excerpt H»
    - src/line_provider/schemas/events.py (созданный в Task 1)
    - src/line_provider/schemas/messages.py (созданный в Task 2)
    - .planning/phases/02-line-provider-domain/02-RESEARCH.md §«Failure-Mode Classes Covered» (строки 1023–1040)
  </read_first>
  <behavior>
    Покрыть:
    - EventCreate happy path (UUID4 string + Decimal "10.50" + future deadline)
    - EventCreate отвергает: event_id="not-a-uuid", coefficient=0, coefficient="-5.00", coefficient="10.123" (>2dp), naive deadline, deadline в прошлом, extra поле "foo"
    - EventCreate с coefficient="10" → итоговое значение Decimal("10.00") (quantize)
    - EventUpdate: НЕТ FutureDeadline (принимает прошлое); НЕТ event_id (extra=forbid если передать)
    - Event: frozen — попытка mutate setattr → ValidationError
    - EventRead.model_validate(event) работает (cross-model conversion)
    - EventFinishedMessage: frozen, отвергает schema_version=0, отвергает new_state=EventState.NEW (только terminal)
    - quantize_coefficient: ("10")→"10.00", ("1.5")→"1.50", ("1.234")→"1.23" (округление)
  </behavior>
  <action>
    Создать `tests/line_provider/test_schemas.py`:
    ```python
    """Unit tests for line_provider.schemas.*.

    LP-02: Event domain model (event_id=UUID4, coefficient=Decimal 2dp >0, deadline aware, state)
    LP-04: HTTP schemas (EventCreate, EventUpdate, EventRead)
    """

    from __future__ import annotations

    from datetime import datetime, timedelta, timezone
    from decimal import Decimal
    from uuid import UUID, uuid4

    import pytest
    from pydantic import ValidationError

    from line_provider.helpers.money import quantize_coefficient
    from line_provider.schemas.events import (
        Event,
        EventCreate,
        EventRead,
        EventState,
        EventUpdate,
    )
    from line_provider.schemas.messages import EventFinishedMessage, EventTerminalState


    def _future() -> datetime:
        return datetime.now(timezone.utc) + timedelta(hours=1)


    def _past() -> datetime:
        return datetime.now(timezone.utc) - timedelta(hours=1)


    class TestQuantize:
        def test_quantize_pads_to_two_places(self) -> None:
            """LP-02: quantize_coefficient('10') -> '10.00'."""
            assert quantize_coefficient(Decimal("10")) == Decimal("10.00")
            assert quantize_coefficient(Decimal("10")).as_tuple().exponent == -2

        def test_quantize_keeps_two_places(self) -> None:
            """LP-02: quantize_coefficient('1.50') stays '1.50'."""
            assert quantize_coefficient(Decimal("1.50")) == Decimal("1.50")

        def test_quantize_rounds_half_up(self) -> None:
            """LP-02: quantize_coefficient rounds to 2dp using ROUND_HALF_UP."""
            assert quantize_coefficient(Decimal("1.235")) == Decimal("1.24")
            assert quantize_coefficient(Decimal("1.234")) == Decimal("1.23")


    class TestEventCreate:
        def test_happy_path(self) -> None:
            """LP-02: EventCreate accepts UUID4 + Decimal('10.50') + future deadline."""
            ec = EventCreate(
                event_id=uuid4(),
                coefficient=Decimal("10.50"),
                deadline=_future(),
            )
            assert isinstance(ec.event_id, UUID)
            assert ec.coefficient == Decimal("10.50")

        def test_quantizes_int_string_input(self) -> None:
            """LP-02: '10' string input quantized to Decimal('10.00')."""
            ec = EventCreate(
                event_id=uuid4(),
                coefficient=Decimal("10"),
                deadline=_future(),
            )
            assert ec.coefficient == Decimal("10.00")
            assert ec.coefficient.as_tuple().exponent == -2

        def test_rejects_non_uuid(self) -> None:
            """LP-02: event_id must be a UUID, not arbitrary str."""
            with pytest.raises(ValidationError):
                EventCreate.model_validate({
                    "event_id": "not-a-uuid",
                    "coefficient": "10.50",
                    "deadline": _future().isoformat(),
                })

        def test_rejects_zero_coefficient(self) -> None:
            """LP-02/LP-08: coefficient must be > 0."""
            with pytest.raises(ValidationError):
                EventCreate(event_id=uuid4(), coefficient=Decimal("0"), deadline=_future())

        def test_rejects_negative_coefficient(self) -> None:
            """LP-02/LP-08: coefficient must be > 0."""
            with pytest.raises(ValidationError):
                EventCreate(event_id=uuid4(), coefficient=Decimal("-1.00"), deadline=_future())

        def test_rejects_more_than_two_decimal_places(self) -> None:
            """LP-02: coefficient must have ≤ 2 decimal places (rejected before quantize)."""
            with pytest.raises(ValidationError):
                EventCreate(event_id=uuid4(), coefficient=Decimal("10.123"), deadline=_future())

        def test_rejects_naive_deadline(self) -> None:
            """LP-02: deadline must be tz-aware."""
            naive = datetime.now() + timedelta(hours=1)
            with pytest.raises(ValidationError):
                EventCreate(event_id=uuid4(), coefficient=Decimal("1.00"), deadline=naive)

        def test_rejects_past_deadline(self) -> None:
            """LP-08/D-07: deadline > now required on CREATE."""
            with pytest.raises(ValidationError):
                EventCreate(event_id=uuid4(), coefficient=Decimal("1.00"), deadline=_past())

        def test_rejects_extra_field(self) -> None:
            """D-04: EventCreate forbids extra fields."""
            with pytest.raises(ValidationError):
                EventCreate.model_validate({
                    "event_id": str(uuid4()),
                    "coefficient": "1.00",
                    "deadline": _future().isoformat(),
                    "state": "NEW",
                })


    class TestEventUpdate:
        def test_happy_path(self) -> None:
            """LP-03: EventUpdate accepts coefficient/deadline/state."""
            eu = EventUpdate(
                coefficient=Decimal("2.00"),
                deadline=_future(),
                state=EventState.FINISHED_WIN,
            )
            assert eu.state == EventState.FINISHED_WIN

        def test_accepts_past_deadline(self) -> None:
            """D-07: PUT does NOT validate deadline > now (deadline pinned once to avoid clock drift)."""
            past = _past()
            eu = EventUpdate(coefficient=Decimal("2.00"), deadline=past, state=EventState.FINISHED_WIN)
            assert eu.deadline == past

        def test_rejects_event_id_in_body(self) -> None:
            """D-04: event_id in PUT body forbidden (extra='forbid')."""
            with pytest.raises(ValidationError):
                EventUpdate.model_validate({
                    "event_id": str(uuid4()),
                    "coefficient": "1.00",
                    "deadline": _future().isoformat(),
                    "state": "NEW",
                })


    class TestEvent:
        def test_frozen(self) -> None:
            """D-17: Event is frozen — setattr raises ValidationError."""
            event = Event(
                event_id=uuid4(),
                coefficient=Decimal("1.50"),
                deadline=_future(),
                state=EventState.NEW,
            )
            with pytest.raises(ValidationError):
                event.coefficient = Decimal("2.50")  # type: ignore[misc]


    class TestEventRead:
        def test_from_event(self) -> None:
            """LP-04: EventRead.model_validate(event) cross-model conversion."""
            event = Event(
                event_id=uuid4(),
                coefficient=Decimal("1.50"),
                deadline=_future(),
                state=EventState.NEW,
            )
            read = EventRead.model_validate(event.model_dump())
            assert read.event_id == event.event_id
            assert read.state == EventState.NEW


    class TestEventFinishedMessage:
        def test_happy_path(self) -> None:
            """D-13: EventFinishedMessage accepts all fields, default schema_version=1."""
            msg = EventFinishedMessage(
                event_id=uuid4(),
                new_state=EventTerminalState.FINISHED_WIN,
                coefficient=Decimal("2.00"),
                occurred_at=_future(),
                correlation_id="req-abc",
            )
            assert msg.schema_version == 1

        def test_frozen(self) -> None:
            """D-13: EventFinishedMessage is frozen."""
            msg = EventFinishedMessage(
                event_id=uuid4(),
                new_state=EventTerminalState.FINISHED_WIN,
                coefficient=Decimal("2.00"),
                occurred_at=_future(),
                correlation_id="req-abc",
            )
            with pytest.raises(ValidationError):
                msg.schema_version = 2  # type: ignore[misc]

        def test_rejects_schema_version_zero(self) -> None:
            """D-13: schema_version must be ≥ 1."""
            with pytest.raises(ValidationError):
                EventFinishedMessage(
                    schema_version=0,
                    event_id=uuid4(),
                    new_state=EventTerminalState.FINISHED_WIN,
                    coefficient=Decimal("2.00"),
                    occurred_at=_future(),
                    correlation_id="req-abc",
                )

        def test_rejects_extra_field(self) -> None:
            """D-13: extra='forbid'."""
            with pytest.raises(ValidationError):
                EventFinishedMessage.model_validate({
                    "event_id": str(uuid4()),
                    "new_state": "FINISHED_WIN",
                    "coefficient": "2.00",
                    "occurred_at": _future().isoformat(),
                    "correlation_id": "req-abc",
                    "extra_field": "evil",
                })

        def test_terminal_state_values_match_event_state(self) -> None:
            """D-13: EventTerminalState values are a subset of EventState values."""
            assert EventTerminalState.FINISHED_WIN.value == EventState.FINISHED_WIN.value
            assert EventTerminalState.FINISHED_LOSE.value == EventState.FINISHED_LOSE.value
    ```

    Тесты — sync (`def`), кроме случаев когда нужен async; здесь async не нужен (Pydantic — sync). pytest-asyncio asyncio_mode=auto не мешает sync-тестам.
  </action>
  <verify>
    <automated>uv run pytest tests/line_provider/test_schemas.py -q -x &amp;&amp; uv run mypy --strict tests/line_provider/test_schemas.py &amp;&amp; uv run ruff check tests/line_provider/test_schemas.py</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/line_provider/test_schemas.py -q` exit 0 (все тесты зелёные)
    - `grep -c "^class Test" tests/line_provider/test_schemas.py` >= 6 (Quantize, EventCreate, EventUpdate, Event, EventRead, EventFinishedMessage)
    - `grep -c "LP-02\|LP-04\|LP-08\|D-04\|D-07\|D-13\|D-17" tests/line_provider/test_schemas.py` >= 6 (REQ-ID traceability на docstrings)
    - `uv run mypy --strict tests/line_provider/test_schemas.py` exit 0
    - `uv run ruff check tests/line_provider/test_schemas.py` → "All checks passed"
  </acceptance_criteria>
  <done>test_schemas.py зелёный, mypy strict проходит, все валидаторы покрыты unit-тестами.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HTTP client → FastAPI route → Pydantic schema | Untrusted JSON body; Pydantic — единственная защитная граница для типов/размеров |
| AMQP body → EventFinishedMessage (P5 only) | Defence-in-depth: extra=forbid отрежет неизвестные поля, schema_version блокирует drift |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | EventCreate body | mitigate | `extra="forbid"` отрезает unknown поля → 422. AwareDatetime + FutureDeadline валидируют тип и временной инвариант. `Coefficient` enforces gt=0 + ≤2dp; quantize нормализует. |
| T-02-02 | Information Disclosure | Pydantic ValidationError detail | accept | FastAPI 422 body содержит структурированные ошибки от Pydantic; не stack traces. RFC7807 deferred (REQUIREMENTS v2 API-04). |
| T-02-03 | Denial of Service | Unbounded coefficient string | mitigate | `max_digits=8` ограничивает Decimal до 999999.99; никаких unbounded коллекций в схемах. Body size limit — Starlette default (~ 1MB), достаточно для test task. |
| T-02-04 | Repudiation | EventFinishedMessage schema drift | mitigate | `schema_version: int = 1` + `extra="forbid"` гарантируют, что P5 consumer отвергнет любую модификацию схемы без явного version bump. |
| T-02-05 | Spoofing | Naive datetime принимается как UTC | mitigate | `AwareDatetime` отвергает naive — никаких неявных tz конверсий. |
| T-02-06 | Information Disclosure | Decimal `"10"` serialization без 2dp | mitigate | quantize-on-input (Pitfall 3 mitigation) гарантирует `"10.00"` round-trip — невозможно отдать клиенту неполный coefficient. |
| T-02-07 | Authorization | — | accept | Authentication/authorization out-of-scope per ТЗ. Зафиксировано здесь явно. |
</threat_model>

<verification>
- `uv run pytest tests/line_provider/test_schemas.py -q` → all passed
- `uv run mypy --strict src/line_provider/schemas src/line_provider/helpers tests/line_provider/test_schemas.py` → 0 errors
- `uv run ruff check src/line_provider/schemas src/line_provider/helpers tests/line_provider/test_schemas.py` → All checks passed
- Все 4 P1-теста + новые тесты схем зелёные: `uv run pytest -q`
</verification>

<success_criteria>
- 4 production файла созданы: schemas/{__init__,events,messages}.py + helpers/{__init__,money}.py
- test_schemas.py покрывает Quantize + EventCreate + EventUpdate + Event (frozen) + EventRead + EventFinishedMessage
- mypy --strict проходит
- Schema layer ready для consumption планами 02-04 (store), 02-05 (interactors), 02-07 (routes)
</success_criteria>

<output>
After completion, create `.planning/phases/02-line-provider-domain/02-02-SUMMARY.md`
</output>
