---
phase: 02-line-provider-domain
plan: 06
type: execute
wave: 3
depends_on: [02, 04]
files_modified:
  - src/line_provider/selectors/__init__.py
  - src/line_provider/selectors/get_event_by_id.py
  - src/line_provider/selectors/list_active_events.py
  - tests/line_provider/test_selectors.py
autonomous: true
requirements: [LP-04, LP-05]
tags: [line-provider, selectors, pure-read, list-active, time-filter]

must_haves:
  truths:
    - "get_event_by_id возвращает Event или None — не raise (route ловит None и отдаёт 404)"
    - "list_active_events фильтрует state==NEW AND deadline > utc_now()"
    - "list_active_events использует module-level imported utc_now (monkey-patch friendly)"
    - "Селекторы не вызывают event_bus и не мутируют store"
  artifacts:
    - path: "src/line_provider/selectors/get_event_by_id.py"
      provides: "async get_event_by_id(store, *, event_id) -> Event | None"
      exports: ["get_event_by_id"]
    - path: "src/line_provider/selectors/list_active_events.py"
      provides: "async list_active_events(store) -> list[Event]"
      exports: ["list_active_events"]
    - path: "tests/line_provider/test_selectors.py"
      provides: "Unit-тесты на оба селектора; включает monkey-patch utc_now для deterministic timing"
  key_links:
    - from: "src/line_provider/selectors/list_active_events.py"
      to: "src/config/time.py"
      via: "from config.time import utc_now (module-level import)"
      pattern: "from config.time import utc_now"
---

<objective>
Read-only selectors layer: `get_event_by_id` (LP-04) и `list_active_events` (LP-05). Pure-функции, никакой mutation, никаких side effects. Импорт `utc_now` на module level — это позволяет тестам monkey-patch'ить время через `monkeypatch.setattr(...)` без freezegun.

Purpose: LP-04 + LP-05 — read-side доменной логики. Селекторы — самый тонкий слой; основной риск — фильтр `deadline > now AND state == NEW` (ROADMAP success criterion 3 для Phase 2). Тесты с monkey-patched `utc_now` гарантируют детерминизм без race-conditions на CI.

Output: 2 selector модуля + 1 unit test файл.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-line-provider-domain/02-CONTEXT.md
@.planning/phases/02-line-provider-domain/02-RESEARCH.md
@.planning/phases/02-line-provider-domain/02-PATTERNS.md
@.planning/phases/02-line-provider-domain/02-02-SUMMARY.md
@.planning/phases/02-line-provider-domain/02-04-SUMMARY.md
@src/config/time.py

<interfaces>
<!-- Exported by this plan, consumed by 02-07 routes. -->
From src/line_provider/selectors/get_event_by_id.py:
```python
async def get_event_by_id(
    store: InMemoryEventStore, *, event_id: UUID
) -> Event | None: ...
```

From src/line_provider/selectors/list_active_events.py:
```python
async def list_active_events(store: InMemoryEventStore) -> list[Event]: ...
# Filters: e.state == EventState.NEW AND e.deadline > utc_now()
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create selectors/get_event_by_id.py + selectors/list_active_events.py</name>
  <files>src/line_provider/selectors/__init__.py, src/line_provider/selectors/get_event_by_id.py, src/line_provider/selectors/list_active_events.py</files>
  <read_first>
    - src/line_provider/infrastructure/store/in_memory.py (Plan 02-04)
    - src/line_provider/schemas/events.py (Event, EventState — Plan 02-02)
    - src/config/time.py (Excerpt F — utc_now)
    - .planning/phases/02-line-provider-domain/02-RESEARCH.md §«Code Examples → list_active_events selector» (строки 853–869)
    - .planning/phases/02-line-provider-domain/02-PATTERNS.md §«§1 → selectors/*»
  </read_first>
  <behavior>
    get_event_by_id:
    - Принимает store + event_id (UUID)
    - Делегирует store.get_by_id
    - Возвращает Event или None

    list_active_events:
    - Принимает store
    - now = utc_now() — взять текущее время ОДИН раз для consistency среди всех events в snapshot
    - Возвращает [e for e in await store.list_all() if e.state == EventState.NEW and e.deadline > now]
  </behavior>
  <action>
    1. Создать `src/line_provider/selectors/__init__.py` (1 строка — пустой).

    2. Создать `src/line_provider/selectors/get_event_by_id.py`:
       ```python
       from __future__ import annotations

       from uuid import UUID

       from line_provider.infrastructure.store.in_memory import InMemoryEventStore
       from line_provider.schemas.events import Event


       async def get_event_by_id(
           store: InMemoryEventStore,
           *,
           event_id: UUID,
       ) -> Event | None:
           return await store.get_by_id(event_id)
       ```

    3. Создать `src/line_provider/selectors/list_active_events.py`:
       ```python
       from __future__ import annotations

       from config.time import utc_now
       from line_provider.infrastructure.store.in_memory import InMemoryEventStore
       from line_provider.schemas.events import Event, EventState


       async def list_active_events(store: InMemoryEventStore) -> list[Event]:
           now = utc_now()
           return [
               e
               for e in await store.list_all()
               if e.state == EventState.NEW and e.deadline > now
           ]
       ```

       Замечания:
       - `from config.time import utc_now` — module-level импорт; тесты могут monkey-patch `line_provider.selectors.list_active_events.utc_now`.
       - Никаких docstrings/комментариев в production. Стиль строго как `src/config/time.py`.
  </action>
  <verify>
    <!-- W-2 (revision): inline `python -c` smoke moved to test_selectors.py::test_smoke_get_and_list_active.
         If Task 2 has not run yet, this verify intentionally fails — execute Task 2 first. -->
    <automated>uv run pytest tests/line_provider/test_selectors.py::test_smoke_get_and_list_active -q</automated>
    <automated>uv run mypy --strict src/line_provider/selectors</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "async def get_event_by_id" src/line_provider/selectors/get_event_by_id.py`
    - `grep -q "async def list_active_events" src/line_provider/selectors/list_active_events.py`
    - `grep -q "from config.time import utc_now" src/line_provider/selectors/list_active_events.py`
    - `grep -q "e.state == EventState.NEW and e.deadline > now" src/line_provider/selectors/list_active_events.py`
    - `grep -q "return await store.get_by_id(event_id)" src/line_provider/selectors/get_event_by_id.py`
    - `grep -c "^# " src/line_provider/selectors/list_active_events.py src/line_provider/selectors/get_event_by_id.py` == 0
    - `uv run mypy --strict src/line_provider/selectors` exit 0
    - `uv run ruff check src/line_provider/selectors` → All checks passed
  </acceptance_criteria>
  <done>Оба selector файла существуют, импортируются, mypy strict проходит.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Unit test — tests/line_provider/test_selectors.py (monkey-patched utc_now)</name>
  <files>tests/line_provider/test_selectors.py</files>
  <read_first>
    - src/line_provider/selectors/get_event_by_id.py (Task 1)
    - src/line_provider/selectors/list_active_events.py (Task 1)
    - .planning/phases/02-line-provider-domain/02-RESEARCH.md §«Open Question 4: utc_now injection vs monkey-patch» (строки 948–951)
    - .planning/phases/02-line-provider-domain/02-PATTERNS.md §«§1 → test_selectors.py»
  </read_first>
  <behavior>
    Покрыть:
    0. test_smoke_get_and_list_active — единый smoke (для verify Task 1): seed 1 NEW+future → get_by_id(eid) != None; get_by_id(random) == None; list_active_events len == 1. Заменяет inline `python -c` из старой ревизии плана (W-2 revision).
    1. test_get_event_by_id_returns_event — happy path
    2. test_get_event_by_id_returns_none — миссинг → None
    3. test_list_active_events_filters_by_state — события с state=FINISHED_WIN/LOSE отфильтрованы
    4. test_list_active_events_filters_by_deadline — события с deadline в прошлом (monkeypatched utc_now=future) отфильтрованы
    5. test_list_active_events_returns_only_new_and_future — комбинированный сценарий: 1 NEW+future, 1 NEW+past, 1 FINISHED_WIN+future → ровно 1
    6. test_list_active_events_empty_store — пустой store → []

    monkeypatch использует `monkeypatch.setattr("line_provider.selectors.list_active_events.utc_now", lambda: <fixed_dt>)`.
  </behavior>
  <action>
    Создать `tests/line_provider/test_selectors.py`:
    ```python
    """Unit tests for line_provider.selectors.

    LP-04: get_event_by_id returns Event or None (route maps None to 404).
    LP-05: list_active_events filters deadline > now AND state == NEW.
    Open Question 4: utc_now is monkey-patched at module level for deterministic timing.
    """

    from __future__ import annotations

    from datetime import datetime, timedelta, timezone
    from decimal import Decimal
    from uuid import uuid4

    import pytest

    from line_provider.infrastructure.store.in_memory import InMemoryEventStore
    from line_provider.schemas.events import Event, EventState
    from line_provider.selectors.get_event_by_id import get_event_by_id
    from line_provider.selectors.list_active_events import list_active_events


    _NOW = datetime(2026, 5, 14, 12, 0, 0, tzinfo=timezone.utc)


    def _event(state: EventState, *, offset_hours: float) -> Event:
        return Event(
            event_id=uuid4(),
            coefficient=Decimal("1.50"),
            deadline=_NOW + timedelta(hours=offset_hours),
            state=state,
        )


    async def test_get_event_by_id_returns_event() -> None:
        """LP-04: get_event_by_id returns the stored Event."""
        store = InMemoryEventStore()
        event = _event(EventState.NEW, offset_hours=1)
        await store.add(event)
        assert await get_event_by_id(store, event_id=event.event_id) == event


    async def test_get_event_by_id_returns_none_when_missing() -> None:
        """LP-04: get_event_by_id returns None when event absent (route maps to 404)."""
        store = InMemoryEventStore()
        assert await get_event_by_id(store, event_id=uuid4()) is None


    async def test_list_active_events_excludes_finished_states(
        monkeypatch: pytest.MonkeyPatch,
    ) -> None:
        """LP-05: list_active_events filters out FINISHED_WIN and FINISHED_LOSE."""
        monkeypatch.setattr(
            "line_provider.selectors.list_active_events.utc_now",
            lambda: _NOW,
        )
        store = InMemoryEventStore()
        active = _event(EventState.NEW, offset_hours=1)
        finished_win = _event(EventState.FINISHED_WIN, offset_hours=1)
        finished_lose = _event(EventState.FINISHED_LOSE, offset_hours=1)
        await store.add(active)
        await store.add(finished_win)
        await store.add(finished_lose)

        result = await list_active_events(store)
        ids = {e.event_id for e in result}
        assert ids == {active.event_id}


    async def test_list_active_events_excludes_past_deadline(
        monkeypatch: pytest.MonkeyPatch,
    ) -> None:
        """LP-05: list_active_events filters out events with deadline <= now."""
        monkeypatch.setattr(
            "line_provider.selectors.list_active_events.utc_now",
            lambda: _NOW,
        )
        store = InMemoryEventStore()
        future = _event(EventState.NEW, offset_hours=1)
        past = _event(EventState.NEW, offset_hours=-1)
        await store.add(future)
        await store.add(past)

        result = await list_active_events(store)
        ids = {e.event_id for e in result}
        assert ids == {future.event_id}


    async def test_list_active_events_excludes_deadline_equal_to_now(
        monkeypatch: pytest.MonkeyPatch,
    ) -> None:
        """LP-05: deadline == now is NOT considered active (strict `>` comparison)."""
        monkeypatch.setattr(
            "line_provider.selectors.list_active_events.utc_now",
            lambda: _NOW,
        )
        store = InMemoryEventStore()
        boundary = _event(EventState.NEW, offset_hours=0)
        await store.add(boundary)

        assert await list_active_events(store) == []


    async def test_list_active_events_returns_only_new_and_future(
        monkeypatch: pytest.MonkeyPatch,
    ) -> None:
        """LP-05: combined filter — 1 NEW+future, 1 NEW+past, 1 FINISHED+future → only NEW+future."""
        monkeypatch.setattr(
            "line_provider.selectors.list_active_events.utc_now",
            lambda: _NOW,
        )
        store = InMemoryEventStore()
        active = _event(EventState.NEW, offset_hours=2)
        new_past = _event(EventState.NEW, offset_hours=-2)
        finished_future = _event(EventState.FINISHED_WIN, offset_hours=2)
        await store.add(active)
        await store.add(new_past)
        await store.add(finished_future)

        result = await list_active_events(store)
        ids = {e.event_id for e in result}
        assert ids == {active.event_id}


    async def test_smoke_get_and_list_active(monkeypatch: pytest.MonkeyPatch) -> None:
        """W-2 revision: smoke replaces inline python -c verify from Task 1.

        Seeds 1 NEW+future event; asserts get_by_id positive/negative + list_active_events length == 1.
        Uses monkey-patched utc_now for deterministic timing.
        """
        monkeypatch.setattr(
            "line_provider.selectors.list_active_events.utc_now",
            lambda: _NOW,
        )
        store = InMemoryEventStore()
        seeded = _event(EventState.NEW, offset_hours=1)
        await store.add(seeded)
        assert await get_event_by_id(store, event_id=seeded.event_id) is not None
        assert await get_event_by_id(store, event_id=uuid4()) is None
        assert len(await list_active_events(store)) == 1


    async def test_list_active_events_empty_store() -> None:
        """LP-05: empty store yields empty list."""
        store = InMemoryEventStore()
        assert await list_active_events(store) == []
    ```
  </action>
  <verify>
    <automated>uv run pytest tests/line_provider/test_selectors.py -q -x &amp;&amp; uv run mypy --strict tests/line_provider/test_selectors.py &amp;&amp; uv run ruff check tests/line_provider/test_selectors.py</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/line_provider/test_selectors.py -q` → 8 passed (including test_smoke_get_and_list_active per W-2 revision)
    - `grep -c "^async def test_" tests/line_provider/test_selectors.py` == 8
    - `grep -q "monkeypatch.setattr" tests/line_provider/test_selectors.py`
    - `grep -c "line_provider.selectors.list_active_events.utc_now" tests/line_provider/test_selectors.py` >= 4 (используется в 4+ тестах)
    - `grep -c "LP-04\|LP-05" tests/line_provider/test_selectors.py` >= 7  (smoke also LP-04/05)
    - `uv run mypy --strict tests/line_provider/test_selectors.py` exit 0
  </acceptance_criteria>
  <done>8 unit-тестов для селекторов зелёные (включая smoke); monkey-patch utc_now работает без freezegun.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| route handler → selector | Pure read; никакой mutation, никакого I/O |
| selector → store (lock-free read) | Snapshot semantics; frozen Event'ы |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Tampering | Selector мутирует store | mitigate | Селекторы используют только `store.get_by_id` и `store.list_all` — оба read-only. Event'ы frozen, попытка mutate в comprehension → ValidationError. |
| T-06-02 | Information Disclosure | list_active_events leak'ает FINISHED events | mitigate | Strict filter `state == EventState.NEW`. Тест `test_list_active_events_excludes_finished_states` доказывает. |
| T-06-03 | Tampering | Boundary case deadline == now | mitigate | Strict `>` (не `>=`) — событие на границе НЕ активно. Тест `test_list_active_events_excludes_deadline_equal_to_now` фиксирует поведение. |
| T-06-04 | Denial of Service | Linear scan по большой коллекции | accept | In-memory list comprehension; для ТЗ объёмы малы. Пагинация deferred (out-of-scope per CONTEXT.md). |
| T-06-05 | Repudiation | Тесты non-deterministic (real-time clock) | mitigate | monkey-patch `utc_now` фиксирует временной reference. CI/local запуски одинаково deterministic. |
</threat_model>

<verification>
- `uv run pytest tests/line_provider/test_selectors.py -q` → 8 passed (including test_smoke_get_and_list_active per W-2 revision)
- `uv run mypy --strict src/line_provider/selectors tests/line_provider/test_selectors.py` → 0 errors
- `uv run ruff check src/line_provider/selectors tests/line_provider/test_selectors.py` → All checks passed
- Full suite после plans 02-01..02-06: `uv run pytest tests/line_provider -q` зелёный
</verification>

<success_criteria>
- Оба selector модуля — pure functions
- list_active_events использует module-level utc_now (monkey-patch friendly)
- 8 unit-тестов покрывают: get happy/miss + list filter by state/deadline/boundary/combined/empty + smoke (W-2 revision)
- mypy strict + ruff чисто
</success_criteria>

<output>
After completion, create `.planning/phases/02-line-provider-domain/02-06-SUMMARY.md`
</output>
