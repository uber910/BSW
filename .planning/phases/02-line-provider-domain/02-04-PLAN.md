---
phase: 02-line-provider-domain
plan: 04
type: execute
wave: 2
depends_on: [01, 02]
files_modified:
  - src/line_provider/infrastructure/__init__.py
  - src/line_provider/infrastructure/store/__init__.py
  - src/line_provider/infrastructure/store/in_memory.py
  - tests/line_provider/test_in_memory_store.py
autonomous: true
requirements: [LP-01, LP-08]
tags: [line-provider, in-memory-store, asyncio-lock, concurrency, snapshots]

must_haves:
  truths:
    - "InMemoryEventStore.add под asyncio.Lock; повторный add того же event_id → EventAlreadyExistsError"
    - "InMemoryEventStore.update под asyncio.Lock; вызов на отсутствующий id → EventNotFoundError"
    - "update возвращает (Event, EventState) — (new_event, previous_state)"
    - "get_by_id и list_all НЕ берут lock (lock-free reads, D-15)"
    - "Возвращаемые Event'ы — frozen Pydantic v2 (mutation через caller невозможна)"
    - "Concurrent asyncio.gather на 100 add разных event_id — все 100 сохраняются"
    - "Concurrent asyncio.gather на add+add того же event_id — ровно 1 успех, остальные EventAlreadyExistsError"
  artifacts:
    - path: "src/line_provider/infrastructure/store/in_memory.py"
      provides: "InMemoryEventStore + EventAlreadyExistsError + EventNotFoundError"
      exports: ["InMemoryEventStore", "EventAlreadyExistsError", "EventNotFoundError"]
    - path: "tests/line_provider/test_in_memory_store.py"
      provides: "Unit-тесты на CRUD + concurrent gather + lock-free reads"
  key_links:
    - from: "src/line_provider/infrastructure/store/in_memory.py"
      to: "asyncio.Lock"
      via: "self._lock = asyncio.Lock(); async with self._lock"
      pattern: "async with self._lock"
    - from: "src/line_provider/infrastructure/store/in_memory.py"
      to: "src/line_provider/schemas/events.py"
      via: "import Event, EventState"
      pattern: "from line_provider.schemas.events import"
---

<objective>
In-memory event store с `asyncio.Lock` на write-операциях и lock-free reads. Возвращает snapshots (frozen Event'ы) — внешний caller не может ничего мутировать через returned references (D-16).

Purpose: LP-01 — главное технологическое требование Phase 2 («хранение событий в in-memory структуре, защищённой asyncio.Lock от гонок»). Antipattern 6 из PITFALLS.md — concurrent dict access — митигируется здесь. `update()` возвращает кортеж `(new_event, previous_state)` чтобы Plan 02-05 interactor мог решить про publish (D-12) без второго round-trip в store.

Output: 1 infrastructure модуль (InMemoryEventStore + 2 доменных Exception) + 1 unit test (включая concurrent gather scenarios).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-line-provider-domain/02-CONTEXT.md
@.planning/phases/02-line-provider-domain/02-RESEARCH.md
@.planning/phases/02-line-provider-domain/02-PATTERNS.md
@.planning/phases/02-line-provider-domain/02-02-SUMMARY.md

<interfaces>
<!-- Exported contract — consumed by 02-05 interactors and 02-06 selectors. -->
From src/line_provider/infrastructure/store/in_memory.py (this plan):
```python
class EventAlreadyExistsError(Exception): ...
class EventNotFoundError(Exception): ...

class InMemoryEventStore:
    def __init__(self) -> None: ...
    async def add(self, event: Event) -> Event: ...   # raises EventAlreadyExistsError
    async def update(
        self,
        event_id: UUID,
        *,
        coefficient: Decimal,
        deadline: datetime,
        state: EventState,
    ) -> tuple[Event, EventState]:  # returns (new_event, previous_state); raises EventNotFoundError
        ...
    async def get_by_id(self, event_id: UUID) -> Event | None: ...   # lock-free
    async def list_all(self) -> list[Event]: ...                     # lock-free, snapshot
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create infrastructure/store/in_memory.py (store + domain exceptions + asyncio.Lock)</name>
  <files>src/line_provider/infrastructure/__init__.py, src/line_provider/infrastructure/store/__init__.py, src/line_provider/infrastructure/store/in_memory.py</files>
  <read_first>
    - src/line_provider/schemas/events.py (Event, EventState — созданы в Plan 02-02)
    - .planning/phases/02-line-provider-domain/02-RESEARCH.md §«Pattern 1» (строки 263–333) + §«Pitfall 5/6» (строки 795–805)
    - .planning/phases/02-line-provider-domain/02-PATTERNS.md §«§1 File-to-analog mapping → in_memory.py»
    - .planning/phases/02-line-provider-domain/02-CONTEXT.md D-15, D-16
  </read_first>
  <behavior>
    add:
    - Принимает frozen Event
    - Если event_id уже есть в _data → raise EventAlreadyExistsError(str(event_id))
    - Иначе кладёт в _data и возвращает тот же Event
    - Берёт asyncio.Lock — конкурентные add того же id → ровно один успех

    update:
    - Принимает event_id + новые coefficient/deadline/state
    - Если event_id отсутствует → raise EventNotFoundError(str(event_id))
    - Иначе строит new_event через model_copy(update={...}) с новыми полями
    - Возвращает (new_event, previous_state) — кортеж под локом, консистентный

    get_by_id:
    - Lock-free; возвращает Event (frozen) или None
    - Никаких side effects

    list_all:
    - Lock-free; возвращает list(self._data.values()) snapshot (новый list-объект, изменения dict после вызова не видны вызывающему)
  </behavior>
  <action>
    1. Создать `src/line_provider/infrastructure/__init__.py` (1 строка — пустой).
    2. Создать `src/line_provider/infrastructure/store/__init__.py` (1 строка — пустой).

    3. Создать `src/line_provider/infrastructure/store/in_memory.py`:
       ```python
       from __future__ import annotations

       import asyncio
       from datetime import datetime
       from decimal import Decimal
       from uuid import UUID

       from line_provider.schemas.events import Event, EventState


       class EventAlreadyExistsError(Exception):
           def __init__(self, event_id: str) -> None:
               self.event_id = event_id
               super().__init__(f"event {event_id} already exists")


       class EventNotFoundError(Exception):
           def __init__(self, event_id: str) -> None:
               self.event_id = event_id
               super().__init__(f"event {event_id} not found")


       class InMemoryEventStore:
           def __init__(self) -> None:
               self._data: dict[UUID, Event] = {}
               self._lock = asyncio.Lock()

           async def add(self, event: Event) -> Event:
               async with self._lock:
                   if event.event_id in self._data:
                       raise EventAlreadyExistsError(str(event.event_id))
                   self._data[event.event_id] = event
                   return event

           async def update(
               self,
               event_id: UUID,
               *,
               coefficient: Decimal,
               deadline: datetime,
               state: EventState,
           ) -> tuple[Event, EventState]:
               async with self._lock:
                   current = self._data.get(event_id)
                   if current is None:
                       raise EventNotFoundError(str(event_id))
                   previous_state = current.state
                   new_event = current.model_copy(
                       update={
                           "coefficient": coefficient,
                           "deadline": deadline,
                           "state": state,
                       }
                   )
                   self._data[event_id] = new_event
                   return new_event, previous_state

           async def get_by_id(self, event_id: UUID) -> Event | None:
               return self._data.get(event_id)

           async def list_all(self) -> list[Event]:
               return list(self._data.values())
       ```

       Замечания:
       - `model_copy(update={...})` на frozen Pydantic v2 возвращает новый объект — не мутирует current.
       - `get_by_id` и `list_all` — async для единообразия (interactor/selector могут быть async), но НЕ берут lock. Это безопасно: dict.get / list(...) — атомарны на уровне CPython bytecode (single GIL-протекторов), а Event frozen.
       - Никаких docstrings и комментариев. Стиль точно как в settings/config.py.
       - Никаких `typing.Optional` — используем PEP 604 `Event | None`.
  </action>
  <verify>
    <!-- W-2 (revision): replaced brittle inline `python -c` smoke with a pytest-based smoke test
         co-located in tests/line_provider/test_in_memory_store.py (added by Task 2 of this plan).
         The smoke fixture covers: add → duplicate raise → update returns (new, prev) → not-found raise → get_by_id → list_all.
         If Task 2 has not yet been executed, this verify intentionally fails — execute Task 2 first. -->
    <automated>uv run pytest tests/line_provider/test_in_memory_store.py::test_smoke_crud_lifecycle -q</automated>
    <automated>uv run mypy --strict src/line_provider/infrastructure</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "class InMemoryEventStore" src/line_provider/infrastructure/store/in_memory.py`
    - `grep -q "class EventAlreadyExistsError" src/line_provider/infrastructure/store/in_memory.py`
    - `grep -q "class EventNotFoundError" src/line_provider/infrastructure/store/in_memory.py`
    - `grep -c "async with self._lock" src/line_provider/infrastructure/store/in_memory.py` == 2 (add + update)
    - `grep -q "self._lock = asyncio.Lock()" src/line_provider/infrastructure/store/in_memory.py`
    - `grep -q "tuple\[Event, EventState\]" src/line_provider/infrastructure/store/in_memory.py`
    - `grep -q "model_copy" src/line_provider/infrastructure/store/in_memory.py`
    - `grep -c "^# " src/line_provider/infrastructure/store/in_memory.py` == 0 (no comments)
    - `uv run mypy --strict src/line_provider/infrastructure` exit 0
    - `uv run ruff check src/line_provider/infrastructure` → All checks passed
  </acceptance_criteria>
  <done>InMemoryEventStore работает с lock, возвращает (new, previous_state) кортеж, домейн exceptions определены.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Unit test — tests/line_provider/test_in_memory_store.py (CRUD + concurrent gather)</name>
  <files>tests/line_provider/test_in_memory_store.py</files>
  <read_first>
    - src/line_provider/infrastructure/store/in_memory.py (созданный в Task 1)
    - tests/line_provider/test_health.py (Excerpt H — стиль)
    - .planning/phases/02-line-provider-domain/02-RESEARCH.md §«Failure-Mode Classes Covered» (concurrent dict mutation)
  </read_first>
  <behavior>
    Покрыть:
    0. test_smoke_crud_lifecycle — единый smoke (для verify Task 1): add → duplicate → update returns (new, prev) → not-found → get_by_id → list_all. Заменяет inline `python -c` из старой ревизии плана (W-2 revision).
    1. test_add_returns_event — add возвращает тот же Event
    2. test_add_persists_event — после add, get_by_id видит event
    3. test_add_duplicate_raises — повторный add того же id → EventAlreadyExistsError
    4. test_update_returns_new_and_previous — update возвращает (new, previous_state) корректно
    5. test_update_creates_new_object — new_event != current (model_copy returns new)
    6. test_update_no_op_state_preserves_previous — update(state=current) returns (new, previous=current.state)
    7. test_update_missing_raises — update на отсутствующий id → EventNotFoundError
    8. test_get_by_id_returns_none_for_missing — get_by_id(random_uuid) is None
    9. test_list_all_returns_snapshot — после list_all и последующего add, ранее возвращённый list не меняется
    10. test_concurrent_add_distinct_ids_all_succeed — gather(100 add разных) → все 100 сохраняются
    11. test_concurrent_add_same_id_exactly_one_succeeds — gather(20 add того же id) → 1 успех + 19 EventAlreadyExistsError
    12. test_concurrent_update_sees_consistent_previous — gather(2 update того же id NEW→FINISHED_WIN и NEW→FINISHED_LOSE), оба update'а под локом сериализуются, второй видит previous_state != NEW (TOCTOU mitigation в interactor; здесь убеждаемся что store сам по себе консистентен)
  </behavior>
  <action>
    Создать `tests/line_provider/test_in_memory_store.py`:
    ```python
    """Unit tests for line_provider.infrastructure.store.in_memory.

    LP-01: in-memory storage guarded by asyncio.Lock.
    LP-08: update() returns (new, previous_state) for publish-decision-without-rtrip.
    Anti-Pattern 6 (PITFALLS.md): concurrent dict access mitigated by single asyncio.Lock.
    """

    from __future__ import annotations

    import asyncio
    from datetime import datetime, timedelta, timezone
    from decimal import Decimal
    from uuid import UUID, uuid4

    import pytest

    from line_provider.infrastructure.store.in_memory import (
        EventAlreadyExistsError,
        EventNotFoundError,
        InMemoryEventStore,
    )
    from line_provider.schemas.events import Event, EventState


    def _future() -> datetime:
        return datetime.now(timezone.utc) + timedelta(hours=1)


    def _event(state: EventState = EventState.NEW, event_id: UUID | None = None) -> Event:
        return Event(
            event_id=event_id or uuid4(),
            coefficient=Decimal("1.50"),
            deadline=_future(),
            state=state,
        )


    async def test_add_returns_event() -> None:
        """LP-01: add returns the stored event."""
        store = InMemoryEventStore()
        event = _event()
        result = await store.add(event)
        assert result == event


    async def test_add_persists_event() -> None:
        """LP-01: stored event is retrievable via get_by_id."""
        store = InMemoryEventStore()
        event = _event()
        await store.add(event)
        assert await store.get_by_id(event.event_id) == event


    async def test_add_duplicate_raises() -> None:
        """LP-01: duplicate event_id raises EventAlreadyExistsError."""
        store = InMemoryEventStore()
        event = _event()
        await store.add(event)
        with pytest.raises(EventAlreadyExistsError) as exc:
            await store.add(event)
        assert str(event.event_id) in str(exc.value)


    async def test_update_returns_new_and_previous() -> None:
        """LP-08: update returns (new_event, previous_state)."""
        store = InMemoryEventStore()
        event = _event(state=EventState.NEW)
        await store.add(event)
        new_event, previous = await store.update(
            event.event_id,
            coefficient=Decimal("3.00"),
            deadline=event.deadline,
            state=EventState.FINISHED_WIN,
        )
        assert previous == EventState.NEW
        assert new_event.state == EventState.FINISHED_WIN
        assert new_event.coefficient == Decimal("3.00")
        assert new_event.event_id == event.event_id


    async def test_update_creates_new_object_not_mutating_current() -> None:
        """D-16/D-17: update produces a new frozen object; original kept references stay intact."""
        store = InMemoryEventStore()
        event = _event(state=EventState.NEW)
        await store.add(event)
        snapshot = await store.get_by_id(event.event_id)
        assert snapshot is not None
        await store.update(
            event.event_id,
            coefficient=Decimal("9.00"),
            deadline=event.deadline,
            state=EventState.FINISHED_LOSE,
        )
        assert snapshot.coefficient == Decimal("1.50")
        assert snapshot.state == EventState.NEW


    async def test_update_no_op_state_preserves_previous_marker() -> None:
        """D-09: previous_state reports the state BEFORE update, even on no-op."""
        store = InMemoryEventStore()
        event = _event(state=EventState.FINISHED_WIN)
        await store.add(event)
        new_event, previous = await store.update(
            event.event_id,
            coefficient=Decimal("4.00"),
            deadline=event.deadline,
            state=EventState.FINISHED_WIN,
        )
        assert previous == EventState.FINISHED_WIN
        assert new_event.state == EventState.FINISHED_WIN
        assert new_event.coefficient == Decimal("4.00")


    async def test_update_missing_raises() -> None:
        """LP-08: update on absent id raises EventNotFoundError."""
        store = InMemoryEventStore()
        with pytest.raises(EventNotFoundError):
            await store.update(
                uuid4(),
                coefficient=Decimal("1.00"),
                deadline=_future(),
                state=EventState.NEW,
            )


    async def test_get_by_id_returns_none_for_missing() -> None:
        """LP-01: get_by_id returns None when event absent (caller decides 404)."""
        store = InMemoryEventStore()
        assert await store.get_by_id(uuid4()) is None


    async def test_list_all_returns_snapshot() -> None:
        """LP-01/D-16: list_all returns a snapshot; later add does not retroactively appear."""
        store = InMemoryEventStore()
        await store.add(_event())
        snapshot = await store.list_all()
        await store.add(_event())
        assert len(snapshot) == 1
        assert len(await store.list_all()) == 2


    async def test_concurrent_add_distinct_ids_all_succeed() -> None:
        """LP-01: asyncio.gather of 100 add() with distinct ids — all stored."""
        store = InMemoryEventStore()
        events = [_event() for _ in range(100)]
        await asyncio.gather(*[store.add(e) for e in events])
        assert len(await store.list_all()) == 100


    async def test_concurrent_add_same_id_exactly_one_succeeds() -> None:
        """LP-01: 20 concurrent add() of same event_id — exactly 1 succeeds."""
        store = InMemoryEventStore()
        event = _event()
        results = await asyncio.gather(
            *[store.add(event) for _ in range(20)],
            return_exceptions=True,
        )
        successes = [r for r in results if isinstance(r, Event)]
        failures = [r for r in results if isinstance(r, EventAlreadyExistsError)]
        assert len(successes) == 1
        assert len(failures) == 19


    async def test_smoke_crud_lifecycle() -> None:
        """W-2 revision: smoke replaces inline python -c verify from Task 1.

        Covers: add -> duplicate raise -> update returns (new, prev) -> not-found raise -> get_by_id -> list_all.
        """
        store = InMemoryEventStore()
        eid = uuid4()
        ev = Event(
            event_id=eid,
            coefficient=Decimal("1.50"),
            deadline=_future(),
            state=EventState.NEW,
        )
        assert (await store.add(ev)).event_id == eid
        with pytest.raises(EventAlreadyExistsError):
            await store.add(ev)
        new_ev, prev = await store.update(
            eid,
            coefficient=Decimal("2.00"),
            deadline=ev.deadline,
            state=EventState.FINISHED_WIN,
        )
        assert prev == EventState.NEW
        assert new_ev.state == EventState.FINISHED_WIN
        with pytest.raises(EventNotFoundError):
            await store.update(
                uuid4(),
                coefficient=Decimal("1.00"),
                deadline=ev.deadline,
                state=EventState.NEW,
            )
        retrieved = await store.get_by_id(eid)
        assert retrieved is not None
        assert retrieved.state == EventState.FINISHED_WIN
        assert len(await store.list_all()) == 1


    async def test_concurrent_update_serialised_under_lock() -> None:
        """LP-08/Anti-Pattern 6: two concurrent update() on same id are serialised — the second
        observes previous_state set by the first, not the original state.
        """
        store = InMemoryEventStore()
        event = _event(state=EventState.NEW)
        await store.add(event)

        async def do(new_state: EventState) -> tuple[Event, EventState]:
            return await store.update(
                event.event_id,
                coefficient=Decimal("2.00"),
                deadline=event.deadline,
                state=new_state,
            )

        results = await asyncio.gather(
            do(EventState.FINISHED_WIN),
            do(EventState.FINISHED_LOSE),
        )
        previous_states = [prev for (_, prev) in results]
        assert EventState.NEW in previous_states
        assert any(p != EventState.NEW for p in previous_states)
    ```
  </action>
  <verify>
    <automated>uv run pytest tests/line_provider/test_in_memory_store.py -q -x &amp;&amp; uv run mypy --strict tests/line_provider/test_in_memory_store.py &amp;&amp; uv run ruff check tests/line_provider/test_in_memory_store.py</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/line_provider/test_in_memory_store.py -q` → 13 passed (including test_smoke_crud_lifecycle per W-2 revision)
    - `grep -c "^async def test_" tests/line_provider/test_in_memory_store.py` == 13
    - `grep -q "asyncio.gather" tests/line_provider/test_in_memory_store.py` (хотя бы 2 раза для concurrent тестов)
    - `grep -c "LP-01\|LP-08\|D-16\|D-09\|Anti-Pattern" tests/line_provider/test_in_memory_store.py` >= 6
    - `uv run mypy --strict tests/line_provider/test_in_memory_store.py` exit 0
  </acceptance_criteria>
  <done>Store covered unit-тестами включая concurrent gather (Anti-Pattern 6 mitigation), все mypy strict.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| interactor → store (write) | Под `asyncio.Lock`; pre-validated Event'ы прошли Pydantic |
| selector → store (read) | Lock-free; возвращает snapshots (frozen Pydantic) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering | Concurrent dict mutation (Anti-Pattern 6) | mitigate | `asyncio.Lock` на add/update — гарантирует атомарность check+write. Unit-тест `test_concurrent_add_same_id` доказывает: 20 concurrent → ровно 1 успех. |
| T-04-02 | Tampering | Caller мутирует Event'ы возвращённые store | mitigate | `Event(frozen=True)` (D-17): попытка setattr → ValidationError. Подтверждено тестом из Plan 02-02 (`test_frozen`). |
| T-04-03 | Repudiation | Lost update без previous_state | mitigate | `update()` возвращает `(new, previous_state)` под локом — атомарный кортеж. Interactor использует это для решения о publish. |
| T-04-04 | Denial of Service | Unbounded growth dict | accept | ТЗ требует in-memory store; в P2 ограничения не введены. Документировано как scope of test task (RESEARCH.md Runtime State Inventory). |
| T-04-05 | Information Disclosure | `str(EventAlreadyExistsError)` leak'ает event_id | accept | event_id — UUID4, client-generated; не PII. 409 message «event {id} already exists» — приемлемая UX. |
| T-04-06 | Tampering | TOCTOU между `get_by_id` (lock-free) и `update` (под локом) | mitigate | Решение в `update()` под локом: повторно читает current → выдаёт consistent `(new, previous_state)`. Interactor (Plan 02-05) использует previous_state для решения о publish (Pitfall 5 mitigation). |
</threat_model>

<verification>
- `uv run pytest tests/line_provider/test_in_memory_store.py -q` → 13 passed (including test_smoke_crud_lifecycle per W-2 revision)
- `uv run mypy --strict src/line_provider/infrastructure tests/line_provider/test_in_memory_store.py` → 0 errors
- `uv run ruff check src/line_provider/infrastructure tests/line_provider/test_in_memory_store.py` → All checks passed
- `uv run pytest -q` → P1 baseline + plan 02-02 schemas + plan 02-03 state_machine + plan 02-04 store все зелёные
</verification>

<success_criteria>
- InMemoryEventStore с lock-guarded writes + lock-free reads + snapshots
- update() returns (new, previous_state) tuple — основа для commit→publish ordering в Plan 02-05
- Concurrent gather тесты покрывают Anti-Pattern 6
- mypy strict + ruff чисто
</success_criteria>

<output>
After completion, create `.planning/phases/02-line-provider-domain/02-04-SUMMARY.md`
</output>
