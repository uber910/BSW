---
phase: 02-line-provider-domain
plan: 07
type: execute
wave: 4
depends_on: [05, 06]
files_modified:
  - src/line_provider/entrypoints/api/__init__.py
  - src/line_provider/entrypoints/api/events.py
  - src/line_provider/entrypoints/lifespan.py
  - src/line_provider/app.py
  - tests/line_provider/test_event_routes.py
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
autonomous: true
requirements: [LP-01, LP-02, LP-03, LP-04, LP-05, LP-07, LP-08, QA-04, QA-05]
tags: [line-provider, http-api, routes, lifespan-wiring, integration-tests, phase-gate, coverage]

# Note (W-5, checker WARNING — accepted): depends_on=[05, 06] is intentionally narrow.
# Plan 05 transitively depends on 02, 03, 04; plan 06 transitively depends on 02, 04.
# Adding all of [02, 03, 04, 05, 06] would be redundant — the DAG is correct via transitivity,
# and execution waves derive correctly from the existing edges.

must_haves:
  truths:
    - "POST /event 201 на новый event_id; 409 на дубль; 422 на coefficient ≤ 0; 422 на deadline в прошлом; 422 на extra поле"
    - "PUT /event/{event_id} 200 на корректный update (full body upsert per D-06); 404 на отсутствующий; 422 на reverse transition с понятным detail; 200 на no-op state (publish skipped)"
    - "GET /event/{event_id} 200 + EventRead; 404 если нет"
    - "GET /events возвращает только active (state=NEW AND deadline>now)"
    - "GET /health 200 + {\"status\":\"ok\"} (P1 invariant)"
    - "X-Request-ID echo'ится в response headers всех 4 новых route'ов (P1 A7 mitigation остаётся целым)"
    - "Lifespan создаёт InMemoryEventStore() и NoopEventBus() в app.state ДО первого request"
    - "Phase-gate: coverage ≥85% на src/line_provider/{helpers,interactors,selectors,infrastructure,facades}"
  artifacts:
    - path: "src/line_provider/entrypoints/api/events.py"
      provides: "APIRouter(tags=['events']) с 4 endpoints"
      exports: ["router"]
    - path: "src/line_provider/entrypoints/lifespan.py"
      provides: "lifespan расширен app.state.event_store + event_bus"
      contains: "app.state.event_store = InMemoryEventStore()"
    - path: "src/line_provider/app.py"
      provides: "build_app() подключает events router"
      contains: "app.include_router(events.router)"
    - path: "tests/line_provider/test_event_routes.py"
      provides: "Integration matrix: POST/PUT/GET/GET-list + happy paths + error codes + request-id propagation + publish ordering"
  key_links:
    - from: "src/line_provider/app.py"
      to: "src/line_provider/entrypoints/api/events.py"
      via: "app.include_router(events.router)"
      pattern: "include_router\\(events.router\\)"
    - from: "src/line_provider/entrypoints/lifespan.py"
      to: "src/line_provider/infrastructure/store/in_memory.py"
      via: "app.state.event_store = InMemoryEventStore()"
      pattern: "app.state.event_store"
    - from: "src/line_provider/entrypoints/lifespan.py"
      to: "src/line_provider/facades/event_bus.py"
      via: "app.state.event_bus = NoopEventBus()"
      pattern: "app.state.event_bus"
    - from: "src/line_provider/entrypoints/api/events.py"
      to: "src/line_provider/facades/deps.py"
      via: "StoreDep, EventBusDep injections"
      pattern: "StoreDep|EventBusDep"
---

<objective>
Wave 4 финал Phase 2 (W-2 revision). Собираем все слои воедино:

1. **API routes (4)** — `entrypoints/api/events.py` с thin handlers, которые вызывают interactors/selectors, переводят доменные исключения в HTTP status codes:
   - `POST /event` → 201 / 409 / 422
   - `PUT /event/{event_id}` → 200 / 404 / 422 (full body upsert per D-06; внутри handler делегирует в `set_event_state` interactor для state-transition + mutation under lock)
   - `GET /event/{event_id}` → 200 / 404
   - `GET /events` → 200 (list of active)

2. **Lifespan wiring** — в `entrypoints/lifespan.py` создаются `InMemoryEventStore` + `NoopEventBus` в `app.state` (D-14).

3. **build_app** — подключение `events.router` рядом с `health.router`.

4. **Integration tests** — `tests/line_provider/test_event_routes.py`: полная матрица HTTP-кейсов через `httpx.AsyncClient` + LifespanManager (Plan 02-01).

5. **Phase-gate** — финальный полный suite + coverage ≥85% на line-provider domain layer.

Purpose: Это единственный план в P2 c integration-тестами; здесь проверяется что все Wave 0/1/2 слои реально работают через HTTP. Phase 2 success criteria (6 пунктов в ROADMAP.md) валидируются именно тестами этого плана.

Output: 4 production файла (1 новый routes module + 2 modified — lifespan/app + 1 minor __init__ stub) + 1 integration test файл + green phase-gate.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-line-provider-domain/02-CONTEXT.md
@.planning/phases/02-line-provider-domain/02-RESEARCH.md
@.planning/phases/02-line-provider-domain/02-PATTERNS.md
@.planning/phases/02-line-provider-domain/02-VALIDATION.md
@.planning/phases/02-line-provider-domain/02-05-SUMMARY.md
@.planning/phases/02-line-provider-domain/02-06-SUMMARY.md
@.planning/phases/02-line-provider-domain/02-04-SUMMARY.md
@.planning/phases/02-line-provider-domain/02-03-SUMMARY.md
@.planning/phases/02-line-provider-domain/02-02-SUMMARY.md
@.planning/phases/02-line-provider-domain/02-01-SUMMARY.md
@.planning/phases/01-skeleton-infrastructure/01-07-SUMMARY.md
@src/line_provider/app.py
@src/line_provider/entrypoints/lifespan.py
@src/line_provider/entrypoints/api/health.py
@src/line_provider/entrypoints/middleware.py
@tests/line_provider/conftest.py
@CLAUDE.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create entrypoints/api/events.py — APIRouter with 4 handlers (POST/PUT/GET-id/GET-list)</name>
  <files>src/line_provider/entrypoints/api/events.py</files>
  <read_first>
    - src/line_provider/entrypoints/api/health.py (Excerpt D — route module shape)
    - src/line_provider/facades/deps.py (StoreDep, EventBusDep — Plan 02-05)
    - src/line_provider/interactors/create_event.py (Plan 02-05)
    - src/line_provider/interactors/set_event_state.py (Plan 02-05)
    - src/line_provider/selectors/get_event_by_id.py (Plan 02-06)
    - src/line_provider/selectors/list_active_events.py (Plan 02-06)
    - src/line_provider/schemas/events.py (EventCreate, EventUpdate, EventRead — Plan 02-02)
    - src/line_provider/infrastructure/store/in_memory.py (Plan 02-04)
    - src/line_provider/helpers/state_machine.py (TransitionForbiddenError — Plan 02-03)
    - .planning/phases/02-line-provider-domain/02-RESEARCH.md §«Pattern 5: HTTP routes как тонкий слой» (строки 510–578)
    - .planning/phases/02-line-provider-domain/02-CONTEXT.md D-01, D-06 (PUT — full body upsert семантика, маршрут `PUT /event/{event_id}`)
  </read_first>
  <behavior>
    POST /event:
    - body: EventCreate
    - вызывает create_event(store, body=body)
    - 201 + EventRead.model_validate(event.model_dump())
    - EventAlreadyExistsError → HTTPException(409, detail=str(exc))

    PUT /event/{event_id} (per D-01/D-06, маршрут без `/state` суффикса; full body PUT):
    - path: event_id: UUID
    - body: EventUpdate (full replace — coefficient, deadline, state per D-06)
    - извлекает correlation_id из request.headers.get("X-Request-ID", "no-request-id")
    - делегирует в `set_event_state` interactor (он содержит state-machine check + store.update under lock + commit→publish ordering).
      Note (W-1 revision): отдельного route `/state` нет; D-06 фиксирует, что PUT обрабатывает полный body upsert на существующем event'е. Семантика «PUT создаёт event если отсутствует» в P2 НЕ реализуется: D-06 говорит «полный replace кроме event_id», D-03 говорит «PUT на несуществующее → 404». То есть PUT — это «full update», а не «upsert with create». Создание остаётся прерогативой POST /event.
    - 200 + EventRead
    - EventNotFoundError → HTTPException(404)
    - TransitionForbiddenError → HTTPException(422, detail=f"state transition {exc.current.value}->{exc.new.value} not allowed")

    GET /event/{event_id}:
    - вызывает get_event_by_id(store, event_id=event_id)
    - 200 + EventRead если есть; 404 (HTTPException) если None

    GET /events:
    - вызывает list_active_events(store)
    - 200 + list[EventRead]
  </behavior>
  <action>
    Создать `src/line_provider/entrypoints/api/events.py`:
    ```python
    from __future__ import annotations

    from uuid import UUID

    from fastapi import APIRouter, HTTPException, Request, status

    from line_provider.facades.deps import EventBusDep, StoreDep
    from line_provider.helpers.state_machine import TransitionForbiddenError
    from line_provider.infrastructure.store.in_memory import (
        EventAlreadyExistsError,
        EventNotFoundError,
    )
    from line_provider.interactors.create_event import create_event
    from line_provider.interactors.set_event_state import set_event_state
    from line_provider.schemas.events import EventCreate, EventRead, EventUpdate
    from line_provider.selectors.get_event_by_id import get_event_by_id
    from line_provider.selectors.list_active_events import list_active_events


    router = APIRouter(tags=["events"])


    @router.post(
        "/event",
        status_code=status.HTTP_201_CREATED,
        response_model=EventRead,
    )
    async def post_event(body: EventCreate, store: StoreDep) -> EventRead:
        try:
            event = await create_event(store, body=body)
        except EventAlreadyExistsError as exc:
            raise HTTPException(
                status_code=status.HTTP_409_CONFLICT,
                detail=str(exc),
            ) from exc
        return EventRead.model_validate(event.model_dump())


    @router.put(
        "/event/{event_id}",
        status_code=status.HTTP_200_OK,
        response_model=EventRead,
    )
    async def put_event(
        event_id: UUID,
        body: EventUpdate,
        request: Request,
        store: StoreDep,
        event_bus: EventBusDep,
    ) -> EventRead:
        correlation_id = request.headers.get("X-Request-ID", "no-request-id")
        try:
            event = await set_event_state(
                store,
                event_bus,
                event_id=event_id,
                coefficient=body.coefficient,
                deadline=body.deadline,
                new_state=body.state,
                correlation_id=correlation_id,
            )
        except EventNotFoundError as exc:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND) from exc
        except TransitionForbiddenError as exc:
            raise HTTPException(
                status_code=status.HTTP_422_UNPROCESSABLE_ENTITY,
                detail=f"state transition {exc.current.value}->{exc.new.value} not allowed",
            ) from exc
        return EventRead.model_validate(event.model_dump())


    @router.get(
        "/event/{event_id}",
        response_model=EventRead,
    )
    async def get_event(event_id: UUID, store: StoreDep) -> EventRead:
        event = await get_event_by_id(store, event_id=event_id)
        if event is None:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND)
        return EventRead.model_validate(event.model_dump())


    @router.get(
        "/events",
        response_model=list[EventRead],
    )
    async def list_events(store: StoreDep) -> list[EventRead]:
        events = await list_active_events(store)
        return [EventRead.model_validate(e.model_dump()) for e in events]
    ```

    Замечания:
    - `EventRead.model_validate(event.model_dump())` — round-trip через dict; альтернатива `model_validate(event, from_attributes=True)` тоже валидна, но `model_dump()` явнее и не использует SQLAlchemy-style flag.
    - `correlation_id` берётся из header `X-Request-ID` (P1 middleware echo'ит этот же header, см. RequestContextMiddleware). Дефолт `"no-request-id"` — для тестов, где client не подставляет header.
    - `from ... as exc ... raise ... from exc` — соблюдает PEP 3134 (chain exceptions) и ruff B904 правило.
    - НЕТ docstrings/комментариев (P1 convention).
    - `Request` параметр для PUT нужен только для headers.get — другие route не требуют Request, не добавляем.
    - PUT routed на `/event/{event_id}` per D-01/D-06 (W-1 revision: откат `/state` суффикса). Route handler делегирует в `set_event_state` interactor — он остаётся единым местом, где живут state-machine check + commit→publish ordering. Метод+путь PUT отличает его от GET того же пути, поэтому никакого конфликта; (method, path)-пары: POST /event, PUT /event/{event_id}, GET /event/{event_id}, GET /events.
  </action>
  <verify>
    <automated>uv run python -c "
from line_provider.entrypoints.api.events import router
api_routes = [r for r in router.routes if hasattr(r, 'methods')]
pairs = sorted({(tuple(sorted(r.methods)), r.path) for r in api_routes})
expected = sorted({
    (('POST',), '/event'),
    (('GET',), '/event/{event_id}'),
    (('PUT',), '/event/{event_id}'),
    (('GET',), '/events'),
})
assert pairs == expected, f'unexpected routes: {pairs}'
"
    </automated>
    <automated>uv run mypy --strict src/line_provider/entrypoints/api/events.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "router = APIRouter(tags=\[\"events\"\])" src/line_provider/entrypoints/api/events.py`
    - `grep -c "@router\." src/line_provider/entrypoints/api/events.py` == 4 (4 декоратора: POST/PUT/GET/GET)
    - `grep -q "\"/event/{event_id}\"" src/line_provider/entrypoints/api/events.py` (PUT и GET оба используют этот путь)
    - `grep -c "\"/event/{event_id}/state\"" src/line_provider/entrypoints/api/events.py` == 0 (W-1 revision: суффикс `/state` удалён)
    - `grep -q "status.HTTP_201_CREATED" src/line_provider/entrypoints/api/events.py`
    - `grep -q "status.HTTP_409_CONFLICT" src/line_provider/entrypoints/api/events.py`
    - `grep -q "status.HTTP_404_NOT_FOUND" src/line_provider/entrypoints/api/events.py`
    - `grep -q "status.HTTP_422_UNPROCESSABLE_ENTITY" src/line_provider/entrypoints/api/events.py`
    - `grep -q "correlation_id = request.headers.get(\"X-Request-ID\"" src/line_provider/entrypoints/api/events.py`
    - `grep -q "StoreDep" src/line_provider/entrypoints/api/events.py`
    - `grep -q "EventBusDep" src/line_provider/entrypoints/api/events.py`
    - `uv run mypy --strict src/line_provider/entrypoints/api/events.py` exit 0
    - `uv run ruff check src/line_provider/entrypoints/api/events.py` → All checks passed
  </acceptance_criteria>
  <done>events.py содержит 4 route handler'а с правильными status codes и exception mapping; PUT routed на `/event/{event_id}` per D-01/D-06.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire lifespan + app.py — register events router and singletons</name>
  <files>src/line_provider/entrypoints/lifespan.py, src/line_provider/app.py</files>
  <read_first>
    - src/line_provider/app.py (текущая 18-строчная версия)
    - src/line_provider/entrypoints/lifespan.py (текущая 24-строчная версия)
    - .planning/phases/02-line-provider-domain/02-PATTERNS.md §«Excerpt A» (app.py diff direction)
    - .planning/phases/02-line-provider-domain/02-PATTERNS.md §«Excerpt B» (lifespan.py diff direction)
    - .planning/phases/02-line-provider-domain/02-CONTEXT.md D-14
  </read_first>
  <behavior>
    app.py:
    - Импортирует events router рядом с health
    - Внутри build_app() после `app.include_router(health.router)` добавляет `app.include_router(events.router)`
    - Никакого изменения порядка `add_middleware` ↔ `include_router`

    lifespan.py:
    - Импортирует InMemoryEventStore + NoopEventBus + EventBus
    - Между `app.state.settings = settings` и `try:` создаёт `app.state.event_store = InMemoryEventStore()` и `app.state.event_bus: EventBus = NoopEventBus()`
    - Логирует `line_provider.startup` остаётся как было (включает service_name)
    - Структура try/yield/finally + log.info shutdown остаётся
  </behavior>
  <action>
    1. Заменить полностью содержимое `src/line_provider/app.py`:
       ```python
       from __future__ import annotations

       from fastapi import FastAPI

       from line_provider.entrypoints.api import events, health
       from line_provider.entrypoints.lifespan import lifespan
       from line_provider.entrypoints.middleware import RequestContextMiddleware


       def build_app() -> FastAPI:
           app = FastAPI(
               title="line-provider",
               version="0.1.0",
               lifespan=lifespan,
           )
           app.add_middleware(RequestContextMiddleware)
           app.include_router(health.router)
           app.include_router(events.router)
           return app
       ```

    2. Заменить полностью содержимое `src/line_provider/entrypoints/lifespan.py`:
       ```python
       from __future__ import annotations

       from collections.abc import AsyncIterator
       from contextlib import asynccontextmanager

       import structlog
       from fastapi import FastAPI

       from config.logging import configure_structlog
       from line_provider.facades.event_bus import EventBus, NoopEventBus
       from line_provider.infrastructure.store.in_memory import InMemoryEventStore
       from line_provider.settings.config import LineProviderSettings


       @asynccontextmanager
       async def lifespan(app: FastAPI) -> AsyncIterator[None]:
           settings = LineProviderSettings()
           configure_structlog(settings.log_level)
           log = structlog.get_logger()
           log.info("line_provider.startup", service=settings.service_name)
           app.state.settings = settings
           app.state.event_store = InMemoryEventStore()
           app.state.event_bus: EventBus = NoopEventBus()
           try:
               yield
           finally:
               log.info("line_provider.shutdown")
       ```

       Замечания:
       - Тип-аннотация `app.state.event_bus: EventBus = NoopEventBus()` — это hint для читающего; mypy и runtime игнорируют annotation на attribute assignment (FastAPI's `State` — это `types.SimpleNamespace`).
       - НЕ удалять `app.state.settings = settings` — оно может понадобиться в P5.
       - Никаких docstrings.
  </action>
  <verify>
    <automated>uv run python -c "
import asyncio
from asgi_lifespan import LifespanManager
from line_provider.app import build_app
from line_provider.infrastructure.store.in_memory import InMemoryEventStore
from line_provider.facades.event_bus import NoopEventBus

async def run():
    app = build_app()
    async with LifespanManager(app):
        assert isinstance(app.state.event_store, InMemoryEventStore)
        assert isinstance(app.state.event_bus, NoopEventBus)

asyncio.run(run())
"
    </automated>
    <automated>uv run mypy --strict src/line_provider/app.py src/line_provider/entrypoints/lifespan.py</automated>
    <automated>uv run pytest tests/line_provider/test_health.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "from line_provider.entrypoints.api import events, health" src/line_provider/app.py` (или две отдельные строки)
    - `grep -q "app.include_router(events.router)" src/line_provider/app.py`
    - `grep -q "app.include_router(health.router)" src/line_provider/app.py`
    - Order: `grep -n "include_router" src/line_provider/app.py` показывает health ПЕРЕД events (по line number)
    - `grep -q "from line_provider.facades.event_bus import EventBus, NoopEventBus" src/line_provider/entrypoints/lifespan.py`
    - `grep -q "from line_provider.infrastructure.store.in_memory import InMemoryEventStore" src/line_provider/entrypoints/lifespan.py`
    - `grep -q "app.state.event_store = InMemoryEventStore()" src/line_provider/entrypoints/lifespan.py`
    - `grep -q "app.state.event_bus" src/line_provider/entrypoints/lifespan.py` && `grep -q "NoopEventBus()" src/line_provider/entrypoints/lifespan.py`
    - `grep -q "app.state.settings = settings" src/line_provider/entrypoints/lifespan.py` (не удалили)
    - `uv run mypy --strict src/line_provider/app.py src/line_provider/entrypoints/lifespan.py` exit 0
    - `uv run pytest tests/line_provider/test_health.py -q` → 2 passed (P1 baseline остался)
  </acceptance_criteria>
  <done>app.py регистрирует events router; lifespan создаёт singleton'ы в app.state; P1 smoke tests остаются зелёными.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Integration tests — tests/line_provider/test_event_routes.py (4-route matrix + propagation)</name>
  <files>tests/line_provider/test_event_routes.py</files>
  <read_first>
    - tests/line_provider/conftest.py (updated в Plan 02-01 Task 3 — fixture `client` + `app`)
    - tests/line_provider/test_health.py (Excerpt H — REQ-ID docstring convention)
    - src/line_provider/entrypoints/api/events.py (Task 1 этого плана)
    - .planning/phases/02-line-provider-domain/02-CONTEXT.md D-01..D-10, D-19, D-20
    - .planning/phases/02-line-provider-domain/02-RESEARCH.md §«Validation Architecture» (строки 990–1012)
  </read_first>
  <behavior>
    Test classes (для organisation, не TestSuite):

    TestCreate:
    - test_post_event_returns_201 — happy path: POST с {event_id, coefficient, deadline} → 201 + EventRead с state=NEW
    - test_post_duplicate_returns_409 — повторный POST same id → 409
    - test_post_negative_coefficient_returns_422
    - test_post_zero_coefficient_returns_422
    - test_post_three_decimal_places_returns_422
    - test_post_past_deadline_returns_422
    - test_post_naive_deadline_returns_422
    - test_post_extra_field_returns_422
    - test_post_invalid_uuid_returns_422

    TestUpdate (все PUT идут на `/event/{event_id}` per D-01/D-06; W-1 revision: суффикс `/state` удалён):
    - test_put_event_returns_200 — PUT NEW→FINISHED_WIN → 200 + state=FINISHED_WIN
    - test_put_missing_returns_404
    - test_put_reverse_returns_422_with_detail — FINISHED_WIN→NEW: 422 + detail содержит "state transition FINISHED_WIN->NEW not allowed"
    - test_put_no_op_state_returns_200_no_publish — PUT same state (после setup'а FINISHED_WIN) → 200, coefficient/deadline обновлены
    - test_put_publishes_via_event_bus_on_terminal_transition — Свапнуть `app.state.event_bus` на FakeEventBus перед запросом; assert published list имеет 1 элемент

    TestGet:
    - test_get_event_by_id_returns_200 — после POST, GET → 200 + EventRead
    - test_get_missing_returns_404

    TestList:
    - test_get_events_returns_active — Setup 2 NEW (1 future, 1 past via store seed) + 1 FINISHED_WIN. **Monkey-patch `line_provider.selectors.list_active_events.utc_now`** на fixed_now чтобы фильтр был детерминирован (D-19). GET /events → 1 элемент (NEW+future).
    - test_get_events_empty — пустой store → 200 + []

    TestRequestId:
    - test_request_id_echoed_on_post — POST с header X-Request-ID: my-id → response header X-Request-ID == my-id
    - test_request_id_propagated_to_publish — POST + PUT NEW→FINISHED_WIN с X-Request-ID: trace-1 → FakeEventBus.calls[0][0].correlation_id == "trace-1"

    TestHealth (sanity, не дубль с test_health.py):
    - test_health_still_200_after_event_routes_added — гарантия что P1 invariant не сломан

    Конструкция fixture:
    - `client` fixture из conftest.py возвращает `AsyncClient` (требует `app` fixture, который оборачивает в LifespanManager).
    - Для тестов которые требуют доступа к store/event_bus напрямую — использовать fixture `app` (теперь публичный в conftest).
    - `FakeEventBus` импортируется из `tests.line_provider._fakes` (создан в Plan 02-05 Task 2).
  </behavior>
  <action>
    Создать `tests/line_provider/test_event_routes.py`:
    ```python
    """Integration tests for line_provider HTTP routes.

    LP-01..LP-08: full HTTP matrix over the 4 event routes.
    QA-05: integration via httpx.AsyncClient + ASGITransport + LifespanManager.
    D-01/D-06: PUT routed at /event/{event_id} (no /state suffix; W-1 revision).
    D-19: deterministic timing via monkey-patched utc_now where router-side filtering is involved.
    """

    from __future__ import annotations

    from datetime import datetime, timedelta, timezone
    from decimal import Decimal
    from uuid import uuid4

    import pytest
    from fastapi import FastAPI
    from httpx import AsyncClient

    from line_provider.infrastructure.store.in_memory import InMemoryEventStore
    from line_provider.schemas.events import Event, EventState
    from tests.line_provider._fakes import FakeEventBus


    def _iso_future(seconds: int = 3600) -> str:
        return (datetime.now(timezone.utc) + timedelta(seconds=seconds)).isoformat()


    def _iso_past() -> str:
        return (datetime.now(timezone.utc) - timedelta(hours=1)).isoformat()


    def _iso_naive_future() -> str:
        return (datetime.now() + timedelta(hours=1)).isoformat()


    def _create_body(coefficient: str = "1.50", deadline: str | None = None) -> dict[str, object]:
        return {
            "event_id": str(uuid4()),
            "coefficient": coefficient,
            "deadline": deadline or _iso_future(),
        }


    class TestCreate:
        async def test_post_event_returns_201(self, client: AsyncClient) -> None:
            """LP-03: POST /event 201 + EventRead with state=NEW."""
            body = _create_body()
            response = await client.post("/event", json=body)
            assert response.status_code == 201
            data = response.json()
            assert data["event_id"] == body["event_id"]
            assert data["state"] == "NEW"
            assert data["coefficient"] == "1.50"

        async def test_post_duplicate_returns_409(self, client: AsyncClient) -> None:
            """LP-03/D-03: duplicate event_id returns 409."""
            body = _create_body()
            await client.post("/event", json=body)
            response = await client.post("/event", json=body)
            assert response.status_code == 409
            assert "already exists" in response.json()["detail"]

        async def test_post_negative_coefficient_returns_422(self, client: AsyncClient) -> None:
            """LP-08: coefficient < 0 -> 422."""
            response = await client.post("/event", json=_create_body(coefficient="-1.00"))
            assert response.status_code == 422

        async def test_post_zero_coefficient_returns_422(self, client: AsyncClient) -> None:
            """LP-08: coefficient == 0 -> 422 (gt=0 enforced)."""
            response = await client.post("/event", json=_create_body(coefficient="0"))
            assert response.status_code == 422

        async def test_post_three_decimal_places_returns_422(self, client: AsyncClient) -> None:
            """LP-02: coefficient with >2 decimal places -> 422."""
            response = await client.post("/event", json=_create_body(coefficient="1.234"))
            assert response.status_code == 422

        async def test_post_past_deadline_returns_422(self, client: AsyncClient) -> None:
            """LP-08/D-07: deadline in the past -> 422 on POST."""
            response = await client.post("/event", json=_create_body(deadline=_iso_past()))
            assert response.status_code == 422

        async def test_post_naive_deadline_returns_422(self, client: AsyncClient) -> None:
            """LP-02: naive datetime -> 422 (AwareDatetime enforced)."""
            response = await client.post("/event", json=_create_body(deadline=_iso_naive_future()))
            assert response.status_code == 422

        async def test_post_extra_field_returns_422(self, client: AsyncClient) -> None:
            """D-04: extra fields forbidden by EventCreate(extra=\"forbid\")."""
            body = _create_body()
            body["state"] = "NEW"
            response = await client.post("/event", json=body)
            assert response.status_code == 422

        async def test_post_invalid_uuid_returns_422(self, client: AsyncClient) -> None:
            """LP-02/D-05: event_id must be a valid UUID."""
            body = _create_body()
            body["event_id"] = "not-a-uuid"
            response = await client.post("/event", json=body)
            assert response.status_code == 422


    class TestUpdate:
        async def test_put_event_returns_200(self, client: AsyncClient) -> None:
            """LP-03/D-01/D-06: PUT /event/{id} NEW->FINISHED_WIN returns 200 + EventRead (W-1 revision: route without /state suffix)."""
            create_body = _create_body()
            await client.post("/event", json=create_body)

            put_body = {
                "coefficient": "2.50",
                "deadline": _iso_future(),
                "state": "FINISHED_WIN",
            }
            response = await client.put(f"/event/{create_body['event_id']}", json=put_body)
            assert response.status_code == 200
            assert response.json()["state"] == "FINISHED_WIN"

        async def test_put_missing_returns_404(self, client: AsyncClient) -> None:
            """LP-04/D-03: PUT on absent event -> 404."""
            put_body = {
                "coefficient": "2.00",
                "deadline": _iso_future(),
                "state": "FINISHED_WIN",
            }
            response = await client.put(f"/event/{uuid4()}", json=put_body)
            assert response.status_code == 404

        async def test_put_reverse_returns_422_with_detail(self, client: AsyncClient) -> None:
            """LP-08/D-08: FINISHED_WIN->NEW returns 422 with explicit message."""
            create_body = _create_body()
            await client.post("/event", json=create_body)
            await client.put(
                f"/event/{create_body['event_id']}",
                json={
                    "coefficient": "2.00",
                    "deadline": _iso_future(),
                    "state": "FINISHED_WIN",
                },
            )
            response = await client.put(
                f"/event/{create_body['event_id']}",
                json={
                    "coefficient": "2.00",
                    "deadline": _iso_future(),
                    "state": "NEW",
                },
            )
            assert response.status_code == 422
            assert response.json()["detail"] == "state transition FINISHED_WIN->NEW not allowed"

        async def test_put_no_op_state_returns_200(self, client: AsyncClient, app: FastAPI) -> None:
            """D-09: PUT with state == current_state mutates fields, does NOT publish."""
            fake = FakeEventBus()
            app.state.event_bus = fake

            create_body = _create_body()
            await client.post("/event", json=create_body)
            response = await client.put(
                f"/event/{create_body['event_id']}",
                json={
                    "coefficient": "9.99",
                    "deadline": _iso_future(),
                    "state": "NEW",
                },
            )
            assert response.status_code == 200
            assert response.json()["coefficient"] == "9.99"
            assert fake.calls == []

        async def test_put_publishes_via_event_bus_on_terminal_transition(
            self, client: AsyncClient, app: FastAPI
        ) -> None:
            """D-12/LP-08: NEW->FINISHED_WIN triggers event_bus.publish with routing key event.finished.win."""
            fake = FakeEventBus()
            app.state.event_bus = fake

            create_body = _create_body()
            await client.post("/event", json=create_body)
            response = await client.put(
                f"/event/{create_body['event_id']}",
                json={
                    "coefficient": "2.00",
                    "deadline": _iso_future(),
                    "state": "FINISHED_WIN",
                },
                headers={"X-Request-ID": "trace-publish"},
            )
            assert response.status_code == 200
            assert len(fake.calls) == 1
            message, routing_key = fake.calls[0]
            assert routing_key == "event.finished.win"
            assert str(message.event_id) == create_body["event_id"]
            assert message.correlation_id == "trace-publish"


    class TestGet:
        async def test_get_event_by_id_returns_200(self, client: AsyncClient) -> None:
            """LP-04: GET /event/{id} returns 200 + EventRead."""
            create_body = _create_body()
            await client.post("/event", json=create_body)
            response = await client.get(f"/event/{create_body['event_id']}")
            assert response.status_code == 200
            assert response.json()["event_id"] == create_body["event_id"]

        async def test_get_missing_returns_404(self, client: AsyncClient) -> None:
            """LP-04: GET on absent id returns 404."""
            response = await client.get(f"/event/{uuid4()}")
            assert response.status_code == 404


    class TestList:
        async def test_get_events_returns_active(
            self, client: AsyncClient, app: FastAPI, monkeypatch: pytest.MonkeyPatch
        ) -> None:
            """LP-05/D-19: GET /events returns only NEW + future events (utc_now monkey-patched for determinism)."""
            fixed_now = datetime(2026, 6, 1, 12, 0, 0, tzinfo=timezone.utc)
            monkeypatch.setattr(
                "line_provider.selectors.list_active_events.utc_now",
                lambda: fixed_now,
            )

            store: InMemoryEventStore = app.state.event_store
            active_id = uuid4()
            finished_id = uuid4()
            past_id = uuid4()
            future = fixed_now + timedelta(hours=1)
            past = fixed_now - timedelta(hours=1)
            await store.add(Event(event_id=active_id, coefficient=Decimal("1.50"), deadline=future, state=EventState.NEW))
            await store.add(Event(event_id=finished_id, coefficient=Decimal("1.50"), deadline=future, state=EventState.FINISHED_WIN))
            await store.add(Event(event_id=past_id, coefficient=Decimal("1.50"), deadline=past, state=EventState.NEW))

            response = await client.get("/events")
            assert response.status_code == 200
            ids = {item["event_id"] for item in response.json()}
            assert ids == {str(active_id)}

        async def test_get_events_empty(self, client: AsyncClient) -> None:
            """LP-05: empty store -> 200 + []."""
            response = await client.get("/events")
            assert response.status_code == 200
            assert response.json() == []


    class TestRequestId:
        async def test_request_id_echoed_on_post(self, client: AsyncClient) -> None:
            """INFR-08: X-Request-ID header echoed in response (P1 middleware invariant preserved)."""
            body = _create_body()
            response = await client.post(
                "/event",
                json=body,
                headers={"X-Request-ID": "trace-echo"},
            )
            assert response.headers["X-Request-ID"] == "trace-echo"

        async def test_request_id_propagated_to_event_bus(
            self, client: AsyncClient, app: FastAPI
        ) -> None:
            """LP-08/A7: X-Request-ID forwarded as correlation_id to published message."""
            fake = FakeEventBus()
            app.state.event_bus = fake

            create_body = _create_body()
            await client.post("/event", json=create_body)
            await client.put(
                f"/event/{create_body['event_id']}",
                json={
                    "coefficient": "2.00",
                    "deadline": _iso_future(),
                    "state": "FINISHED_LOSE",
                },
                headers={"X-Request-ID": "trace-correlate"},
            )
            assert fake.calls[0][0].correlation_id == "trace-correlate"


    class TestHealth:
        async def test_health_still_returns_ok_after_events_router(self, client: AsyncClient) -> None:
            """LP-07: GET /health continues to return 200 after events router is added."""
            response = await client.get("/health")
            assert response.status_code == 200
            assert response.json() == {"status": "ok"}
    ```

    Замечания:
    - `app: FastAPI` параметр в некоторых тестах берёт fixture из conftest.py (Plan 02-01 добавил её).
    - `app.state.event_bus = fake` — переопределение на лету; это работает потому что `get_event_bus` читает `request.app.state.event_bus` на каждом запросе, не кэширует.
    - `app.state.event_store: InMemoryEventStore` — для прямого seed'инга (TestList) — общая практика для integration-тестов с in-memory store.
    - `TestList.test_get_events_returns_active` использует `monkeypatch.setattr` для `utc_now` (D-19 deterministic integration testing): selector читает время через module-level `utc_now`, тест pin'ит его на fixed_now и собирает seed относительно этого момента — никаких клок-дрифтов между записью и фильтром.
    - Прочие тесты (POST/PUT/GET-id) не зависят от `utc_now` напрямую — они используют валидаторы EventCreate (`deadline > now()` в Pydantic), для которых офсеты ±1 час безопасны и без monkey-patch.
    - W-1 revision: все PUT тесты используют путь `/event/{event_id}` (без `/state` суффикса) per D-01/D-06.
  </action>
  <verify>
    <automated>uv run pytest tests/line_provider/test_event_routes.py -q -x</automated>
    <automated>uv run mypy --strict tests/line_provider/test_event_routes.py</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/line_provider/test_event_routes.py -q` → все тесты passed (≥ 20)
    - `grep -c "^class Test" tests/line_provider/test_event_routes.py` >= 6 (Create, Update, Get, List, RequestId, Health)
    - `grep -c "^    async def test_" tests/line_provider/test_event_routes.py` >= 20
    - `grep -q "X-Request-ID" tests/line_provider/test_event_routes.py`
    - `grep -q "FakeEventBus" tests/line_provider/test_event_routes.py`
    - `grep -q "app.state.event_bus" tests/line_provider/test_event_routes.py`
    - `grep -q "app.state.event_store" tests/line_provider/test_event_routes.py`
    - `grep -q "state transition FINISHED_WIN->NEW not allowed" tests/line_provider/test_event_routes.py`
    - `grep -q "line_provider.selectors.list_active_events.utc_now" tests/line_provider/test_event_routes.py`
    - `grep -c "/event/.*/state" tests/line_provider/test_event_routes.py` == 0 (W-1 revision: суффикс `/state` удалён из всех тестов)
    - `grep -c "LP-0[1-9]\|QA-05\|D-0[1-9]\|D-1[0-9]\|INFR-08\|A7" tests/line_provider/test_event_routes.py` >= 15
    - `uv run mypy --strict tests/line_provider/test_event_routes.py` exit 0
    - `uv run ruff check tests/line_provider/test_event_routes.py` → All checks passed
  </acceptance_criteria>
  <done>Integration matrix полностью покрыта; lifespan через LifespanManager работает; events router + P1 health смотрятся вместе зелёные; TestList deterministic через monkey-patched utc_now; все PUT-вызовы используют `/event/{event_id}` per D-01/D-06.</done>
</task>

<task type="auto">
  <name>Task 4: Phase-gate — full suite + coverage ≥85% on line-provider domain + sync REQUIREMENTS/ROADMAP</name>
  <files>.planning/REQUIREMENTS.md, .planning/ROADMAP.md</files>
  <read_first>
    - pyproject.toml ([tool.coverage.run] и [tool.coverage.report] — добавлены в Plan 02-01 Task 1)
    - .planning/phases/02-line-provider-domain/02-VALIDATION.md (Task ID 2-07-06)
    - .planning/REQUIREMENTS.md (статусы LP-* + QA-* для перевода в Complete)
    - .planning/ROADMAP.md (Phase 2 entry — план-чеклисты)
  </read_first>
  <action>
    Финальный verification step. Выполняется последовательно:

    1. **Full suite green:**
       `uv run pytest -q` — полный suite (line_provider + bet_maker + e2e empty) должен быть весь зелёный.

    2. **Coverage gate (≥85% enforced):**
       `uv run pytest --cov=src/line_provider --cov-fail-under=85 --cov-report=term-missing tests/line_provider` — `--cov-fail-under=85` явно enforce'ит порог на уровне pytest-cov (без него `fail_under` в `[tool.coverage.report]` срабатывает только для отдельной команды `coverage report`). Дополнительная защита: после прохождения теста — `uv run coverage report` (читает уже накопленный .coverage и применит fail_under из pyproject).
       - Если показывает <85% — добавить недостающие тесты в соответствующий test файл соответствующего плана (НЕ дописывать в test_event_routes.py не относящиеся к routes тесты — оставить чистоту слоёв).
       - Coverage focus: `src/line_provider/{helpers,interactors,selectors,infrastructure,facades}` — каждая подпапка ≥85%.

    3. **Static checks:**
       `uv run mypy --strict src/line_provider tests/line_provider` → 0 errors.
       `uv run ruff check src/line_provider tests/line_provider` → All checks passed.
       `uv run ruff format --check src/line_provider tests/line_provider` → all files formatted.

    4. **Sync `.planning/REQUIREMENTS.md`:**
       - LP-01..LP-05, LP-07, LP-08, QA-04, QA-05 → `[x]` (по аналогии с обработкой INFR-* в P1).
       - Traceability table: статусы соответствующих строк → "Complete".

    5. **Sync `.planning/ROADMAP.md`:**
       - `### Phase 2: line-provider domain` — `**Plans**: TBD` → `**Plans:** 7/7 plans executed (Phase 2 complete YYYY-MM-DD)`
       - Чек-боксы планов 02-01..02-07 → `[x]`.

    Если на этапе coverage обнаружатся пробелы — добавить тесты в соответствующие test_*.py файлы (Wave 1/2/3 планы), повторить шаг 2.
  </action>
  <verify>
    <automated>bash -c "set -e
uv run pytest -q
uv run pytest --cov=src/line_provider --cov-fail-under=85 --cov-report=term-missing tests/line_provider
uv run coverage report
uv run mypy --strict src/line_provider tests/line_provider
uv run ruff check src/line_provider tests/line_provider
uv run ruff format --check src/line_provider tests/line_provider
grep -q '^- \\[x\\] \\*\\*LP-01\\*\\*' .planning/REQUIREMENTS.md
grep -q '^- \\[x\\] \\*\\*LP-08\\*\\*' .planning/REQUIREMENTS.md
grep -q 'Phase 2 complete' .planning/ROADMAP.md
"
    </automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest -q` exit 0
    - `uv run pytest --cov=src/line_provider --cov-fail-under=85 --cov-report=term-missing tests/line_provider` exit 0 (coverage ≥ 85% enforced by --cov-fail-under)
    - `uv run coverage report` exit 0 (additional safety net via [tool.coverage.report].fail_under)
    - `uv run mypy --strict src/line_provider tests/line_provider` exit 0
    - `uv run ruff check src/line_provider tests/line_provider` exit 0
    - `uv run ruff format --check src/line_provider tests/line_provider` exit 0
    - `grep -c "^- \[x\] \*\*LP-0[12345789]\*\*" .planning/REQUIREMENTS.md` == 7 (LP-01..05, 07, 08)
    - `grep -c "^- \[x\] \*\*QA-0[45]\*\*" .planning/REQUIREMENTS.md` == 2
    - `grep -q "Phase 2 complete" .planning/ROADMAP.md`
    - `grep -c "^- \[x\] 02-[0-9]+-PLAN.md" .planning/ROADMAP.md` == 7
  </acceptance_criteria>
  <done>Phase 2 success criteria из ROADMAP.md полностью валидированы тестами; coverage gate (≥85%) enforce'ен через --cov-fail-under; REQUIREMENTS + ROADMAP синхронизированы.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Untrusted HTTP client → POST/PUT body | Pydantic первая защитная граница; extra=forbid отрезает unknown поля |
| Untrusted X-Request-ID header → correlation_id | Принимается as-is, прокидывается в structlog kwargs; никакого eval/format |
| app.state.event_bus override в тестах | Test-only path; runtime app.state защищён serverside |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Tampering | Malformed JSON body crashes route | mitigate | FastAPI парсит JSON через starlette.requests + Pydantic; на ошибку отдаёт 422 со структурированным `detail`. Никакого raw access к request.body() в route handlers. |
| T-07-02 | Tampering | UUID path param injection (`/event/<sql>`) | mitigate | Pydantic путь validation: FastAPI парсит `event_id: UUID` — невалидный UUID → 422 ДО handler'а. |
| T-07-03 | Information Disclosure | 422 leak stack trace | mitigate | `HTTPException(422, detail=str)` — Pydantic detail структурирован; никакого `raise` без catch — все доменные исключения mapped явно. |
| T-07-04 | Information Disclosure | 404/409 leak event_id existence | accept | API design intentionally сообщает existence (необходимо для idempotency и UX). Authentication out-of-scope — нет авторизации на read. |
| T-07-05 | Denial of Service | Unbounded POST body | mitigate | Starlette default body limit ≈1MB; для нашего use case (UUID + Decimal + datetime + 3-char state) хватает с запасом. |
| T-07-06 | Repudiation | Publish без correlation_id | mitigate | Route handler передаёт `request.headers.get("X-Request-ID", "no-request-id")` в interactor — никогда не None. `correlation_id` обязательное поле EventFinishedMessage. |
| T-07-07 | Log Injection | X-Request-ID содержит `\n` или log-syntax | mitigate | structlog рендерит через JSONRenderer (P1) — newlines escapings; никакого f-string concatenation в log calls (используем kwargs). RequestContextMiddleware echo'ит header как-is, но structlog JSON encoding защищает stdout. |
| T-07-08 | Spoofing | NoopEventBus подменяется в lifespan на attacker-controlled bus | accept | Lifespan — серверный код; attacker'у недоступно. P5 swap через RabbitEventBus — на серверном boot, не runtime. |
| T-07-09 | Authorisation | Нет auth на mutating endpoints | accept | Out-of-scope per ТЗ. Зафиксировано в `02-CONTEXT.md` (нет requirement). |
| T-07-10 | Tampering | Race condition concurrent POST + PUT same id | mitigate | store.add под локом → ровно один POST успешен. `set_event_state` использует previous_state guard (Pitfall 5 mitigation, проверено Plan 02-05 test). |
</threat_model>

<verification>
- `uv run pytest -q` → весь suite зелёный (test_health.py × 2 services + Phase 2 unit + integration)
- `uv run pytest --cov=src/line_provider --cov-fail-under=85 --cov-report=term-missing tests/line_provider` → ≥85% enforced
- `uv run coverage report` → fail_under safety net pass
- `uv run mypy --strict src/line_provider tests/line_provider` → 0 errors
- `uv run ruff check src/line_provider tests/line_provider` → All checks passed
- `uv run ruff format --check src/line_provider tests/line_provider` → all formatted
- ROADMAP.md Phase 2 checkboxes отмечены; REQUIREMENTS.md LP-* статусы Complete
</verification>

<success_criteria>
- 4 HTTP endpoints (POST/PUT/GET/GET-list) работают через `httpx.AsyncClient + LifespanManager`; PUT routed на `/event/{event_id}` per D-01/D-06 (W-1 revision)
- 6 ROADMAP success criteria для Phase 2 валидированы integration-тестами:
  1. POST/PUT создают/обновляют события с правильной валидацией
  2. GET /event/{id} 200/404
  3. GET /events filters active
  4. Reverse transitions → 422 с понятным detail
  5. GET /health 200 (P1 invariant сохранён)
  6. Unit + integration tests покрывают все слои
- Lifespan создаёт InMemoryEventStore + NoopEventBus; integration-тесты подтверждают что app.state.event_bus можно overridе'нуть FakeEventBus'ом на лету
- Phase-gate: coverage ≥85% (enforced by --cov-fail-under=85), mypy strict, ruff clean
- REQUIREMENTS.md + ROADMAP.md обновлены
</success_criteria>

<output>
After completion, create `.planning/phases/02-line-provider-domain/02-07-SUMMARY.md`
</output>
