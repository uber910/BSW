---
phase: 08-flatten-api
plan: 02
type: execute
wave: 2
depends_on:
  - 08-01
files_modified:
  - src/line_provider/api/__init__.py
  - src/line_provider/api/events.py
  - src/line_provider/api/health.py
  - src/line_provider/api/messaging.py
  - src/line_provider/lifespan.py
  - src/line_provider/middleware.py
  - src/line_provider/app.py
  - src/line_provider/facades/event_bus.py
  - tests/line_provider/conftest.py
  - tests/line_provider/test_lifespan.py
  - tests/bet_maker/conftest.py
  - tests/bet_maker/test_e2e_rabbitmq.py
  - tests/bet_maker/e2e/test_reconciler_drop_publish_e2e.py
  - tests/audit/test_static.py
autonomous: true
requirements:
  - REFACTOR-01
  - REFACTOR-05

must_haves:
  truths:
    - "[D-01] line_provider FastAPI app boots and serves both HTTP routers (events, health) and the FastStream publisher router from the flat src/line_provider/api/ paths — no nested api/http/ or api/amqp/."
    - "[D-02] line_provider lifespan.py and middleware.py live at src/line_provider/ root (NOT inside src/line_provider/api/, NOT inside an app/ subpackage); src/line_provider/app.py wires them from the new location."
    - "[D-03] src/line_provider/messaging/routing.py (AMQP routing-key constants) is untouched by this plan — Plan 08-02 files_modified does NOT include it."
    - "[D-04] Cross-service tests (tests/bet_maker/test_e2e_rabbitmq.py line 113, tests/bet_maker/conftest.py line_provider_app fixture) and tests/audit/test_static.py::test_no_entrypoints_dir line_provider assertion are updated in lockstep with the line_provider rename — no cross-plan deferral."
    - "[D-05] Plan 08-02 does not change the tests/ directory structure or fixture wiring — only import statements and the audit assertion body are edited."
    - "line_provider tests collect and pass with imports rewritten to src/line_provider/api/* and src/line_provider/{lifespan,middleware}.py."
    - "tests/audit/test_static.py::test_no_entrypoints_dir is expanded to assert BOTH bet_maker AND line_provider directories do not exist."
  artifacts:
    - path: "src/line_provider/api/__init__.py"
      provides: "api package root for line_provider"
    - path: "src/line_provider/api/events.py"
      provides: "POST/PUT/GET /event(s) HTTP router"
    - path: "src/line_provider/api/health.py"
      provides: "GET /health HTTP router"
    - path: "src/line_provider/api/messaging.py"
      provides: "FastStream RabbitRouter (publisher-only singleton)"
    - path: "src/line_provider/lifespan.py"
      provides: "FastAPI lifespan (broker connect + exchange declare + event_store + RabbitEventBus)"
    - path: "src/line_provider/middleware.py"
      provides: "RequestContextMiddleware"
  key_links:
    - from: "src/line_provider/app.py"
      to: "src/line_provider/api/{events,health,messaging}.py"
      via: "from line_provider.api import ..."
      pattern: "from line_provider\\.api import"
    - from: "src/line_provider/app.py"
      to: "src/line_provider/{lifespan,middleware}.py"
      via: "from line_provider.lifespan import lifespan; from line_provider.middleware import RequestContextMiddleware"
      pattern: "from line_provider\\.(lifespan|middleware) import"
    - from: "src/line_provider/lifespan.py"
      to: "src/line_provider/api/messaging.py"
      via: "from line_provider.api.messaging import router as rabbit_router"
      pattern: "from line_provider\\.api\\.messaging import"
    - from: "tests/bet_maker/conftest.py"
      to: "src/line_provider/api/messaging.py"
      via: "late import inside line_provider_app fixture"
      pattern: "from line_provider\\.api\\.messaging import router as lp_rabbit_router"
    - from: "tests/bet_maker/test_e2e_rabbitmq.py"
      to: "src/line_provider/api/messaging.py"
      via: "late import on line 113 inside DLQ test (bet_maker import on line 131 was already updated by plan 08-01)"
      pattern: "from line_provider\\.api\\.messaging import router as lp_router"
---

<objective>
Migrate line_provider's `entrypoints/` package to the new layout per CONTEXT.md D-01/D-02:

- HTTP routers (`events.py`, `health.py`) and the FastStream RabbitRouter (`messaging.py`, publisher-only) move to `src/line_provider/api/`.
- `lifespan.py` and `middleware.py` move to `src/line_provider/` root (next to `app.py`).
- `src/line_provider/entrypoints/` directory is deleted.
- All in-repo imports — production code, line_provider tests, AND the line_provider-side cross-service references in `tests/bet_maker/conftest.py` (line_provider_app fixture late import) + `tests/bet_maker/test_e2e_rabbitmq.py` line 113 — are rewritten to the new paths.
- `tests/audit/test_static.py::test_no_entrypoints_dir` is expanded to assert BOTH service directories are gone (the bet_maker-only assertion landed in plan 08-01; this plan adds the line_provider arm).

Purpose: deliver REFACTOR-01 for line_provider, while keeping REFACTOR-05 intact. Plan 08-03 will then run the full quality gate across both services.

Output: line_provider boots and serves the same API surface; cross-service e2e tests still find their imports; both services' isolated test suites green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/08-flatten-api/08-CONTEXT.md
@.planning/phases/08-flatten-api/08-01-PLAN.md

@src/line_provider/app.py
@src/line_provider/entrypoints/lifespan.py
@src/line_provider/entrypoints/middleware.py
@src/line_provider/entrypoints/messaging.py
@src/line_provider/entrypoints/api/events.py
@src/line_provider/entrypoints/api/health.py
@src/line_provider/facades/event_bus.py
@tests/line_provider/conftest.py
@tests/line_provider/test_lifespan.py
@tests/bet_maker/conftest.py
@tests/bet_maker/test_e2e_rabbitmq.py
@tests/bet_maker/e2e/test_reconciler_drop_publish_e2e.py
@tests/audit/test_static.py

<interfaces>
<!-- Existing exports the executor must NOT alter, only relocate. -->

src/line_provider/entrypoints/messaging.py (becomes src/line_provider/api/messaging.py):
- `router: RabbitRouter` (publisher-only singleton — no @router.subscriber decorators)

src/line_provider/entrypoints/lifespan.py (becomes src/line_provider/lifespan.py):
- `lifespan(app: FastAPI) -> AsyncIterator[None]` (broker.connect → exchange declare → app.state.event_store + app.state.event_bus)

src/line_provider/entrypoints/middleware.py (becomes src/line_provider/middleware.py):
- `class RequestContextMiddleware(BaseHTTPMiddleware)` (identical to bet_maker's — both are Phase-10 consolidation candidates, NOT touched here)

src/line_provider/entrypoints/api/events.py / health.py (become src/line_provider/api/...):
- each module exports `router: APIRouter`

src/line_provider/app.py:
- `build_app() -> FastAPI` (factory, signature unchanged)
</interfaces>

<dependency_note>
This plan depends on plan 08-01 having completed. Specifically:
- 08-01 added a new audit test `test_no_entrypoints_dir` to `tests/audit/test_static.py` with ONLY the `bet_maker` assertion. Plan 08-02 expands `test_no_entrypoints_dir` to also assert the `line_provider` directory does not exist (Task 3 below).
- 08-01 already edited line 131 of `tests/bet_maker/test_e2e_rabbitmq.py` (the bet_maker import). Plan 08-02 edits ONLY line 113 of that file (the line_provider import).
If 08-01 is not yet merged when the executor begins, abort and resume after 08-01 lands.
</dependency_note>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Move line_provider files with git mv (no content edits yet)</name>
  <files>
    src/line_provider/api/__init__.py,
    src/line_provider/api/events.py,
    src/line_provider/api/health.py,
    src/line_provider/api/messaging.py,
    src/line_provider/lifespan.py,
    src/line_provider/middleware.py
  </files>
  <read_first>
    src/line_provider/entrypoints/__init__.py,
    src/line_provider/entrypoints/api/__init__.py,
    src/line_provider/entrypoints/api/events.py,
    src/line_provider/entrypoints/api/health.py,
    src/line_provider/entrypoints/messaging.py,
    src/line_provider/entrypoints/lifespan.py,
    src/line_provider/entrypoints/middleware.py,
    src/line_provider/app.py,
    .planning/phases/08-flatten-api/08-CONTEXT.md
  </read_first>
  <action>
    Move the line_provider files using `git mv` (preserves history). Run these exact commands from repo root:

    ```
    mkdir -p src/line_provider/api
    git mv src/line_provider/entrypoints/api/__init__.py src/line_provider/api/__init__.py
    git mv src/line_provider/entrypoints/api/events.py  src/line_provider/api/events.py
    git mv src/line_provider/entrypoints/api/health.py  src/line_provider/api/health.py
    git mv src/line_provider/entrypoints/messaging.py   src/line_provider/api/messaging.py
    git mv src/line_provider/entrypoints/lifespan.py    src/line_provider/lifespan.py
    git mv src/line_provider/entrypoints/middleware.py  src/line_provider/middleware.py

    # Remove now-empty package root:
    git rm src/line_provider/entrypoints/__init__.py
    rmdir src/line_provider/entrypoints/api 2>/dev/null || true
    rmdir src/line_provider/entrypoints 2>/dev/null || true
    ```

    Notes:
    - The route files (`events.py`, `health.py`) contain ZERO references to `entrypoints` internally (verified). Their imports come only from `line_provider.facades.deps` / `line_provider.selectors` / `line_provider.interactors` — none touched here.
    - line_provider's `messaging.py` is publisher-only — no `@router.subscriber`, no `set_sessionmaker`. Just the singleton `router = RabbitRouter(...)`.
    - The `entrypoints/__init__.py` and `entrypoints/api/__init__.py` are empty 1-line files — drop the package roots rather than relocate.

    After moving, the executor MUST verify with the acceptance criteria below before proceeding.
  </action>
  <verify>
    <automated>find src/line_provider -type d -name entrypoints | wc -l | grep -q '^0$' &amp;&amp; test -f src/line_provider/api/messaging.py &amp;&amp; test -f src/line_provider/lifespan.py &amp;&amp; test -f src/line_provider/middleware.py</automated>
  </verify>
  <acceptance_criteria>
    - `find src/line_provider -type d -name entrypoints` produces empty output.
    - `test -f src/line_provider/api/__init__.py` succeeds.
    - `test -f src/line_provider/api/events.py` succeeds.
    - `test -f src/line_provider/api/health.py` succeeds.
    - `test -f src/line_provider/api/messaging.py` succeeds.
    - `test -f src/line_provider/lifespan.py` succeeds.
    - `test -f src/line_provider/middleware.py` succeeds.
    - `git status --porcelain | grep -E '^R' | wc -l` reports ≥6 renames (history preserved).
  </acceptance_criteria>
  <done>
    All line_provider files relocated to the new layout via git mv; `src/line_provider/entrypoints/` directory is gone; no file contents edited yet.
  </done>
</task>

<task type="auto">
  <name>Task 2: Rewrite line_provider production imports to new paths</name>
  <files>
    src/line_provider/app.py,
    src/line_provider/lifespan.py,
    src/line_provider/facades/event_bus.py
  </files>
  <read_first>
    src/line_provider/app.py,
    src/line_provider/lifespan.py,
    src/line_provider/api/messaging.py,
    src/line_provider/facades/event_bus.py
  </read_first>
  <action>
    Rewrite every `from line_provider.entrypoints...` import to its new home. Concrete replacements (old → new):

    In `src/line_provider/app.py`:
    - `from line_provider.entrypoints.api import events, health` → `from line_provider.api import events, health`
    - `from line_provider.entrypoints.lifespan import lifespan` → `from line_provider.lifespan import lifespan`
    - `from line_provider.entrypoints.messaging import router as rabbit_router` → `from line_provider.api.messaging import router as rabbit_router`
    - `from line_provider.entrypoints.middleware import RequestContextMiddleware` → `from line_provider.middleware import RequestContextMiddleware`

    In `src/line_provider/lifespan.py` (the moved file at its new location):
    - `from line_provider.entrypoints.messaging import router as rabbit_router` → `from line_provider.api.messaging import router as rabbit_router`

    In `src/line_provider/facades/event_bus.py`:
    - The docstring of `RabbitEventBus` says `Constructed in lifespan with the FastStream RabbitBroker from line_provider.entrypoints.messaging.router.broker.` → replace `line_provider.entrypoints.messaging.router.broker` with `line_provider.api.messaging.router.broker`. This is a docstring-only edit; no code changes.

    Do NOT touch route module file contents (`events.py`, `health.py`) — they have no entrypoints references.

    Do NOT edit `src/line_provider/middleware.py` — it has no internal entrypoints reference.

    Do NOT edit `src/line_provider/api/messaging.py` — verified to have no internal entrypoints references (publisher-only stub).

    After the edits, run from repo root:
    - `git grep -E 'line_provider\.entrypoints' src/` — must return zero lines.
    - `git grep -E 'line_provider/entrypoints' src/` — must return zero lines (path-style residue).
  </action>
  <verify>
    <automated>git grep -E 'line_provider\.entrypoints' src/ | grep -v '^#' | wc -l | grep -q '^0$' &amp;&amp; git grep -E 'line_provider/entrypoints' src/ | grep -v '^#' | wc -l | grep -q '^0$' &amp;&amp; python -c "from line_provider.api import events, health, messaging; from line_provider import lifespan, middleware, app; print('imports ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `git grep -E 'line_provider\.entrypoints' src/` exits non-zero (zero matches).
    - `git grep -E 'line_provider/entrypoints' src/` exits non-zero (zero matches — path-style).
    - `python -c "from line_provider.api import events, health, messaging"` exits 0.
    - `python -c "from line_provider import lifespan, middleware"` exits 0.
    - `python -c "from line_provider.app import build_app; build_app()"` runs without ImportError. (If `LineProviderSettings()` fails on missing env, set `LINE_PROVIDER_RABBITMQ_URL=amqp://x:x@localhost/` for this smoke check.)
    - `mypy --strict src/line_provider` reports zero errors.
    - `ruff check src/line_provider` reports zero issues.
  </acceptance_criteria>
  <done>
    All line_provider production code imports the relocated modules from `line_provider.api.*`, `line_provider.lifespan`, `line_provider.middleware`; the docstring self-reference in `facades/event_bus.py` is updated; the package imports cleanly and passes mypy strict + ruff.
  </done>
</task>

<task type="auto">
  <name>Task 3: Rewrite line_provider test imports, line 113 of test_e2e_rabbitmq.py, line_provider_app fixture, the test_reconciler_drop_publish_e2e.py docstring, and expand test_no_entrypoints_dir</name>
  <files>
    tests/line_provider/conftest.py,
    tests/line_provider/test_lifespan.py,
    tests/bet_maker/conftest.py,
    tests/bet_maker/test_e2e_rabbitmq.py,
    tests/bet_maker/e2e/test_reconciler_drop_publish_e2e.py,
    tests/audit/test_static.py
  </files>
  <read_first>
    tests/line_provider/conftest.py,
    tests/line_provider/test_lifespan.py,
    tests/bet_maker/conftest.py,
    tests/bet_maker/test_e2e_rabbitmq.py,
    tests/bet_maker/e2e/test_reconciler_drop_publish_e2e.py,
    tests/audit/test_static.py
  </read_first>
  <action>
    Rewrite every `line_provider.entrypoints` reference in tests/ to the new layout AND expand the regression-guard audit test.

    `tests/line_provider/conftest.py` (line ~23, inside the `app` fixture):
    - `from line_provider.entrypoints.messaging import router as lp_rabbit_router  # noqa: PLC0415` → `from line_provider.api.messaging import router as lp_rabbit_router  # noqa: PLC0415`

    `tests/line_provider/test_lifespan.py` (four late imports — each variant identical):
    - Each occurrence of the multi-line import `from line_provider.entrypoints.messaging import (  # noqa: PLC0415` followed by `router as lp_router,` and `)` → rewrite the module path to `line_provider.api.messaging`. Preserve the noqa comment and the alias.

    `tests/bet_maker/conftest.py` (line ~150, inside the `line_provider_app` fixture):
    - `from line_provider.entrypoints.messaging import router as lp_rabbit_router  # noqa: PLC0415` → `from line_provider.api.messaging import router as lp_rabbit_router  # noqa: PLC0415`

    `tests/bet_maker/test_e2e_rabbitmq.py` (BLOCKER 1 fix from checker iteration 1 — line 113 ONLY):
    - Line ~113 `from line_provider.entrypoints.messaging import router as lp_router  # noqa: PLC0415` → `from line_provider.api.messaging import router as lp_router  # noqa: PLC0415`
    - DO NOT touch line ~131 — `from bet_maker.api.messaging import router as bm_router` was already updated by plan 08-01. If it still reads `bet_maker.entrypoints.messaging`, plan 08-01 is incomplete and this plan should abort.

    `tests/bet_maker/e2e/test_reconciler_drop_publish_e2e.py` (INFO-level checker note from iteration 1 — owned by plan 08-02 for cleaner intermediate state):
    - Module docstring line `- app.state.event_store: confirmed in line_provider/entrypoints/lifespan.py` → `- app.state.event_store: confirmed in line_provider/lifespan.py`
    - This is a docstring-only edit; no code changes. Moved from plan 08-01 to plan 08-02 because the file path on disk only becomes accurate after this plan's Task 1 moves the line_provider directory.

    `tests/audit/test_static.py` — EXPAND `test_no_entrypoints_dir` (BLOCKER 2 fix from checker iteration 1, Option A — earliest regression guard expansion):

    Plan 08-01 created this test asserting ONLY the bet_maker directory does not exist. After this plan's Task 1 deletes `src/line_provider/entrypoints/`, expand the test body to also cover line_provider. Replace the existing function body with the version below. The two assertions are split for clearer failure messages. After this plan lands, both assertions are active (non-vacuous): both directories have been removed and the audit guards against re-introduction.

    ```python
    def test_no_entrypoints_dir() -> None:
        """REFACTOR-01: src/<svc>/entrypoints/ must not exist for either service.

        Phase 8 flattened HTTP routers + FastStream RabbitRouter into
        src/<svc>/api/ and relocated lifespan.py + middleware.py to the
        service-package root. The legacy entrypoints/ directory was
        deleted for both services; this audit fails if a future commit
        recreates either one.

        Plan 08-01 added the bet_maker assertion. Plan 08-02 added the
        line_provider assertion.
        """
        bm = SRC / "bet_maker" / "entrypoints"
        lp = SRC / "line_provider" / "entrypoints"
        assert not bm.exists(), (
            f"{bm} re-introduced — Phase 8 flattened entrypoints/ → api/."
        )
        assert not lp.exists(), (
            f"{lp} re-introduced — Phase 8 flattened entrypoints/ → api/."
        )
    ```

    After all edits, this combined command must return zero output:
    ```
    git grep -E '(bet_maker|line_provider)\.entrypoints' src/ tests/
    ```

    And the path-style residue check (combining 08-01 and this plan):
    ```
    git grep -E '(bet_maker|line_provider)/entrypoints' src/ tests/ | grep -v '^\.planning/'
    ```
    must also return zero output (planning docs may legitimately reference the old path).
  </action>
  <verify>
    <automated>git grep -E 'line_provider\.entrypoints' tests/ | grep -v '^#' | wc -l | grep -q '^0$' &amp;&amp; git grep -E '(bet_maker|line_provider)\.entrypoints' src/ tests/ | grep -v '^#' | wc -l | grep -q '^0$' &amp;&amp; pytest -q tests/audit/test_static.py::test_no_entrypoints_dir -x</automated>
  </verify>
  <acceptance_criteria>
    - `git grep -E 'line_provider\.entrypoints' tests/` exits non-zero (zero matches).
    - `git grep -E '(bet_maker|line_provider)\.entrypoints' src/ tests/` exits non-zero (zero matches across the entire repo, combining 08-01 and this plan).
    - `git grep -E '(bet_maker|line_provider)/entrypoints' src/ tests/ | grep -v '^\.planning/'` exits non-zero (zero matches — covers any remaining Path string literals; planning docs may legitimately reference the old path).
    - `find src -type d -name entrypoints` produces empty output.
    - `pytest -q tests/audit/test_static.py::test_no_entrypoints_dir` exits 0 (now both bet_maker AND line_provider assertions active, both passing non-vacuously).
    - `pytest -q tests/line_provider` exits 0 (line_provider test suite green).
    - `pytest --collect-only -q tests/bet_maker/test_e2e_rabbitmq.py` exits 0 (both lines 113 and 131 resolve via the new paths).
    - `ruff check tests/line_provider tests/bet_maker` reports zero issues.
    - `mypy --strict src` still reports zero errors.
  </acceptance_criteria>
  <done>
    Every `line_provider.entrypoints` reference in tests/ is rewritten to the new path; line 113 of `tests/bet_maker/test_e2e_rabbitmq.py` is fixed (line 131 already done by 08-01); the `test_reconciler_drop_publish_e2e.py` docstring is updated; `test_no_entrypoints_dir` is expanded to cover both services; combined with plan 08-01 the entire repo has zero `entrypoints` import references and zero `entrypoints` path-style residue in `src/` or `tests/`; line_provider test suite passes in isolation.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| filesystem → Python import system | Renamed modules must resolve via the new import path; stale `from line_provider.entrypoints...` would crash at startup. |
| cross-service tests → both packages | `tests/bet_maker/conftest.py` and `tests/bet_maker/test_e2e_rabbitmq.py` import from both `bet_maker` and `line_provider` — each side must be updated when its package moves. |
| audit regression guard | `test_no_entrypoints_dir` must reach its FULL form (both assertions) by end of plan 08-02, not stay at bet_maker-only. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-06 | Tampering | line_provider import surface | mitigate | Step 2 rewrites every `from line_provider.entrypoints...` import; acceptance criterion `git grep` must return zero matches. |
| T-08-07 | Repudiation | git history | mitigate | Use `git mv` (Task 1) so `git log --follow` continues to track file history across the rename. |
| T-08-08 | Information Disclosure | cross-service tests stale on one side | mitigate | Task 3 explicitly updates `tests/bet_maker/conftest.py` line_provider_app fixture and `tests/bet_maker/test_e2e_rabbitmq.py` line 113 (line 131 already handled by 08-01) — these would otherwise stay broken in a "line_provider only" partial migration. |
| T-08-09 | Denial of Service | lifespan startup ImportError → service won't boot | mitigate | Acceptance criterion runs `python -c "from line_provider.app import build_app; build_app()"` after Task 2 to confirm the factory resolves all imports before lifespan would attempt to start. |
| T-08-10a | Information Disclosure | audit test stays at bet_maker-only forever | mitigate | Task 3 explicitly EXPANDS `test_no_entrypoints_dir` with the line_provider assertion (Blocker 2 Option A fix); acceptance verifies both assertions are active and passing. |
| T-08-10 | Elevation of Privilege | n/a | accept | This is a pure internal refactor with no new attack surface. |
</threat_model>

<verification>
After all three tasks complete, the executor must run this sequence and confirm green:

```
git grep -E '(bet_maker|line_provider)\.entrypoints' src/ tests/   # zero output
git grep -E '(bet_maker|line_provider)/entrypoints' src/ tests/ | grep -v '^\.planning/'   # zero output
find src -type d -name entrypoints                                  # empty output
ruff check src tests
mypy --strict src
pytest -q tests/line_provider tests/audit                           # all green
pytest --collect-only -q tests/bet_maker/test_e2e_rabbitmq.py       # both lines 113 + 131 resolve
pytest -q tests/bet_maker/conftest.py --collect-only > /dev/null    # conftest imports resolve
```

Full repo `pytest` + coverage gate happens in plan 08-03.
</verification>

<success_criteria>
- `src/line_provider/entrypoints/` directory does not exist.
- `src/line_provider/api/{events,health,messaging}.py` exist and import without raising.
- `src/line_provider/{lifespan,middleware}.py` exist at the service-package root next to `app.py`.
- Zero `(bet_maker|line_provider).entrypoints` references remain anywhere in `src/` or `tests/`.
- Zero `(bet_maker|line_provider)/entrypoints` path-style residue remains in `src/` or `tests/` (planning docs excluded).
- `tests/audit/test_static.py::test_no_entrypoints_dir` passes with BOTH assertions active.
- `mypy --strict src` and `ruff check src tests` are clean.
- line_provider test suite passes in isolation (`pytest tests/line_provider tests/audit`).
- `pytest --collect-only tests/bet_maker/test_e2e_rabbitmq.py` exits 0.
</success_criteria>

<output>
After completion, create `.planning/phases/08-flatten-api/08-02-SUMMARY.md` documenting:
- Files moved (with `git log --follow` confirmation that history is preserved).
- All import sites rewritten in line_provider production code and in the cross-service test files (specifically: line 113 of `tests/bet_maker/test_e2e_rabbitmq.py` and the line_provider_app fixture late import in `tests/bet_maker/conftest.py` — line 131 of the e2e file was already handled by 08-01).
- The `test_reconciler_drop_publish_e2e.py` docstring update (INFO-level checker note from iteration 1; moved here for cleaner intermediate state).
- Confirmation that `test_no_entrypoints_dir` is now fully expanded — both `bet_maker` and `line_provider` assertions active and passing non-vacuously.
- Any deviations from the action text (should be none — concrete commands provided).
</output>
