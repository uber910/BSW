---
phase: 08-flatten-api
plan: 03
type: execute
wave: 3
depends_on:
  - 08-01
  - 08-02
files_modified:
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
  - .planning/phases/08-flatten-api/08-03-SUMMARY.md
autonomous: true
requirements:
  - REFACTOR-05

must_haves:
  truths:
    - "[D-01] Final repo state: both services use flat src/<svc>/api/ for all transports — `find src -type d -name 'api/http' -o -name 'api/amqp'` returns no results."
    - "[D-02] Final repo state: src/bet_maker/{lifespan,middleware}.py and src/line_provider/{lifespan,middleware}.py exist at the service-package root; no api/lifespan.py or api/middleware.py exist; no app/ subpackage exists."
    - "[D-03] Final repo state: src/bet_maker/messaging/routing.py and src/line_provider/messaging/routing.py are unchanged by Phase 8 (verified via git log) — AMQP routing-key constants remain in place; Phase 10 may revisit."
    - "[D-04] Phase closeout confirms tests/, audit, and src/ migrated in lockstep within Phase 8 — Plans 08-01 and 08-02 each touched both src/ and tests/audit (verified by git history); no follow-up audit deferral required."
    - "[D-05] Phase closeout confirms tests/ directory structure and fixture-разводка are unchanged — only imports + audit path strings changed (verified via `git diff --stat` showing no test-file moves/renames)."
    - "The full repository test suite (355+ tests) passes with the new layout — no new skips, no new xfails."
    - "mypy --strict src reports zero errors across both packages on the new paths."
    - "ruff check src tests reports zero issues."
    - "pytest coverage gate (≥85%) still passes."
    - "Both services still boot via docker-compose semantics — `python -c 'from bet_maker.app import build_app; build_app()'` and the line_provider equivalent both succeed."
    - "REQUIREMENTS.md BM-03 prose no longer names `entrypoints` as a layer; the canonical layer descriptor reads `api / facades / interactors / selectors / helpers`."
  artifacts:
    - path: ".planning/phases/08-flatten-api/08-03-SUMMARY.md"
      provides: "Final phase verification log + Phase 8 closeout summary"
  key_links:
    - from: "phase 08 closure"
      to: "ROADMAP.md Phase 8 plans list"
      via: "checkbox flip on 08-01-PLAN.md, 08-02-PLAN.md, 08-03-PLAN.md"
      pattern: "- \\[x\\] 08-(01|02|03)-PLAN\\.md"
    - from: "phase 08 closure"
      to: "REQUIREMENTS.md BM-03 layer descriptor"
      via: "in-place text replacement of `entrypoints` → `api` in the layer list"
      pattern: "api / facades / interactors / selectors / helpers"
---

<objective>
Run the full quality gate after plans 08-01 (bet_maker rename) and 08-02 (line_provider rename) have landed, and produce the phase closeout summary. The only source/code files modified in this plan are planning artefacts: `.planning/ROADMAP.md`, `.planning/REQUIREMENTS.md`, and the new `.planning/phases/08-flatten-api/08-03-SUMMARY.md`. No `src/` or `tests/` files are touched.

The single Task 1 (a gate plus planning-ledger sync) verifies that REFACTOR-01 fully landed and REFACTOR-05's invariants hold across the entire codebase, then updates the planning ledger.

Scope:
- Re-run all repo-wide checks that 08-01 and 08-02 only ran per-service.
- Update ROADMAP.md Phase 8 plan checkboxes.
- Update REQUIREMENTS.md: REFACTOR-01 Traceability row → Complete; BM-03 description layer list (line 34) → `api / facades / interactors / selectors / helpers` (the BM-03 prose is the in-repo canonical layer description and was not refreshed by 08-01/08-02 — Warning 3 from checker iteration 1).
- Produce the phase-closeout SUMMARY at `.planning/phases/08-flatten-api/08-03-SUMMARY.md`.

If any gate fails, the executor MUST stop and surface the failure rather than patching it in this plan — patches belong in a follow-up plan (08-04) so the failure is traceable.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-flatten-api/08-CONTEXT.md
@.planning/phases/08-flatten-api/08-01-PLAN.md
@.planning/phases/08-flatten-api/08-02-PLAN.md

<interfaces>
<!-- This plan reads source code only. The verification commands are the contract; planning files are edited per Step 10. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run the full quality gate and update closeout artefacts</name>
  <files>
    .planning/ROADMAP.md,
    .planning/REQUIREMENTS.md,
    .planning/phases/08-flatten-api/08-03-SUMMARY.md
  </files>
  <read_first>
    .planning/ROADMAP.md,
    .planning/REQUIREMENTS.md,
    .planning/phases/08-flatten-api/08-CONTEXT.md,
    .planning/phases/08-flatten-api/08-01-PLAN.md,
    .planning/phases/08-flatten-api/08-02-PLAN.md
  </read_first>
  <action>
    Run the full quality gate from the repo root. Each command MUST exit 0. If any fails, STOP — do not patch in this plan; surface the failure and propose a follow-up plan 08-04.

    Step 1 — Layout invariant (no `entrypoints/` directories anywhere under `src/`):
    ```
    find src -type d -name entrypoints
    # expected: empty output
    ```

    Step 2 — No stale import references anywhere in tracked code:
    ```
    git grep -E '(bet_maker|line_provider)\.entrypoints' src/ tests/
    # expected: zero matches (grep exits 1)
    ```

    Step 3 — No stale Path string literals (handles cases where tests open files by path):
    ```
    git grep -E '(bet_maker|line_provider)/entrypoints' src/ tests/ | grep -v '^\.planning/'
    # expected: zero matches (grep exits 1)
    ```

    Step 4 — Linter clean:
    ```
    ruff check src tests
    # expected: "All checks passed!" (exit 0)
    ```

    Step 5 — Strict type-check clean across both packages:
    ```
    mypy --strict src
    # expected: "Success: no issues found in N source files" (exit 0)
    ```

    Step 6 — Full test suite green:
    ```
    pytest -q
    # expected: 355+ passed, no errors, no new skips/xfails relative to Phase 7 baseline
    ```

    Step 7 — Coverage gate at ≥85% (REFACTOR-05 SC#4):
    ```
    pytest --cov=src --cov-report=term-missing --cov-fail-under=85
    # expected: exit 0, "Required test coverage of 85% reached."
    ```

    Step 8 — Smoke check that both app factories build with the new layout (Pitfalls-prevented "Hidden Alembic / Dockerfile / docker-compose path leaks" — verifies imports resolve without docker-compose):
    ```
    BET_MAKER_POSTGRES_DSN=postgresql+asyncpg://x:x@localhost/x \
    BET_MAKER_RABBITMQ_URL=amqp://x:x@localhost/ \
    LINE_PROVIDER_BASE_URL=http://lp:8000 \
    python -c "from bet_maker.app import build_app; build_app(); print('bet_maker ok')"

    LINE_PROVIDER_RABBITMQ_URL=amqp://x:x@localhost/ \
    python -c "from line_provider.app import build_app; build_app(); print('line_provider ok')"
    # expected: both print "ok"
    ```

    Step 9 — AsyncAPI docs route still resolves (pitfall guard: "AsyncAPI docs at /asyncapi must still resolve after move"). Use FastAPI's TestClient inline:
    ```
    BET_MAKER_POSTGRES_DSN=postgresql+asyncpg://x:x@localhost/x \
    BET_MAKER_RABBITMQ_URL=amqp://x:x@localhost/ \
    LINE_PROVIDER_BASE_URL=http://lp:8000 \
    python -c "
    from fastapi.testclient import TestClient
    from bet_maker.app import build_app
    # Build app but DO NOT enter lifespan (PG/RMQ unavailable).
    app = build_app()
    # The /asyncapi route is mounted by FastStream at import-time, no lifespan needed.
    routes = {r.path for r in app.routes}
    assert '/asyncapi' in routes, f'AsyncAPI route missing; have {sorted(routes)}'
    print('bet_maker /asyncapi route registered')
    "

    LINE_PROVIDER_RABBITMQ_URL=amqp://x:x@localhost/ \
    python -c "
    from line_provider.app import build_app
    app = build_app()
    routes = {r.path for r in app.routes}
    assert '/asyncapi' in routes, f'AsyncAPI route missing; have {sorted(routes)}'
    print('line_provider /asyncapi route registered')
    "
    # expected: both print "/asyncapi route registered"
    ```

    Step 10 — Update ROADMAP.md Phase 8 entry AND REQUIREMENTS.md (BM-03 prose + REFACTOR-01 Traceability row). Edits:

    10a — `.planning/ROADMAP.md`:
    - Find the line `### Phase 8: Flatten entrypoints/ → api/`.
    - Below it, find the `**Plans:** TBD` line — replace with `**Plans:** 3/3 plans executed (Phase 8 complete YYYY-MM-DD)` using today's UTC date.
    - Below that, add a Plans block (matching the style of completed phases):
      ```
      Plans:
      - [x] 08-01-PLAN.md — bet_maker entrypoints/ → api/ + lifespan + middleware relocate + tests/audit sync + tests/bet_maker/test_e2e_rabbitmq.py line 131 (Wave 1)
      - [x] 08-02-PLAN.md — line_provider entrypoints/ → api/ + lifespan + middleware relocate + cross-service test sync + test_e2e_rabbitmq.py line 113 + audit test expansion (Wave 2)
      - [x] 08-03-PLAN.md — full quality gate + ROADMAP + REQUIREMENTS.md BM-03 closeout (Wave 3)
      ```
    - In the `## Phases` checklist near the top of ROADMAP.md, flip `- [ ] **Phase 8: Flatten entrypoints/ → api/**` to `- [x] **Phase 8: Flatten entrypoints/ → api/**`.
    - In the `## Progress` table at the bottom, update the Phase 8 row: `Plans Complete: 3/3`, `Status: Complete`, `Completed: <today UTC YYYY-MM-DD>`.

    10b — `.planning/REQUIREMENTS.md` BM-03 layer descriptor refresh (Warning 3 fix from checker iteration 1):
    - Find the BM-03 line (around line 34): `- [x] **BM-03**: Слоистая архитектура: entrypoints / facades / interactors / selectors / helpers (множественное число; helpers — pure functions)`
    - Replace `entrypoints / facades / interactors / selectors / helpers` with `api / facades / interactors / selectors / helpers`
    - Keep the rest of the BM-03 line text intact, including the parenthetical `(множественное число; helpers — pure functions)`.

    10c — `.planning/REQUIREMENTS.md` Traceability table:
    - Change the REFACTOR-01 row from `| REFACTOR-01 | Phase 8 | Pending |` to `| REFACTOR-01 | Phase 8 | Complete |`.
    - The REFACTOR-05 row stays `Pending` (still cross-cutting through Phases 9 and 10) but add a parenthetical to Phase 8: `| REFACTOR-05 | Phases 8, 9, 10 (cross-cutting quality bar) | Phase 8 met — pending Phases 9, 10 |`.

    Verify the BM-03 prose refresh leaves no stray references:
    ```
    grep -nE 'entrypoints' .planning/REQUIREMENTS.md
    # expected: any remaining matches are ONLY in the Phase 8 narrative (e.g., section titles, the
    # REFACTOR-01 line itself "Cлой `entrypoints/` упразднён") or the Traceability table —
    # the BM-03 layer descriptor on line ~34 must NOT contain the word `entrypoints` anymore.
    ```

    Step 11 — Write the closeout SUMMARY at `.planning/phases/08-flatten-api/08-03-SUMMARY.md`. Required content:
    - `# Phase 8: Flatten entrypoints/ → api/ — Closeout Summary`
    - Date completed (UTC).
    - "Requirements satisfied" section listing REFACTOR-01 (Complete) and REFACTOR-05 for Phase 8 (Complete; carries forward into Phases 9 and 10). Note that BM-03's layer-list prose was refreshed in lockstep (`entrypoints` → `api`).
    - "Quality gate" section with the exact output of `pytest -q` (PASS line), `mypy --strict src` (success line), `ruff check src tests` (clean line), and `pytest --cov --cov-fail-under=85` (coverage percentage).
    - "Layout" section showing the resulting directory tree fragments for `src/bet_maker/` and `src/line_provider/` (one level deep, listing `api/`, `lifespan.py`, `middleware.py`, etc.) — produced by `tree -L 2 src/bet_maker src/line_provider 2>/dev/null` or `find src/{bet_maker,line_provider} -maxdepth 2 -not -path '*/__pycache__*' | sort`.
    - "Pitfalls-prevented evidence":
      - Stale import paths → `git grep` zero matches (paste the empty result).
      - Hidden path leaks in Dockerfile / docker-compose / alembic env.py → `grep entrypoints Dockerfile docker-compose.yml src/bet_maker/alembic/env.py` zero matches (paste empty result).
      - AsyncAPI docs at `/asyncapi` resolve for both services (Step 9 output).
      - `tests/audit/test_static.py::test_no_entrypoints_dir` passes with both assertions active (Step 6 confirms).
      - BM-03 prose refreshed to `api / facades / interactors / selectors / helpers` (Warning 3 fix).
    - "Hand-off to Phase 9" section noting that the cleaner layout makes UoW redesign + Repository removal targets stable, and the AbstractUnitOfWork seam is now `src/bet_maker/facades/uow.py` (unchanged) with the api/ layer as the only DI consumer surface.
  </action>
  <verify>
    <automated>find src -type d -name entrypoints | wc -l | grep -q '^0$' &amp;&amp; ! git grep -E '(bet_maker|line_provider)\.entrypoints' src/ tests/ &amp;&amp; ruff check src tests &amp;&amp; mypy --strict src &amp;&amp; pytest -q &amp;&amp; pytest --cov=src --cov-fail-under=85 -q &amp;&amp; test -f .planning/phases/08-flatten-api/08-03-SUMMARY.md &amp;&amp; ! grep -q 'entrypoints / facades / interactors' .planning/REQUIREMENTS.md &amp;&amp; grep -q 'api / facades / interactors / selectors / helpers' .planning/REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - `find src -type d -name entrypoints` produces empty output.
    - `git grep -E '(bet_maker|line_provider)\.entrypoints' src/ tests/` exits non-zero (zero matches).
    - `git grep -E '(bet_maker|line_provider)/entrypoints' src/ tests/ | grep -v '^\.planning/'` exits non-zero (zero matches — covers Path string literals).
    - `ruff check src tests` reports "All checks passed!" (exit 0).
    - `mypy --strict src` reports "Success: no issues found" (exit 0).
    - `pytest -q` exits 0 with ≥355 passed and no new skips/xfails relative to Phase 7 baseline.
    - `pytest --cov=src --cov-fail-under=85` exits 0 (coverage gate ≥85% holds — REFACTOR-05 SC#4).
    - Both `python -c "from <svc>.app import build_app; build_app()"` smoke checks (Step 8) exit 0.
    - Both AsyncAPI route checks (Step 9) print the expected confirmation lines.
    - `.planning/ROADMAP.md` Phase 8 row in the Progress table reads "Plans Complete: 3/3, Status: Complete".
    - `.planning/ROADMAP.md` Phase 8 plan list shows three `- [x] 08-NN-PLAN.md ...` entries.
    - `.planning/ROADMAP.md` `## Phases` checklist shows `- [x] **Phase 8: Flatten entrypoints/ → api/**`.
    - `.planning/REQUIREMENTS.md` BM-03 line contains the substring `api / facades / interactors / selectors / helpers` and does NOT contain `entrypoints / facades / interactors / selectors / helpers`.
    - `grep -nE 'entrypoints' .planning/REQUIREMENTS.md` returns ONLY references in the Phase 8 v1.1 narrative (REFACTOR-01 line, Traceability rows, section headings) — NOT in the BM-03 layer descriptor.
    - `.planning/REQUIREMENTS.md` Traceability shows REFACTOR-01 as Complete.
    - `.planning/phases/08-flatten-api/08-03-SUMMARY.md` exists and contains all required sections (Requirements satisfied, Quality gate, Layout, Pitfalls-prevented evidence, Hand-off to Phase 9).
  </acceptance_criteria>
  <done>
    Full quality gate is green; ROADMAP.md and REQUIREMENTS.md reflect Phase 8 completion (including the BM-03 prose refresh from `entrypoints` to `api`); 08-03-SUMMARY.md documents the closeout. Phase 8 is closed and Phase 9 can begin.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| verification gate → developer trust | A failing gate that gets silently patched in this plan would hide the regression; gate-only plans must surface failures, not absorb them. |
| ROADMAP truth → REQUIREMENTS truth | Checkbox flips in ROADMAP and the BM-03 prose + REFACTOR-01 Traceability row in REQUIREMENTS.md must all agree; a green gate without all three updates means downstream phases inherit drifted planning truth. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-11 | Tampering | quality gate hidden failures | mitigate | Action text explicitly says "STOP — do not patch in this plan; surface the failure and propose a follow-up plan 08-04." This keeps the gate honest. |
| T-08-12 | Repudiation | planning ledger drift | mitigate | ROADMAP.md, REQUIREMENTS.md Traceability, AND REQUIREMENTS.md BM-03 layer descriptor are all listed in acceptance criteria with explicit grep gates; the SUMMARY pastes the actual command output (not paraphrased) so a reviewer can re-run the gates and reproduce. |
| T-08-12a | Repudiation | BM-03 prose drifting from layout | mitigate | Step 10b plus the acceptance grep gate (`grep -q 'api / facades / interactors / selectors / helpers'`) make the BM-03 refresh non-optional. Without this, BM-03 would still claim `entrypoints` is a layer after REFACTOR-01 says it isn't (Warning 3 from checker iteration 1). |
| T-08-13 | Information Disclosure | n/a | accept | Read-only gate plus planning-ledger writes. |
| T-08-14 | Denial of Service | full pytest run cost | accept | Phase 7's coverage gate already runs in CI; one local re-run for closeout is well within phase budget. |
| T-08-15 | Elevation of Privilege | n/a | accept | No new attack surface. |
</threat_model>

<verification>
The single Task 1 IS the verification — the acceptance criteria are the gate.
</verification>

<success_criteria>
- All commands in Steps 1–9 of Task 1 exit 0 (or grep-style "expected zero matches" hold).
- ROADMAP.md Phase 8 marked complete with 3/3 plans.
- REQUIREMENTS.md Traceability marks REFACTOR-01 as Complete.
- REQUIREMENTS.md BM-03 layer descriptor reads `api / facades / interactors / selectors / helpers` (Warning 3 fix).
- `.planning/phases/08-flatten-api/08-03-SUMMARY.md` exists with the required sections.
- Coverage gate ≥85% (REFACTOR-05 SC#4) holds.
</success_criteria>

<output>
After completion, create `.planning/phases/08-flatten-api/08-03-SUMMARY.md` per Step 11 above.
</output>
