---
phase: 08-flatten-api
plan: 01
type: execute
wave: 1
depends_on: []
# scope_justification: 18 files in files_modified after revision (above 15-file soft threshold).
# Not split: every edit is mechanical and per-edit gated by explicit acceptance grep commands;
# splitting bet_maker into 01a/01b would force tests/audit + bet_maker prod into separate plans
# without reducing context cost (one Read pass per file regardless of plan boundary).
files_modified:
  - src/bet_maker/api/__init__.py
  - src/bet_maker/api/bets.py
  - src/bet_maker/api/events.py
  - src/bet_maker/api/health.py
  - src/bet_maker/api/messaging.py
  - src/bet_maker/lifespan.py
  - src/bet_maker/middleware.py
  - src/bet_maker/app.py
  - src/bet_maker/facades/deps.py
  - src/bet_maker/jobs/__init__.py
  - tests/bet_maker/conftest.py
  - tests/bet_maker/test_health.py
  - tests/bet_maker/test_lifespan.py
  - tests/bet_maker/test_lifespan_reconciler.py
  - tests/bet_maker/test_messaging.py
  - tests/bet_maker/test_e2e_rabbitmq.py
  - tests/audit/test_static.py
autonomous: true
requirements:
  - REFACTOR-01
  - REFACTOR-05

must_haves:
  truths:
    - "[D-01] bet_maker FastAPI app boots and serves all four routers (health, bets, events, messaging) from the flat src/bet_maker/api/ paths — no nested api/http/ or api/amqp/."
    - "[D-02] bet_maker lifespan.py and middleware.py live at src/bet_maker/ root (NOT inside src/bet_maker/api/, NOT inside an app/ subpackage); src/bet_maker/app.py wires them from the new location."
    - "[D-03] src/bet_maker/messaging/routing.py (AMQP routing-key constants) is untouched by this plan — Plan 08-01 files_modified does NOT include it."
    - "[D-04] Plan 08-01 updates bet_maker tests AND tests/audit/test_static.py in the same plan as the rename — no cross-plan deferral of the audit / test-import sync."
    - "[D-05] Plan 08-01 does not change the tests/ directory structure or fixture wiring — only import statements and audit path strings are edited."
    - "bet_maker tests collect and pass with imports rewritten to src/bet_maker/api/* and src/bet_maker/{lifespan,middleware}.py."
    - "tests/audit/test_static.py reads src/bet_maker/api/messaging.py and the subscriber/durable invariants still hold."
    - "After Plan 08-01: zero bet_maker.entrypoints references anywhere in src/ or tests/; pytest can collect tests/bet_maker/test_e2e_rabbitmq.py without ImportError."
  artifacts:
    - path: "src/bet_maker/api/__init__.py"
      provides: "api package root for bet_maker"
    - path: "src/bet_maker/api/bets.py"
      provides: "POST /bet + GET /bet/{id} HTTP router"
    - path: "src/bet_maker/api/events.py"
      provides: "GET /events HTTP router (proxy to line-provider)"
    - path: "src/bet_maker/api/health.py"
      provides: "GET /health HTTP router (PG + RMQ + subscriber + reconciler)"
    - path: "src/bet_maker/api/messaging.py"
      provides: "FastStream RabbitRouter + on_event_finished subscriber"
      contains: "AckPolicy.MANUAL"
    - path: "src/bet_maker/lifespan.py"
      provides: "FastAPI lifespan (PG + httpx + broker + reconciler)"
    - path: "src/bet_maker/middleware.py"
      provides: "RequestContextMiddleware"
  key_links:
    - from: "src/bet_maker/app.py"
      to: "src/bet_maker/api/{bets,events,health,messaging}.py"
      via: "from bet_maker.api import ..."
      pattern: "from bet_maker\\.api import"
    - from: "src/bet_maker/app.py"
      to: "src/bet_maker/{lifespan,middleware}.py"
      via: "from bet_maker.lifespan import lifespan; from bet_maker.middleware import RequestContextMiddleware"
      pattern: "from bet_maker\\.(lifespan|middleware) import"
    - from: "src/bet_maker/lifespan.py"
      to: "src/bet_maker/api/messaging.py"
      via: "from bet_maker.api.messaging import router as rabbit_router; set_sessionmaker"
      pattern: "from bet_maker\\.api\\.messaging import"
    - from: "src/bet_maker/facades/deps.py"
      to: "src/bet_maker/api/messaging.py"
      via: "late import inside get_rabbit_broker"
      pattern: "from bet_maker\\.api\\.messaging import router"
    - from: "tests/audit/test_static.py"
      to: "src/bet_maker/api/messaging.py"
      via: "Path read_text() in test_subscribers_have_manual_ack and test_durable_queue_and_exchange"
      pattern: "bet_maker / .api. / .messaging.py"
    - from: "tests/bet_maker/test_e2e_rabbitmq.py"
      to: "src/bet_maker/api/messaging.py"
      via: "late import inside DLQ test (line 131)"
      pattern: "from bet_maker\\.api\\.messaging import router as bm_router"
---

<objective>
Migrate bet_maker's `entrypoints/` package to the new layout per CONTEXT.md D-01/D-02:

- HTTP routers (`bets.py`, `events.py`, `health.py`) and the FastStream RabbitRouter (`messaging.py`) move to `src/bet_maker/api/`.
- `lifespan.py` and `middleware.py` move to `src/bet_maker/` root (next to `app.py`).
- `src/bet_maker/entrypoints/` directory is deleted.
- All in-repo imports — production code AND tests — that reference `bet_maker.entrypoints` are rewritten to the new paths. This includes the late import on line 131 of `tests/bet_maker/test_e2e_rabbitmq.py` (the bet_maker side of that cross-service test).
- `tests/audit/test_static.py` path strings updated in lockstep so the static audit still runs against the new location.

Purpose: deliver REFACTOR-01 (entrypoints/ слой упразднён) for bet_maker, while keeping REFACTOR-05 (test suite green, mypy strict, ruff clean, coverage ≥85%) intact.

Output: bet_maker boots and serves the same API surface; bet_maker test files green when run alone; audit tests green; `tests/bet_maker/test_e2e_rabbitmq.py` collects without ImportError on the bet_maker import path (the line_provider import on line 113 still resolves through the legacy path until plan 08-02 lands — see scope note below).

OUT OF SCOPE for plan 08-01:
- `tests/bet_maker/test_e2e_rabbitmq.py` line 113 (`from line_provider.entrypoints.messaging import router as lp_router`) — this references the line_provider package which has not moved yet. Plan 08-02 owns that single edit.
- All line_provider source files, line_provider tests, and `tests/bet_maker/conftest.py`'s `line_provider_app` fixture late import.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/08-flatten-api/08-CONTEXT.md
@.planning/quick/20260518-batch1-cleanup/SUMMARY.md

@src/bet_maker/app.py
@src/bet_maker/entrypoints/lifespan.py
@src/bet_maker/entrypoints/middleware.py
@src/bet_maker/entrypoints/messaging.py
@src/bet_maker/entrypoints/api/bets.py
@src/bet_maker/entrypoints/api/events.py
@src/bet_maker/entrypoints/api/health.py
@src/bet_maker/facades/deps.py
@src/bet_maker/jobs/__init__.py
@tests/bet_maker/conftest.py
@tests/bet_maker/test_health.py
@tests/bet_maker/test_lifespan.py
@tests/bet_maker/test_lifespan_reconciler.py
@tests/bet_maker/test_messaging.py
@tests/bet_maker/test_e2e_rabbitmq.py
@tests/audit/test_static.py

<interfaces>
<!-- Existing exports the executor must NOT alter, only relocate. -->

src/bet_maker/entrypoints/messaging.py (becomes src/bet_maker/api/messaging.py):
- `router: RabbitRouter` (singleton — sole owner)
- `set_sessionmaker(sm) -> None`
- `_require_sessionmaker() -> async_sessionmaker[AsyncSession]`
- `on_event_finished(payload, msg)` subscriber (AckPolicy.MANUAL, prefetch=10, durable queue/exchange)
- internal symbols also patched by tests: `_settle_with_retry`, `_SCHEMA_VERSION_SUPPORTED`

src/bet_maker/entrypoints/lifespan.py (becomes src/bet_maker/lifespan.py):
- `lifespan(app: FastAPI) -> AsyncIterator[None]` (async contextmanager)

src/bet_maker/entrypoints/middleware.py (becomes src/bet_maker/middleware.py):
- `class RequestContextMiddleware(BaseHTTPMiddleware)`

src/bet_maker/entrypoints/api/bets.py / events.py / health.py (become src/bet_maker/api/...):
- each module exports `router: APIRouter`

src/bet_maker/app.py:
- `build_app() -> FastAPI` (factory, signature unchanged)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Move bet_maker files with git mv (no content edits yet)</name>
  <files>
    src/bet_maker/api/__init__.py,
    src/bet_maker/api/bets.py,
    src/bet_maker/api/events.py,
    src/bet_maker/api/health.py,
    src/bet_maker/api/messaging.py,
    src/bet_maker/lifespan.py,
    src/bet_maker/middleware.py
  </files>
  <read_first>
    src/bet_maker/entrypoints/__init__.py,
    src/bet_maker/entrypoints/api/__init__.py,
    src/bet_maker/entrypoints/api/bets.py,
    src/bet_maker/entrypoints/api/events.py,
    src/bet_maker/entrypoints/api/health.py,
    src/bet_maker/entrypoints/messaging.py,
    src/bet_maker/entrypoints/lifespan.py,
    src/bet_maker/entrypoints/middleware.py,
    src/bet_maker/app.py,
    .planning/phases/08-flatten-api/08-CONTEXT.md
  </read_first>
  <action>
    Move the bet_maker files using `git mv` (preserves history). Run these exact commands from repo root:

    ```
    mkdir -p src/bet_maker/api
    git mv src/bet_maker/entrypoints/api/__init__.py src/bet_maker/api/__init__.py
    git mv src/bet_maker/entrypoints/api/bets.py    src/bet_maker/api/bets.py
    git mv src/bet_maker/entrypoints/api/events.py  src/bet_maker/api/events.py
    git mv src/bet_maker/entrypoints/api/health.py  src/bet_maker/api/health.py
    git mv src/bet_maker/entrypoints/messaging.py   src/bet_maker/api/messaging.py
    git mv src/bet_maker/entrypoints/lifespan.py    src/bet_maker/lifespan.py
    git mv src/bet_maker/entrypoints/middleware.py  src/bet_maker/middleware.py

    # Remove now-empty package roots:
    git rm src/bet_maker/entrypoints/api/__init__.py 2>/dev/null || true   # already moved above
    git rm src/bet_maker/entrypoints/__init__.py
    rmdir src/bet_maker/entrypoints/api 2>/dev/null || true
    rmdir src/bet_maker/entrypoints 2>/dev/null || true
    ```

    Notes:
    - The route files (`bets.py`, `events.py`, `health.py`) contain ZERO references to `entrypoints` internally (verified). Their only imports are from `bet_maker.facades.deps` / `bet_maker.selectors` / `bet_maker.interactors` — none touched here.
    - The `entrypoints/__init__.py` and `entrypoints/api/__init__.py` are empty 1-line files — drop them rather than relocate.
    - Do NOT yet edit file contents — that comes in Tasks 2 and 3. This task is pure relocation so a single grep can later show all references are stale and need updating.

    After moving, the executor MUST verify with the acceptance criteria below before proceeding.
  </action>
  <verify>
    <automated>find src/bet_maker -type d -name entrypoints | wc -l | grep -q '^0$' &amp;&amp; test -f src/bet_maker/api/messaging.py &amp;&amp; test -f src/bet_maker/lifespan.py &amp;&amp; test -f src/bet_maker/middleware.py</automated>
  </verify>
  <acceptance_criteria>
    - `find src/bet_maker -type d -name entrypoints` produces empty output.
    - `test -f src/bet_maker/api/__init__.py` succeeds.
    - `test -f src/bet_maker/api/bets.py` succeeds.
    - `test -f src/bet_maker/api/events.py` succeeds.
    - `test -f src/bet_maker/api/health.py` succeeds.
    - `test -f src/bet_maker/api/messaging.py` succeeds.
    - `test -f src/bet_maker/lifespan.py` succeeds.
    - `test -f src/bet_maker/middleware.py` succeeds.
    - `git status --porcelain | grep -E '^R' | wc -l` reports ≥7 renames (history preserved).
  </acceptance_criteria>
  <done>
    All bet_maker files relocated to the new layout via git mv; `src/bet_maker/entrypoints/` directory is gone; no file contents edited yet.
  </done>
</task>

<task type="auto">
  <name>Task 2: Rewrite bet_maker production imports to new paths</name>
  <files>
    src/bet_maker/app.py,
    src/bet_maker/lifespan.py,
    src/bet_maker/api/messaging.py,
    src/bet_maker/facades/deps.py,
    src/bet_maker/jobs/__init__.py
  </files>
  <read_first>
    src/bet_maker/app.py,
    src/bet_maker/lifespan.py,
    src/bet_maker/api/messaging.py,
    src/bet_maker/facades/deps.py,
    src/bet_maker/jobs/__init__.py
  </read_first>
  <action>
    Rewrite every `from bet_maker.entrypoints...` import to its new home. Concrete replacements (old → new):

    In `src/bet_maker/app.py`:
    - `from bet_maker.entrypoints.api import bets, events, health` → `from bet_maker.api import bets, events, health`
    - `from bet_maker.entrypoints.lifespan import lifespan` → `from bet_maker.lifespan import lifespan`
    - `from bet_maker.entrypoints.messaging import router as rabbit_router` → `from bet_maker.api.messaging import router as rabbit_router`
    - `from bet_maker.entrypoints.middleware import RequestContextMiddleware` → `from bet_maker.middleware import RequestContextMiddleware`

    In `src/bet_maker/lifespan.py` (note: this is the moved file at its new location):
    - `from bet_maker.entrypoints.messaging import router as rabbit_router` → `from bet_maker.api.messaging import router as rabbit_router`
    - `from bet_maker.entrypoints.messaging import set_sessionmaker` → `from bet_maker.api.messaging import set_sessionmaker`

    In `src/bet_maker/api/messaging.py` (only a docstring/comment self-reference):
    - The docstring of `set_sessionmaker` says `Called by `src/bet_maker/entrypoints/lifespan.py` after engine init.` → change to `Called by \`src/bet_maker/lifespan.py\` after engine init.`

    In `src/bet_maker/facades/deps.py`:
    - The docstring of `get_rabbit_broker` says `There is exactly ONE RabbitRouter (declared in bet_maker.entrypoints.messaging) -- its \`broker\` attribute is the sole broker instance.` → replace `bet_maker.entrypoints.messaging` with `bet_maker.api.messaging`.
    - Late import `from bet_maker.entrypoints.messaging import router  # noqa: PLC0415` → `from bet_maker.api.messaging import router  # noqa: PLC0415`

    In `src/bet_maker/jobs/__init__.py`:
    - Module docstring line `Distinct from \`entrypoints/\` (HTTP routes + AMQP subscribers): a job` → `Distinct from \`api/\` (HTTP routes + AMQP subscribers): a job`

    Do NOT touch route module file contents (`bets.py`, `events.py`, `health.py`) — they have no entrypoints references.

    Do NOT edit middleware.py — it has no internal entrypoints reference.

    After the edits, run from repo root:
    - `git grep -E 'bet_maker\.entrypoints' src/` — must return zero lines (covers dotted-module imports).
    - `git grep -E 'bet_maker/entrypoints' src/` — must return zero lines (covers path-style string literals / docstring references; Task 1 already removed the directory, so any path residue is a stale reference to fix).
  </action>
  <verify>
    <automated>git grep -E 'bet_maker\.entrypoints' src/ | grep -v '^#' | wc -l | grep -q '^0$' &amp;&amp; git grep -E 'bet_maker/entrypoints' src/ | grep -v '^#' | wc -l | grep -q '^0$' &amp;&amp; python -c "from bet_maker.api import bets, events, health, messaging; from bet_maker import lifespan, middleware, app; print('imports ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `git grep -E 'bet_maker\.entrypoints' src/` exits non-zero (zero matches — dotted-module style).
    - `git grep -E 'bet_maker/entrypoints' src/` exits non-zero (zero matches — path-style residue in docstrings/comments/string literals; this must run AFTER Task 1's `git mv` / `rm` of the bet_maker/entrypoints directory).
    - `python -c "from bet_maker.api import bets, events, health, messaging"` exits 0.
    - `python -c "from bet_maker import lifespan, middleware"` exits 0 (modules import without raising).
    - `python -c "from bet_maker.app import build_app; build_app()"` runs without ImportError. (Lifespan/broker is not started here — just module-level imports + factory call. Settings reads env, so the call may raise pydantic ValidationError if env is missing; if so, the executor should set `BET_MAKER_POSTGRES_DSN=postgresql+asyncpg://x:x@localhost/x BET_MAKER_RABBITMQ_URL=amqp://x:x@localhost/ LINE_PROVIDER_BASE_URL=http://lp:8000` for this smoke check.)
    - `mypy --strict src/bet_maker` reports zero errors.
    - `ruff check src/bet_maker` reports zero issues.
  </acceptance_criteria>
  <done>
    All bet_maker production code imports the relocated modules from `bet_maker.api.*`, `bet_maker.lifespan`, `bet_maker.middleware`; the comment/docstring self-references in `messaging.py`, `facades/deps.py`, and `jobs/__init__.py` are updated; the package imports cleanly and passes mypy strict + ruff; both dotted-module and path-style grep gates exit non-zero (zero matches).
  </done>
</task>

<task type="auto">
  <name>Task 3: Rewrite bet_maker test imports (including test_e2e_rabbitmq line 131) and update audit path strings</name>
  <files>
    tests/bet_maker/conftest.py,
    tests/bet_maker/test_health.py,
    tests/bet_maker/test_lifespan.py,
    tests/bet_maker/test_lifespan_reconciler.py,
    tests/bet_maker/test_messaging.py,
    tests/bet_maker/test_e2e_rabbitmq.py,
    tests/audit/test_static.py
  </files>
  <read_first>
    tests/bet_maker/conftest.py,
    tests/bet_maker/test_health.py,
    tests/bet_maker/test_lifespan.py,
    tests/bet_maker/test_lifespan_reconciler.py,
    tests/bet_maker/test_messaging.py,
    tests/bet_maker/test_e2e_rabbitmq.py,
    tests/audit/test_static.py
  </read_first>
  <action>
    Rewrite every `bet_maker.entrypoints` reference in tests/ to the new layout. Concrete edits per file:

    `tests/bet_maker/conftest.py` (line ~77 — the bet_maker-side import only):
    - `from bet_maker.entrypoints.messaging import router as rabbit_router  # noqa: PLC0415` → `from bet_maker.api.messaging import router as rabbit_router  # noqa: PLC0415`
    - Do NOT touch the `line_provider.entrypoints.messaging` late import inside the `line_provider_app` fixture in this file — that belongs to plan 08-02.

    `tests/bet_maker/test_health.py`:
    - Mock target string `"bet_maker.entrypoints.api.health.ping_postgres"` → `"bet_maker.api.health.ping_postgres"`
    - Two late imports `from bet_maker.entrypoints.messaging import router  # noqa: PLC0415` → `from bet_maker.api.messaging import router  # noqa: PLC0415`

    `tests/bet_maker/test_lifespan.py`:
    - Four late imports `from bet_maker.entrypoints.messaging import router  # noqa: PLC0415` → `from bet_maker.api.messaging import router  # noqa: PLC0415`
    - `from bet_maker.entrypoints.lifespan import lifespan  # noqa: PLC0415` → `from bet_maker.lifespan import lifespan  # noqa: PLC0415`
    - The patch string `with patch("bet_maker.entrypoints.lifespan.wait_for_postgres") as mock_wait:` → `with patch("bet_maker.lifespan.wait_for_postgres") as mock_wait:`

    `tests/bet_maker/test_lifespan_reconciler.py` (top of file):
    - `from bet_maker.entrypoints.lifespan import lifespan` → `from bet_maker.lifespan import lifespan`

    `tests/bet_maker/test_messaging.py`:
    - Top-level `from bet_maker.entrypoints.messaging import router, set_sessionmaker` → `from bet_maker.api.messaging import router, set_sessionmaker`
    - Every patch string `"bet_maker.entrypoints.messaging._settle_with_retry"` (×6 occurrences) → `"bet_maker.api.messaging._settle_with_retry"`
    - Patch string `"bet_maker.entrypoints.messaging._SCHEMA_VERSION_SUPPORTED"` → `"bet_maker.api.messaging._SCHEMA_VERSION_SUPPORTED"`
    - Path read string `Path("src/bet_maker/entrypoints/messaging.py").read_text()` → `Path("src/bet_maker/api/messaging.py").read_text()`

    `tests/bet_maker/test_e2e_rabbitmq.py` (BLOCKER fix from checker iteration 1):
    - Line 131 `from bet_maker.entrypoints.messaging import router as bm_router  # noqa: PLC0415` → `from bet_maker.api.messaging import router as bm_router  # noqa: PLC0415`
    - DO NOT touch line 113 (`from line_provider.entrypoints.messaging import router as lp_router`) — that line is owned by plan 08-02 and the line_provider package has not moved yet at the end of plan 08-01. Editing it here would break the file.
    - The intermediate state after this task: line 113 still reads `line_provider.entrypoints.messaging` (resolves through the as-yet-unmoved line_provider directory) and line 131 now reads `bet_maker.api.messaging` (resolves through the just-moved bet_maker directory). Both imports resolve, so pytest can collect the file without ImportError.

    DO NOT touch `tests/bet_maker/e2e/test_reconciler_drop_publish_e2e.py` in this plan. The docstring at line ~11 still says `line_provider/entrypoints/lifespan.py`; that edit was moved to plan 08-02 Task 3 (INFO-level checker note: cleaner intermediate state to update the docstring in the same plan that actually moves the line_provider directory). This is a deliberate scope reduction vs. the iteration-0 plan.

    `tests/audit/test_static.py` (two existing tests reference bet_maker entrypoints path):
    - `test_subscribers_have_manual_ack`: `src = (SRC / "bet_maker" / "entrypoints" / "messaging.py").read_text()` → `src = (SRC / "bet_maker" / "api" / "messaging.py").read_text()`
    - `test_durable_queue_and_exchange`: `src = (SRC / "bet_maker" / "entrypoints" / "messaging.py").read_text()` → `src = (SRC / "bet_maker" / "api" / "messaging.py").read_text()`
    - Do NOT touch `test_repositories_use_for_update_skip_locked`, `test_async_sessionmaker_expire_on_commit_false`, `test_compose_command_exec_form`, `test_dockerfile_pinned_python_bookworm`, `test_pythonunbuffered_set` — they reference unrelated files.

    Add a new static audit test `test_no_entrypoints_dir` to `tests/audit/test_static.py` as a regression guard (per CONTEXT.md "Specific Ideas"). Place it after `test_durable_queue_and_exchange`.

    BLOCKER 2 fix from checker iteration 1 (Option A — earliest regression guard activation): the test asserts ONLY the `bet_maker` directory does not exist. The `line_provider` directory still exists at the end of plan 08-01 (it is removed in plan 08-02), so asserting on it here would FAIL. Plan 08-02 Task 3 expands the test to also cover `line_provider`.

    ```python
    def test_no_entrypoints_dir() -> None:
        """REFACTOR-01: src/bet_maker/entrypoints/ must not exist.

        Phase 8 plan 08-01 flattened bet_maker's HTTP routers + FastStream
        RabbitRouter into src/bet_maker/api/ and relocated lifespan.py +
        middleware.py to the service-package root. The legacy entrypoints/
        directory was deleted; this audit fails if a future commit recreates
        it.

        NOTE: line_provider/entrypoints/ is NOT asserted here yet — plan
        08-02 moves that directory and at that point expands this test to
        also cover line_provider.
        """
        bm = SRC / "bet_maker" / "entrypoints"
        assert not bm.exists(), (
            f"{bm} re-introduced — Phase 8 flattened entrypoints/ → api/."
        )
    ```

    Note: line_provider-side test files (`tests/line_provider/conftest.py`, `tests/line_provider/test_lifespan.py`), the `line_provider_app` fixture in `tests/bet_maker/conftest.py`, line 113 of `tests/bet_maker/test_e2e_rabbitmq.py`, and the docstring in `tests/bet_maker/e2e/test_reconciler_drop_publish_e2e.py` are OUT OF SCOPE for this plan — they belong to plan 08-02 which moves the line_provider package.

    After all edits, this command must return zero output when scoped to bet_maker:

    ```
    git grep -E 'bet_maker\.entrypoints' tests/
    ```

    The `line_provider.entrypoints` references will still exist (lines in `tests/line_provider/`, `tests/bet_maker/conftest.py`'s `line_provider_app` fixture, line 113 of `tests/bet_maker/test_e2e_rabbitmq.py`) — those are 08-02's scope.
  </action>
  <verify>
    <automated>git grep -E 'bet_maker\.entrypoints' tests/ | grep -v '^#' | wc -l | grep -q '^0$' &amp;&amp; git grep -E 'bet_maker/entrypoints' tests/ | grep -v '^#' | wc -l | grep -q '^0$' &amp;&amp; pytest -q tests/audit/test_static.py::test_subscribers_have_manual_ack tests/audit/test_static.py::test_durable_queue_and_exchange tests/audit/test_static.py::test_no_entrypoints_dir -x &amp;&amp; pytest --collect-only -q tests/bet_maker/test_e2e_rabbitmq.py &gt; /dev/null</automated>
  </verify>
  <acceptance_criteria>
    - `git grep -E 'bet_maker\.entrypoints' tests/` exits non-zero (zero matches — dotted-module imports).
    - `git grep -E 'bet_maker/entrypoints' tests/` exits non-zero (zero matches — Path() string literals).
    - `pytest -q tests/audit/test_static.py::test_subscribers_have_manual_ack` exits 0.
    - `pytest -q tests/audit/test_static.py::test_durable_queue_and_exchange` exits 0.
    - `pytest -q tests/audit/test_static.py::test_no_entrypoints_dir` exits 0 (asserts bet_maker only at this stage).
    - `pytest --collect-only -q tests/bet_maker/test_e2e_rabbitmq.py` exits 0 (both imports — line 113 line_provider via legacy path, line 131 bet_maker via new path — resolve at collection time).
    - `pytest -q tests/bet_maker/test_messaging.py` exits 0 (handler-level unit tests pass).
    - `ruff check tests/bet_maker tests/audit` reports zero issues.
    - `mypy --strict src` still reports zero errors (no regression introduced by docstring/import edits in src).
  </acceptance_criteria>
  <done>
    Every `bet_maker.entrypoints` reference under `tests/bet_maker/` (including line 131 of `test_e2e_rabbitmq.py`) and `tests/audit/` is rewritten to the new paths; the audit test now points at `src/bet_maker/api/messaging.py`; the new `test_no_entrypoints_dir` regression guard is added with the bet_maker assertion active (line_provider assertion deferred to plan 08-02); pytest can collect `tests/bet_maker/test_e2e_rabbitmq.py` without ImportError.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| filesystem → Python import system | Renamed modules must resolve via the new import path; stale `from bet_maker.entrypoints...` would crash at startup or at pytest collection. |
| filesystem → static audit | `tests/audit/test_static.py` reads source files by hardcoded path; an unupdated path silently passes (reads empty / wrong file). |
| plan-boundary intermediate state | Between plan 08-01 and 08-02 the repo has a mixed state: bet_maker moved, line_provider not. `tests/bet_maker/test_e2e_rabbitmq.py` lines 113 vs 131 must both resolve at this midpoint. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | Tampering | bet_maker import surface | mitigate | Step 2 rewrites every `from bet_maker.entrypoints...` import; acceptance criteria `git grep -E 'bet_maker\.entrypoints'` and `git grep -E 'bet_maker/entrypoints'` must both return zero matches before the task is done. |
| T-08-02 | Repudiation | git history | mitigate | Use `git mv` (Task 1) so `git log --follow` continues to track file history across the rename. |
| T-08-03 | Information Disclosure | static audit silently passing on stale path | mitigate | Task 3 updates the two hardcoded `Path` strings in `test_static.py` and adds `test_no_entrypoints_dir` as a positive regression guard (bet_maker assertion active; line_provider deferred to 08-02). |
| T-08-04 | Denial of Service | lifespan startup ImportError → service won't boot | mitigate | Acceptance criterion runs `python -c "from bet_maker.app import build_app; build_app()"` after Task 2 to confirm the factory resolves all imports before lifespan would attempt to start. |
| T-08-05a | Denial of Service | pytest collection ImportError on test_e2e_rabbitmq.py | mitigate | Task 3 edits line 131 to the new path; acceptance criterion runs `pytest --collect-only tests/bet_maker/test_e2e_rabbitmq.py` to confirm both imports (line 113 still legacy, line 131 new) resolve. |
| T-08-05 | Elevation of Privilege | n/a | accept | This is a pure internal refactor with no new attack surface; auth/authz boundaries unchanged. |
</threat_model>

<verification>
After all three tasks complete, the executor must run this sequence and confirm green:

```
git grep -E 'bet_maker\.entrypoints' src/ tests/   # zero output
git grep -E 'bet_maker/entrypoints' src/ tests/    # zero output (catches Path string literals)
find src/bet_maker -type d -name entrypoints       # empty output
ruff check src/bet_maker tests/bet_maker tests/audit
mypy --strict src
pytest -q tests/bet_maker tests/audit              # all green (bet_maker side)
pytest --collect-only -q tests/bet_maker/test_e2e_rabbitmq.py   # collects without ImportError
```

Note: full repo `pytest` (including `tests/line_provider/`) is NOT in scope for this plan — that depends on plan 08-02. Final all-suite green gate lives in plan 08-03.

The repo still contains `line_provider.entrypoints` references (production code + line_provider tests + line_provider_app fixture in tests/bet_maker/conftest.py + line 113 of tests/bet_maker/test_e2e_rabbitmq.py). That is expected and is plan 08-02's scope.
</verification>

<success_criteria>
- `src/bet_maker/entrypoints/` directory does not exist.
- `src/bet_maker/api/{bets,events,health,messaging}.py` exist and import without raising.
- `src/bet_maker/{lifespan,middleware}.py` exist at the service-package root next to `app.py`.
- Zero `bet_maker.entrypoints` references (dotted-module style) remain anywhere in `src/` or `tests/`.
- Zero `bet_maker/entrypoints` references (path style) remain anywhere in `src/` or `tests/`.
- `tests/audit/test_static.py::test_subscribers_have_manual_ack`, `test_durable_queue_and_exchange`, and new `test_no_entrypoints_dir` (bet_maker assertion) all pass.
- `pytest --collect-only tests/bet_maker/test_e2e_rabbitmq.py` exits 0 (both imports resolve at the plan-boundary intermediate state).
- `mypy --strict src` and `ruff check src tests` are clean.
- bet_maker test files pass when run in isolation (`pytest tests/bet_maker tests/audit`).
</success_criteria>

<output>
After completion, create `.planning/phases/08-flatten-api/08-01-SUMMARY.md` documenting:
- Files moved (with `git log --follow` confirmation that history is preserved).
- All import sites rewritten (grep-confirmed zero hits for both `bet_maker\.entrypoints` and `bet_maker/entrypoints`).
- The new `test_no_entrypoints_dir` audit and its current state: bet_maker assertion passing; line_provider assertion deferred to plan 08-02 (per Blocker 2 Option A fix from checker iteration 1).
- The single-line bet_maker fix on `tests/bet_maker/test_e2e_rabbitmq.py` line 131; line 113 still uses the legacy line_provider path (plan 08-02's scope).
- Any deviations from the action text (should be none — concrete commands provided).
</output>
